# Plan 02 — bounded exploration and critique ledger

Status: **COMPLETE — bounded three-round design review**. No unresolved material blocker was found within the reviewed scope after correction verification. This is not implementation or runtime acceptance.

Design: [event-mapping-refactoring_plan_02.md](event-mapping-refactoring_plan_02.md). Existing [handoff](dashboard_cli_workflow_parity/research-stack-implementation-handoff.md) remains the implementation-status owner.

## 1. Scope, baseline and review budget

User authorized the proposed exploration/critique loop. Maximum three rounds: independent discovery, cross-examination/adjudication, then correction verification. Only the coordinator edits plan/review documentation. Reviewers inspect source independently; no package imports/tests, services, credentials, database access, simulation or inference experiments. Host documentation checks are allowed. Runtime questions become explicit verification obligations, not invented results.

- Git HEAD: `45a0b26466104e8da92de37034dcb3f37b1166ac`; branch `dev/0.3.0-prerelease`.
- Initial plan SHA-256: `1f4e89cfe35d414da06731f5190dc88c0fe89cee4319a3cd46ccab2ea0d9090c`.
- Source inventory: 928 tracked regular, nonsymlink `.py`, `.ts`, `.tsx` files under `isaaclab_arena/`, `isaaclab_arena_examples/`, `isaaclab_arena_gr00t/`, `web/arena-workbench/src/`, `web/arena-workbench/tests/e2e/functional-v7/`, `scripts/`, plus `pyproject.toml`, `web/arena-workbench/package.json`, `AGENTS.md`.
- Source manifest SHA-256: `33929c61ad2811f079749e051a28dc6a5b57fe5388f38b357f9c50fb2b8aa619`. Algorithm: sort repository-relative paths; create `[path, sha256(file_bytes)]` pairs; JSON serialize with separators `(',', ':')` and hash the UTF-8 result. This is a drift fingerprint, **not a claim that all inventoried files were reviewed**.
- Pre-existing changes: modified reference index and untracked plans 01/02. Preserve them; no staging/commit.
- Source fingerprint at review closure matched the initial manifest exactly; all 928 selected source files were unchanged. Plan revision verified by the completed loop had SHA-256 `4cf4653dd92a81233ab8a549215886de5f9d461f55677f1f7faddf3fbd086cbb`. Subsequent documentation elaboration is recorded in section 8; this historical digest is not a claim about the latest plan bytes.

## 2. Coverage matrix

The coverage rows record traced boundaries and final design dispositions. Section 4 retains the counterarguments; section 6 owns correction verification. Requirements are review units, not an exhaustive inventory of every possible defect. Refer to plan invariants I1–I12 and scenarios A–O.

| Unit | Requirement / plan scope | Primary reviewer | Source boundary to trace | Status / evidence |
| --- | --- | --- | --- | --- |
| R01 | Prompt-first New vs exact-base Refine; §§1,3,9 | Explorer | Runner → managed generation → agent → retriever | Traced runner 277–297, generation 118–175, spec_inference 119–220; no additional contradiction. |
| R02 | Immutable request/candidate identity and canonicalization; §3 | Persistence | Spec loader, hashes, receipts, Journal | Journal 209–250 and editor 318–400; PE-02 supported and design correction verified. |
| R03 | Per-criterion acceptance and missing/advisory proof; §§4–5 | Domain | Critic/trajectory assessment → criterion evaluator | Assessor schema/tier traced; DM-01 supported, DM-02 narrowed; design corrections verified. |
| R04 | Realized support, frames, relations and effective correction; §§5–6 | Domain | Schema → conversion → relation solver/placer → measured state | Conversion 242–246, placer 757–764, task sensor 117–124 and settle loop 214–248; DM-01. Named-support instrumentation remains explicit new work. |
| R05 | Full structural/cumulative repair permission and final fallback; §6 | Domain | Refiner/oracle/DCRG validation → candidate | Agent 474–598 and DCRG guard traced; fallback/stagnation limitations already addressed by final full validation and runtime-effect checks. |
| R06 | Exact prior context, eligibility and publication separation; §9 | Explorer | Graph-RAG queries → consumed prompt → publication | graph_access/managed_retrieval/prior_receipt/spec_inference traced; legacy read authority and prompt omissions remain explicit limitations. |
| R07 | Admission, decision CAS, idempotency, event replay; §7 | Persistence | Journal/state contracts → proposed Neo4j transactions | Journal 406–454 and 816–858; PE-01/PE-02 design corrections verified; no live race execution. |
| R08 | Whole-run budgets, nested calls, timeouts; §§3,7 | Explorer | Agent/backend/critic retry paths → release budget | Constructor/JSON/image/fallback/bounded-client paths traced; EX-01/EX-02 narrowed corrections verified. |
| R09 | Attempt/owner fences, authorization, cancellation/outage; §7 | Persistence | Grants → worker release → owner cleanup/reconciliation | workflow_authorization 149–186, editor_execution 182–211, Journal 601–804 and parent watch traced; outage stop already explicit. |
| R10 | Filesystem/graph receipt durability and readback; §§3,7 | Persistence | Artifact storage → receipt commit → adoption | research_artifacts 446–530 and editor_execution 258–284 traced; no new gap beyond declared implementation obligations. |
| R11 | New/legacy ownership, GPU coexistence and cutover; §8 | Integration | Dispatcher/leases/direct SQL consumers/migration | Supervisor/snapshot_process/build_execution traced; IF-01 narrowed liveness correction verified; direct SQL migration audit not exhaustive. |
| R12 | Reusable core extraction and import closure; §11 | Explorer | Example/GUI workers → core services; indirect dependencies | EX-03; rendering closure includes asset_usd/kit_viewport/boot beyond named helpers. |
| R13 | Optional policy/DCRG semantics and state authority; §10 | Domain | Policy runner → DCRG loop/evaluation/state and outer contract | DCRG loop 261–281/evaluation 120–141 traced; improvement versus task proof and later state-port extraction preserved. |
| R14 | CLI ownership and later UI/HTTP/GraphQL; §12 | Integration | CLI/admission/status → API composition → mounted UI | Legacy CLI/API/session/Journal authorizer and mounted editor traced; IF-02 clarified P0 deliverable verified; no route-completeness claim. |
| R15 | Phase dependencies, test admission, positive live path; §§13–14 | Integration | Existing test runner and actual producer capabilities | stage/backend_checks/runner traced; IF-03 missing-prerequisite claim rejected, profile-classification checklist clarified; measured-support adapter remains explicit new work. |
| R16 | Documentation scope, P0 unknowns and no false completion; §§1,15–16 | Coordinator | Plan/current handoff/source versus proposed capability | Reviewed: plan §§1,12–16 labels proposal, new CLI not runnable, phases not started, runtime gates open; handoff lines 7–27 preserves unresolved UI issues and narrow historical evidence. Historical handoff HEAD differs from current checkout; use section 1 baseline here, not the historical hash. No production-readiness claim accepted. |

## 3. Round 1 — independent exploration

Four roles: source/caller explorer, domain/evidence critic, persistence/execution critic, integration/feasibility critic. Review both plan→code and code→plan. Findings require plan/source references, a concrete counterexample, minimal correction, verification requirement and confidence. Reviewers do not initially receive one another's conclusions.

Completed: delegation `deleg_fa1e04b7`. Each role returned source-bound coverage without seeing other conclusions. Abbreviations: `C/` = `isaaclab_arena/agentic_environment_generation/`; `W/` = `isaaclab_arena_examples/agentic_environment_generation/web_api/`; `H/` = `web/arena-workbench/tests/e2e/functional-v7/`. Source line anchors are baseline-relative.

| ID | Initial classification / severity | Counterexample and source evidence | Minimal correction / verification | Round-1 disposition (historical) |
| --- | --- | --- | --- | --- |
| EX-01 | Missing integration; high | `C/visual_critic.py:78–110,167–182` falls from failed cloud to local HTTP endpoint/model outside the authorized bounded backend. | Disable implicit managed fallback; profile/authorize/reserve alternatives; cloud failure produces no local call unless explicitly authorized. | Pending |
| EX-02 | Missing integration; high | `C/inference_backend.py:306–315,362–443,473–496` constructor ping/image calls absent from run_json telemetry; `W/provider_security.py:65–93` allowance per context; agent 250/472 couples semantic loops to max_retries. | Count before backend construction at transport boundary; separate transport retries from semantic iterations; test exact ping/JSON/prim/image calls and uncertainty. | Pending |
| EX-03 | Indirect validation/import boundary; medium | `environment_spec/arena_env_graph_types.py:71–75` validation calls `assets/registries.py:41–64`; agent imports Torch/Isaac relation code. Import-only tests miss runtime imports during validation. | Separate pure request checks from runtime candidate/catalogue checks; test contract construction as well as module import. | Pending |
| DM-01 | Evidence composition; high | Same candidate/profile realizes randomized placement (`arena_env_graph_conversion_utils.py:242–246`, `relations/object_placer.py:757–764`); complementary support/visibility passes from different realizations can form a false conjunction. | Bind acceptance to compatible realization/reset/window/seed cohort; negative complementary-pass and positive complete-cohort tests. | Pending |
| DM-02 | Criterion-producer adapter; medium | `C/trajectory_assessment.py:33–59,98–117` has no criterion/rubric argument and aggregate-only output; generic satisfactory does not prove required camera/rubric. | Explicit criterion-aware request/response adapter; preserve old diagnostic helper; absent/duplicate/mismatched criterion coverage never passes. | Pending |
| PE-01 | Replay/prune consistency; medium | Journal 823–858 serializes floor/ceiling/page and pruning; plan names snapshot locking but not replay-page consistency. Prune between validation/read silently skips prefix. | Same control lock/equivalent protocol for replay validation/page and pruning; interleaving yields eligible page or ReplayGap. | Pending |
| PE-02 | Admission replay ordering; medium | `W/editor.py:318–400` recovers acceptance before mutable source/config/grants; plan explicitly orders replay only before capacity. Expired authority after lost response can block exact recovery. | Auth/pure request normalization → scoped replay/conflict/public screening; fresh only source/grant/capacity; test expired-grant/unavailable-source replay with zero dispatch. | Pending |
| IF-01 | CLI resource prerequisite; high | `W/supervisor.py:42–65` pause does not retire `snapshot_process.py:65,89–113` warm renderer lease; Build 48–68 closes only its own snapshot object. | Require acknowledged active-worker/warm-renderer retirement plus common lease identity before CLI; acquire without releasing queued jobs. | Pending |
| IF-02 | Concrete P0 composition; medium | `W/workflow_authorization.py:27–48,120–159` needs Journal/session binding; managed CLI 771–813 creates HTTP session, not foreground service owner. | Name reusable authorization/credential port and local-principal lifecycle; positive synthetic CLI release without HTTP/SQLite job; expiry blocks next release. | Pending |
| IF-03 | Test admission prerequisite; low | `H/stage.py:33–66,113–121`, `backend_checks.py:16–29,64–106` reject some named existing compatibility suites and classify unlisted pure tests as API-dependent. | Assign P0 admission/profile owner for old/new suites and verified exact commands; do not weaken offline isolation. | Pending |

Additional explored paths: spec_inference, inference_backend, visual_critic, provider_security, managed_retrieval; GUI asset_usd/kit_viewport/boot/sim_preview/thumbnail_capture; schema/type/conversion/task-conversion modules; asset registries; relation/placer/solver modules; pick-and-place task/predicates; policy runner; DCRG loop/evaluation; Journal/artifacts; API worker/auth/routes/application/lease modules; frontend app/editor mounts; isolated staging/runner modules. This is targeted caller/consumer coverage, not an exhaustive repository/security audit.

## 4. Round 2 — cross-examination and adjudication

A different reviewer must attempt to disprove each material finding, identify existing safeguards, and check whether the proposed correction creates another contradiction. The coordinator verifies accepted findings against source before changing the plan. Distinguish accepted gap, rejected claim, deliberate P0 selection and runtime-only unknown.

Completed: independent fresh cross-examiners in `deleg_9b097b13` challenged every finding. The coordinator read the decisive source directly: visual critic 78–182; backend 306–496; provider guard 65–93; editor 318–400; snapshot process 65–113; Build execution 38–82; AssetSpec validator 71–75 and registry 41–64; conversion 235–246 and placer 734–773; authorization 27–187; backend_checks 16–106; stage 33–121; Journal 816–858; agent semantic-loop references. Existing trajectory assessment schema/prompt was already directly inspected during plan drafting.

| ID | Final adjudication | Strongest counterargument / boundary retained | Accepted residual and correction location |
| --- | --- | --- | --- |
| EX-01 | Narrowed; conditional provider-authority risk | Existing managed generation has a bounded SDK wrapper and supplies no images to critic, so no demonstrated current-path fallback. Do not remove legacy diagnostic behavior. | New managed image adapter must disallow implicit direct-HTTP fallback and authorize/budget alternatives; plan §5.2, §11, case H extension. |
| EX-02 | Narrowed; medium integration clarification | Ping/image calls already pass through the wrapper, and plan already requires total metering. No claim they are currently universally unbounded. | Explicitly adapt preconstruction transport guard to cumulative reservation; telemetry insufficient; preserve semantic loops while disabling uncertain transport retries. Plan §7.2/§11/case H. |
| EX-03 | Narrowed; low extraction clarification | Pure workflow contracts never required every legacy environment model to be simulator-free; separation already selected. | Contract construction must avoid runtime catalogue validation; real candidate validation stays in admitted adapter. Plan §3/acceptance extensions; no duplicate schema. |
| DM-01 | Supported; high false-acceptance consequence | Realization IDs already exist, but presence of identity is not a mandatory compatibility check. Single-realization restriction must not invalidate legitimate policy trials. | Joint state-dependent scene evidence cohort plus declared multi-trial aggregation exception, actual reset/window membership and failure retention. Plan §3.2, cases A/D/E. |
| DM-02 | Narrowed; low adapter-contract clarification | Plan already states helpers lack criterion verdicts and funds adapters; this is not absent acceptance architecture. | Adapter explicitly sends and validates supported visual rubric/view/criterion coverage; old aggregate helper remains diagnostic; physical proof unchanged. Plan §5.2, cases A/D/E. |
| PE-01 | Supported; medium protocol specificity | Serialized writes and consistent snapshot already selected; replay-page materialization is a distinct read boundary. | Share consistency boundary with pruning, validate floor/ceiling and materialize page; return intact page or gap. Plan §7.3/case K. |
| PE-02 | Supported; medium ordering omission | Exact retry/read authorization already required, but ordering before mutable resolution was unstated. | Authenticated scoped pure-request replay before fresh-only resolution/grants; retained acceptance is not new execution authority. Plan §7.2/case K. |
| IF-01 | Narrowed; medium liveness/readiness | Correct existing lease prevents overlap; likely failure is timeout, not demonstrated unsafe concurrent GPU execution. Warm retirement already required later. | Require verified retirement/common physical lease before initial CLI native gate, owner-mediated or already clean inactive owner; queued jobs stay paused. Plan §8 Stage A/case N. |
| IF-02 | Narrowed; explicit low-priority P0 selection | CLI/API authority composition already explicitly deferred to P0, not a missing architecture or reason for a new auth framework. | Name concrete principal/resolver/binding port and positive release/expiry test in P0 exit work. Plan §8/§11/acceptance extensions. |
| IF-03 | Rejected as missing prerequisite; retain checklist clarification | Plan already qualified existing tests by admission and required exact approved paths; no new admission architecture needed. | Assign owner for old/new exact suite admission and core/API profile classification, preserving offline denial. Plan §13. |

No finding was accepted merely by majority agreement. Supported means a design counterexample survives source/counterargument review; it does not establish an executed bug in the not-yet-built workflow. Narrowed/rejected severities above supersede initial ratings.

## 5. Plan corrections

Applied only the bounded corrections above to plan 02. Added a link back to this ledger and explicit regression cases without introducing another execution/test framework. No production code changed. Existing three-phase runtime distinction remains: synthetic protocol tests, real isolated Neo4j transaction tests, separately authorized native/model acceptance.

Reviewer-requested runtime tests remain **not run**. P0 still owns numerical support predicates/tolerances, actual banana profile IDs/effective relation fields, port signatures, local principal composition, test admission commands and live budgets. None has been replaced by an invented default.

## 6. Round 3 — correction and coverage verification

Verify every accepted correction, its affected requirements and at least one counterexample/positive path. Stop after this round; any remaining blockers remain explicit rather than spawning an unbounded review cycle.

Completed: fresh final reviewers in `deleg_83b3106a` returned bounded design-level passes. No remaining material blocker was identified within either assigned scope. Parent source checks and document validation corroborate the correction references; no runtime outcome was inferred.

| Findings | Final design verification | Positive path retained / execution still required |
| --- | --- | --- |
| EX-01, EX-02 | Managed fallback restriction and cumulative preconstruction transport accounting explicit (§§5.2,7.2,11) | Authorized image/generation calls and semantic validation remain possible; real/synthetic transport accounting tests still to implement. |
| EX-03 | Pure contract construction distinct from admitted actual-schema validation (§§3,11) | Existing environment schema reused; runtime import/construction tests remain future work. |
| DM-01, DM-02 | Compatible scene evidence cohort and criterion-aware visual adapter explicit (§§3.2,5.2) | Complete scene cohort and declared multi-trial aggregation permitted; physical proof never delegated to VLM. |
| PE-01, PE-02 | Replay/prune consistency and authenticated exact admission recovery ordered (§§7.2–7.3) | Eligible page or explicit gap; retained acceptance without new dispatch/grants; live Neo4j concurrency tests still required. |
| IF-01, IF-02 | Owner-mediated renderer retirement/shared lease and foreground principal binding explicit (§8) | Initial scene CLI does not require full SQLite migration, HTTP app, GraphQL or DCRG; positive release/expiry/resource tests remain future work. |
| IF-03 | Existing admission obligation preserved, profile owner/checklist clarified (§13) | No broader runner admission or offline denial weakened; exact commands must be verified during implementation. |

Final reviewers also checked cross-cutting cancellation fencing, ownership identity, no arbitrary process kills, separate scene/policy outcomes, and consistency of all R01–R16 coverage units with the plan. The final round verified design closure, not an exhaustive new source audit.

## 7. Closure and limits

Loop stopped after the agreed three rounds. Programmatic reconciliation found 16 unique coverage units and 10 unique finding IDs: 3 supported, 6 narrowed, 1 rejected as a missing-prerequisite claim. All accepted residual corrections and the retained checklist clarification received final design verification. No unresolved material design blocker remains in this review scope.

Verification: scoped host pre-commit, `git diff --check`, relative links, balanced code fences, phase/scenario inventories and unique finding/coverage IDs passed. The final source fingerprint is unchanged. Only plan/review/index documentation changed in the repository; no runtime code, tests, databases, services, credentials or queued jobs were executed/modified, and nothing was staged or committed. The reusable review procedure is saved separately as the `bounded-plan-code-review` agent skill; it is not an Arena runtime dependency.

Remaining gates are explicit: freeze P0 concrete profiles, measurement predicates/tolerances, interfaces and local authorization composition; implement/admit offline tests; verify real Neo4j races/recovery; then run separately authorized native generation/render/VLM repair acceptance before dashboard integration. Historical UI-refresh reliability remains unresolved. No completeness proof, deployed workflow, physical success, policy transfer result or implementation approval is claimed.

## 8. Post-review plan maintenance

The user subsequently requested updating plan 02. Its §13.1–13.3 now assigns P0 work packages, file/role ownership, dependencies, positive/negative exit evidence and review-ID follow-through; §15 separates P0 decisions from later Neo4j/live/GraphQL/policy/migration gates. This is documentation elaboration of the reviewed direction, not a fourth broad review round or evidence that any P0 implementation is complete. The original review digest/results above remain preserved. New P0 contract artifacts require the focused integration review specified in the updated plan; no runtime code or workload is authorized by this update.

Further user-requested extension: plan §10.1–10.7 specifies opt-in VLM-assisted DCRG scene assessment, post-trial observations/hypotheses and permitted proposal guidance, with measured acceptance unchanged and a controlled utility/cost comparison. The code-reference note is corrected for the completed trajectory fix and explicitly mapped to current/deferred modules and test proof limits. This is new proposal scope beyond the original review digest; no model, graph, simulation or policy experiment has run.

Focused read-only review `deleg_c94fbe6d` found the proposal feasible and bounded at design level, with one material wording contradiction: online observe-only model overhead can change completed policy work/terminal budget outcome even without influencing decisions. The coordinator corrected P6-V2, §10.6 and §10.7 to distinguish immutable retained-evidence replay from online resource effects, fixed policy allowance from fixed total cost/time, and unchanged comparator semantics from fresh stochastic outcomes. All attempted work/stops remain in cost/accounting. The reviewer otherwise supported the three-role scope, evidence-alignment gate, supported-proposal boundary, transport/restart protocol and controlled-comparison design. The amendment was checked by the coordinator; no live feasibility, measured benefit or additional independent approval of unimplemented code is claimed.

The user then identified a missing explicit service-readiness admission boundary. Source review found existing host startup controls and bounded runtime/provider/policy/resource probes, but plan 02 did not require their composition before model construction. The authorized documentation correction adds §12.1 under P0-05, a full-request dependency matrix, structured blocked/not-accepted handling, per-release rechecks and the required regression: unavailable required dependency → zero model calls including initialization pings. It preserves scene-only policy optionality, exact read-only recovery and separate startup authority. P3/P0 handoff/test obligations now include this gate. This addition is not retroactively covered by the original hash-bound review; no implementation, probe, service start or model call was performed.
