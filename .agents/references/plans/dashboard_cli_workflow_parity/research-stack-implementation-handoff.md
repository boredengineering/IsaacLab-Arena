# Research workflow implementation checkpoint

This is the single current implementation handoff. The user has authorized implementing [event-mapping plan 03](../event-mapping-refactoring_plan_03.md) through V0/V1/V2 with bounded implementer/critic/verifier cycles, beginning with M0's original domain/event-modeling records. Earlier study and narrow-fix evidence remain in the [session memory](../../quick_notes/session_memory.md#current-study-and-progress--2026-09-18). Plan 02's completed isolated evidence below remains historical, not fresh plan-03 acceptance.

## 1. Current authorization and implementation gate

### Plan 04 implementation campaign — IF-A complete; execution not authorized

**IF-A complete:** [S2 initialization approval packet](../event-mapping-plan04-s2-initialization-approval.md) is ready for the explicit implementation/effect/resource decision in §8. Safety/lifecycle critic passed; budget critic's one closeout finding is resolved by the timestamped conservative ledger. IF-A is charged15minutes/one round/zero diagnostics;45active minutes/one round/three attempts remain, not renewed. Requested future changes include a4GiB/256PID client,32MiB evidence tmpfs,300s work+120s cleanup per attempt and fixed new diagnostic routing. None is implemented or already permitted. No IF-B/IF-C selector exists today; final-source/preflight/critic gates remain mandatory and IF-D needs separate admission. Exact baseline/final hashes and budget/evidence readback: `outputs/workflow/plan04-planning/s2-if-a/`. All discovery/critic tasks finished; no test resources were launched or fresh live resource-state claim made. Next action: user decision on this single packet. Do not execute IF-B, change strategy, or start a new budget implicitly.

**IF-A intake:** the user explicitly authorized the selected S2 approval-packet preparation and independent review, stopping before IF-B. Intake HEAD `4dd679599dea3e9682d463380d31d8fe89bb3fb1`, branch `dev/0.3.0-prerelease`, clean worktree. Source/planning baseline is `outputs/workflow/plan04-planning/s2-if-a/baseline.json` (394 captured files; inventory is not reviewed coverage). Prior selected-S2 source hashes match. Six historical cohorts still match their repository and generated retained source hashes; 563 is a historical overlapping case count, not a rerun. Retained final-probe evidence leaf hashes also match. Corrected static readback is `s2-if-a/retained-evidence-check-corrected.json`; the initial generated-file path classification error is retained separately. No current container/image/resource liveness inspection or runtime acceptance is claimed. Two bounded read-only discovery tasks (`deleg_0f7bd2fb`) cover harness controls/roles and initialization/cache bindings before packet drafting and fresh critics. All application/harness edits, imports/tests, containers, native effects and IF-B remain unauthorized. Earlier discussion-hold entries below are historical; source-only IF-A work now proceeds.

**Selection correction:** S2 (runtime-faithful isolated initialization) is the selected approach through IF-A–IF-D. Earlier wording below that says “no solution selected” or asks to choose S1–S4 describes superseded discussion; it must not restart strategy selection. S1/S3 are contingencies requiring an explicit evidence-backed pivot, S4 complementary testing. Next substantive deliverable is S2's IF-A admission contract, not another options comparison. The contract/effect/implementation approvals remain pending; this correction authorizes no probes or execution. HEAD at correction intake is `41125b5fb5dc35563aeb161d1087b371fa437264`; prior source hashes remain unchanged. Review correction/evidence: NR04-14 and `outputs/workflow/plan04-planning/selected-s2-correction/`.
**Latest planning update:** the user requests the hypothesis-driven controlled-initialization approach be incorporated into Plan 04 and reviewed for gaps. P1 now proposes IF-A source-only contract/critique → IF-B real cold-role registration/schema → IF-C failure/deadline/cleanup → IF-D joined E1. This advances the S2 feasibility proposal, not implementation or effect authorization. No final numerical admission packet exists; every gate is unrun. §8 separates discovery from defect correction, defines shared budget consumption, requires informative repeats and prevents using IF-D or a new agent as a budget reset. Cleanup remains obligatory after launch-budget exhaustion; unknown cleanup blocks new work. See NR04-13 and `outputs/workflow/plan04-planning/initialization-feasibility/` for this documentation revision. Older “latest” entries below are prior discussion checkpoints, not expanded authority.

**Current direction:** S2 controlled initialization is selected; the user identified and corrected the obsolete undecided-strategy wording. No native initialization or implementation is authorized by that choice. Current authority remains documentation/review only: no package tests, containers, further dependency probes or native/provider/production effects. No commits/pushes. Preserve source/user edits, including `.agents/references/quick_notes/hermes_plan_04_review.md`.

Current plan references: [Plan 04](../event-mapping-refactoring_plan_04.md) G04-13/P1 selected S2 and A04-17 acceptance; §8's shared budget; [review ledger](../event-mapping-refactoring_plan_04-review.md) NR04-10–14; [S1 contingency](../event-mapping-registration-only-design.md#first-implementation-contract-to-approve); [cross-agent memory](../../quick_notes/session_memory.md#plan-04-checkpoint--2026-09-22--discussion-hold). The blocked E1 is server startup/catalogue loading, not the later P5 two-seed simulation. The IF-A–IF-D path is selected but unrun; no four-way strategy decision remains pending by default.

| State | Evidence level and limit |
| --- | --- |
| Implemented | Offline readiness, already-reset seam, request envelopes, shared admission/drive and cancel lock fix; E0 configuration; synthetic execution composition/owner/schema/client and lifecycle code. Registration-metadata redesign is **not implemented**. |
| Isolated-tested | Historical initial 1,603 case occurrences/10 cohorts and later 563/six cohorts, recorded below with exact source manifests. E0/query compatibility and seven in-process lifecycle cases passed. These overlap, are not additive coverage, and are not new test runs or E1 acceptance. |
| Blocked/unverified | E1 has no installed submission→worker→result positive; active installed-worker cancellation/shutdown/recovery remain unverified. Real catalogue/schema registration crosses an unapproved native initialization boundary; metadata-only alternative has design review only. |
| Live/deployed | No live scene/policy trial, production access or deployment acceptance is established by these isolated/design checkpoints. Runtime-class parity/native calibration remain separate gates. |

Why stopped: static/image-byte evidence reaches `wp.init()` and native library/initializer effects before conditional CUDA handling; the strict no-CUDA permission does not establish a safe expanded-import route. `proposal_only=True` avoids later USD grounding but does not solve the present import/schema cascade. The user now explicitly wants design debate, not continued probing.

Source checkpoint at consolidation intake: HEAD `acda01b159d8ce07e86330c12727ba9d343b7009`, existing dirty/untracked work preserved. During documentation work, HEAD advanced outside this task to `fd67f5022f20d6854cf407989d10eac153081313`; the captured dirty Python/RST source bytes remained unchanged, and that commit is preserved. No assistant commit/push was made. Baseline dirty-source hashes and final document-only checks are retained in `outputs/workflow/plan04-planning/registration-consolidation/`; previous runtime evidence retains its own source identities. Resource state: the prior stopped checkpoint records all exact owned test jobs/resources cleaned up; this documentation pass launches none and makes no new live resource-state claim. On eventual resumption, recheck source/evidence and exact owned resources rather than assuming old agents/jobs remain alive.

**Next step for selected S2:** prepare IF-A's explicit initialization/admission contract under bounded source-only authority, settling exact effects/dependencies, files/tests, limits and acceptance. This is contract preparation, not renewed S1–S4 selection. It permits no production edits, package execution or runtime probes and precedes implementation/test approval. Only after that approval may S2 proceed toward real registration/schema and joined E1. The shared investigation budget and execution hold remain; no reset, automatic extension or new investigation follows from this documentation correction.

#### Historical registration investigation and prior verification

The entries below preserve prior evidence and approval chronology. Present-tense directions, pending delegates and “current/latest” labels inside this historical section describe their original checkpoints, **not active work or permission to resume**. The discussion hold above controls.

**Registration-only design investigation completed; implementation not authorized by this decision.** The user selected “Keep strict no-CUDA limits; investigate a registration-only design.” Deliverable: [registration-only design](../event-mapping-registration-only-design.md), based on independent read-only analyses `deleg_42c38c89` and fresh design challenge `deleg_b369d6f0` (PASS limited design). Recommend an opt-in immutable first-party metadata view sharing canonical declarations with runtime binding, unchanged legacy class registries, pure catalogue projection and explicit context through the existing real schema. Preserve hidden schema members, inheritance/signatures/docstrings/order and separate full metadata identity from the vocabulary digest. No fake class registries or historical digest substitutions.

The selected E1 proposal-only generate/refine path returns before USD/native grounding; parent traced its existing early returns and the critic confirmed them. Registration separation therefore has a plausible scoped route, not proven runtime acceptance. Ordering/version, extension/mutation policy, exact wire binding and eventual runtime parity remain implementation-contract decisions. Source fingerprint/document verification: `outputs/workflow/plan04-implementation/installed-execution/registration-only-design-verification.json`. Only design/handoff documents were authored in this phase; no package imports, tests, containers, native initialization or production-source edits. The execution STOP below remains in force until a bounded implementation contract is approved.

**STOP checkpoint after the approved registration investigation:** fresh adjudication `deleg_1c02878c` found no established no-CUDA expanded-import route. Actual pinned-image source confirms the catalogue/schema registration path reaches Isaac Lab `wp.init()`, which constructs `Runtime`, loads `warp.so` through `ctypes.CDLL`, and calls native `wp_init` before conditional CUDA queries/device enumeration. Native DLL/initializer effects are not established by the retained Python units. No expanded registration import or Warp initialization was executed; no GPU work is claimed to have occurred. Quiet mode, a later CPU default, absent devices or an invented `enable_cuda` flag cannot satisfy the exclusion.

Latest verified bytes run: `arena-neo4j-a82eef70f240418d8897a8c566bb9a02`; `registration-context-parent-verification.json` checks 65 complete selected units, exact whole-file identity and cleanup. The preceding oversized-file refusal `9640c86c08ca4898be5c4e056a9c242e` is retained. Its measured 476553-byte exact-file exception was explicitly reviewed (`deleg_05f94267`) while total-read/report/resource budgets stayed unchanged; selected units are not complete initialization closure. The final current-source fingerprint is `b88690480fd3e6e120ac5986fcdd25632d1adc92f3e878e8fe2110c36add3a7a`, recorded in `installed-execution/registration-final-checkpoint.json`. The 563-case checkpoint below still matches current source. All owned jobs have ended; cleanup and fresh owned-resource absence are verified.

**Next decision, not permission:** `outputs/workflow/plan04-implementation/installed-execution/native-initialization-decision.md` consolidates the evidence and alternatives: retain strict no-CUDA behavior and investigate a registration-only design preserving real semantics, or separately review the exact native-initialization/probing boundary before any execution. Neither is an existing proven route or a live-trial authorization. E1 and Plan04 remain incomplete. Do not launch another initialization probe implicitly; preserve user changes and all failed evidence.

Registration amendment progress: reviewed bytes-only probe `deleg_1e67de99` ran in `arena-neo4j-d77d0ac20d0b496982d66097fd77047f` and completed candidate discovery without expanded imports. Parent source/evidence/cleanup readback is `installed-execution/registration-first-parent-verification.json`. It located the immutable Isaac Lab installation and pinned initializer/setup bytes; overall E1 remains failed at the existing catalogue import boundary. Checkout safety tracing raised a possible `warp.init()` path, not an observed GPU effect. Second fixed bytes-only source frontier (lazy-export stubs, exact Warp initializer/ops and Warp metadata) is source-frozen at 379 repository/383 total leaves in `registration-second-source-measurement.json`; `deleg_9492f0fa` is reviewing it before runtime. No new imports, simulation or CUDA work are admitted by these probes.

**Current checkpoint: original four slices plus E0 configuration and in-process lifecycle are isolated-verified; E1 joined execution remains RED while its approved import/registration amendment is implemented.** The user explicitly replied, “approve the bounded import/registration amendment,” approving the exact packet at `outputs/workflow/plan04-implementation/installed-execution/registration-import-approval.md`. This permits reviewed E1-only dependency imports/configuration registration from existing immutable bytes, not unrestricted native actions or a live trial. Freeze exact bindings and obtain independent pre-run review; do not fabricate catalogue digests, bypass real schema validation, broaden old cohorts or increase resources. This is not completion of Plan 04, P1/P2, or V0/V1/V2.

Amendment intake: unchanged HEAD `acda01b159d8ce07e86330c12727ba9d343b7009` / branch `dev/0.3.0-prerelease`; all existing modifications and the newly observed untracked review note are preserved. `deleg_8d6caf01` assigns separate E1 discovery/admission implementation and read-only import-time safety tracing. Neither may execute expanded imports before fresh review. The untracked `hermes_plan_04_review.md` has broader P1/P2 completion and live deployment/budget claims; those do not supersede this evidence-bound handoff or grant effects and are left untouched.

**Latest verified checkpoint (supersedes intermediate active/pending chronology):** `outputs/workflow/plan04-implementation/installed-execution/checkpoint-verification.json` records **563 passing case occurrences across six current-source cohorts**: core 459, existing scene 11, E0 1, GraphQL 43, query-network 42, lifecycle 7. Every recorded repository source hash matches, each passing cohort has zero forbidden counters and verified cleanup/fresh owned-resource absence. These are overlapping isolated cases, not E1 acceptance. Historical earlier totals remain snapshots, not newly rerun whole-Plan04 proof.

Implemented but unverified end-to-end: fixed synthetic API composition, owner/drive, typed submit/result and shutdown observation. Verified narrower integrations: query-only compatibility, private setup selection, real-owner/in-process ASGI lifecycle with inert external ports, actual YAML binding and existing scene regressions. No installed submission-to-result positive, active-worker/incomplete-body stop, network cancel/resume/recovery or live/deployed trial has passed.

Latest E1 diagnosis `arena-neo4j-9114cd29901a48fdb3e6c46a64f85607` records catalogue hashing importing the model agent/OpenAI in the server role. Real default registries and worker schema validation further require Torch/USD/Isaac Lab registration imports; pure formatting extraction cannot remove that closure. Fresh source adjudication `deleg_05bb2b89` recommends exact E1-only import/registration admission over existing immutable bytes, preserving runtime/network/provider/legacy denials. The approval packet discloses native-library initialization risk and stops on acquisition, broader paths/resources or actual native actions. Owned containers/networks are absent; no background test/server remains. Parent and implementers made no commits/pushes, real credential reads, shared-service changes, installations/downloads, production access or GPU/native simulation.

| Slice | Accepted scope | Final verification |
| --- | --- | --- |
| Offline operator report | `workflow.cli setup-readiness [--selection FILE]`; public selections/shared aliases, outcome-scoped blockers, no credential/probe/grant effects | Fresh critic PASS; actual emitted unresolved report at `outputs/workflow/plan04-implementation/setup-readiness/operator-report.json`; report included in final 479-case core/CLI cohort; query-network compatibility passes |
| Already-reset settle helper | Explicit initialized observation avoids another reset while preserving legacy single-reset and strict hold/charge/reject mechanics | Fresh critic PASS; final 164-case isolated trajectory cohort. Actual native/reset-recorder integration remains unverified |
| Request-envelope callable | Optional exact-role direct-OpenAI request guard, serialized bounds, fixed clone rejection and actual assessment overflow tests | Fresh re-critic PASS; final 123 scene/128 backend cases. No installed profile/grant/rate guarantee or arbitrary-Python sandbox |
| Shared admission/drive and lock correction | One trusted owner receives a parameterless one-shot drive only after acknowledged fresh admission; replay never drives; keyed cancel uses authority→DB ordering after local stop | Fresh re-critic `deleg_0eea48b3` PASS; actual disposable-Neo4j fresh/replay/precommit-denial race RED→GREEN, unchanged 12 children/24 SDK calls. Not a durable queue, independent-owner exactly-once scheme or API activation |

Final parent readback: `outputs/workflow/plan04-implementation/final-verification.json` (reproducible via `verify_final.py` beside it): **1,603 JUnit case occurrences across 10 cohorts**, zero failures/errors/skips, current source matches, verified cleanup and fresh owned-resource absence. Includes core/report (479), trajectory (164), scene envelope (123), backend (128), ordinary Neo4j (611), scene/race (11), process (one composite), legacy foreground CLI (one composite), GraphQL queries (43), installed query network (42). Counts are not unique requirements or live trials. The final legacy CLI run is `arena-neo4j-8d285013254d4cfca010fd18e0718377`; its tracked process has exited successfully. Source/protocol/harness guards and protected deployment files remain unchanged except the explicitly listed application/test/doc edits; nothing staged or committed.

**Approved additional test scope:** `outputs/workflow/plan04-implementation/next-isolated-approval.md` defines the now-approved separately reviewed, explicit-only synthetic installed-execution cohort using existing pinned test images, owned disposable Neo4j, denied external egress and no GPUs/live keys. Existing query-only cohorts cannot witness workflow-owner execution. Approval permits additive harness/fixture work, not weaker old guards or any live campaign effects. Freeze exact roles, commands, source closure, child/resource/deadline limits and proof obligations and obtain fresh review before the first new-cohort run. Continue installed owner/command wiring through this joined verification route; do not substitute component counts or manual orchestration.

- Approved-cohort intake: HEAD remains `acda01b159d8ce07e86330c12727ba9d343b7009`, branch `dev/0.3.0-prerelease`; prior dirty/untracked source and documentation preserved. `deleg_d55c4ea2` assigns one bounded implementer to additive runner routing/new fixed execution harness/test only, with no new-cohort execution before review, and an independent read-only analyst to installed config/server/schema/client/application integration. Parent retains handoff and acceptance ownership. No new isolated result is claimed by this intake.
- E0 review checkpoint: `deleg_d55c4ea2` returned additive `workflow-graphql-execution` runner routing plus a fixed helper/test and `outputs/workflow/plan04-implementation/installed-execution/admission.md`. This first increment admits exactly two setup interpreters and **zero server/owner/SDK children**; it has not run and is not joined execution acceptance. Parent selected explicit v2 mode `isolated-synthetic-execution-v1` rather than ambiguous `execution`; fixture/spec amendment and fresh guard review are assigned to `deleg_bfcb107c`. Existing query-only guards remain in place. The ensuing joined slice must reuse one lifespan-owned shared application, retain drive independently of request cancellation, keep query/cancel capacity separate, and verify real owned synthetic workers plus fresh-client durable readback. No additional user approval is needed for increments within the approved cohort; each changed admission contract still requires independent review before execution.

- E0 execution checkpoint supersedes “not run” above: fresh pre-run critic `deleg_bfcb107c` PASS; parent run `arena-neo4j-b5f9731a949c496a876c048ec1ef709c` reached a genuine installed-loader assertion RED. Query setup returned 0/accepted; the v2 synthetic selection returned 2/rejected after the actual loader call. One JUnit failure, no errors/skips; both children verified, forbidden counters zero, cleanup verified and owned container/network labels freshly absent. Exact source/evidence readback: `outputs/workflow/plan04-implementation/installed-execution/e0-parent-red.json`. `deleg_ec10e264` now owns separate E0 exact-pair config correction and new E1 joined helper/test implementation. Runner/E0 guarded bytes stay frozen during the E0 run; E1 routing waits for that freeze to end. E1 requires its own pre-run review and remains unexecuted.

- **E0 accepted, configuration only:** fresh critic `deleg_00de0fbb` PASS. Parent final formatted-source E0 `arena-neo4j-79d8ba8c23d248f6a71c9bd7d94703bc` (one composite/two setup children) and query-network `arena-neo4j-9c98e28415f94988852d8243f5f3b9d2` (42 cases) pass with current-source/evidence hashes, zero forbidden counters, verified cleanup and fresh owned-resource absence. Record: `outputs/workflow/plan04-implementation/installed-execution/e0-parent-green.json`. Scoped production hooks pass. Only exact v1/query-only and v2/isolated-synthetic-execution-v1 setup selections are admitted; credential schema is unchanged and server execution is still unimplemented.
- E1 pre-run review `deleg_00de0fbb` permits the first bounded application-RED attempt after reviewed runner additions and source refresh. `deleg_0458f148` owns that application/run and historical metadata provenance preflight. **Positive-proof blocker:** inherited synthetic generation pause auto-expires; before E1 acceptance retain worker-side marker-removal versus timeout evidence and check matching start ticks/non-zombie worker state at detachment. Partial launch failures may require outer owned-container teardown; do not label that graceful application shutdown. No E1 positive or installed-owner acceptance is claimed.

- E1 first run `arena-neo4j-14297f9e2b104ce2ac5cc6294e6b199a` reached **application RED**: setup/admin and real launch succeeded, but query-only readiness lacked execution capabilities. One JUnit failure/no errors or skips; 11 completed process witnesses (10 direct plus server), zero model children/forbidden effects. Installed stop/status succeeded and outer containers/network were removed. Parent verified captured source/evidence and fresh label absence in `outputs/workflow/plan04-implementation/installed-execution/e1-parent-red.json`; child exact-ID/name checks and frozen fingerprint are in `join-first-run-verification.json`. Historical Git metadata now has confined pinned-source/projection validation before resources, with tamper checks retained; it is not fresh Git attestation. `deleg_adb44c75` separately owns production API/owner/commands implementation and E1 barrier-proof correction. No new runs until their source/inventory and admission review are reconciled; positive execution remains unverified.

- E1 production vertical is now source-implemented (fixed synthetic composition, one lifespan owner/drive, typed submit/result and cleanup checks), **not runtime verified**. Fresh review `deleg_3d3b23d1` found legacy closure overflow (GraphQL 335/128, network 361/96, E0 357/96) and physical stop waiting behind incomplete HTTP/drains. Correction round 1 `deleg_2d7a7b6e` owns fixed execution-loader separation plus independent stop initiation/explicit cleanup-unknown; old limits must remain unchanged. Retired-owner fixture now verifies exact clean historical retirement/HTTP identity rather than incorrectly requiring absence. E1 measured inventory was 379 repository/383 total leaves before the current correction and must be refreshed again.
- Barrier re-critic passed its explicit-removal/timeout and live-identity source contract, but runtime negatives remain unrun. Parent reproduced the fixed-name `.json.pending` collector rejection using the actual AST-extracted predicate, then admitted only exact `join-*.json` fixed pending counterparts; evidence `pending-evidence-{red,green}.json` is stdlib predicate evidence, not container cleanup acceptance. E1 reader guard now pins `result-status`/`result` (the actual cleanup-aware query), retaining 120 polls/spacing/budget. Both narrow harness corrections need final review/refreeze before the next run. No positive installed execution or bounded shutdown claim yet.

- Correction re-critic `deleg_e7539e1b` approved the next idle E1 attempt, not full lifecycle acceptance. Parent fixed audit explicit roots and scoped formatting/lint, refroze 379 repository/383 total leaves, and reran legacy E0 `6ed4d75ec69c4831968121e524c2d050`, GraphQL `a9de4c00ea184c0a815433ed6aee1fb4`, network `57648c9f2d3447f6a59fe21ecffb371d`: 1/43/42 cases pass with current-source/cleanup readback in `correction-round1-parent-regressions.json`. Original limits remain unchanged; active-body/worker lifecycle tests are still unadmitted/unrun.
- Next E1 `arena-neo4j-a26ef24f9ae84ffba3a8716b25787514` failed pre-owner startup: setup/admin passed, server returned 2 with zero owner/Bolt/SDK activity and zero forbidden counters; launcher reported `exited_unclean`. All ten pure detachment-checker negatives ran; they do not establish actual worker detachment. Outer cleanup and fresh owned absence verified, evidence in `e1-startup-failure-parent.json`. `deleg_814ec8be` is diagnosing the exact suppressed startup cause using only bounded nonsecret code-location/type observations before a minimal correction; no acceptance predicate or guard weakening is allowed.

- **In-process lifecycle slice verified:** additive `workflow-graphql-execution-lifecycle` passed fresh pre-run review `deleg_4eedc67d` and parent run `arena-neo4j-55d59952634946e198182aea27ea0df0`: seven cases, zero forbidden effects, no child/listener/SDK permissions, current-source/evidence hashes and cleanup/fresh absence verified. Same blocked-admission observation fails for hash-pinned historical owner and passes current owner; actual in-process ASGI token/body/query stop and sticky cleanup unknown tested with inert external ports. Evidence: `lifecycle-parent-verification.json`. This does not prove installed active-worker/control-socket shutdown or durable lease retention. Separate inventory 335 repository/339 total leaves; old limits unchanged.
- Startup diagnosis `arena-neo4j-7a249908cc9c41fe9a72a5cd8187efc3` identified actual `ModuleNotFoundError` at `document_yaml.py`'s `import yaml`, before owner construction. Source-only diagnostics record exception types/static sites, never messages/locals. Reviewed bytes-only probe `deleg_7a90f7b2`, parent run `arena-neo4j-40fc829fd7854bae98c91e15c4c7ebb6`, found PyYAML 6.0.3 physically present under immutable `/isaac-sim/exts/omni.pip.compute/pip_prebundle`, outside fixed query paths; initializer/metadata hashes and cleanup verified in `dependency-location-parent.json`. Both joined runs still fail startup; no imports/path expansion or installation was performed by discovery. `deleg_76709628` now owns an E1-only exact YAML-package binding with origin/metadata checks, pending fresh review before execution; query paths and all effect guards remain unchanged.

Remaining implementation/deployment work: private provider/prior-role storage/loading; actual model/price/capability choices; durable envelope/profile/grant and campaign-allocation binding; retained-prior recovery; visual grounding; candidate/seed/policy-worker joining; typed evidence/artifact API completion and configuration handover. Native calibration, production DB/paired recovery and live trials remain unrun and separately authorized. All operator selections in the delivered no-selection report are intentionally unresolved. No real credentials were inspected.

- The user activated Plan 04 through a standing implementation goal: begin with the operator-facing setup/readiness report, then continue dependency-ready source and isolated-verification slices with bounded implementer → fresh critic → parent verifier cycles. Starting branch `dev/0.3.0-prerelease`, clean HEAD `acda01b159d8ce07e86330c12727ba9d343b7009`. Canonical design: [Plan 04](../event-mapping-refactoring_plan_04.md) and its [review ledger](../event-mapping-refactoring_plan_04-review.md); Plan 03 modeling/contracts remain mandatory inputs.
- Authorized: source edits and existing repository-approved isolated tests, host formatting, sentinel-only setup tests, plus the explicitly approved additive installed-execution cohort described above. Not authorized: real credential inspection, production/research DB access, paid Arena model calls, GPU/native workloads, shared-service changes, installations/downloads, protected Docker/CI/submodule changes, unrelated harness admission changes, staging, commits or pushes. Missing model/endpoint/key choices remain explicit operator blockers, not reasons to stop independent software work.
- Intake plan/source identity inventory: `outputs/workflow/plan04-implementation/baseline.json`. Fingerprinting is not reviewed coverage. No live readiness or test result is asserted by this intake.
- First active slice: bounded pure offline setup/readiness report and actual installed CLI entry, with unresolved nonsecret selections, role/alias sharing, outcome-scoped owner-labelled blockers, and separate configured/access/capability/authorization states. It performs no credential discovery, service probes or execution grants. Subagent owns report/CLI and admitted CLI/readiness tests; parent owns this handoff and final verification.
- Setup/report implementation now supplies `workflow.cli setup-readiness [--selection FILE]`, pure `setup_readiness.py`, documented public schema/example and strict static errors. Child witnessed four RED→GREEN increments; parent combined CLI/readiness reruns `arena-f0-backend-11eeebc7977a` and `arena-f0-backend-6144f4dc2014` passed 84 JUnit cases (child also reports 17 passing subtests). The latter retains actual no-selection CLI stdout, copied unchanged to `outputs/workflow/plan04-implementation/setup-readiness/operator-report.json`. This is an unresolved operator inventory, not discovered operator configuration. Fresh critic `deleg_8e8d617f` pending. Owned report sources match the passing snapshot, but whole static closure drifted in concurrently edited `request_envelope.py`; final full-closure rerun is still required. Interim evidence: `setup-readiness/parent-interim-verification.json`.
- Setup/report fresh critic `deleg_8e8d617f` **PASS within offline scope**; parent clarified documentation that the format has no fields for secrets but cannot detect a key pasted into an otherwise-valid public label. Report/CLI/docs scoped pre-commit passes. Existing installed query-only network regression `arena-neo4j-8a76f2e3cb544094827503ebe2b5bda5` passed 42 cases with captured/current source match, retained cleanup and fresh absence of owned resources; evidence `setup-readiness/query-network-regression.json`. Whole backend-closure final verification remains pending sibling source freeze. No fresh-process installed report invocation or live setup/readiness is claimed by in-process CLI tests.
- **First offline report slice accepted:** parent final rerun `arena-f0-backend-b65957896b02` passed 84 JUnit cases with the whole captured repository-source closure matching current bytes, zero failures/errors/skips/forbidden effects and fresh owned-resource absence. Source/evidence readback: `setup-readiness/parent-verification.json`. Actual unresolved operator report matches final CLI stdout byte-for-byte. Together with fresh critic, scoped hooks and the 42-case existing query-network regression, this closes only the offline report slice; private provider-role storage/loading, observed readiness, installed execution and live choices remain open. P0 report assumes generation/visual assessment/repair/prior reads, explicitly not generic contract admission.
- Dependency analysis proceeds read-only in parallel to identify the smallest joined installed-client/application slice and independent provider/visual work. No source completion is claimed yet. Existing Plan 03 isolated/native-unverified boundaries below remain unchanged.
- Independent nonoverlapping implementation slice also active: expose already-reset hold/settle mechanics in `native_realization.py` while preserving legacy reset-at-entry; only that module and the admitted trajectory-assessment test file are owned by its implementer. This is a synthetic/CPU lifecycle seam, not native PhysX proof or a concrete policy worker.
- Already-reset implementer completed RED `arena-f0-backend-96c023c395f6` (17 missing-keyword failures) → GREEN `arena-f0-backend-fe4ac25b26cc` (164 cases). Parent rerun `arena-f0-backend-5ee06e17de29` independently passed 164 cases with matching current-source hashes, zero errors/failures/skips/forbidden effects and fresh owned-resource absence. Native-source pre-commit and trajectory-test flake8 pass. Fresh read-only critic `deleg_a2cbe978` is pending; do not claim accepted slice yet. API adds explicit `initialized_observation` to `initialize_and_settle`; omitted value preserves reset once, supplied value reuses hold/step/reject logic without resetting. Evidence: `outputs/workflow/plan04-implementation/already-reset/{evidence,parent-verification}.json`.
- **Already-reset helper slice accepted (isolated only):** fresh critic `deleg_a2cbe978` passed against the same scoped hashes after tracing actual runner/autoreset and capture callers; parent independently verified execution above. No hidden reset or changed strict gate found. Keep subsequent actual `post_reset` composition, recorder reset/index, late-terminal mutable-buffer and retained-frame-content witnesses assigned to the policy/capture integration slices. Helper observations are in-process references, not immutable evidence. No native calibration, concrete policy-worker or live acceptance follows from this narrow closure.
- Parent baseline `arena-f0-backend-7ad42dcb295c` passed the 19-case scene-model singleton with zero failures/errors/skips and all forbidden-effect counters zero. Captured/current source matched; owned-container/network absence was freshly checked. Record: `outputs/workflow/plan04-implementation/scene-engine-baseline.json`. Baseline synthetic SDK evidence does not establish new functionality or live capabilities.
- Next independent work active: bounded opt-in request envelope through actual SDK/scene entrypoints (owns transport/scene-engine sources and singleton tests, no profile/grant enablement), and narrow shared admission→once-only-drive extraction (owns application.py and admitted import-boundary tests). Both require RED/GREEN, fresh critic and parent reruns before acceptance; neither enables installed live execution.
- Request-envelope implementation returned: explicit optional `RequestEnvelope` and per-approved-role map on `BoundedSceneModels`, detached kwargs, actual SDK serialized request and final HTTPX pretransport validation with single-send allowance. V1 is direct official OpenAI chat-completions only, PNG structure/dimensions/CRC validation rather than pixel decoding, universal trusted pricing attestation rather than actual billing. Parent reruns `arena-f0-backend-8d08fa99ee3c` (106 scene-model cases) and `arena-f0-backend-4394af379463` (128 backend cases) passed with final captured-source match, zero forbidden effects and verified/fresh owned cleanup. Evidence: `request-envelope/parent-{verification,backend-regression}.json`. Fresh critic `deleg_9e4d73e4` pending; do not claim accepted envelope slice or durable profile/grant integration yet.
- Envelope critic `deleg_9e4d73e4` passed the current direct scene-tool call path but identified a proposed public SDK `copy/with_options(http_client=...)` escape outside those callers, and missing actual `assess` overflow coverage (direct SDK image negatives were insufficient). Correction round 1 `deleg_43499924` is reproducing the real-runtime clone path, adding explicit unsupported-clone rejection while preserving absent-envelope legacy behavior, and testing retained-image negatives through actual assessment. It may add a bounded auth-resend witness. No code-sandbox or provider-price guarantee is claimed. Final envelope/core fingerprints and regression readbacks must be refreshed after that correction; prior passes remain historical snapshots.
- **Request-envelope callable slice accepted after correction:** real-runtime RED `arena-f0-backend-37167d1c4e18` reproduced both public clone aliases sending an oversized completion with zero charges; explicit clone rejection closed the supported callable path, preserving absent-envelope cloning behavior. Actual `assess` dimension/byte/count negatives allow only its separately charged constructor ping; HTTPX auth replay is two yields/one send/one charge (not SDK token refresh). Fresh re-critic `deleg_33408c52` PASS. After host pyupgrade moved `Mapping` to `collections.abc` and import ordering was normalized, all scoped production/doc hooks passed and parent final-source reruns `arena-f0-backend-eebd605b75ac` (123 scene cases) / `arena-f0-backend-b3d2a40d047e` (128 backend cases) passed with matching captured sources, zero forbidden effects and fresh owned cleanup. Evidence: `request-envelope/parent-final-{scene,backend}.json`. Full provider-rate derivation, durable profile/grant binding and installed activation remain unjoined; no arbitrary-Python/credential sandbox claim.
- Shared admission/drive implementation returned: `ForegroundWorkflow.admit()` yields public result plus an owner-retained parameterless one-shot callable only for acknowledged fresh admission; synchronous `run()` delegates and replay never dispatches. This assumes one trusted root/authority interlock, not an exactly-once multi-owner queue. Child cohort `arena-f0-backend-9910b04b0f31` retained one integration failure from the historical transport-body fingerprint. Parent confirmed `CallAllowance` AST unchanged and restored its original formatting/fingerprint; updated only the explicitly versioned `bounded_client` fingerprint for the intentional opt-in envelope extension (legacy SDK regressions retained), then minimally formatted the new admission return. No case removed/skipped or harness guard changed. Parent final core cohort `arena-f0-backend-1eba85748452` passed 455 cases with captured/current source match, zero forbidden effects and fresh cleanup; legacy scene `arena-neo4j-f8569045927f4d9b85fbd0fa9b3dab13` (11 cases) and process `arena-neo4j-60dda14d40e64fcf9627004f6e727571` (one composite) passed final-source verification. Evidence: `admission-handoff/parent-*.json`. Fresh critic `deleg_33e840da` and tracked legacy CLI `proc_92a2dc181e46` remain pending. No API execution/owner queue/restart acceptance yet.
- Admission critic `deleg_33e840da` found an inherited same-root cross-thread authority→DB / DB→authority inversion: admission holds `mutation_guard` during control-locked DB reads/submission, while keyed cancellation calls `protect_public` under the DB transaction. Other scoped seam properties passed source review, but concurrent-owner acceptance is blocked. Parent confirmed `cancel_command` is the sole production callback-under-DB case and its only application caller is `cancel_keyed`. Correction round 1 `deleg_856e21a6` owns application/import-boundary and existing scene-Neo4j tests to reproduce and fix consistent ordering after local stop, preserving precommit screening and worker waits outside locks. Actual disposable-DB race evidence is required where admitted; no schema-wide rewrite or weaker guard authorized. Prior passing snapshots do not close this finding. Legacy CLI run `arena-neo4j-30a2153302b64399b165e0c5d5e30c0d` remains tracked/running under its unchanged collector deadline.
- **Joined execution verification admission blocker (source-confirmed):** existing GraphQL ASGI/network cohorts are deliberately query-only. `scripts/workflow_process_harness.py:252` denies ForegroundWorkflow construction; `scripts/workflow_graphql_network_harness.py:45–92,120` fixes query-only CLI roles/guards. Legacy scene/CLI cohorts have their own exact child traces and are not installed execution proof. A separately reviewed additive installed-execution test cohort is required before claiming the joined submit/owned-drive/network acceptance. Do not weaken existing guards/limits. Continue independent admitted software slices, then include this narrow authorization request in the consolidated blocker packet.

### M2/M3 resumption — adapter/lifecycle implementation, not live milestone closure

- Implemented `native_realization.py` (typed builder reuse, action-term-correct DROID hold, strict unrounded linear-speed `< 1e-3 m/s` gate), `native_capture.py` (SHA-bound executable settings, one reset, absolute windows, bounded RGB/raw measurements and immutable receipts), and separately fenced capture/assessment with shared-GPU release after verified cleanup and before model waiting. `native-unverified` remains explicit; no physics calibration or successful A2 manipulation is claimed.
- Added credential-free native/numeric child transports over existing owned-process supervision, plus explicit V2 foreground composition. Receipts are retained and emitted before potentially process-terminating Kit shutdown; the receiver still requires genuine zero exit and verified cleanup. These new child paths were exercised with synthetic native/process seams and real local pipes/artifacts, not launched native children.
- Implemented the `policy_evaluation.py` → existing `rollout_policy`/`TaskBase` bridge, pure policy codecs, actual Arena episode-JSONL decoding, deadline checks after blocking callbacks, duplicate-reset rejection, and durable required-policy handoff in the existing Neo4j transaction boundary. Scene acceptance remains independent of workflow/policy outcome. Supported policy semantics are one exact task-success-rate criterion mapped to its frozen complete-episode threshold; other rubrics/obligations fail closed. One retained trial is bounded to 512 episodes. A2 and another task/embodiment are synthetic contract fixtures, not operational adapter certification.
- Independent review counterexamples were reproduced and corrected: deadline crossing before physics; duplicate reset aggregation; wrong graph-subject/runtime-object mapping; real Warp vec3 ProxyArray contact shape; criterion/aggregation mismatch; budget-stop projection; producer-failure loss of scene facts; oversized episode collections; historical producing-intent identity. Capture and policy windows now charge their distinct reservations. Real CPU ProxyArray tests retain endpoint/filter/finiteness checks.
- Extended retained GraphQL queries with policy budgets and typed trial/pin/episode projection. The installed API remains query-only: no execution mutations, native launcher enablement, or live GraphQL-submitted V1/V2 run was added. The concrete native/numeric workers exist; a supported owned policy-worker composition with verified runtime/task/checkpoint adapters remains to be joined to the foreground execution composition. Do not label all M2/M3 acceptance gates complete merely from these component and durable-lifecycle tests.
- Reused the admitted isolated harnesses without increasing source limits. Extracted pure policy codecs and shared synthetic fixtures to keep optional native dependencies out of V1 imports. Source-bound final run counts, hashes, cleanup readback, and interrupted attempts are recorded under `outputs/workflow/plan03-implementation/m2-m3-parent/`. Operator/developer boundaries and reproduction commands: `docs/pages/example_workflows/agentic_env_gen/workflow_native_policy.rst`.
- No GPU simulation, real policy/provider inference, credential inspection, research-database access, shared-service modification, protected deployment-file changes, staging, commit or push was performed. Full native calibration, live two-seed A2 pilot, installed execution composition and joined V1/V2 acceptance remain open. Existing query-only M5 evidence below retains its original narrower scope.

### Plan 03 campaign — V0 in progress; modeling prerequisite reviewed

- Starting HEAD: `8f40c55584c4998ac657128f433d4dd407a2905b`, branch `dev/0.3.0-prerelease`. Existing uncommitted plan-03 and review-ledger amendments are preserved. No commit/push is authorized.
- Authorized: source implementation and existing admitted isolated tests. No real credential access, live inference, native/GPU workloads, shared-service changes, research-database operations or protected Docker/CI/submodule changes without additional explicit authorization. Missing dependencies/images are not permission to install, pull or weaken isolation.
- Canonical requirements and design remain in plan 03 and its [review ledger](../event-mapping-refactoring_plan_03-review.md). Neo4j is the sole application database; GraphQL is the application boundary; A2 is a reference fixture; dashboard work is deferred. The coding agents implement the application's coordinator, not an external-agent runtime workaround.
- Current activity: independent network rerun `arena-neo4j-e4f05d3c665347a79a28471ad19cdc03` passed 42 cases with zero forbidden counters, 23 fresh CLI processes and 3 server witnesses; source, case order and fresh disposable cleanup verified (`graphql-query-launch/implementation/parent-network-verification.json`). Configuration/client and lifecycle/admission review results `deleg_10da5ed4` are pending delivery; selected-contract acceptance remains PARTIAL. A separate unreproduced client/loopback-instance binding hypothesis is recorded, not treated as a confirmed defect. Alias/identity and adversarial lifetime/transport/readback gaps remain open. Accepted ASGI bytes are unchanged; no deployment, protected topology changes, real credentials/shared services/native/GPU/live-provider work or commits/pushes. Full V0 remains unfinished.
- Parent baseline: `python3 scripts/run-workflow-neo4j-checks.py workflow` passed in `arena-neo4j-cde160305e794466938a61bac471d51c`: 139 JUnit cases, zero failures/errors/skips. Captured and current repository source hashes match; the generated `closure.json` is checked against captured bytes, not mistaken for a repository file. Container/network cleanup is verified and exact recorded names are independently absent. Local readback: `outputs/workflow/plan03-implementation/store-baseline-verification.json`. This is real disposable-Neo4j store regression evidence, not native/provider/GraphQL acceptance or a claim about nonexistent forbidden-counter fields in ordinary workflow mode.
- Each source slice follows witnessed RED → minimal implementation → GREEN → fresh critique → independent verification. At most three correction rounds for the same unresolved finding; continue other authorized dependency-ready work before reporting a terminal blocker. Preserve source-bound evidence, failed runs and cleanup records. Do not interpret an agent summary or a passing synthetic test as live acceptance.
- GraphQL provisioning is authorized and its bounded image/import increment is accepted. Three pinned additions remain Strawberry 0.327.7, graphql-core 3.2.6 and cross-web 0.6.0; selected existing dependency versions/origins were exercised under the fixed denied interpreter/path order, not merely found in metadata. Incidental Rich requirements (including markdown-it-py) are recorded, not a claimed complete optional dependency closure. The real application/schema/lifespan/Neo4j cohort is a separate in-progress increment. Earlier metadata-only absence observations remain historical evidence, not current installation status.
- Dependency discovery: the stdlib-only probe in the existing import-boundary file found FastAPI 0.120.4, Uvicorn 0.52.4 and HTTPX 0.28.1 metadata/package directories, but no Strawberry/graphql-core metadata or package directories in 18 checked read-only roots of approved image `sha256:e20b3cc8258b793aaf1fe47c130f54e677fa9c0a6427991cfc1045b743162da5`. No global absence, framework import or compatibility claim. The singleton invocation exposed an unchanged first-test import-order assumption (103 pass/one failure); parent preserved it and ran the normal three-file cohort with the same image/manifest: `arena-f0-backend-bde061763b44`, 494 pass, zero failures/errors/skips/forbidden counters, matching source and verified cleanup. Raw coverage and readback: `outputs/workflow/plan03-implementation/graphql-dependency-probe/parent-{verification,observed-metadata}.json`. No acquisition or expanded service testing was performed.

### Pending design: producer-target evidence discovery

- Read-only source analysis `deleg_26f10b0f`, checked against `neo4j_store.py:3689–3759`, identifies the retained observe intent as authoritative for the producer's intended candidate target. It does not establish correct observed realization/cohort. `finish_scene` adopts stale-cohort observations even when assessment and `FOR_CANDIDATE` are absent; the existing v1 evidence point view must keep `candidate_id=None` in that case.
- Proposed next bounded contract, not implemented: creation-only evidence membership codec plus `producer_intent_id` and explicitly named `producer_target_candidate_id`, derived from the retained observe intent in the adoption transaction. New original/repair candidate creation may establish a separate coverage-protocol marker; replay and old candidates must never be silently stamped. Missing coverage means provenance unavailable, not empty history. Require exact target/candidate agreement without interpreting it as successful observation; preserve current IDs, payloads, assessment edges and point-read semantics.
- Enumerate adopted operational observations, not every artifact, failed verification, unknown execution or rejected repair. Compact parent/query/scope-bound pages need exact candidate/run ownership, strict metadata and lookahead checks, explicit nonunique target/record indexing and real first/continuation PROFILE evidence. Indexed selected-row validation cannot audit damaged omitted membership. Quiescent writer cutover and no backfill remain explicit limits.
- Joined acceptance must distinguish failed-parent versus accepted-child observations and include stale unassessed child evidence by producer target while leaving its old point-view candidate/assessment absent. This applies only to current synthetic combined observe/assess adoption; separately fenced native capture/assessment remains future versioned work. Actual implementation and Neo4j profiling are pending resolution of the existing test-resource gate; no additional user-domain choice was identified by the study.

### Verified increment: retained event source identity

- `workflow/neo4j_store.py` now projects the already-stored `source_id` into frozen `WorkflowEvent`, with a trailing optional `None` default for legacy missing/null sources and positional construction. No write, existing ID or schema-version change. This is partial G10/T10/T15, not a complete typed causal envelope or GraphQL traversal.
- Witnessed implementer RED: `arena-f0-backend-5a012e5d3d59` (88 cases, 3 failures) and `arena-neo4j-03ea2e8a7a61432ab1ca2b4c7ea087d8` (140 cases, 6 failures). Parent read actual JUnit and captured source; no errors/skips. Implementer GREEN: `arena-f0-backend-254a8ec6e93b` and `arena-neo4j-366cdf8a75e64c1faa2d726c9cdc5bba`.
- Fresh critic `deleg_e1f5613d`: PASS within the narrow contract after tracing consumers, legacy construction, actual Cypher and tests. Parent independently reran the exact backend/store-import pair in `arena-f0-backend-46be0112eeec` (88 passed) and real store suite in `arena-neo4j-01a350374244424abda1c0a12af815b6` (140 passed). Both have zero failures/errors/skips, matching captured/repository source hashes and verified cleanup; exact owned container/network names are absent.
- Evidence root is `web/arena-workbench/tests/e2e/functional-v7/.runs/`; readback record is `outputs/workflow/plan03-implementation/event-source-verification.json`. Verified production SHA-256 was `db4ba5acee74037f4125918b7bd33543651ee283f8db40584054a5ebc8a32b23`. Later edits require fresh verification; do not apply this result to changed bytes.
- Focused lint/diff checks passed. Neo4j test formatting has pre-existing Black/isort differences; unrelated formatter churn was reverted rather than claiming clean whole-file formatting. Process/scene/CLI and native/live suites were not rerun for this increment.

### Historical scene-query verification record

The intermediate checkpoints below retain their original scope; their pending language is superseded by the final closure record following H04-R1, not a claim of full V0 acceptance.

Historical scene-query increment under verification: `WorkflowService.read_scene_candidate`, `read_scene_decision`, `read_scene_assessment`, `read_scene_evidence` call bounded scoped repository reads and return frozen views from new `workflow/queries.py`. Authorization precedes store IO and mandatory public protection precedes return. Existing writers remain unchanged. Parent read implementer RED/GREEN proof/source/JUnit artifacts: initial backend RED `arena-f0-backend-2a0d0b0b8283` (164 cases, 4 failures), initial real-store RED `arena-neo4j-568a8312f7c646afbe801049bcd2f33a` (140, 4), integrity/evidence-binding REDs `arena-neo4j-4d8b9486c2554adebefd208b1860c5d7` and `arena-neo4j-427a322ce8834ec6b093fb1662a097f4` (140, 1 each), then backend GREEN `arena-f0-backend-9e0b762338e6` (216 passed) and store GREEN `arena-neo4j-4a5946152530488fa2af915a2d047350` (140 passed). No errors/skips; cleanup/source hashes verified. Independent acceptance is pending; no GraphQL/pagination/new-producer/byte-verification claim.

Targets below are existing seams or proposed implementation locations, not assertions that new modules already exist. Exact source revisions, verification commands and evidence paths are filled when each slice is exercised. S00's modeling prerequisite passed; S03 has a verified event-source increment and historical-query work in progress; other rows remain pending. No complete R/G/T obligation is marked satisfied by this inventory alone.

Historical-query parent reruns: backend `arena-f0-backend-eed6f2559f5b` (216 passed) and store `arena-neo4j-de6129fe41f941f1994538bd5c5012a9` (140 passed), zero failures/errors/skips, matching source and verified/independently absent owned resources. Record: `outputs/workflow/plan03-implementation/historical-query-verification.json`. Passing these suites does not discharge the fresh critic's counterexamples:

| Finding | Counterexample / required correction evidence | Status |
| --- | --- | --- |
| H01 | Escape-heavy writer-admitted candidate exceeded query/public encoding ceilings. Candidate-only limits now derive from the existing 1 MiB scene bound plus finite record/wrapper allowances; whole-response protection stays single-call/reject-only, legacy 2 MiB defaults unchanged | Reproduced RED and corrected GREEN; fresh review/regressions pending. Other unbounded writer shapes/scope strings are not claimed universally readable |
| H02 | Valid later observe intent for the same run/candidate/action substituted for an earlier decision's next intent. Present links now require exact writer derivation `identity(decision_id, "intent")`; legacy None preserved | Reproduced RED and corrected GREEN; fresh review/regressions pending |
| H03 | Corrupt retained-contract parsing leaked a sentinel before protection. Historical projection now translates retained decoding/validation failures into static corruption errors with suppressed chaining, including sibling parsing paths | Reproduced RED and corrected GREEN; exception/traceback sentinel assertions pass; fresh review pending |
| H04 | Exact lookups used label scans despite LIMIT. Six nonunique scoped RANGE indexes are now explicit admin schema requirements, verified ONLINE; actual plans show index seeks and duplicate detection remains | Reproduced RED and corrected GREEN; no query-triggered DDL. Broader setup regressions pending because schema prerequisites changed |

Correction round-1 parent readback is `outputs/workflow/plan03-implementation/history-correction-round1-readback.json`, covering six retained RED runs and final GREEN `arena-f0-backend-c09750f6b1e3` (227 passed), `arena-neo4j-904105da484c4072a8d58aa09277e49f` (142 passed). Parent verified JUnit outcomes, captured/current GREEN source hashes, cleanup receipts, `history-plans.json` and `history-indexes.json`. This is not yet independent acceptance of the corrected bytes. Added indexes must be explicitly provisioned and ONLINE before schema verification/open; no shared database was migrated.

Re-review H04-R1 is a source-supported cold-start race, not an observed failed run: existing process/scene/CLI test-admin fixtures could verify immediately after CREATE INDEX while population remained pending. Parent added `CALL db.awaitIndexes(10)` after DDL in those four setup sites, matching the ordinary store fixture's existing bounded wait; runtime verification/reads still perform no DDL/wait. Static AST ordering checks are retained at `outputs/workflow/plan03-implementation/history-admin-order-checks.json`; actual final-source regression and focused verification are pending. This test-setup correction changes no production writer or harness admission policy.

**Final component closure:** `deleg_2f914e41` passed the focused source review. Parent verified current-source, zero-failure/error/skip proofs: backend `arena-f0-backend-064e5eb590e1` (227 cases), store `arena-neo4j-74331cfd7c314eed94331359f0b46329` (142), process `arena-neo4j-eb2216cf58034bba80fdcebbb859c58f` (one composite case), scene `arena-neo4j-da9333bfe02c4f3395d92fa56c973f51` (11), CLI `arena-neo4j-52acadfe1431441b969c8ef9f613810d` (one composite case). The earlier process/scene passes had stale fixture hashes and were explicitly rerun. All final captured/current source hashes match; cleanup and exact container/network absence were verified. File hashes and proof paths are in `outputs/workflow/plan03-implementation/historical-query-final-verification.json`. This closes the bounded scene-query component and its regressions, not GraphQL/pagination/profile administration/full causal envelope or native/live acceptance.

### Per-run cleanup query — independent verification pending

`RunCleanupView` / `IntentCleanupView`, `Neo4jWorkflowStore.get_run_cleanup` and `WorkflowService.read_run_cleanup` implement the bounded M06/M15 query. They report compact exact retained cleanup references separately from current scope owner and hash public content independently of the internal control-lock revision. No overall clean boolean, OS verification or changed legacy `workflow_result` semantics is claimed. Three additional nonunique lookups require explicit admin provisioning/ONLINE verification; existing fixtures already wait for indexes.

Implementer evidence: initial core RED `arena-f0-backend-da4fb3295782` (299 cases, 5 failures), initial store RED `arena-neo4j-fc02adb4cd2e4228adc01ca3d6d50a59` (183, 1), final GREEN `arena-f0-backend-602a53650885` (305 passed) and `arena-neo4j-bcb8dd15fdd24ad6bef8a761501480a7` (216 passed). Six vertical increments are enumerated in `outputs/workflow/plan03-implementation/cleanup-query-verification.json`; parent revalidated their JUnit/artifact/source hashes and cleanup receipts in `cleanup-query-child-readback.json` in that directory, and inspected the actual no-scan lookup plans. Final staged source matched the working files at readback. Fresh critique and parent reruns remain pending, including investigation of the reported intermittent catalogue failure. Bounds are 1,000 intents, 64 KiB retained JSON, 8 MiB aggregate selected rows, 2 MiB public response and the existing transaction timeout; none permits truncation into favorable cleanup.

Parent reruns `arena-f0-backend-9bf4be1a6a47` (305 passed) and `arena-neo4j-33c4040d7a114c33b73176ff5d6fb876` (216 passed) have matching captured/current source, zero failures/errors/skips and verified exact resource cleanup. Readback: `outputs/workflow/plan03-implementation/cleanup-query-parent-verification.json`. These results do not close the fresh findings:

- CQ01: deleting the ownership JSON member during a reserved-but-claimed scene can default the fence to None and report `not_started`. Distinguish explicit writer-emitted null from missing metadata.
- CQ02: released then cancelled generation can lose its release timestamp while independent release authorization/receipt witnesses remain, yet report `known_unreleased`. Check bounded witness-presence flags without exposing private payloads and preserve actual unreleased cancellation.
- CQ03: nested validation queries while the outer result streams can buffer rows before the aggregate accumulator rejects them. Verify the actual admitted driver, drain/check before nested reads and bound fetch batches; do not confuse selected-row bytes with total process memory.
- PC03: retained evidence ties the sole `scalar_revision_float` failure to a unique-index/cached-revision plan, versus scope-index passes. Failed run `arena-neo4j-166e04e373db4edd98905f5d8004a486` is retained; physical float persistence versus equality-cached projection is not yet established. Require physical-property readback and deterministic index-path tests before changing code. Earlier catalogue closure remains historical evidence, not resolution of this newly discovered gap.

Correction round 1 reproduced CQ01–CQ03 on the actual admitted driver. Ownership JSON now requires explicit members before defaults; independent release witnesses are presence booleans only; cleanup alone opts into `fetch_size=1` and drains/byte-checks rows before nested validation. Driver 6.2.0 / Neo4j 5.26.30 / Bolt 5.8 showed pending-batch buffering under the old default, not the Bolt-3-only `_buffer_all` path. The limit covers selected serialized rows plus a bounded crossing row/batch and protocol/Python overhead, not an 8 MiB RSS promise.

PC03 is now deterministic: physically retained `2.0` hydrated as integer `2` under the composite equality-index cache. `properties(p).revision` also failed due to `PropertiesUsingCachedProperties`; that rejected fix's evidence is retained. Fixed trusted dynamic access `p[$revision_property]`, with parameter fixed to `revision`, preserves physical scalar validation under both tested index paths. Get/list/replay reject corruption independently without repairing it. Recheck this behavior on server/driver upgrades.

Parent readback `outputs/workflow/plan03-implementation/cleanup-correction-round1-readback.json` covers REDs, the rejected PC03 fix, and final GREEN `arena-neo4j-8c73a81522ca43f28613c3b9f9f7f5a2` (235 passed), `arena-f0-backend-6ef14e878639` (315 passed). JUnit, captured/final source and cleanup receipts match; actual buffer/physical-scalar observations are in the final DB evidence directory. Fresh independent review and broader regressions remain pending; this supersedes the unresolved-reproduction language above, not component acceptance or full V0.

**Final cleanup/PC03 component closure:** focused review `deleg_12a2c90b` passed. Parent verified final-source core `arena-f0-backend-9ea58cc5b60b` (315 cases), store `arena-neo4j-f6fe23961abf4186bde4abc85aa4ed76` (235), process `arena-neo4j-e6e5974259234d5cb62cdd21118dfc28` (one composite case), scene `arena-neo4j-5a637a7e8cb54f8188b723ca6ca860e5` (11), and CLI `arena-neo4j-27796f3f875e41848b5af36fc41cefb7` (one composite case). All have zero failures/errors/skips, matching source hashes and verified exact owned-resource cleanup. Record: `outputs/workflow/plan03-implementation/cleanup-query-final-verification.json`. This closes CQ01–CQ03/PC03 within their declared query/driver/optimizer scope, not native cleanup assurance or full G10/V0. Intermediate pending language above is superseded by this closure.

### Admission/frozen-intent readers — independent verification pending

New repository `get_submission_inspection` / `get_run_intent` and service `read_submission` / `read_run_intent` return frozen `FrozenSubmissionInspection` / `FrozenRunIntentView`. Legacy admission facts and exact byte digests stay separate from current lifecycle; unavailable original receipt revision/cause stay None. Frozen contract reconstruction rejects non-roundtripping codecs rather than supplying current defaults. Full-width counters and an intent-only content revision do not imply joined operational outcomes/actions/cleanup or a complete projection revision.

Implementer ledger: `outputs/workflow/plan03-implementation/admission-intent/final-verification.json`; parent verified all recorded cohorts against actual JUnit, source manifests and cleanup receipts in `parent-child-readback.json` beside it. Five genuine store RED increments precede GREEN; final source-matching runs are `arena-f0-backend-42f4ad1f76d0` (363 passed) and `arena-neo4j-8a06ab73173d4815b71a77c5c08fd96d` (295 passed), no errors/skips. Fresh parent reruns and critique remain pending. No CLI/GraphQL wiring, original-receipt invention, mutable catalogue lookup or execution permission is supplied by this component.

**Final admission/intent component closure:** `deleg_0da145b3` passed bounded source/caller review. Parent reruns `arena-f0-backend-a0dfb3974fa2` (363 cases) and `arena-neo4j-5d2ecefcbc1b4874bd44a463736b48ec` (295) have zero failures/errors/skips, matching captured/current source at verification and verified exact resource cleanup. Record: `outputs/workflow/plan03-implementation/admission-intent/parent-final-verification.json`. This supersedes component-pending language above; it does not close joined operational projections, permission-filtered actions, keyed cancel/resume, API/CLI or full V0.

### Pure provider-configuration extraction — verification pending

`workflow/provider_configuration.py` owns unchanged `PROVIDERS`/`ENDPOINTS`, `checked_config` and `reject_secret`. Legacy `web_api/provider_security.py` re-exports them while keeping `bounded_client`/`worker_environment` unchanged; foreground authorization imports the pure module/accounting with all function/class bodies preserved. This removes its eager web dependency without loading credentials or altering grants, private settings hashing or execution policy.

Parent verified actual RED `arena-f0-backend-9278df47040c` (364 cases, one cold-construction failure, forbidden counters zero), minimal GREEN `arena-f0-backend-5585b75d39de`, final core `arena-f0-backend-2e9f75f38d19` (392 passed), store `arena-neo4j-4a1e3290ef4b466eadd4a1c377c80021` (295 passed) and scene `arena-neo4j-945a10dc2401462fb71ec66c95080855` (11 passed), with captured-source/JUnit/cleanup readback. Durable records: `outputs/workflow/plan03-implementation/provider-extraction/child-verification-summary.json` and `parent-child-readback.json`. Independent acceptance is pending. Undocumented external rebinding of legacy module globals is not certified.

Next extraction prerequisite from `deleg_fced2e3b`: move existing orchestration methods, not a second coordinator; inject the current lease/receiver/scene/recovery/artifact/cancellation adapters and preserve a patchable compatibility subclass in examples. Leave `foreground_control.py` and `foreground_cancellation.py` at their existing paths: UDS code identity and the exact control-fixture file set prevent moving them under current harness admission. The independent installed runtime remains unfinished even after a core class extraction.

**Final provider-helper component closure:** `deleg_481f44b2` passed source review. Parent verified `arena-f0-backend-4dc24d75dfdc` (392 cases) and `arena-neo4j-0c002667ed2146a2bb81620772973cd4` (11 scene cases), zero failures/errors/skips, matching staged/current bytes and exact owned-resource cleanup. Source hashes and scope limits: `outputs/workflow/plan03-implementation/provider-extraction/parent-final-verification.json`. The guarded test reloads selected modules, not an entirely pristine interpreter; it proves real authority construction with inert callbacks, not grant issuance or installed runtime composition. Legacy module-global rebinding outside inspected callers remains uncertified.

### Shared orchestration extraction — independent verification pending

`workflow/application.py` now owns the existing `ForegroundWorkflow` methods with required trusted lease/receiver/scene/recovery/artifact/cancellation dependencies. The examples class preserves its constructor as a thin subclass; the old factory and synthetic capture bodies remain unchanged. Parent independently compared all 19 operational methods to the pre-extraction captured source, permitting only explicit adapter-call substitutions, and confirmed constructor-signature/factory preservation. Configured cached Black/isort/flake8 checks pass without source edits; early checks lacking the repository hook arguments were not valid project-format verdicts.

Implementer RED `arena-f0-backend-09026e09a091` (393 cases, one missing-application failure) precedes production extraction. GREEN: core `arena-f0-backend-68d1dc228530` (394), process `arena-neo4j-f9fe89e937184748a5c5f57939361f2b` (one composite case), scene `arena-neo4j-76abc9bf69c049a793259bcadbb8f8cc` (11), CLI `arena-neo4j-d041a2ce3a234a2b8bc6d4ec7bcf0b5e` (one composite case, 31 child launches). Parent verified JUnit/source/cleanup receipts against those artifacts. Records: `outputs/workflow/plan03-implementation/application-extraction/final-verification.json`, `parent-child-readback.json`, `parent-parity-check.json`. Fresh review and independent runtime reruns remain pending. No installed composition, native capture, live provider or GraphQL claim; construction-captured factory rebinding remains outside compatibility scope.

**Final shared-orchestration component closure:** `deleg_3a1535f7` passed focused review and independently matched operational parity. Parent reruns core `arena-f0-backend-35860c6c6460` (394), process `arena-neo4j-e313d5abd55d4e03a7214cc022fdd1d8` (one composite case), scene `arena-neo4j-c7fabe6d05294928b5436bb2e49f8b9e` (11), and CLI `arena-neo4j-c3186f009f6a4ee6a70e7084cce814b0` (one composite case) have zero failures/errors/skips, matching captured/current source and verified exact owned-resource cleanup. Record: `outputs/workflow/plan03-implementation/application-extraction/parent-final-verification.json`. The working orchestration is shared, but its installed live composition and independent API owner are not delivered. Intermediate pending language above is superseded within this component only.

Keyed-cancel prerequisite: reuse the existing cancellation transaction through a private transaction-taking helper, not cancellation followed by a separate receipt write. Current authenticated cancellation authority is an explicit check-only port, not inferred from read permission or provider credentials. Exact local stop precedes command lookup; original retained observation/disposition and fresh delivery remain separate. The source analysis is `deleg_d4439da9`; bounded decisions are in plan §7.4. Preserve fixed UDS adapter identity and leave keyed CLI/GraphQL integration pending separately reviewed admission.

### Keyed CANCEL — independent verification pending

Added bounded `commands.py` receipts/results, `ForegroundWorkflow.cancel_keyed(..., authorize_cancel=...)`, `Neo4jWorkflowStore.cancel_command` / `get_cancel_receipt`, shared `read_command` and the trusted stop-only adapter. The existing cancellation transaction is reused; receipt and cancellation cause commit together, while first retained delivery and current delivery remain separate. Explicit cancel authority is not read permission or credential resolution. Legacy functions/control module and parser/harness are preserved.

Parent verified retained RED JUnit and final source/cleanup records: core `arena-f0-backend-ba6a0d0a98f1` (420), store `arena-neo4j-232d94443bdb48d28d65d2cf74a5a503` (318), process `arena-neo4j-ed3d9094ee5541cf8bbe150383b4b48a` (one composite), scene `arena-neo4j-43e6f42d11cb4ef4b8ddf817904aa854` (11), CLI `arena-neo4j-50d3938255f945018e4711a52b1387d8` (one composite). Actual final total is 751 cases, no errors/failures/skips. Evidence: `outputs/workflow/plan03-implementation/keyed-cancel/verification.json` and `parent-child-readback.json`. The child's `proof_sha256` names the producer backend/client proof, not host `run-proof.json`; parent verified each against the correct file and independently hashed the host proof. An earlier interrupted CLI attempt remains failed evidence, not a pass; the tracked retry used unchanged deadlines.

Acceptance remains pending fresh critique and parent reruns. Real shared-handler/stop-only/Neo4j composition currently proves a pending-run cancellation, not active keyed worker/IPC or ambiguous-ACK composition. The existing 31-child unkeyed CLI suite is regression evidence only. Keyed CLI/GraphQL, installed cancel-authority integration, RESUME receipts and full G01/V0 remain explicit gates.

Parent keyed-cancel baseline readback: core `arena-f0-backend-8ce9245e04ce` (420), store `arena-neo4j-ca82a630c89e41a786f865f872133ce0` (318), scene `arena-neo4j-26ad0211ec65410ea0e160c20a06759c` (11), zero failures/errors/skips, matching source and exact resource cleanup. Record: `outputs/workflow/plan03-implementation/keyed-cancel/parent-baseline-verification.json`. Fresh findings override acceptance:

- KC01: write-path cause searches scan event history under the scope lock. Reproduce actual query plans; select/link the newly emitted event by exact scoped sequence and skip cause searches for nontransitions.
- KC02: inherited scene cancellation interleaving can regress `cancelled` to `cancel_requested` when late uncertainty follows exact cleanup; duplicate cleanup then returns without repairing state. Reproduce the real writer ordering, preserve terminal cancellation plus diagnostic uncertainty, and keep the command receipt immutable.
- KC03: cleanup-specific public rejection occurs outside the fallible cleanup boundary and can suppress a known receipt. Validate/screen optional cleanup there, omit rejected cleanup and re-screen the complete fallback, never suppressing receipt rejection.
- KC04: pending `no_owner` composition is insufficient active-handler coverage. Exercise the real same-process stop/finalizer with owned resources and unresolved cleanup inside an already admitted fixture if possible; otherwise retain the exact admission blocker. Active keyed IPC/CLI remains unclaimed either way.

KC correction round 1: exact sequence-index retrieval/linking removes KC01 scans without new indexes; late diagnostic uncertainty preserves terminal cancellation for KC02; cleanup-specific validation/screening and combined-envelope failures drop only optional cleanup for KC03, with complete fallback screening and fatal receipt-only denial. Parent read back all recorded RED/GREEN JUnit, separately named producer/host/ownership hashes and final source manifests at `outputs/workflow/plan03-implementation/keyed-cancel/correction-1/parent-child-readback.json`. Final implementer cohorts total 761 cases (427 core, 321 store, one process composite, 11 scene, one CLI composite), zero failures/errors/skips; warning-free execution is not claimed. Fresh parent acceptance remains pending.

KC04 is still unverified: scene mode caps/validates exactly 12 launches and their original traces (`workflow_process_harness.py:333–339,500–530`; runner `:141–150`). `correction-1/findings.json` records the remaining positive/unresolved-owner requirements and exact guards. A separate reviewed cohort may solve this; changing the admitted policy was not authorized. No default approval was inferred from the unanswered form, and no active-owner/IPC/keyed-CLI or GraphQL claim follows from legacy regression passes.

**KC01–KC03 correction closure (not full keyed-CANCEL acceptance):** `deleg_61af549e` passed focused re-review. Parent independently verified core `arena-f0-backend-3e6b79f22075` (427), store `arena-neo4j-e99e8c7e7d7e4bff9cfa8c098894184f` (321), scene `arena-neo4j-aedd6da6bfe2438a9d94e2c2fcb2a051` (11), zero failures/errors/skips, matching final source and exact owned-resource cleanup. Record: `outputs/workflow/plan03-implementation/keyed-cancel/correction-1/parent-final-verification.json`. Declared warnings remain. KC04 and keyed IPC/CLI/GraphQL are still open; a narrow correction pass does not satisfy them.

Keyed-RESUME prerequisite/selected first increment: `deleg_49450e3a` traced exact stored intent/attempt and private-authority boundaries. The first internal API admits and retains one pinned branch, with effect-free replay and fresh/replayed status separate from the immutable receipt; claims revalidate that selection/version atomically. Existing unclaimed generation/scene and separate registered-generation reconciliation are the selected subset. Missing private binding without explicit renewal blocks; renewal on reconciliation is refused as not applicable. No grant is issued or executor invoked in this increment. Remaining pending-without-reservation/validation-to-scene branches, private post-ack handoff and active tests are not removed from M03/G01 acceptance. No generic queue/journal or legacy behavior change is authorized.

### Internal RESUME admission — independent verification pending

Added immutable RESUME payload/selection/receipt and separate admission freshness; service `admit_resume`, exact command lookup, store preview/admission and optional `resume_operation_id` guards inside generation/scene claims. Permission/eligibility and public-screening callbacks stay outside the admission transaction. The transaction rechecks selection and commits receipt/cause together; unknown commit or postcommit screening failure returns no fresh ticket. Coordinator, executing application/authority and legacy resume remain unchanged.

Parent readback: `outputs/workflow/plan03-implementation/resume-admission/parent-child-readback.json`, with detailed child `HANDOFF.md`, `verification-summary.json` and `tdd-runs.json`. All 25 recorded cohorts' source/host-proof/JUnit/cleanup receipts were checked. Final backend `arena-f0-backend-1c3d6305af50` has 447 passing cases; final store `arena-neo4j-f62387199e56427dae035b5a8388bfab` has 375, retaining all 321 prior names and adding 54. No errors/skips in final runs; failed intermediate fixtures remain failed evidence. Fresh critique and independent reruns are pending. No grant renewal, selected worker execution, full M03/G01/V0 or new active-keyed coverage is claimed.

RESUME parent baseline: `arena-f0-backend-9369217dfdb8` (447), `arena-neo4j-986c435706954fe4a3fc94fc60c40aa8` (375), `arena-neo4j-c0488258062f4d1eb00b69063de947a4` (one process composite), and `arena-neo4j-fc8372c4eead46bfae5ab6e3e5963dfc` (11 scene), zero failures/errors/skips, matching source and exact cleanup. Record: `outputs/workflow/plan03-implementation/resume-admission/parent-baseline-verification.json`. Critics passed the cooperative valid-writer contract and explicitly limited legacy preview corruption coverage. Parent is checking two narrow consistency obligations before acceptance: missing ownership proof must not become a positive unclaimed selection (RA01, analogous to CQ01), and retained preview/contract parsing must not expose sentinel input before screening (RA02). These are pending hypotheses, not fabricated execution findings; no executing-handler coverage follows from these baseline runs.

RA01/RA02 correction evidence: deleting ownership proof from an actual claimed reserved scene reproduced favorable preview/admission and another claim; explicit presence/null/contradiction checks now guard the same preview used by admission and claim. Selected fence/registration/scene/profile/contract decoder failures reproduced input-bearing errors and now yield static `CorruptResumeSelection`, with driver/commit/close uncertainty preserved. Valid explicit-null and reconciliation paths remain passing. Parent readback is `outputs/workflow/plan03-implementation/resume-admission/correction-1/parent-child-readback.json`; full child proof is `verification.json` beside it. Final source-matching child runs: `arena-f0-backend-49e93ff2ab61` (450 passed) and `arena-neo4j-d33f7f03b9d14fc797a2b6d65bbca76f` (393 passed). Actual REDs and the corrected interim unsupported-branch regression remain retained. Fresh independent acceptance is pending; this is targeted selected-resume validation, not universal legacy corruption certification or resume execution.

**Internal RESUME component closure:** `deleg_27190184` passed RA01/RA02 focused review. Parent independently verified core `arena-f0-backend-6595bbd9e0b0` (450), store `arena-neo4j-e35ca90aa7b64475874521a1ac3c36c5` (393), process `arena-neo4j-a0c035f154e84937bc22aa388af8cfcb` (one composite), and scene `arena-neo4j-e5b594eda9c14d8cad0f991fb28bcb01` (11), zero failures/errors/skips, matching final source and exact resource cleanup. Record: `outputs/workflow/plan03-implementation/resume-admission/correction-1/parent-final-verification.json`. This supersedes internal-component pending language above, not the missing private renewal/execution handoff, unsupported branches, active keyed coverage or full M03/G01/V0.

Executing RESUME handoff contract (isolated implementation subsequently verified below): require both fresh admission and an admitted disposition, since fresh refusals exist; consume only an internal once-delivery latch before private mutation, not a client-provided ticket. Explicit bind/renew eligibility is checked without issuance; apply only the retained action under the private guard, exit it before coordinator/lease calls, and keep known receipt versus execution observation separate. Generation and first scene claims receive the actual resume key and committed `after_version`; stale selection cannot mark replacement work unknown. Recovery carries the exact original fence through all reads and never auto-enters scene. Existing default behavior and narrowly scoped compatibility assertions must remain covered. Source/isolated tests do not close active keyed-owner, CLI/GraphQL, unsupported initial branches or full M03 gates.

### Plan 03 requirement-to-delivery map

Latest RESUME checkpoint: `resume-execution/correction-1/parent-final-verification.json` binds parent `arena-f0-backend-f6026f9ad036` (493 cases) and `arena-neo4j-7ded394f6e914cba91b07fc6e807d22c` (11 scene cases), zero failures/errors/skips, exact final source, and independently absent owned containers/networks. Fresh review `deleg_dd7f0b21` passed the bounded reentry/never-latched-recovery corrections. Child final metadata/process/CLI proofs are independently hash-checked in `parent-readback.json`; earlier parent full suites remain pre-correction history. This closes isolated implementation/correction checks, not active keyed, API, installed or full M03/G01 acceptance. The synchronous reentry veto is not arbitrary trusted-callback safety; uncertain cached/latched/release-fault ownership remains retained. Initial no-reservation and validation-to-first-scene resume branches remain unsupported.

Profile prerequisite evidence (not integrated G02): `workflow/profiles.py` supplies frozen public settings/policy/accounting, registration shape and compatible legacy settings hashing. Parent read captured-source/JUnit/cleanup proofs for three RED→GREEN steps, final backend `arena-f0-backend-2ee028f9c147` (249 passed) and unchanged store baseline `arena-neo4j-7ae56166805e45b5960913cdee873041` (142 passed). `arena-neo4j-d0db6060fe92435390e9b32213e9bad8` failed static staging with no JUnit/container execution; it is an admission blocker, not feature RED. The unexecuted integration test was restored out by its owner. Record: `outputs/workflow/plan03-implementation/profile-codec-prerequisite-readback.json`. Repository/admin/read APIs and independent integrated review remain unfinished.

Subsequent catalogue integration, acceptance pending: new `workflow/accounting.py` contains the unchanged validators; `inference_transport` retains compatibility exports and unchanged allowance/transport bodies. This fixes source-layer coupling rather than raising sandbox limits. Extraction RED `arena-f0-backend-d3ec5afd0d9c` → GREEN `arena-f0-backend-196fdaa7b691`; genuine DB missing-method RED `arena-neo4j-15336baec32940fead2a416f63c9f1bf` → GREEN `arena-neo4j-ab092073032145869700fd385fd8b475`. Final implementer GREEN: `arena-f0-backend-3294d204bbac` (280 passed), `arena-neo4j-b6987892e378438a9ed846f262b48075` (175 passed), no errors/skips, verified cleanup and matching final source. Parent readback: `outputs/workflow/plan03-implementation/profile-catalogue-child-readback.json`. Explicit catalogue codec 1, immutable scoped anchors/revisions, admin registration and effect-free exact/list service reads are implemented; current-catalogue validation is only for new registration, not history/replay. No GraphQL/CLI exposure, workflow-reference migration or legacy-wrapper migration is claimed.

Independent parent catalogue reruns: `arena-f0-backend-83c975091b8a` (280 passed) and `arena-neo4j-340f7ae022914e9991982d3d88250b76` (175 passed), with source/cleanup/exact resource absence verified at those bytes. Record: `outputs/workflow/plan03-implementation/profile-catalogue-parent-verification.json`. These passes do not discharge correction round 1:

- PC01 (critic, reproduced): actual exact/list/count plans scanned the revision label with valid foreign scopes. Corrected explicit-admin ONLINE nonunique scope index and direct predicates produce scoped index seeks. Exact lookup is not claimed to force the full-key unique-index operator; bounded work relies on the existing valid-scope capacity invariant.
- PC02 (parent, reproduced): retained literal/nested profile corruption exposed a sentinel through validation errors before screening. Local retained decoding now raises static `CorruptProfileRecord` with suppressed display chaining; query/transport/unknown-commit failures remain outside that catch.

Correction evidence: DB RED `arena-neo4j-f655e502cd35439580f13ab93435309d` (180 cases, 5 failures), schema RED `arena-f0-backend-8291014c48f6` (286, 7), final GREEN `arena-neo4j-90c436af23a5470486d46f29a210055b` (182 passed) and `arena-f0-backend-b445bd961ab9` (294 passed). Parent verified JUnit/source/cleanup and actual `profile-plans.json` index-seek/no-scan operators. Record: `outputs/workflow/plan03-implementation/profile-correction-round1-readback.json`. Fresh independent acceptance remains pending; scoped new/production formatting is now checked while baseline test formatting is preserved.

**Final catalogue component closure:** `deleg_47624233` passed PC01/PC02 focused source re-review. Parent independently verified final-source backend `arena-f0-backend-934edbe1d53b` (294 cases), store `arena-neo4j-9ab282d9ad5848b48b1a254b73453e4a` (182), scene `arena-neo4j-263c74c3ea354a0496f2e538d7ca3267` (11) and CLI `arena-neo4j-ab6ae8ab92694fd589f8ab671a770f83` (one composite case): zero failures/errors/skips, matching staged/current source hashes, verified cleanup and exact resource absence. Record: `outputs/workflow/plan03-implementation/profile-catalogue-final-verification.json`. This supersedes the intermediate pending component-acceptance language above, not the explicitly unfinished GraphQL exposure, workflow-reference migration, full G02/V0 and live gates. The reviewer did not independently read saved run artifacts; those were verified by the parent.

No harness admission, package installation or protected-file changes are authorized by these corrections.

| Slice / delivery target | Requirements | GraphQL coverage | Acceptance obligations | Current evidence/status |
| --- | --- | --- | --- | --- |
| S00: completed original-question records in plan §7.4 and schema-field traceability | R06, R13, R14 | G01, G02, G03, G04, G05, G06, G07, G08, G09, G10 | T20 | M01–M15 independently reviewed PASS; executable schema/trace assertions remain pending |
| S01: pure application/facade extraction and versioned command/query contracts under `core/workflow/` | R06, R07, R10, R11 | G01, G02, G03, G10 | T01, T02, T11 | Submission/frozen-intent readers, pure provider boundary and shared orchestration class verified; installed composition still pending |
| S02: Neo4j public profile/admin repositories and explicit initialization | R01, R04, R11, R12 | G02, G03 | T13, T17 | Model-only catalogue/admin/read and PC03 correction verified; no request-revision migration or all-kind claim |
| S03: retained commands, causal history and run-bound cleanup projections in service/store/read model | R06, R09, R10 | G01, G03, G05, G06, G07, G08, G10 | T04, T10, T11, T15 | Event source, historical scene queries and separate cleanup query verified; command handlers/full causal envelope/pagination/atomic operational view unfinished |
| S04: independent API owner, executable GraphQL schema and network CLI under proposed `core/workflow/api/` | R04, R06, R10, R12 | G01, G02, G03, G04, G05, G06, G07, G08, G09, G10 | T09, T12, T16, T20 | Pending; complete V0 requires integrated clients, not schema text |
| S05: operator bootstrap, explicit role profiles, private loading and existing model/prior adapters | R01, R02, R04, R07, R15, R16 | G02, G04, G10 | T02, T03, T05, T13, T17, T21 | Pending; sentinel-only isolation allowed; actual mounts/providers/prior access separately gated |
| S06: existing Arena construction/capture, fenced native stages and supported repair | R03, R07, R08, R12 | G03, G05, G06, G07, G08, G10 | T06, T07, T14, T18 | Pending; isolated composition precedes separately authorized calibration/joined live V1 |
| S07: task-bound policy lifecycle/aggregation and cross-task adapters | R05, R09, R12, R13 | G03, G06, G07, G08, G09 | T08, T18, T19 | Pending; isolated generality is not live V2 |
| Cross-cutting evidence and approval boundaries | R08 | All affected operations | T01–T21 retain their declared evidence level | Full completion remains blocked until required live/deployment authorizations and positive evidence exist |

`core/` denotes `isaaclab_arena/agentic_environment_generation/`. Slice labels track dependencies, not a requirement to finish all repositories before testing a vertical API path. V0 → joined V1 → V2 remains the delivery order. Reuse the [isolated runbook](foreground-workflow-runbook.md) and existing proof checkers; do not replace them with a new test framework. Goal continuation is not a substitute for checkpoint/reconciliation after interruption.

## Historical plan 02 execution record

The remaining checkpoint narratives describe earlier increments and authorizations. Their old “current” labels do not override the plan-03 campaign and boundaries above.

### Resumption checkpoint after the SBOM detour

The user has returned to event mapping/refactoring. SBOM collection remains parked for its separate delivery/license study. Fresh source inspection began from clean `8e6842190ad4fe3af095b8a26c261e8bd1f7f4bb` on `dev/0.3.0-prerelease`; older baselines below remain historical. This checkpoint is source inspection, not a new test run or native acceptance.

Current gate: **connect existing components into a working isolated CLI workflow**, rather than restart contracts or add unrelated infrastructure. The previously approved implementation/isolated-test scope remains the boundary; live inference, GPU/native acceptance, shared-service changes, research-database access and queue release are not newly authorized by returning to the topic.

#### Scene composition checkpoint after adapter delivery

The retained-prior/model adapters and synthetic capture/evidence ports exist, but their separate tests do **not** establish an assembled owned-worker scene loop. The attempted assembler delivered exact evidence-receipt reopening, per-role ceilings and repair-output protection; it did not deliver the scene worker or full service/Neo4j/SDK composition. Step 2 remains open; the CLI and milestone review remain downstream. Native/live/shared-service/research-database operations remain disabled.

Parent extracted `freeze_configuration` into the pure `inference_profiles` module, preserving the legacy generation export and removing the foreground authority's unnecessary generation-adapter import. Actual missing-feature RED: `arena-f0-backend-86766d29e1e7`; final scoped lint and SDK-profile GREEN: `arena-f0-backend-8c71557c013c` (128 cases, no failures/errors/skips, forbidden effects zero, owned cleanup verified).

The combined process cohort still exceeded its historical 256-file capture limit after that extraction. A stdlib-only AST inventory (no application imports or effects) recorded 258 source files in `outputs/workflow/source-closure-diagnostic.json`. The explicitly admitted `workflow-process` ceiling is now 320, mirrored in the child verifier, with a regression accepting the expanded closure and rejecting oversize capture. Ordinary modes retain 128; the 8 MiB byte cap, source hashes, topology and execution guards are unchanged. Parent runner units and the existing one-case real-Neo4j/deterministic-child cohort passed at captured revision `arena-neo4j-9aabc626d0dd40cc9f4f0130d138be8b`, with zero forbidden effects and verified cleanup. This restores the existing cohort, not actual-model scene acceptance. The two script executable-bit hooks still report the known index-mode issue; no staging occurred.

Multi-role authority is implemented and parent-verified separately from worker acceptance. Fresh composition must call `protect_workflow_contract` before persistence/first grant under `mutation_guard`, then `bind_run` and `bind_workflow_models`. Scene checks use `require_scene_execute`; private config resolution and bounded send occur inside `release_guard(..., run_id=...)` after exact stage/lease checks. Use `private_model_deadline` as well as the original run/stage limits: the unchanged generation snapshot can outlive assessment's grant. Parent witnessed missing-deadline RED `arena-f0-backend-5ba8a16a5acf`; scoped lint and final authority GREEN `arena-f0-backend-576a635f8e2b` passed 112 cases, with zero failures/errors/skips/forbidden effects and verified cleanup. No live model or assembled scene acceptance is implied.

The Neo4j/service stage-worker seam is now implemented. Parent read back `arena-neo4j-2498b8eb1c334e7a925296398edae96a`: 138 cases, zero failures/errors/skips, passed proof and verified cleanup. These tests use synthetic physical ports, not OS ownership evidence. Trusted `ScenePortProfile.owned_worker` enables `require_held_owner`, `prepare_worker` and `cleanup_worker`; the service retains a prepare fence, registers before release, rechecks the committed release, acknowledges cleanup before completion and never automatically re-prepares ambiguous work. Store APIs are `claim_scene_worker`, `register_scene_worker`, `acknowledge_scene_cleanup`, `get_scene_intent` and fenced `release_scene`/`check_scene_release`. Initial generation lookup remains separate.

The actual bounded scene-model child now has separate process evidence: `arena-f0-backend-0909f1ad8c34` passed 35 cases, preserving the 30 original cases, with actual refine/assess entrypoints and SDK serialization over synthetic HTTP. Parent read back zero failures/errors/skips/forbidden effects and verified cleanup. The suite used a temporary 300-second host collector wait (not a worker deadline extension); permanent scoped runner support and real-Neo4j/SDK composition are follow-ups. Parent subsequently fixed boolean/integer binding equivalence with canonical comparisons (RED `arena-f0-backend-5b86ebb2586f`, GREEN `arena-f0-backend-f572902710ee`); the older child proof does not cover those changed bytes.

`InitialGenerationWorker` and `InitialGenerationReceiver` now connect actual proposal-only generation plus retained priors to the existing generation coordinator, artifact writer, schema validation and adoption over a synthetic store. Parent reran the four owned-child cases in `arena-f0-backend-54e4904d7c0b`: real `generate_spec`, exact retained context, required-prior-unavailable zero calls including ping, current/frozen-key vetoes and receipt replay without inference. This is not full-outcome dependency admission or native grounding. The reported free-price workaround was removed: grant scanning now exempts only exact validated origin-bound accounting metadata, not keys or unknown credentials. Literal `"0"` is preserved. Parent RED `arena-f0-backend-95d61504cd6f`, final 121-case grant GREEN `arena-f0-backend-07a067f29f74`, scoped lint and the four initial-child tests all pass, with zero forbidden effects and verified cleanup.

**Current gate: complete shared application root and CLI.** The scoped scene-stage gate is verified. Parent read back `arena-f0-backend-bc34e1b9262c` (39 cases, preserving the earlier 35), including the retained three-child observe-failure → repair → fresh-observation → acceptance trace. Actual model entrypoints/SDK serialization, physical cleanup, same-flock advance and terminal retirement are exercised over synthetic capture/store/HTTP. `arena-neo4j-63cb281a8cea46778c3ec67aefa4b584` separately passed both real-Neo4j cases with four SDK children, including the three-stage ports trace. Both proofs have zero failures/errors/skips/forbidden effects and verified cleanup. Synthetic capture is not native physical validity. Retire generation ownership before scene reservation, establish the fresh physical scene owner before `start_scene`, and retain the same flock across scene stages. Database-deserialized cleanup is not the supervisor's physical cleanup capability.

The joined initial-generation → scene path now passes through `ForegroundWorkflow` and the default CLI factory **inside the admitted harness**. Parent reran `workflow-scene` in `arena-neo4j-8f6af11eba6343ceaca3c391c331c704`: 10 cases, including actual generation/adoption, generation retirement, a fresh scene owner, three scene stages, terminal acceptance/retirement, and all six required-dependency negatives with no generation factory/SDK child. The joined trace retains four child registrations and two owner epochs. Parent also reran 465 core cases in `arena-f0-backend-e48e960edc20` and 138 real store cases in `arena-neo4j-82a363cab08e445ba3bc3cad9c5e9290`; all have zero failures/errors/skips and verified cleanup. Do not sum overlapping cohorts.

The milestone is **not closed**, but the first focused recheck now closes CLI-01, RESUME-01 and CANCEL-01 for the guarded isolated scope. `workflow-cli` launches actual fresh interpreters; `arena-neo4j-233f07663f79447089a2948192b03167` retains 28 commands/20 SDK workers including active generation/scene cancellation, delivery before cancel-client DB access and verified owner retirement. Shared observation-only bootstrap is wired. The injected outage is cancel-client `StoreUnavailable`, not a stopped DB server. Two residuals remain: **B1** real pre-prepare dispatch interruption can strand known-unreleased ownership (seeded pending tests were insufficient); **B2** frozen-model mismatch needed typed not-ready/exit-3 mapping instead of generic failure. B2 unit RED→GREEN and scoped lint now pass (`arena-f0-backend-faba8aabffc6`, `arena-f0-backend-bfce99c4be9f`); fresh assertions are being joined to the B1 correction cohort. Parent reruns also verify 471 core cases (`arena-f0-backend-b97e6c7603b8`) and 139 store cases (`arena-neo4j-a6eb8b67a3ae4c9999d8e3e2d3ef4698`) at their captured revisions. See the [implementation ledger](../event-mapping-refactoring_plan_02-implementation-review.md) for remaining counterexamples and the [updated runbook](foreground-workflow-runbook.md) for JSONL/exit contracts. Unknown released effects remain blocked. Production deployment, installed startup, native/live inference and research retrieval remain outside this isolated proof.

Fresh submission guards include `operation_id=` against all required current keys before persistence/grants (parent GREEN `arena-f0-backend-669e725a3485`). CLI/parser/bootstrap passed 14 isolated core cases in `arena-f0-backend-93f9a6e20526` and scoped lint; exact admission passed 110 sandbox checks in `arena-core-units-bcdbc860be82`. These units are not the default-factory positive proof.

#### Final bounded correction gate

Independent final recheck `deleg_52a9b06d` closes all five original findings for the guarded isolated scope, including B1 real-run pre-prepare continuation and B2 exact model-profile not-ready/exit-3 semantics. No confirmed residual blocker or direct regression was found. Parent verified `arena-neo4j-58c39b49bd3b4ea9a1e79554b0bae724` (31 distinct CLI processes / 28 SDK children, zero forbidden counters, verified cleanup) and reran the final scene harness in `arena-neo4j-3c602a82680e48cb95166879c7e293b9` (11 passing cases), 13 host runner units and scoped lint. The earlier status paragraphs are historical increments, not unresolved findings.

**Isolated milestone closed.** Final parent CLI rerun `arena-neo4j-6fe820f218de4774a8cb46c049125986` passed with 31 distinct CLI processes, 28 verified SDK workers, matching captured Python hashes, zero failures/errors/skips/forbidden effects, verified container/network cleanup and no remaining owned resources. It includes the final copyright normalization. CLI outer collection is 780 seconds for the expanded cohort, with unchanged worker deadlines and isolation. No further review or implementation is pending within this guarded isolated gate. Native/live/retrieval/installed-startup/production deployment remain unverified and disabled; default prior is explicitly unavailable fallback, with retained nonempty-prior consumption proved separately. Dashboard integration, optional policy/VLM-DCRG and legacy migration remain later work.

#### Agreed implementation sequence and acceptance contract

The checkpoint narratives below are historical increments. The current verified state and unresolved delivery/review gate are recorded above.

**Current checkpoint:** the scoped isolated worker-integration gate is closed; step 2 (scene stages) is in progress. The parent reran real Neo4j plus actual deterministic owned child/private authority/lease/receiver/schema/receipt/cleanup/retirement in `arena-neo4j-0f2fe4d1ba35457ea858d1eb9a1b2a75`, using the additive `workflow-process` mode. It uses a fixture registry/producer, not SDK generation or native simulation; its recovery is a fresh object. Separately, `arena-f0-backend-bc26fb7e0a80` covers actual SDK synthetic transport and distinct-process recovery, including a surviving leader/TERM-resistant descendant, historical replay after replacement and cancellation during artifact verification, over a synthetic persisted store. Do not merge these scopes into a claim of real-Neo4j crashed-owner/native acceptance. Missing/unanchored/unregistered recovery remains explicitly blocked, never permission to resend. The runner's deliberately forced assertion failure checks proof failure handling, not a newly reproduced production defect. Existing runner regressions and legacy modes pass; its executable-bit hook is index-sensitive and no changes were staged.

The first step-2 parent fix closes a real unmetered escape: managed rendered VLM failures now stop rather than fall through to local urllib or geometric/advisory success. The managed marker survives context closure; legacy fallback and non-rendered specification checks remain. USD geometry introspection is now imported only on geometric paths, rather than requiring Shapely for a VLM-only call. Missing-dependency admission evidence `arena-f0-backend-8065a7b5a406` is separate from actual fallback RED `arena-f0-backend-7a8ff1578ff6`; final parent `arena-f0-backend-91a6fd32ec55` passed 87 SDK-profile cases and scoped production/test lint. Scene loop/durable identities, retained observation producers and conservative attested token/cost bounds are being implemented in disjoint files. None yet establishes the complete scene loop or CLI. Scene-suite exact admission passed `arena-core-units-5486944efcb9`; `stage.py` still reports a complexity warning, so whole-change lint is not claimed clean.

After recovering the original event-modeling/DDD discussion, the user explicitly confirmed this order and that implementation plus isolated tests need no additional approval. The purpose remains a coherent application model linking research entities, ownership, lifecycle, retained state and available actions, with application-owned coordination of the tools previously exercised through an external orchestrator. Later GraphQL exposes that model; new local schemas or worker infrastructure alone do not deliver it.

1. **Worker integration:** connect the existing bounded generation worker to the coordinator, private authorization, owner lease and committed reserved call allowance. Verify a deterministic worker path in isolation, including cancellation, timeout and restart recovery. Recovery of uncertain released work is reconciliation, not permission for another model call.
2. **Scene stages:** connect retained Graph-RAG context, actual-schema validation, observation, assessment and permitted repair through explicit application rules. Close cumulative budget and fallback paths. Revised candidates require fresh relevant evidence; unsupported criteria stop explicitly. Capture/measurement adapters may be implemented and tested with synthetic inputs now, but physical correctness remains unverified until separately approved native tests.
3. **Complete CLI:** implement `run`, `status`, `cancel`, `resume` through the shared application service plus host bootstrap/readiness. Isolated acceptance: submit → generate → validate/observe → assess → permitted repair → fresh observation/reassessment → accept or explain the stop. Prove each unavailable required dependency causes zero model calls, including initialization pings, and retain an all-ready positive control. Status/replay/refresh reads do not dispatch work or mutate the accepted request.
4. **Milestone closure:** integrated tests, one bounded independent milestone review, current plan/handoff and a reproducible CLI runbook. Report implemented, isolated-tested and live-verified separately. Preserve the stable IDs, candidate/evidence relationships, authoritative states and allowed-action semantics needed by the later API/dashboard.

Only after that: separately authorized native acceptance, dashboard integration, optional policy/VLM-DCRG and later selected legacy migration. Native simulation, live inference, shared-service changes and research-database operations remain disabled. Do not treat the accepted order as permission to weaken either positive-path behavior or evidence boundaries.

Fresh first-slice baselines: the existing sandbox harness checks passed in `arena-core-units-1b09ea8e5b8f`; `test_environment_workflow_service.py` passed 65 isolated core cases in `arena-f0-backend-8c8b4564d642`; `test_workbench_execution_grants.py` passed 59 isolated API-profile cases in `arena-f0-backend-a4bdd64f423c`. Parent read back passed proofs and empty remaining-owned listings; both pytest reports have zero failures/errors/skips. API testing reused the existing immutable test image and retained provision manifest. These are baseline component checks, not worker-integration acceptance. The initial source/harness work is tracing the committed allowance and existing worker receipt/cleanup interfaces, and verifying exact admission for a deterministic child process without relaxing the default isolation profile.

Worker integration increments (still not step-1 acceptance):

- Committed reservation/admission/release times now flow through `AttemptView` and the foreground `workflow_execution` packet into the existing generation worker. Its shared allowance is installed before backend construction; legacy generation retains its old default when no workflow allowance is supplied. Child deadline/watchdog is release-bounded; the process adapter must also account for preparation/startup and verify actual identity/cleanup. Tokens/cost and remote effects are not newly certified.
- Parent inspected source and read back clean proofs for packet tests `arena-f0-backend-7ca7980b4c06` (77), real isolated store `arena-neo4j-b37badf8236542429f3150bf33365402` (89), and synthetic SDK `arena-f0-backend-0c428245a707` (82). Parent separately reran the SDK suite after formatting in `arena-f0-backend-0668436a6477`, which passed. Counts are distinct scoped suites, not a workflow acceptance total.
- Existing generation-result validation has been extracted to pure `workflow/generation_output.py`; the legacy editor method delegates without a behavior redesign. `GenerationArtifacts.write(producer_metadata=...)` now retains exact prior/warning metadata under the existing protection, bounded storage and immutable retry rules. Parent witnessed missing-feature REDs `arena-f0-backend-3d226f98ccf6` and `arena-f0-backend-d95f9f666f4d`; latest final-source core checks passed 16 cases in `arena-f0-backend-fc3a32e3b8ac`. Legacy API receipt characterization is still pending its exact admission.
- The additive exact-singleton generation-process profile passed its initial actual-child proof in `arena-f0-backend-d484fb41659b` (8 cases), with owned cleanup and no forbidden effects. Its initial `production-result` mode substitutes the producer callback and is not SDK/generation-pipeline acceptance. The current integration work extends that private fixture and connects the actual coordinator/owner/receiver path.
- Scoped formatting exposed the existing E731 `clock = lambda: f.now[0]` in `test_workbench_execution_grants.py` (also present at HEAD). It is not a new budget-plumbing regression; do not claim an entirely clean lint run or silently refactor unrelated baseline code.

Subsequent parent verification: the concrete foreground worker/receiver now exercised real child registration, one-shot send, artifact adoption and trusted cleanup through the coordinator in `arena-f0-backend-77f1b064387a` (14 cases, no failures/errors/skips). Its successful child retained two synthetic SDK calls (ping, completion) through the real generation wrapper and agent/backend constructors; only `generate_spec` is substituted, so this does not establish the full generation algorithm or physical validity. That composition still used synthetic store/authority; real foreground-authority composition and further failure/recovery regressions are the next checks. Timeout, resistant descendant and post-release cancellation cases passed at this revision. Legacy receipt suites also passed the parent's exact admitted rerun `arena-f0-backend-99933896a1dc`.

The initial SDK child failure was a test-profile rejection of `SyncAPIClient.__init__`, with zero transport calls, not an application generation failure. Parent preserved `arena-f0-backend-fc388bb68bda` with public call-site diagnostics, then admitted only the real OpenAI superclass constructors under exact type/code/image-source checks. A separate cloned-code regression failed in `arena-core-units-6498f00b39b9` and passed after enforcing identity rather than code equality. Final scoped lint and sandbox checks passed in `arena-core-units-b115ba8908ea`; no default profile or network/GPU boundary was relaxed.

Restart remains unfinished, but the permanent clean-owner blockage has now been addressed at the store boundary. Historical reads are separate from execution authority; `get_generation_attempt`, `get_owner` and explicit `retire_owner` support exact recovery and clean epoch advancement. Parent reran all 109 real disposable Neo4j cases in `arena-neo4j-16172ced00c34739b9b9777865a35b62` with no failures/errors/skips and verified cleanup. Dirty takeover and stale mutations remain rejected. Retirement is conservative (including a 1,000-retained-path limit), retains all budget reservations, and is not OS cleanup proof. The application still must hold the physical lease, reconcile the original fence, read back retirement, then release the flock. A same-object receiver retry or database handover test is not process-restart acceptance.

Worker cohort `arena-f0-backend-c285e16edc64` passed 26 cases using real foreground authority/grants, lease, worker, coordinator and schema validation over a synthetic store. SDK witnesses establish cap-one ping-only, cap-two ping/completion and pre-release cancellation with zero calls. Its successor `arena-f0-backend-990d9c65c3f6` passed 29 cases, preserving all original cases and adding separate old-owner/recovery interpreters with persisted synthetic store state. Parent read back JUnit and passed cleanup proofs. This establishes restart after promoted output with zero replacement inference, but the old worker was already stopped before owner exit. Surviving-group recovery and settled/retired replay are still being closed; hung kernel syscalls and ambiguous unregistered preparation remain blocked. No real generation algorithm or native acceptance is implied. Parent formatted the recovery surfaces with `/usr/local/bin/pre-commit` and reran scoped lint successfully.

Parent reran all eight admitted pure workflow suites in `arena-f0-backend-54787b9fca9e`: 446 cases, zero failures/errors/skips, empty remaining-owned listing. This includes added recovery-read characterization for authorization-first rejection, original principal/run/contract binding, and no grant resolution, probes or execution on read. These are component regressions, not complete scene-loop acceptance.

Receipt reconstruction increment: `GenerationArtifacts.load_receipt` now reopens only the exact final identity derived from the original fence/registration/contract, protects and verifies bytes, and re-syncs through the existing exact-final retry path. Parent witnessed missing-loader RED `arena-f0-backend-b3a673d9839a`, resealed boolean-binding REDs `arena-f0-backend-3b4b53ab92ab` and durability RED `arena-f0-backend-04d060fc9021`; final `arena-f0-backend-254b188fe645` passed 30 cases with no failures/errors/skips. This is reopened-area verification, not a process-restart claim. Loader/test scoped lint passed; the touched existing `research_artifacts.py` still reports baseline R506/SIM105 sites also present at HEAD. Fresh-process recovery composition is now in progress using these verified store/artifact surfaces.

Source-confirmed components now include:

- `workflow/service.py`: admission/readiness plus artifact-verified generation adoption; submit does not dispatch.
- `workflow/neo4j_store.py`, `attempts.py`, `results.py`, `artifacts.py`: generation lifecycle, retained receipts, artifact verification and cleanup/reconciliation surfaces. Their existing real-database tests cover later increments than the early counts below; no fresh pass is claimed here.
- `workflow/coordinator.py`: dispatch, authenticated cancellation and completion receiver with exact readback; it still requires an injected `WorkerPort` and is explicitly not a scene loop.
- `foreground_authorization.py`, `foreground_owner.py`, `foreground_generation.py`: foreground authority/renewal/release guard, file lease and actual bounded owned-process adapter/receiver. Isolated same-owner cleanup and retry now pass; fresh-process recovery and durable owner handover remain open.
- `workflow/inference_transport.py`: shared process-local SDK `CallAllowance`; not durable token/cost accounting or direct-HTTP fallback coverage.
- `workflow/validation.py`, `evidence_contracts.py`, `evidence.py`, `repairs.py`: actual-schema validation, frozen criterion/cohort assessment and contract-bound structural repair checks. Schema validity does not establish physical support or task success.
- `workflow/cli.py`: `inspect-contract` only, explicitly returning `execution_released=false` and `readiness_checked=false`. No complete `run/status/cancel/resume` interface is delivered.

The earlier bullets describing completion reception, foreground authority or schema validation as absent are superseded by this checkpoint; keep their failed/fixed evidence as history, not active implementation assignments.

#### Next bounded vertical slice

1. Finish fresh-process worker recovery using the original retained fence, exact artifacts and local ownership witnesses. Preserve the already passing real-authority/lease/worker path; do not create a second job queue or coordinator.
2. Verify that a separate replacement process, with no surviving Python handles, can reconcile promoted output and physical cleanup without another model call. Missing result/locality witnesses must explain a blocked outcome, not authorize regeneration. Commit/read back clean retirement before releasing the scope flock.
3. Rerun final worker/source cohorts, preserving cancellation, timeout, uncertain-send and initialization-cleanup regressions. Keep the result generation/schema validation, not scene acceptance; the composed CLI zero-call admission control remains a milestone gate.
4. Then connect the scene-stage loop and CLI. Use one bounded independent milestone review after integration rather than treating component counts as acceptance.

Step-2 source map (preparation only, not implementation): reuse `EnvironmentGenerationAgent.refine_spec` with the exact base, retained feedback and `publish_to_graph=False`; do not substitute `auto_heal` or DCRG. `project_required_criteria` and `assess_scene_evidence` match required criterion/cohort bindings but do not evaluate raw measurements. Their current projection supports exact structural/geometry/runtime/visual profiles and one common window; unsupported criteria must stop. The existing repair guard permits only explicit scalar XY leaves for one non-anchor target with unique support, bounded from the original baseline. Schema, retained candidate lineage, fresh observation and runtime-effective displacement remain separate checks. `CallAllowance` still covers SDK attempts only, not tokens/cost or direct HTTP fallback. Stage decisions/reservations and candidate/evidence/assessment identities must be persisted in Neo4j, not an in-memory second lifecycle. A source-grounded isolated tracer should use the real-schema fixture, real engine entrypoints with explicitly synthetic runtime/inference seams, and a newly observed child candidate; this does not reconstruct the user's historical external-orchestrator experiments or certify native physics.

Remaining milestone order: worker integration → retained Graph-RAG/candidate/observation/assessment/permitted-repair loop with cumulative budgets and fallback controls → shared CLI/bootstrap `run/status/cancel/resume` → separately authorized native acceptance → dashboard parity and optional policy/VLM-DCRG. Evidence producer/profile selections that block a required scene criterion must be resolved before claiming that loop works.

### Retained authorization and historical evidence

- User explicitly approved scoped `docker/workbench/` bootstrap/readiness wiring changes while preserving existing security and paused queues. This is not approval to start/replace research services.
- User explicitly approved a disposable test-only Neo4j container from a cached image, isolated storage and no access to research data/credentials. No image downloads, model/GPU work or shared-service changes are included.
- Initial parent baseline: sandbox runner `arena-core-units-7447c10bd0e7` passed; focused trajectory baseline `arena-f0-backend-d1aeb15aff3e` passed 59 tests, zero forbidden-effect counters, unchanged staged source and verified owned cleanup. Evidence under `web/arena-workbench/tests/e2e/functional-v7/.runs/` is ignored and not retained by ordinary commits.
- No commits/pushes or shared queue release. Live native/model acceptance remains a separately bounded gate. Existing dashboard reliability and physical placement issues below remain unresolved.
- User declined the proposed bounded native smoke test and selected **continue implementation and isolated tests only**. Do not run the proposed simulator/GPU smoke or treat earlier isolated-Neo4j approval as native execution approval. Continue offline/container-isolated development; native acceptance remains blocked on a future explicit authorization.

### Current verified increments (not end-to-end acceptance)

- Exact offline workflow-test admission: eight explicitly named test files, nonempty unique pure-workflow subsets use the existing core sandbox; legacy defaults/mixed profiles remain unchanged. Implementer RED `arena-core-units-8a326c0cd304` retained four expected failures; GREEN `arena-core-units-a9d27b1d10ec` and parent rerun `arena-core-units-fc4f85104141` passed all 101 sandbox checks. Independent read-only critic found no blocker. No isolation/proof guards were weakened.
- Pure dependency gate: RED `arena-f0-backend-c3f95fa00074` failed because the new module did not exist; GREEN `arena-f0-backend-cc664cd8337b` passed. Verified negative and positive deferred-factory behavior with synthetic probes only. This is **not** the composed application/host readiness acceptance; current probe transports remain caller-owned and the gate cannot forcibly interrupt arbitrary Python callbacks.
- Scene scout traced the actual banana fixture: `on` without `at_position` ignores an authored non-anchor `initial_pose`. Raw fixture is not an existing demonstrated XY-repair success. The proposed guard must reject missing effective relation parameters; any initial enrichment and physical threshold calibration remain explicit later work, not invented measurements.
- Neo4j admission/event-store and initial attempt fencing now have real disposable-database verification (latest evidence below). Complete recovery, composed CLI, native measurement production and scene acceptance remain open.

Current follow-up evidence and open gates:

- Harness final scoped bytes: `arena-core-units-a64f7ccaf262` passed 101 checks; parent verified final source hashes. Incidental formatter churn was removed. Pre-existing lint findings remain unchanged, not silently fixed.
- Contract semantics were expanded after parent review: preservation/intervention paths and bounds, criterion modalities/frames/windows, distinct model roles, seed/capture configuration and cumulative operation/deadline budgets. Canonical-output expansion bug reproduced and fixed; the surrogate allegation was rejected because Pydantic already prevents invalid strings. Parent contract run `arena-f0-backend-3bd7ecbfaf7d` passed. DCRG requests remain explicitly unsupported in this initial schema.
- Admission service currently composes injected scoped authority, producer-support validation, full-outcome dependency derivation and store admission only. Replay skips fresh support/probes/execution authority. Fresh operational-write denial regression is RED `arena-f0-backend-8368af4e5378`, GREEN `arena-f0-backend-0ab1e0cea9af`. No worker is released and no CLI composition is delivered by this utility.
- Repair critic found missing non-anchor/support-profile guards. Regression RED `arena-f0-backend-21869b444666` had seven expected failures; final implementation GREEN `arena-f0-backend-8690c67abe7e` and parent `arena-f0-backend-4b4fc4863561` passed. Guard now rejects target anchors/missing or ambiguous support identity; scalar-XY structural permission is not physical feasibility or original-baseline authentication.
- Contract-bound repair adapter additionally derives exact allowed x/y paths, subject, env-local frame and minimum cumulative displacement ceiling from the frozen contract, with contract/original/candidate digests. Parent `arena-f0-backend-30391d2e0056` passed; independent critic approved the structural scope. Actual schema validation, baseline provenance, immediate-parent lineage and realized effect remain separate obligations.
- Scene evidence metadata utility requires explicit selected cohort; historical failures remain diagnostics instead of permanent vetoes. Frozen criterion projection binds full criterion digest, producer, frames and exact window; unsupported modalities/windows/counts reject. Independent binding review closed after a 128/129 boundary regression; parent `arena-f0-backend-3e93783158d0` passed. Native measurements and image artifact verification remain missing.
- Disposable runner safety gaps were corrected with synthetic regressions and parent source review: confined output creation, ambiguous-create cleanup unknowns, staged import origin checks and runner-specific proof/JUnit validation. Parent real workflow tests resumed. This new runner is not validated by the legacy F0 proof checker.
- Real admission races initially deadlocked with dependent-only control updates. Constant-SET locking passed repeated same/different-key races, conflicting replay, mixed snapshot/admission and injected post-write rollback. Administrative scope initialization remains a quiescent prerequisite, not a concurrent migration guarantee.
- Initial generation-attempt persistence supports reservation, owner/epoch claim, exact registration, one-winner release and cancellation fencing. Release/readiness DTO mismatch found by independent critic was corrected: reservation and release now derive the same contract-required dependency set, bind optional trusted instance pins and conservatively map probe-start monotonic time into wall time. Missing pins remain profile-only verification, not service identity proof.
- Latest parent verification: `arena-neo4j-3fd8735bf9864034b6c1243cc2a4db0f` passed **33 real database tests** including concurrent releases/reservations, cancellation race and committed-release ACK loss with retry returning false. `arena-f0-backend-26da3df40f3e` passed **344 isolated tests** across seven current workflow suites. Both had zero failures/errors/skips and verified owned cleanup. These overlapping counts are not an end-to-end acceptance total.
- Still missing: concrete host/bootstrap and runtime probe wiring, private credential/owner lease composition, actual bounded worker adapter, invocation metering, receipt/cleanup/recovery completion, image/measurement capture, application coordinator/CLI loop, native calibration/acceptance, policy/DCRG extension and dashboard parity. No paid inference, GPU trial, shared queue release or research-database write has been performed.
- Subsequent generation-result slice reuses actual `ArtifactArea` bytes and service verification before immutable Neo4j adoption. Exact receipt plus synthetic owner-cleanup evidence advances `running/generation` to `running/validation`, not scene acceptance. Parent real `arena-neo4j-a6f2004e6b62425b86d3aecd2a5f86ab` passed and independent adoption critic approved this scope. Kernel cleanup, YAML/JSON semantic equivalence and truthful catalogue/prior provenance are not established by hashes.
- Lifecycle phase is now separate from operational state. Released uncertainty can be marked for reconciliation without retry/refund; exact retained results/cleanup reconcile the original attempt and cancellation wins. Parent added a real RED for receipt-present/cleanup-uncertain and verified the fix in `arena-neo4j-5d12a9c9a5544850922602c8e72e1eb6`.
- `GenerationCoordinator` dispatch/local-stop utility exists with injected authority/lease/worker ports, but no actual adapters or completion receiver. Parent isolated `arena-f0-backend-e84f3d76f7fc` passed and scoped critic found no safety blocker. Real-store integration then exposed three reds in `arena-neo4j-fd6978ad773d45d5b88192014054fa9f`: missing pre-claim/claim-ACK recovery IDs and nonterminal pending/no-worker cancellation. Targeted fixes are in progress; retain these as unresolved until rerun. No full application/CLI delivery is implied.
- Some isolated test logs contain an ignored teardown `AttributeError` at existing harness `api.py:171` (module name becomes null during interpreter shutdown). Recorded pytest/proof status is passing, but logs are not warning-free. No unrelated harness change has been made for this.

### Historical interruption and limits

Latest interruption: **“have you messed up the cli again ? I cannot see the terminal anymore.”** Code work stopped; the user then requested saving progress and the event-modeling study. Hermes was observed running with a responsive tool channel, but the host terminal display could not be inspected. Cause and whether the panel disappeared versus text became blank are unknown. No terminal/Hermes configuration change or service restart was performed. Preserve current edits; do not resume from older approvals.

The earlier **“I refreshed the page and everything disappeared”** dashboard report remains independently unresolved. The study established a missing application-owned visual-assessment/repair coordination path, not a proven cause of either display symptom.

The user subsequently authorized finishing the narrow `render_policy_trajectory.py` correction before continuing the event-mapping study. The tool delegates to core task-driven capture/assessment helpers, preserves completed capture evidence across cleanup failures, retains the resolved policy instruction, forwards device selection, and preserves failure exit status through native shutdown. The parent reran **59 isolated tests** successfully. A final-source native smoke check retained nine real RGB frames over two zero-action steps and verified exit 1 for an injected pre-provider failure. No live GR00T/VLM inference, graph operation or full repair-loop acceptance occurred. See the session-memory follow-up for exact evidence and material/texture warnings. This narrow authorization does not resume dashboard implementation or shared services.

**The prior implementation pause is superseded only by the current scoped plan-02 authorization above. Dashboard reliability remains unresolved; full V7/CLI parity is not accepted.** Earlier “live delivery” and “fixed” reports describe bounded observations, not reliable user delivery. It is not established whether the disappearance affects rendering, session state, unsaved drafts or persisted data. No data-loss diagnosis or recovery is claimed; shared workload release remains outside this checkpoint.

## 2. Checkout and commit boundary

- Checkout: `/workspaces/IsaacLab-Arena`, branch `dev/0.3.0-prerelease`.
- Implementation baseline: clean `8058fcf42d89bc4bba7d88576843e684c5fedca3`, the user's committed planning/reference changes. Production source still starts from the trajectory-fix baseline `45a0b26466`; older checkout observations are historical.
- Implementation now creates scoped changes from that baseline. No staging or commit is authorized; the user owns the commit decision.
- A commit preserves selected source/docs, **not** the installed host helper, private profiles, SQLite journals, browser state, cached images/dependency volumes or ignored verification artifacts.
- Application-owned bounded orchestration, Neo4j workflow authority and core consolidation are now authorized for implementation according to plan 02. They are not delivered merely because implementation is authorized; record verified increments here.

## 3. Intended workflow retained

The baseline remains the existing [agentic generation README](../../../../isaaclab_arena_examples/agentic_environment_generation/README.md): prompt-first generation using Graph-RAG, inspect, build, and separately evaluate. Reuse the existing agent, retriever, relation solver and policy runner; this handoff does not propose a replacement architecture.

LPG/Neo4j, RDF/reification, provenance, measured outcomes and bounded DCRG corrections remain the research context. Do not equate declared relations with physically realized placement, or RDF reification with proven native RDF-star/lossless round-tripping. The A2 banana/plate placement failure remains unresolved: valid YAML and a short Build did not prove objects spawned on the table.

## 4. Earlier dashboard implementation and observations (2026-09-17)

| Area | Source / bounded observation | Remaining limit |
| --- | --- | --- |
| V7 editor | Existing authoring, validation, save/recovery, Library and inspection work; earlier isolated and live checks | Latest refresh disappearance unresolved; no claim of reliable complete interface |
| Model profiles | Create-only nonsecret Journal catalogue; capability settings frozen through settings, grants, jobs, workers and SDK requests; Add/readback/select UI | User-defined profiles remain unverified; no live compatibility matrix or automatic provider test |
| Live profile example | `openai-gpt-4.1-mini-user` saved through UI, API and independent SQLite readback; no key activated | Observation predates latest refresh failure; current user-visible state not rechecked |
| General readiness | Scenario selector removed; runtime/graph/model/GPU checks follow general generation/inspection/build | Last check passed API/runtime/GPU, but graph was not configured; not successful Graph-RAG generation |
| Existing services | Neo4j and GR00T started using their existing containers/cache; resource caps read back; GR00T ping and DROID modalities checked from Arena | Service state is not model identity, codec/inference acceptance or task success |
| Host control | Private v2 helper installed; paired browser Start with all services already running completed by exact GET readback | No live browser cold-start test; time-limited pairing is separate from API session and queue resume |
| Policy endpoint | Trusted configured GR00T port flows through readiness and frozen evaluation inputs; live catalogue showed 5559 | First-party metadata wrapper exists in source, but running server lacked `get_server_info`; verified-policy acceptance remains open |
| Queue resume | Global `Resume queue` added outside diagnostics, with whole-queue warning, confirmation and session/route fencing | Agent only tested Cancel live. User subsequently reported disappearance after refresh |

## 5. Last-known queue and runtime state — not a fresh live probe

Immediately before the user's latest report, both jobs were still queued:

| Job | ID | Last observed state |
| --- | --- | --- |
| Generation | `2da78c386c234556b012c65c5d31a830` | queued |
| Build | `1e537acb672246edb9520f060c9fe7e2` | queued |

The agent did not resume or cancel these jobs. Queue resume affects the entire shared queue, not just generation. Re-observe it before any future release; do not duplicate submissions or assume an old execution grant remains valid.

- Dashboard URL used: `http://127.0.0.1:3010/?layout=v7`.
- Existing target graph: `neo4j-arena`, Bolt **7688**, HTTP **7475**. Other Neo4j instances are not substitutes.
- Existing target policy: `gr00t-server`, loopback **5559**, cached `nvidia/GR00T-N1.6-DROID`. OpenPI's separate port remains 8000.
- Last API reload used `--start-paused`. The Supervisor dispatch pause is distinct from journal `queue_paused`; a false journal flag alone does not establish active dispatch.
- Neo4j 2 GiB/256 tasks; GR00T 16 GiB/1024 tasks; equal memory/swap caps. Recorded owned caps including Arena/editor/frontend/helper were below daemon RAM; this did not bound unrelated applications.
- Host helper: `arena-workbench-control-v2-final-6e74675cd31c.service`, private installation `/home/tarfy/.local/state/arena-workbench-control-6e74675cd31c/installed-v2-final`. No credentials or pairing values belong in Git. Pairing lasts 30 minutes and is browser-session-specific; it does not resume jobs.
- Host-installed bytes/profile are separate from checkout edits. Reviewed file drift can invalidate startup admission. Private state and ignored `outputs/control-setup/` scripts are not a reproducible committed deployment recipe.
- API graph credentials were not configured at the last check. Permission to reuse the existing database account was asked but unanswered. Current user-session generation-key availability is unknown; do not infer it from another session's metadata.

## 6. Evidence map — historical scopes, not one acceptance total

| Checkpoint | Recorded result | Where to look |
| --- | --- | --- |
| Combined model/endpoint backend | 682 isolated cases; SDK transport separate/synthetic | [Integration evidence](model-endpoint-integration-backend.json) |
| Production frontend before Resume extraction | 1,989 cases / 72 files; preview excluded; sequential bounded batches | [Aggregate](frontend-integration-evidence.json), [file inventory](frontend-integration-files.json) |
| Model-profile owner / policy-port owner | Focused tests, typecheck/build, source hashes and limitations | [Model evidence](model-profile-owner-evidence.json), [port evidence](policy-port-evidence.json) |
| Host helper | Parent 82 control + 24 launcher tests; real stat-only probes; scoped hooks | [Live startup record](startup-live-evidence.json), [runbook](../../../../docker/workbench/CONTROL.md) |
| Profile live save/readback | UI, authenticated API and independent SQLite readback, no model call | [Live profile record](model-profile-live-evidence.json) |
| Resume extraction, later than full frontend run | 165 focused cases across 8 files, typecheck/build; live Cancel-only check | Runs below and [historical log](implementation-progress.md) |

Resume artifacts under `web/arena-workbench/tests/e2e/functional-v7/.runs/`:
`arena-functional-frontend-bb0f7cdb65f1`, `arena-f0-typecheck-1b6671c4b75d`, `arena-f0-build-254174d33037`.
These ignored local artifacts may not exist in a fresh clone. Counts overlap and must not be added. The later Resume patch was not followed by another whole-frontend run. The user report supersedes any claim of repeatable refresh acceptance.

Older 382/1,925-case records, synthetic Chromium traces, API inventories and owner status files remain historical evidence. A route ledger is not full mutation coverage; a successful component test or mocked transport is not an executed research workflow.

## 7. Where the changes live

- `isaaclab_arena/agentic_environment_generation/`: inference profiles/backend, strict scene adaptation, policy contract and Journal-backed model-profile store.
- `isaaclab_arena_examples/agentic_environment_generation/web_api/`: settings, authorization/grants, worker propagation, readiness, evaluation endpoints and paused API startup.
- `isaaclab_arena_gr00t/`: policy metadata/codec wrapper and optional verified native-client integration.
- `web/arena-workbench/src/`: model settings, readiness, host-control panel, queue-resume panel and existing editor changes.
- `docker/workbench/`, `docker/resource_limits.py`: helper, launcher/API lifecycle, proxy/socket integration and resource budgets.
- Tests beside those components and the existing `functional-v7` verification runner.

Review tracked **and untracked** source files when preparing the commit. Do not bulk-add model caches, private profiles, keys, runtime state or datasets. This checkpoint is not commit approval or a claim that all pending changes are correct.

## 8. Reading order and resumption boundary

1. This checkpoint: current implementation gate, authorization and limits.
2. [Session memory](../../quick_notes/session_memory.md): short decision record and conversation recovery anchor.
3. [Historical progress log](implementation-progress.md): provenance only; contradictory “current” paragraphs are superseded.
4. [Dashboard design](../dashboard_cli_workflow_parity.md), [endpoint contracts](endpoint-contract-plan.md), [research-stack design](research-stack-readiness.md), [model-profile contract](model-profile-contract.md): retained design records, not a new execution mandate.

Continue the currently authorized plan-02 implementation through its dependency and verification gates. Older approvals do not authorize additional shared-service changes, paid/GPU experiments or queue release. Preserve historical evidence rather than upgrading it to current acceptance.
