CLI binding contract for root owner/parent (frozen early):

New example module foreground_workflow_cli.py will expose main(argv=None, *, application_factory=None). CLI paths: run --config PATH --credentials-fd N --principal ID --operation-id ID CONTRACT; status|cancel|resume --config PATH --credentials-fd N --principal ID RUN_ID. inspect-contract remains delegated to unchanged pure core inspector. Optional --allow-startup only run. No import strings/callbacks/commands accepted from JSON.

Default factory import is fixed: from .foreground_workflow import application_from_cli; call application_from_cli(config, credentials, *, allow_startup=False). Root child: implement this exact factory if feasible; otherwise coordinate public explicit ctor and CLI child can bind. Config is bounded strict JSON object schema_version=1, composition='isolated-synthetic-v1' (initial milestone only), database={uri,database,username}, deployment_id,workspace_id,artifact_root,lease_root. Config no secrets. Credentials from bounded private FD JSON (not path/argv/environment), opaque to CLI after strict JSON decoding. Suggested credentials shape {database_password:..., models:{...}}. Config allowlist can expand explicitly with steer before tests. Factory must be inert with respect to model config/grant/probe/start; app.run handles replay-before-execution. status/cancel/resume must work with no model credentials. All return safe dict via frozen API run(principal,operation_id,raw_contract), status/cancel/resume(principal,run_id), close(). No parallel orchestrator/SQLite.

CLI stdlib parser tests will run host via AST/explicit file loading without Arena package imports. App acceptance test must run inside parent combined cohort; CLI imports fixed root lazily after config validation.

Host bootstrap discovery: existing docker/workbench/control.py has full-stack Controller.start (not scoped workflow services). No new daemon/Docker commands. Adapter will use explicit installed helper capability port only; absence of exact scoped capability => not-ready, never broad start.
