# Architectural Session Checkpoint: Blackwell SM120 & Dual-Container Orchestration

- **Date / Timestamp**: 2026-08-27 01:56:08 UTC
- **Session UUID**: `sm120dock`
- **Target Hardware**: NVIDIA RTX PRO 6000 Blackwell Workstation Edition (`sm_120`, Driver 595.84)
- **Status**: Completed / Active

---

## 1. Context & Architecture Invariants
- **Decoupled Dual-Runtime Architecture**:
  - **Simulation Engine**: Isaac Sim 6.0.1 / Isaac Lab 3.0 / Python 3.12 executed inside `isaaclab_arena` container on the host via `./docker/run_docker.sh`.
  - **Agent & Tooling Workspace**: Antigravity, MCP servers, Terraform, Playwright, and agent skills executed inside DevContainer.
  - **Policy Foundation Daemon**: `Isaac-GR00T` server running in host Python 3.10 with `uv` over ZeroMQ IPC (`tcp://127.0.0.1:5556`).
  - **Port Safety Invariant**: Strictly use ZeroMQ port `5556` (avoiding VS Code collision on 5555).

---

## 2. Milestone Deliverables & Fixes

### A. Hardware Auditor & SM120 Verification
- Ran `.agents/skills/isaac-installer/scripts/check_hardware.py`.
- Detected Blackwell microarchitecture (`sm_120`), Driver 595.84, 96GB VRAM, Vulkan readiness.

### B. Isaac-GR00T PyProject `cu128` Alignment
- Updated `submodules/Isaac-GR00T/pyproject.toml`:
  - Added PyTorch `cu128` index (`https://download.pytorch.org/whl/cu128`) and mapped `torch` / `torchvision`.
  - Bumped `flash-attn` to `2.8.0.post2` (native SM120 support).
  - Pinned build dependencies in `[tool.uv.extra-build-dependencies]`.
  - Validated dry-run resolution with `uv lock --dry-run` (148 packages resolved in 1.55s).

### C. NVIDIA Agent Skills Root Resolution & Gitignore Isolation
- Fixed path resolution bug in `.agents/skills/install-nvidia-skills/scripts/install_nvidia_skills.sh` and `.devcontainer/init_agent_workspace.sh`: replaced fragile relative traversal with dynamic `git rev-parse` root discovery.
- Cleaned up erroneous nested `.agents/.agents/` artifacts.
- Implemented whitelist `.gitignore` policy for 340+ external skills in `.agents/skills/` while keeping core Arena skills tracked.

### D. DevContainer & Host Container Conjunction (Option 1: Docker Socket Pass-Through)
- Updated `.devcontainer/devcontainer.json`:
  - Added `ghcr.io/devcontainers/features/docker-outside-of-docker:1`.
  - Added `/var/run/docker.sock:/var/run/docker.sock` volume mount in `runArgs`.
  - Enabled direct `docker exec` control from inside the DevContainer into the host-managed `isaaclab_arena-latest` container.
