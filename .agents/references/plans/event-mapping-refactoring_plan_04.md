# Event Mapping Refactoring Plan 04: Close the Live-Execution Gaps and Validate the Research Workflow

Status: proposed implementation and validation plan. Creating or approving this document is not permission to use live credentials, write production Neo4j, start/recreate containers, or run GPU/provider workloads. Each effectful gate requires the bounded authorization described below.

**Current direction — orchestration/Neo4j pivot and bounded P1 installed slices:** the installed isolated E1 path has passed (canonical checkpoint: `arena-neo4j-750f875f18d244ecaa5c1772f5dcd27c`). Subsequent bounded slices cover authenticated keyed cancellation, known-unreleased resume, replay/reapproval, retained typed details/artifact bytes, explicit operator/private-role setup with checked configuration handover, and schema-2 nonempty restart-safe prior retrieval. The final-source `prior`, `operator`, separate unchanged-configuration `rotation`, happy/cancel/resume/evidence cases all passed. Independent source/evidence, retained-state, exact-context consumption, privacy and owned-cleanup results are in `outputs/workflow/plan04-implementation/installed-execution/prior-parent-verification.json`, `prior-attempt-privacy-cleanup.json` and the canonical handoff. Full simulator registration, comprehensive metadata parity and the former S2 campaign are not prerequisites. Validation uses the vocabulary actually consumed by the selected adapter, its genuine digest and the existing schemas. Deterministic model/observation outputs and structural-only priors do not certify simulator fidelity, model quality or policy performance; ordinary production/provider composition, managed-prior support, native/policy calibration and live effects remain separate gates. This is not all P1 or Plan 04 complete. The S2 records below are retained history, not current instructions or authority.

**Historical S2 continuation authority — 2026-09-23 (superseded):** the latest explicit user goal authorizes autonomous source/isolated verification, including a specified and independently reviewed fixed non-root stdlib evidence exporter inside the exact owned container. Prove actual known-byte tmpfs transport before real initialization; then verify positive, startup-failure and timeout cases. The user removes campaign-wide time, diagnostic-attempt, review-round and fixed correction-count ceilings; earlier exhausted/remaining allowances below are historical accounting, not stop conditions. Per-attempt300s work/120s cleanup, per-process deadlines, resource/output quotas, ownership, forbidden effects and final-source review remain binding. Stop for a concrete authority/design boundary or evidence-supported inability to progress, not elapsed campaign time. IF-D requires its existing exact admission or one consolidated request; no live effects or S1 pivot. Intake/evidence reconciliation: `outputs/workflow/plan04-implementation/s2-initialization/autonomous-continuation/intake.json` (HEAD `272d0374607b3dfe86d83e4c8008eafd24f7cf15`; five source hashes match prior final verification, retained385-leaf failed snapshot intact). This active direction supersedes older STOP/permission/budget paragraphs, not their historical evidence.

**Historical selected approach — S2 (superseded):** proceed through IF-A → IF-B → IF-C → IF-D to define, approve and verify controlled real initialization before joined workflow acceptance. The engineering direction is selected; its concrete admission contract and execution authorization are **not**. S1/S3 are contingency options requiring an explicit evidence-backed decision to switch, not parallel workstreams or alternatives to keep debating by default. S4 is complementary contract testing. Existing native exclusions remain until explicitly amended; this documentation correction launches no implementation, tests or initialization. The [registration-only design](event-mapping-registration-only-design.md) is retained as the S1 contingency. The [shared session memory](../quick_notes/session_memory.md) records the selection. Goal A remains software/isolated verification; live scene and policy goals remain separate.

**Historical S2 checkpoint — 2026-09-23 (superseded):** IF-A and the shared-helper amendment were approved and implemented; one reviewed positive diagnostic ran, but evidence collection failed. The `docker cp`/quotaed-tmpfs collection assumption is unsupported by Docker's documented contract. No native initialization result is established. The exact owned client was removed and absence verified; all initialization cases are closed again. Required next dependency is an explicitly admitted bounded tmpfs evidence-export protocol, not a registry redesign or S1 pivot. Read `outputs/workflow/plan04-implementation/s2-initialization/collection-stop.md`, `first-attempt-parent-readback.json` and the canonical handoff. One of three diagnostic slots is consumed; two remain. The 49-minute continuation is conservatively charged through 01:15:27Z, with its review round consumed; no automatic renewal. Earlier unapproved/unrun descriptions below are historical planning baselines, not current authority. IF-B/IF-C acceptance and joined IF-D remain incomplete.

Baseline: `d8a75da704be268b32a11952cb84be638a80222d`, branch `dev/0.3.0-prerelease`; worktree clean at planning intake. Runtime services and real credentials were not inspected. Container names, ports, available memory and installed dependencies remain deployment-time observations, not facts inferred from old notes.

Inputs and ownership:
 - **Grouped source evidence acquired:** inspection-bearing positive `arena-s2-init-daa09bc22ff34e38808ffaba7d60a57c` intentionally retained the lazy_loader refusal but captured all ten fixed files,242688 actual bytes, complete import summaries and663 baseline entries within unchanged inspection bounds. Parent rehashed every source text against its physical witness, the385 staged files/evidence, and verified exact cleanup. Canonical observations/local review copies: `autonomous-continuation/observed-frontier/manifest.json`. This is successful source acquisition, not positive initialization or new import permission. The preceding zero-read deadline result is retained; linear accounting/source-first correction passed135 parent methods and fresh review. Pure/lazy and Torch/NumPy effect reviews now select the next exact batch.
 - **Observed bootstrap milestone:** `arena-s2-init-2339d5215d5840e7b65df34d2d1f5faf` records completed original ctypes PythonAPI bootstrap and the actual single `python3→python3.12` alias, physical executable hash and matching kernel inode. Real canonical YAML parsing reached `ArenaEnvGraphSpec.model_validate` and default background registration, then refused the additional `lazy_loader` dependency. This is not full initialization success. Parent source/evidence readback and fresh exact cleanup passed (`autonomous-continuation/positive-05-parent-verification.json`);120 source-isolated checks/fresh `deleg_95b3eb0c` review preceded it. Next is grouped source-grounded dependency-frontier inspection, not blanket purelib permission or another blind import retry.
 - **Current compatibility frontier:** fixture-loader/teardown corrections passed fresh review `deleg_1170c62e` and96 parent source-isolated methods. Actual positive `arena-s2-init-3084aa261d9c43e799524fd8a5a2f123` cleared the pytest edge, then refused stdlib `ctypes.pythonapi = PyDLL(None)` as an unnamed file load; diagnostics and cleanup are verified. This bootstrap opens the current process namespace and requires a distinct, exact once-per-role witnessed check—not a fabricated filename/hash or blanket None exemption. Source-grounded investigation `deleg_443397c7` selected that in-scope correction; implementation/fresh review precede another run. Evidence: `autonomous-continuation/positive-03-parent-verification.json`. No native success or IF-D acceptance is claimed.
 - **Latest diagnostic:** required-evidence completeness is corrected (fresh `deleg_8b6a795f` PASS, parent85 source-isolated methods); common-code transport regression `arena-s2-init-6d72526ec62e4e7ab6a3c619762a74f6` passed. Reviewed positive `arena-s2-init-7d862afef3af41a891a7fba5195d7cad` retained seven real evidence leaves and failed first cold-role fixture import: existing `minimal_spec_dict` helper imports unadmitted `pytest`. Partial evidence correctly remained nonpass; exact cleanup/fresh absence verified. Continue source-grounded fixture/dependency correction without broad origin exemption or S1 pivot. Evidence: `autonomous-continuation/positive-02-parent-verification.json` under the existing S2 output root.
 - **Transport acceptance:** actual fixed non-root exporter run `arena-s2-init-510b10d87ab74c40b10f37d5a949f7a3` passed exact known-byte/digest/source-bound collection and verified cleanup. Parent checked all385 staged hashes and three evidence leaves plus fresh absence. See `outputs/workflow/plan04-implementation/s2-initialization/autonomous-continuation/transport-parent-verification.json`. This closes the tmpfs collection prerequisite only. C2 deadline corrections have fresh source-review PASS and24 source-isolated checks; no native/C runtime acceptance follows. Next dependency-ready step is reviewed positive initialization; failure/timeout and IF-D remain gated.
- [Plan 03](event-mapping-refactoring_plan_03.md), especially §§2–6, §7 modeling records, §9 G01–G10 and §10 T01–T21, remains the architectural/semantic contract.
- [v03 notes](../quick_notes/event-mapping-refactoring_v03_notes.md) preserve the brainstorm and historical progress claims; corrections below govern this plan without rewriting their attribution.
- [Canonical implementation handoff](dashboard_cli_workflow_parity/research-stack-implementation-handoff.md) remains the implementation-status owner. This plan does not create another completion history.
- [Native/policy adapter documentation](../../../docs/pages/example_workflows/agentic_env_gen/workflow_native_policy.rst) describes the implemented boundary. Resolve repository paths from the root; relative documentation links are verified during review.
- [Plan 04 review ledger](event-mapping-refactoring_plan_04-review.md) records findings, challenges and planning verification. Local source/evidence fingerprints live in `outputs/workflow/plan04-planning/`.

## 1. Goal and definition of success

Deliver a usable, application-owned, GraphQL-submitted research workflow using the actual approved Neo4j deployment, IsaacLab-Arena runtime and Isaac-GR00T policy service. Prove the application invokes the existing tools and owns the next transitions; an agent manually inspecting results and issuing the next stage is not end-to-end acceptance.

The delivery path is:

`authenticated submission → exact retained priors → generation → schema validation → owned native realization/settling/capture → retained numeric + visual assessment → permitted repair/fresh evidence if needed → scene disposition → required task-bound policy evaluation → retained result → fresh-client causal readback`

The first reference scenario is A2, not a scenario-specific application architecture. Preserve generic task/embodiment/policy/evaluator contracts and cross-task isolated tests. Preserve scene-only workflows. Publication and successful-prior eligibility are separate from workflow acceptance and remain disabled for the pilot unless independently authorized.

Four verdicts must remain separate:
1. **Software integration:** the installed interface drives the real declared adapters, persists exact identities and returns truthful results.
2. **Operational safety:** authorization, cumulative ceilings, cleanup and production-data boundaries behaved as declared.
3. **Scientific/task result:** scene validity and measured task success, failure or unknown for every predeclared trial.
4. **Release decision:** whether the evidence is sufficient to advance to frontend integration. Passing isolated tests or an explained failed manipulation is not successful A2 validation.

No dashboard implementation, general service manager, new job database, new simulator, policy training, automatic policy-diagnosis repair, production secret-management platform or broad cleanup is included. Reuse existing coordinators, owned workers, artifacts, runner and repository APIs. Production data warrants stronger operational discipline, not an unrelated infrastructure rewrite.

## 2. Reconcile progress before implementing

### 2.1 Evidence-supported baseline

Parent verification read `outputs/workflow/plan03-implementation/m2-m3-parent/final-verification.json` and compared its recorded repository-source hashes with the current checkout. They match at intake. The recorded sum is **1,550 test-case occurrences across 11 cohorts**, not 1,550 unique requirements or live experiments. The manifest explicitly excludes live M2/M3 and V1/V2 acceptance. This planning pass did not rerun those tests or recheck current service state.

Implemented foundations worth preserving include: Neo4j authority/recovery/read models; private bootstrap/provider boundaries; installed query-only GraphQL/CLI; native realization/hold/strict settle/capture; split capture/assessment and cleanup-gated GPU release; policy contracts/runner bridge/episode decoder; durable nonterminal policy handoff; typed retained policy projections. The native/numeric child transports were tested with synthetic native/process seams and real local pipes/artifacts, not launched Kit children.

### 2.2 Corrections to the v03 brainstorm

| Notes claim / shortcut | Plan 04 interpretation and correction |
| --- | --- |
| M2/M3 complete and V0/V1/V2 sealed after automated checks | Adapter/lifecycle implementation is isolated-tested. Plan 03 V0 also requires submission/recovery through GraphQL; V1/V2 require joined live execution. These gates remain open. |
| SQLite retired completely; pure Neo4j event sourcing | Neo4j is the required authority for the refactored application. Legacy SQLite code still exists outside it. Audit the selected transitive execution/retrieval path, not global absence of a filename. Retained commands/events do not alone imply full event sourcing or deterministic replay of stochastic physics/models. |
| `client run`, example YAML/configuration and fixed ports already work | Existing installed mode is query-only. Derive future executable commands from implemented parser/schema/help and test them from a fresh client. Historical ports/configurations are not an operator recipe. |
| RGB-D snapshot | Current DROID capture supports RGB; no depth capability is established. Keep policy and evaluator image transformations distinct. |
| Start the listed containers before reviewing dependencies | Discover exact existing services and their owners first. No implicit restart, rebuild, image pull, download or migration. |
| Live trial is a dry run | Native steps, paid model calls and production writes are real effects. Only a no-effect contract/preflight inspection is a dry run. |
| All fixtures passing implies causal logging is deterministic | Verify exact retained cause/identity/version relationships and monotonic reservations. Do not require identical stochastic outputs or infer physics fidelity from hashes. |
| Run all formatters and clean temporary outputs before a PR | Use scoped checks first; classify baseline failures and avoid unrelated churn. Preserve failed runs and hash-bound evidence; retention/deletion and commits/PRs need separate decisions. |
| Frontend work follows automatically | Frontend remains gated on the joined evidence and an explicit handoff decision; this plan does not authorize deleting REST/SSE/legacy data. |

Historical assistant interpretations in the notes are not new user requirements. Preserve the user's actual goal: a coherent research application, safely validated in live simulation before frontend integration.

## 3. Gap register and smallest intended corrections

All paths below are repository-relative. Source observations are bounded to the baseline; review them again before implementation. An open integration seam is not automatically a defective completed component.

| ID | Current boundary / evidence | Required correction or runtime proof | Gate |
| --- | --- | --- | --- |
| G04-01 | `workflow/api/schema.py` reports `query_only=True`; `api/installed_config.py` admits only query-only mode; `api/server.py` composes query resources | Add an explicit versioned execution composition and GraphQL/CLI command adapters over shared application methods. Preserve query-only mode; never remove guards to repurpose the isolated factory. | P1 |
| G04-02 | `workflow/application.py`, `service.py` and durable handlers exist, but query-server lifetime is not an execution-owner integration proof | Join one long-lived scope owner, private authority interlock and bounded driver; requests submit/query rather than own native children. Exercise active cancel, disconnect, lost ACK and known-unreleased resume through the same installed boundary. | P1 |
| G04-03 | `workflow/policy_evaluation.py:26–53` explicitly requires trusted runtime/cohort/evaluator ports and leaves creation, watchdog, cleanup and retention to its caller; `foreground_split_scene_ports.py` supplies capture/assess/repair, not policy | Supply the supported owned policy worker and concrete runtime/task/checkpoint/recorder adapters. Reuse `rollout_policy`; preserve exact released intent, deadlines and cleanup-before-adoption. | P2 |
| G04-04 | `native_capture.py`, `native_realization.py`, `scene_observation.py` are isolated-tested; receipts still say `native-unverified` | Calibrate actual posture, velocity, action ordering, prim/contact mapping, coordinates, camera activation/freshness and authored→solver→realized poses using actual PhysX. Integrity hashes do not certify the measurement. | P3 |
| G04-05 | Single-reset capture and post-reset policy hooks exist; a separately built policy environment is a new cohort | Re-establish all state-dependent prerequisites for every actual policy reset, with fresh IDs/evidence and charged steps. Decide how visual prerequisites are certified without holding the GPU across a slow VLM call; unsupported policies remain blocked, not assumed equivalent by seed. | P2/P3 |
| G04-06 | Native shared flock and verified cleanup ordering exist; they do not prove a separately running GR00T server releases resident VRAM | Select a tested co-residency topology with enough headroom or distinct devices. A same-device joint rollout needs simultaneous policy + simulator availability. Record service residency separately from the native slot; no unsafe lease release or kill to manufacture readiness. | P0/P3 |
| G04-07 | Source supports prior snapshots; Plan 03 identifies managed-prior metadata paths that used Journal/SQL; `_generate` calls its prior factory again on continuation | Freeze the retrieval selection in the accepted request and recover exact retained prior references before any new retrieval. Required unavailable priors block before model initialization. Prove nonempty eligible consumed priors; do not silently call legacy-only support a managed-prior migration. | P1/P4 |
| G04-08 | Model role/profile/allowance primitives exist; live compatible model pair and total transport behavior are not certified by configuration | Pin literal endpoint/model/routing, actual request/image capability, constructor pings, retries, pricing bounds and all paid attempts. Reuse actual generation/assessment/refiner entrypoints with private role injection and no ambient fallback. | P1/P4 |
| G04-09 | Query/store regressions used disposable Neo4j, not the target production database/schema/privileges | Separate research reads, operational pilot writes and admin DDL; approve backup/restore and a fresh pilot scope. Verify actual schema/index compatibility and bounded queries before any pilot write. | P0/P4 |
| G04-10 | Synthetic native transport tests do not prove Kit/process-group/parent-death behavior | Rehearse real owned-child startup, hard deadline, cancellation, nonzero shutdown and cleanup on dedicated test work, not fault injection against shared DB/GR00T. Unknown cleanup blocks further release. | P2/P3 |
| G04-11 | A2 contract fixtures and typed policy projections exist; successful live manipulation is absent | Pin real GR00T instance/weights/processor/serializer/modalities and operational PickAndPlace evaluator. Retain completed episodes, raw predicates, denominator and independently inspect video. | P3/P5 |
| G04-12 | Installed typed generation/candidate/assessment/evidence/prior queries and bounded immutable bytes are implemented in the current P1 slice; final-source witnesses are owned by the canonical handoff | Verify authenticated causal traversal, exact bytes, integrity/scope rejection and query-only operation without execution credentials. Keep policy-trial/live-reader and operational recovery obligations separate; interface implementation alone does not complete P1/P4/P5/P6. | P1/P4/P5/P6 |
| G04-13 | The former catalogue path coupled descriptive reads to global simulator registration. The approved pivot instead binds the selected adapter's consumed vocabulary and real content digest; isolated installed E1 now passes without full registration | Preserve real request/result validation and server/owned-thread/worker context binding. Require supplied information actually consumed by the workflow, never fabricated production metadata. Do not reinstate S2 or comprehensive metadata parity as an orchestration gate; simulator compatibility and native/live operation require their own acceptance and authority. | A04-01/02/03/17; scoped adapter contract, not an S2 prerequisite |

P0 expands this bounded register if source review finds another relevant gap. Use finding IDs, a concrete counterexample and a smallest correction; do not turn every unknown into speculative infrastructure work.

## 4. Authorization, secrets and production boundaries

### 4.1 One bounded approval packet per campaign

After source/isolated gates, present a consolidated packet for approval; avoid repetitive approvals within an unchanged approved envelope. Reapproval is required when a listed boundary changes, a ceiling is exhausted or an outcome is unknown. No missing field inherits an unlimited default.

The packet must contain:
- source revision plus dirty-content digest; resolved entrypoints and dependency/image digests; scope, run/operation identities and the exact plan revision;
- exact discovered container IDs/mount mappings, runtime account/UID/GID/groups/HOME, simulator interpreter, policy-service ownership and endpoints; allowed actions for each existing service;
- explicit Neo4j endpoint/database/deployment/workspace; read-prior versus write-operational roles; allowed schema operations, backup/restore owner and pilot retention policy;
- literal generation/assessment/refinement provider models/endpoints/routing, task/policy/runtime/capture/evaluator revisions and seed schedule;
- total monetary ceiling and conservative rates, calls including pings/retries, input/output/image/token bounds, candidate/repair/observation/episode/native-step counts, startup/model/native/cleanup deadlines and whole-campaign wall time;
- GPU UUID/device allocation, co-residency decision, measured headroom and failure margin, host RAM/CPU/disk/artifact quotas, one active native job, approved asset/model-cache sources and whether any downloads are allowed;
- private credential source references/generation only, release authority/expiry, local API-auth instance, escalation contact and exact-owner emergency stop/reconciliation procedure.

Budget values are mandatory deployment selections, not invented estimates. Derive cold startup and per-stage allowances from approved calibration, then freeze final-run ceilings before submission. Calibration itself has its own small approved envelope. Preserve spent reservations across stages; a restart, failed process, unknown response or second seed does not reset the campaign cap. Distinguish measured provider usage from conservative reservations and actual billing. The allocation inventory includes the scene-only V1 run and the separate V2 generation-to-first-seed-policy run; neither is free because the other already passed.

**Selected cross-run enforcement:** existing reservations are per run (`neo4j_store.py:3302–3339`), not a campaign spending account. Before campaign effects, partition the approved envelope into fixed, nonoverlapping suballocations for paid readiness, calibration, lifecycle rehearsal, the scene-only V1 run, the V2 generation-to-first-policy run, any controlled repair witness and the second-seed reuse run. Validate that their conservative sums fit every cumulative ceiling and freeze an absolute campaign expiry. Freeze each operation identity, scope, resource slice, seed/policy pins and all already-known payload fields. For generation-dependent operations, also freeze a trusted deterministic derivation rule and designated predecessor operation: the second seed uses the candidate selected by that predecessor's exact retained accepted **scene decision**, including a permitted repaired candidate if that decision selects it. This is not a mutable “latest successful candidate” lookup or arbitrary model-supplied transform. After verified adoption, durably seal the resulting full payload digest and exact source decision/candidate references once, before the dependent operation's admission/release; then bind the returned run/attempt identity. Fully known requests bind their exact payload immediately. Recovery reopens sealed bindings; competing bindings, changed predecessors, seeds or caps reject without effects. Grants and the installed driver reject unlisted or mismatched operations.

Reuse Neo4j command/admission/profile records for durable allocation linkage and exact replay, not a mutable counter in an evidence file or another billing database. Diagnostic effects outside ordinary workflow runs need an equally exact one-shot owned invocation/retained receipt before they may use their allocation. Per-run guards enforce each slice; the finite allowlist prevents extra runs spending the envelope again. Never recycle unused/unknown allocations automatically after restart or failure. Additional runs or repartitioning need amended approval and must preserve earlier allocations. A04-15 requires isolated multi-run/restart exhaustion and unknown-candidate→one-time-binding positives; this is implementation work, not an existing campaign-budget capability.

### 4.2 Live credential handling

Use the existing owner-private, explicitly configured bootstrap source and trusted launcher. The operator supplies real keys through the supported private setup path; agents/reviewers never request key values in chat or inspect credential files. Saving keys is not authorizing calls. Do not discover `.env`, copy secrets into public configuration, argv, model prompts, Neo4j or evidence, or use whichever provider key happens to exist.

The launcher may read the selected secret source as part of separately approved execution; the plan does not prohibit that necessary runtime access. Give each child only its required private authority: native capture/numeric evaluation need no model keys; policy execution does not need cloud-provider keys unless the explicitly selected policy requires them. Never hand all DB/provider slots to every worker.

Review SDK errors, tracebacks, stdout/stderr, HTTP logging, GraphQL variables, process-environment capture and Docker diagnostics before live use. Redact at the emission boundary; scanning artifacts afterward cannot undo a leak. Use synthetic sentinels for positive and failure paths, do not scan real keys by printing them. Avoid unfiltered `docker inspect`/environment dumps. Record masked configured/authorized state, not secret hashes or values. Check provider data handling for the intended prompts/images and prohibit private unrelated research data in requests. A suspected leak stops the campaign and triggers operator-directed provider-side revocation; deleting a local file is not revocation.

The local single-user secret file remains plaintext accessible to its owner/root and backups. Loopback/token restrictions do not isolate a hostile same-user process or Docker-socket holder. This research trial does not certify multi-user production security.

### 4.3 Production Neo4j safety contract

Treat the user's production database as valuable shared data, even if it is on a local workstation.

1. **Observe first, explicitly authorized:** targeted metadata/schema/index/privilege and bounded exact prior reads only. No broad corpus dump, profile/write query disguised as readiness, lazy schema installation or test-fixture initialization. Access mode is not a privilege boundary.
2. **Choose deployment shape:** prefer a dedicated operational pilot database on the approved production service if supported and provisioned by its administrator. Otherwise use a new deployment/workspace pilot scope with reviewed scoped writers in the existing database. Namespace scoping is application isolation, not database ACL isolation; explicitly accept that residual risk or block. No automatic fallback from one choice to the other.
3. **Backup/restore gate before writes:** administrator selects the procedure supported by the actual Neo4j version, edition and deployment. Retain a recent verified recoverable backup/snapshot identifier, retention location and restore rehearsal/evidence from a separate target. Do not presume online backup or multiple user databases are available. If a safe backup requires downtime, schedule it; never stop production opportunistically. Restoring a whole production database is not normal pilot rollback.
4. **Schema gate:** compare required constraints/indexes and label/property conventions to the current database; review collision and legacy compatibility. DDL is separate approved administration, with bounded ONLINE waits and readback. Normal startup/submission performs no DDL. A scoped pilot still shares global schema in a single database.
5. **Writer gate:** open only the explicit DB/scope/artifact binding; same binding replay is idempotent, mismatch rejects. Disable legacy runner graph sync, version-tree publication and automatic promotion. Pilot operational facts are authorized writes; research graph updates/publication are not included.
6. **Noninterference evidence:** retain exact touched IDs/relationships and scoped before/after observations; audit reviewed writer predicates and selected foreign sentinels. Sentinels/counts cannot prove global absence of changes in a concurrent database. Where stronger claims are needed, use administrator-provided transaction/audit evidence or a maintenance window, not full private-data exports.
7. **Rollback:** stop only owned execution, retain diagnostic records/artifacts, revoke further grants and mark the pilot disposition. Never issue wildcard deletion or destructive cleanup of shared records. Any later deletion uses a separately approved exact-owned-ID plan and referential checks; publication remains a separate action. On ambiguous commit, reconcile exact operation/intent IDs instead of resubmitting or restoring the database.

**Paired evidence recovery:** the approved recovery point must include a DB cut plus a compatible snapshot or retained superset of all immutable artifacts referenced by that cut, with store/registry markers and exact manifests. A successful database restore alone cannot restore missing generation/prior/capture/policy bytes. Coordinate an administrator-reviewed quiescent cut or another demonstrated inventory/coverage procedure; do not claim atomic cross-store snapshots. Preserve orphan/staging files diagnostically and ambiguous work as unknown. On a separate recovery target, a fresh authenticated reader must reconstruct representative retained chains and verify exact bytes without generation, inference or simulation. Missing/tampered files block a recovery-complete claim; regeneration is not evidence restoration. Initial activation is read-only; any resumed effects require separate reconciliation/authorization. Preserve the existing restriction against divergent restored history under an unchanged cursor/binding identity: select and verify an explicit invalidation/identity-transition procedure before such a history is reactivated. Never silently reuse old cursors or weaken marker checks. This extends the existing backup acceptance, not a new backup platform or routine whole-production restore.

The artifact-coverage claim is scoped to retained workflow references in the explicitly approved recovered deployment/workspace, not every unrelated research record in a full database backup. Record that coverage boundary; do not silently certify or inspect foreign artifacts. The administrator still owns the broader database recovery procedure.

### 4.4 Heavy-container and GPU discipline

Resolve editor→host→simulator mount correspondence using the repository's [dev-container skill](../../skills/dev-container/SKILL.md); do not infer host identity from the editor's root HOME. Require exactly one selected runtime, not the first container matching a loose name. Run Arena package/native code with `/isaac-sim/python.sh` as the mapped non-root runtime user. Discover GR00T and Neo4j by their approved identity/configuration, not remembered names/ports.

Prefer already running, pinned services. Check accessible asset/model cache, writable run-specific output/TMPDIR/Kit paths, actual CUDA device identity and required Python dependencies before native startup. A query-test image or import success is not evidence the production simulator has the same optional API packages. Installations, image pulls, container recreation, mount changes and model downloads are distinct approvals. Never chmod broad runtime/cache trees to bypass permissions.

Keep one API owner inside the simulator namespace and its locally supervised children. The GR00T container is an independently owned inference service, not a child the workflow may kill by default. Preserve service state/settings and foreign jobs. For shared-GPU execution, show both simulator and served checkpoint fit together under the approved margin; serializing initialization alone cannot solve a rollout that needs both resident. Prefer distinct approved devices if co-residency cannot be established. Stop as unsupported-resource rather than silently offloading/changing model/dtype/physics.

A released native GPU flock is a physical-worker ownership fact, not proof all GPU allocations disappeared. Verify owned CUDA/process cleanup and account for declared persistent policy-server allocations. Slow VLM calls must not retain the native child/GPU lease; scope ownership may remain. Unknown process cleanup or contention blocks the next stage. No container-wide kill, shared-server shutdown, cache purge or `docker system prune` is a recovery mechanism.

## 5. Work packages and stop/go sequence

### P0 — Reconcile, review and freeze the campaign (no live effects)

**First operator-facing deliverable: setup-and-readiness report.** At the beginning of the project, Hermes presents one consolidated report of the nonsecret selections, predictable setup blockers and required operator actions. The user supplies nonsecret selections and enters keys privately once through the supported setup path; Hermes resolves the predictable setup blockers before expensive live work, then continues within the approved envelope without repeated setup questions. This cannot eliminate later service outages, expired/revoked credentials or physics failures, but it must prevent reaching the live trial before discovering missing provider configuration.

The report must list each role (generation, visual assessment, repair, local GR00T policy, Neo4j prior reads and operational writes), its exact selected provider/endpoint/model or database, credential alias/source binding and whether that binding is shared with another role. Show a provider-issued nonsecret key/project identifier when available and explicitly supplied, never key values or secret-derived fingerprints. Do not choose a provider by ambient key availability. Mark genuinely undecided selections as unresolved rather than inserting defaults.

Collect operator prerequisites together: model/image/structured-output access, billing/quota and approved cost ceilings, private credential setup, database permissions, runtime identity/mounts, checkpoint/assets and missing dependencies. Every blocker names its owner and next action: **operator setup**, **implementation missing**, or **runtime verification required**. Missing execution/provider setup support is implementation work, not something another API key can fix; the current installed query-only credential schema accepts operational Neo4j credentials, not the complete provider-role flow.

Include a concrete database compatibility row: actual URI scheme/address or DNS/routing requirements, TLS/certificate requirements, authentication type, and which private credential each read/runtime/admin invocation selects. The current installed slice accepts numeric IPv4 `bolt://` and Basic operational credentials, and uses that same credential slot for explicit administration; different application principal strings do not select different DB logins. Choose an explicitly approved compatible local topology or assign a narrowly required versioned adapter extension. Do not downgrade TLS, replace endpoints implicitly or grant runtime DDL privileges as a compatibility workaround; administration may remain operator-owned. Mark each missing prerequisite by the outcome it blocks: absent GR00T/policy setup blocks P5, not an otherwise supported policy-free P4.

Report **configured**, **access checked**, **capability verified** and **execution authorized** separately, with check time and configuration/credential-generation binding. P0 produces the offline inventory and checklist without reading secrets or contacting services. P1 implements the supported private setup/report path and tests it with sentinels. Subsequently approved bounded access checks and small actual text/structured-output/image probes complete readiness before the heavy live trial; any paid probe consumes its fixed campaign suballocation. No speculative setup command is advertised as working. Revalidate affected readiness before release when configuration, credential generation, model/endpoint or authorization changes; never silently switch providers/models/keys to bypass a blocker. The report contains no secrets and does not itself grant execution authority.

Read Plan 03's original per-interaction models and the current implementation, not only the notes. For each changed operation answer: what happened, to which identity, who knows authoritatively, where it is retained, and what the user may see/do next. Extend the existing model/requirement map, not a competing architecture.

Perform a bounded source/security review of the selected call graph: installed API/CLI → shared handlers → grants/reservations → workers → actual tools → artifacts → Neo4j adoption → retained readback. Include all runtime imports and legacy defaults; check SQL/Journal dependencies and unauthorized provider/publication effects. Review native and policy lifetime separately. Preserve positive paths and known isolated proofs.

Deliver: updated gap dispositions; explicit production deployment/topology choices and approval-packet template; dependency/source fingerprint; narrow implementation file ownership; mapping to Plan 03 R/G/T obligations. Run scoped host formatting/license/secret-sentinel checks and the admitted isolated regressions after source changes. Record baseline lint separately. No all-files autoformat churn, no protected-file changes without asking, no secret inspection.

Exit: no unassigned critical path. A changed source snapshot requires review/rerun of affected evidence. No fixed completion date or unsupported assurance that all possible bugs have been found.

### P1 — Finish the joined installed application boundary (isolated first)

#### Historical registration/runtime-boundary investigation — G04-13

This section records the superseded S2 decision and its then-current blockers. Its statements that E1 has not passed, that S2 remains selected, or that IF-A–IF-D must precede orchestration are historical. The current pivot above and canonical handoff supersede those gates; the investigation remains evidence about the old registration path, not instructions to restart it.

The installed synthetic integration attempts demonstrated that apparently descriptive catalogue/schema work imports runtime dependencies. The observed chain is catalogue hashing → model agent/catalogue builders → default registry loading → Isaac Lab environment/scene/articulation imports → `isaaclab.utils.warp`. Verified image source calls `wp.init()`; first-time Warp initialization loads a native library and invokes native `wp_init` before conditional CUDA queries/device enumeration. This is evidence of a required initialization path, **not evidence that it was executed**. Quiet mode, absent device passthrough and a later CPU default do not establish no-CUDA behavior. Full native internals remain unverified. Exact findings/reviews are retained under `outputs/workflow/plan04-implementation/installed-execution/`; the canonical handoff owns the status and evidence references.

**What is being tested:** E1 starts a disposable database and the installed workflow server, then should prove authenticated submit → client disconnect → application-owned synthetic workers → retained result → fresh-client readback and cleanup. Ownership, DB transactions, SDK request serialization and worker lifecycle must be real in this test; provider responses and scene capture are synthetic. This joined positive has not occurred. The current blocker is server composition/catalogue loading **before joined submission**, not failure of a simulator launched with two seeds. P5's later seed-bound policy pilot is a separate native/live obligation and is not a substitute for workflow correctness.

**Root cause and terminology:** lazy registration is a legitimate way to defer work and handle dependency cycles; it is not itself a defect. Here discovery/registration imports executable class modules whose transitive imports initialize a native runtime. Eager registration or moving the same import into a later function merely relocates the effect. Distinguish (a) no physics/rendering/policy inference or GPU computation, (b) no GPU device exposure, and (c) no native/CUDA-related initialization or probing. These are different contracts. Missing devices does not establish (c), and permitted probing would not authorize the workloads excluded by (a). Current permission still excludes the unreviewed initialization path; reconsidering that permission is a design decision, not evidence of a broken CUDA setup or permission to weaken guards.

##### Selected S2 approach, retained contingencies and complementary testing

**Decision:** S2 is the selected engineering direction. Remaining decisions concern its exact effects, controls, resource limits and admission—not which of four equal strategies to pursue. S1 and S3 remain documented only for a bounded, evidence-backed pivot if S2 cannot meet the agreed boundary or an explicit architectural requirement changes. Do not automatically switch or implement them in parallel. S4 complements S2 and never discharges real integration.

| Strategy | Mechanism and benefit | Cost, limit and required proof |
| --- | --- | --- |
| S1 — side-effect-free declarations plus explicit runtime bootstrap | Separate registration descriptions from class loading/runtime initialization; an explicit runtime composition step resolves classes only where needed. The opt-in canonical metadata design is one concrete form. A lightweight API/control process becomes possible. | Cross-cutting metadata ownership, inheritance/order/aliases/extensions and schema-context compatibility work. Cannot assume an Arena-only deferred import neutralizes eager upstream Isaac Lab initializers; protected upstream edits are not authorized. Prove cold **catalogue construction and real schema validation**, separately bound full metadata identity, and later real-class parity under its own authority. |
| S2 — runtime-faithful isolated registration | Keep the real registry/schema route in the pinned environment; define a separately reviewed test profile that could explicitly permit bounded native initialization/driver probing while excluding simulation, GPU computation, real providers and production stores. Preserves dependency realism and may avoid a broad metadata refactor. | Feasibility is unverified. Must identify expected initialization, native dependencies, cache/filesystem effects, device exposure, resource needs, enforcement/observation limits and owned cleanup. No-device exposure and denied egress are useful containment, not proof that arbitrary native code cannot probe or perform effects. No broad exemptions or silent weakening of old cohorts; if initialization needs more effects/resources, stop and revise the packet. |
| S3 — owned-process registration boundary | Keep heavy registry work in a separately admitted process using existing worker supervision; communicate bounded, exact versioned metadata/validation results to the application. Contains lifetime/failure and can keep the server lightweight. | Relocates native initialization; does not remove it or grant permission. Adds process/protocol/identity/cleanup costs. Catalogue export alone does not solve worker/parent schema revalidation. Prove the full validation boundary and reject metadata drift; reuse existing ownership, not a new metadata service or orchestration system. |
| S4 — dependency inversion for workflow contracts | Inject explicitly synthetic catalogue/validation ports into narrowly named contract tests while exercising real coordinator/DB transactions, replay, accounting and cancellation. Separates orchestration failures from runtime setup. | Useful complementary coverage only. Such tests cannot close real catalogue/schema compatibility or joined E1 acceptance, cannot supply fabricated production digests, and must not be presented as the production path. Keep a required real-adapter integration gate. |

Selected path: S2 through the hypothesis-driven IF-A–IF-D protocol. This selection does **not** establish feasibility or authorize initialization, implementation or experiments. Preserve layered testing: workflow/DB contracts → real registration/schema installed integration → separately admitted native calibration → live scene/policy pilot. Passing one layer does not discharge the others.

S2 admission packet before implementation: specify the required deployment/runtime arrangement, permitted initialization versus workload effects, exact supported catalogue/schema/extension behavior, final source/image identity, resource/cleanup/enforcement limits, changes/tests and acceptance traceability. Do not reopen strategy selection merely because the execution packet is pending. Current source/design inspection supplies a hypothesis, not a feasible executed route. Use §8's shared investigation budget and one consolidated decision if S2 requires an actual scope change or pivot; no new probes are admitted by selecting it.

If S1 is selected, its dependency sequence remains canonical declarations → pure projection → real-schema context at every seam → server/worker/adoption identity binding → joined E1. Preserve hidden members, effective inheritance/signatures/direct docstrings, trace-versus-reject semantics and exact catalogue ordering/digest meaning. Bind full validation metadata separately; never silently fall back to runtime registration in that mode. The [candidate design contract](event-mapping-registration-only-design.md#first-implementation-contract-to-approve) applies conditionally, not to every possible solution. S2/S3 require their own exact route/admission contract; existing runtime schemas and identity/replay checks remain mandatory.

E1 already selects the agent's `proposal_only=True` generate/refine routes, which return before USD/native grounding; earlier refinement affordance discovery is string-only. Keep that scoped route and its schema-only warnings. Do not advertise full generation, physical validation or policy execution as no-native. The agent's remaining import dependencies and every nested parser/revalidation call still require isolation and binding tests.

Exit for this prerequisite: record the chosen route and authority, pass A04-17's common and route-specific positive/negative witnesses on final source, preserve query-only/legacy regressions, then pass A04-02's installed client→owner→synthetic workers→fresh-client readback. No route drops that real integration obligation. Component counts, source probes, synthetic contract-only ports and safe stops cannot close E1/P1. Native simulation and live V1/V2 remain distinct acceptance levels.

**Current stop:** IF-A's packet was approved for the next bounded step. Implementation is partial and release remains closed: reused discovery/provision helpers need a command-forwarding seam in one shared file outside the packet's file scope. The canonical handoff/NR04-16 and `outputs/workflow/plan04-implementation/s2-initialization/scope-amendment.md` retain the reviewed boundary, corrected partial source, remaining budget and single amendment request. No diagnostic/native attempts have run and E1 has no joined positive. The protocol remains the governing contract for unrun IF-B–IF-D; S2 stays selected, with no automatic S1 fallback or expanded effects.

##### Hypothesis-driven initialization feasibility protocol

**IF-A packet prepared:** [S2 initialization-feasibility and approval packet](event-mapping-plan04-s2-initialization-approval.md) specifies commands/roles, proposed implementation/effect changes, finite run matrix and numerical limits. Independent safety/lifecycle review passed; the budget review's accounting finding was resolved by a timestamped conservative ledger. IF-A preparation is complete, not execution approval: no new selector or control has been implemented and IF-B/IF-C/IF-D remain unrun. The packet records45 active minutes, one round and three diagnostic attempts remaining, not a renewed budget. Implementation and effects require its explicit approval and final-source gates.

**One question:** can real catalogue construction and schema validation run in the pinned environment, within an approved initialization-only effect boundary, sufficiently to exercise the installed workflow? Success is a source/environment/role-specific result, not proof about arbitrary native code or the whole dependency stack. Keep uncertainty visible rather than aiming to reverse-engineer every native library.

**Hypothesis:** the actual registration dependencies can initialize without simulator launch, rendering, policy inference, GPU computation or external-service effects. This hypothesis may be disproved. Merely observing an effect does not authorize it; subsequent approval cannot retroactively turn a boundary violation into a passing run. No monkeypatched initializer, fake registry/catalogue digest, skipped real validator or automatic fallback is permitted.

**Proposed lifecycle:** validate configuration and execution-mode authority → enter the reviewed registration/bootstrap phase → construct the real catalogue and exercise real schema checks → construct the workflow owner → report execution readiness → admit submissions. Check early imports before the nominal bootstrap call: moving `wp.init()` into a named phase is ineffective if module import has already initialized it. This lifecycle is proposed, not implemented. Preserve query-only startup and distinguish a health listener from readiness to accept workflow work. Initialization failure never admits a workflow; failed/partial initialization is disposed with its owned process rather than assumed safe for an in-process retry. Every cold server/model-worker role must use its admitted path; warming the parent does not establish worker initialization.

| Gate | Work and required decision | Evidence/exit |
| --- | --- | --- |
| IF-A — source-only contract and pre-run critique | Reuse retained import evidence. Freeze the exact real entry points and cold-process roles, source/image/native dependency identity, trusted dependency assumptions, permitted initialization/probes/cache writes, forbidden effects and controls below. Fresh read-only critic challenges whether the proposed controls support each claim. Assign bounded source ownership and exact tests for any necessary harness changes. | A complete approval packet with explicit unresolved risks and numerical limits, not runtime proof. User approval of implementation/effect scope and a final-source pre-run review are prerequisites to IF-B. Existing import approval alone is insufficient. |
| IF-B — real initialization/validation witness | In newly admitted disposable resources, execute the smallest representative **real catalogue plus real schema** path for the required cold roles. Validate ordinary supported inputs and representative invalid keys/relations/references; establish catalogue/digest agreement without stubs. Capture startup boundaries, observed side effects, resource usage and exact cleanup. | Supported within the agreed boundary, a bounded fix, disproved feasibility, or inconclusive. A guard/staging refusal is not an application pass. No one-probe-per-import cascade without a discriminating question. |
| IF-C — lifecycle/failure witness | Within the same explicitly approved plan, exercise an exact owned initialization failure and deadline/termination path; a bounded fixture may trigger failure without suppressing successful runtime initialization. Preserve diagnostics before teardown; verify no readiness/admission on failure and no remaining owned children or shared cache modifications. | Actual failure disposition plus exact supervisor/container/process cleanup evidence. Outer teardown proves containment only; graceful application cleanup requires its own witness. Unknown cleanup or missing diagnostics blocks further runs. |
| IF-D — return to installed E1 | Once IF-B/IF-C and their review gates pass, stop speculative initialization investigation. Run the actual authenticated submit→disconnect→owned synthetic workers→durable result→fresh-client readback route with the proven source/image/effect profile. | A04-02/03/17 evidence, unchanged query-only regressions and final-source cleanup proof. Existing E1 still needs an explicitly amended/reviewed admission for this initialization route. No manual orchestration, contract-only substitute, live provider/simulation or P5 seed claim. |

**IF-A contract fields (must be concrete before any execution):**

- Exact command/role inventory and import entry points, including server startup and fresh model-worker schema/catalogue revalidation; source, image and selected native-library identity; allowed module origins and trusted dependencies. Do not execute setup hooks or acquire dependencies to discover them implicitly.
- An effect matrix classifying each effect as **prevented by a named mechanism**, **permitted and observed**, or **trusted/unobservable residual risk requiring explicit acceptance**. Cover native library load/initialization and driver/device probing separately from computation; OS device access, network destinations, process creation, writable/cache paths and shared mounts. Logs alone are not enforcement and absence from a log is not proof of absence. If a mandatory prohibition lacks an adequate control/accepted assurance basis, do not launch.
- No GPU device exposure in the initial proposed cohort; freeze actual device/mount configuration and don't rely on that fact to claim no driver probing. No physics, Kit launch, rendering, GPU kernels, policy/provider inference, asset-stage loading, remote acquisition, production DB or shared-service effects. Exact owned disposable DB/loopback and synthetic provider transport remain as admitted; install synthetic transport before any allowed provider constructor. No server-role provider construction exception follows from native initialization approval.
- Owned temporary cache/output locations and an approved sanitized environment; no credential discovery, shared HOME cache writes or mutation of image/dependency bytes. Document any import-time cache/default-path behavior that cannot be redirected under the existing API; unsupported routing stops rather than assumes containment. Instrumentation must not itself initialize the forbidden runtime or require new privileges.
- Numerical memory/CPU/PID/time/output/child limits, a separate cleanup allowance, and an outer deadline that still bounds initialization if Python tracing or native calls block. Define cleanup ownership, exact identities and non-root execution using existing harness supervision. Specify failure diagnostics and incomplete/overflow behavior; no silent truncation interpreted as a clean pass, and no blanket scan/kill of unrelated jobs.
- A finite run matrix mapping IF-B/IF-C cases to counted invocations and child roles, including negative/failure/timeout witnesses. Freeze canonical catalogue/schema outcomes, disagreement rejection, acceptance predicates and subsequent IF-D admission. Changes to source/profile/roles invalidate affected evidence and need re-review; they do not replenish the investigation budget.

**Agent/evidence loop:** one parent owns scope, ledger and budget; a bounded implementer prepares only authorized files; a fresh critic checks the exact proposed run before its first effect and each material boundary change; a verifier reads the final source-bound results and exact owned cleanup. After each invocation, classify what was observed, what was inferred, and what remains unknown. Before any repeat, record the prior result, explicit changed variable/hypothesis, why the next run discriminates between explanations, expected outcomes and the action for each. No repeat solely because the last run failed. Reviewer agreement does not replace actual evidence.

**Predeclared dispositions:** (1) IF-B/IF-C establish feasibility within the approved boundary → proceed to IF-D only under its admitted scope; (2) a reproduced implementation defect inside that boundary → bounded correction, charged to both relevant budgets; (3) required effect/resource exceeds it → stop and compare amended authority with S1/S3, never silently widen; (4) evidence is missing/inconclusive or budget expires → retain that result and stop, without claiming impossibility. Unknown cleanup first requires exact-owner reconciliation. A disproved narrow hypothesis may justify the next predeclared alternative only within remaining scope/budget; it does not authorize a new investigation automatically.

#### Remaining installed application work

Add explicit execution mode/configuration and typed GraphQL submit/cancel/resume command adapters that delegate to existing shared handlers. CLI is an authenticated client, not a second owner or fallback executor. Preserve query-only startup and old retained shapes; never remove guards to repurpose the isolated factory.

Deliver P0's setup-and-readiness report and private provider-role setup as the first operator-facing P1 slice, before heavy-runtime integration. Sentinel-only tests must prove exact role/provider/model/credential-alias reporting, shared bindings, consolidated missing-setup actions, no effects during offline inspection, and invalidation of stale readiness. Live probes remain separately authorized; collecting keys is never a prerequisite for continuing dependency-ready source/isolated work.

**Bounded setup status:** configuration v3/private credentials v2 and the existing offline report are implemented and isolated-tested, including role-scoped sentinel delivery, fail-closed aliases/transport/missing sources, source-generation invalidation and preserved current-read access. Repair must explicitly share the generation profile and alias under the current workflow contract; independent repair bindings remain unsupported. Supported DB transport remains numeric IPv4 Bolt/basic. The guarded synthetic factory is not ordinary production composition. The isolated nonempty-prior slice is now verified as described below; see the canonical handoff for all seven final-source installed cases and reproducible commands.

One long-lived execution owner handles committed intents independent of requests; offload blocking work without holding API request/global authority locks across simulation/model calls. Use current private grant interlocks at release. Bind operation payload/command receipt/run identity atomically and recover lost acknowledgements without creating duplicate work. Re-authenticate read/replay without demanding fresh provider secrets; new release after owner restart needs explicit reapproval.

**Changed-configuration handover:** retain the old immutable configuration C1 for exact stop/reconciliation before changing endpoint/profile/mode to C2. The bounded operator implementation now provides explicit `api-handover --previous-config C1 --previous-instance OLD --config C2 --instance NEW` under the existing lifetime/metadata locks. It requires C1's exact retained configuration, process absence and `stopped/drained` outcome; unclean or unresolved ownership must be recovered with C1 first. It preserves tombstones, durable scope/private roots and allocations. Bare launch or control using C2 still cannot substitute for C1. The installed `operator` case covers C1→C2 query-only handover/replay and fresh retained reads; the separate `rotation` case replaces sentinel credentials under unchanged C1 and resumes known-unreleased work after restart and fresh authorization. See the canonical handoff for final-source evidence and remaining limitations; this is not production composition or all-P1 completion.

Join exact supported provider/prior/capture/policy profile revisions and whole-outcome readiness. Requests needing an unsupported later phase fail before paid initialization. Complete the chosen Neo4j-only prior adapter; retain exact consumed bytes and label empty/optional/unavailable distinctly. Do not call the full managed-prior path complete if only a declared legacy Neo4j profile works.

**Executable provider price bounds:** freeze a versioned per-role request envelope and a reviewable conservative pricing basis in the selected existing model profile. Before every SDK send, validate the complete serialized request against it: prompt/catalogues, response schema, retained prior or repair context, image count/dimensions/detail, output/reasoning allowance and routing parameters. Include constructor/readiness probes. A total-token assertion cannot simply be used as the completion-token limit while ignoring input; image byte bounds alone are not a provider billing bound. A universal conservative price for every request inside the envelope is acceptable—exact per-request tokenization is not mandatory—but its derivation must cover every admitted billable component. Reserve before transmission; unknown pricing, unsupported shape or out-of-envelope parameters reject before transport. Actual generation/refinement/assessment serialization tests must cover boundary positives and oversized aggregate input/schema/image negatives, plus profile/rate/route changes invalidating the bound. Retain pricing source/version and assumptions; these controls bound authorized attempts conditional on the verified rate basis, not the provider's entire account invoice. No new pricing service is required.

**Visual subject grounding:** extend the versioned assessment request, worker packet and answer/replay binding together with a bounded subject-grounding record derived from verified candidate bytes and selected asset/runtime mapping. Existing `CandidateBinding` carries digests and the current prompt may expose only an opaque ID such as `item_7`; a hash does not tell the VLM which depicted object that ID denotes. Retain the exact ID→asset/instance/description association and transformed frame identities; reject missing, tampered or swapped mappings. A descriptor is not itself observed identity. Restrict the first supported profile to visually unambiguous targets and represent unresolved/indistinguishable instances as inconclusive/unsupported—not “not visible,” a pass, or permission for visibility repair. Verify the actual prompt through the owned worker, not only a helper dictionary. P3 calibrates visible/occluded target examples with a visible distractor under the frozen image transform, without introducing a general segmentation subsystem or converting VLM claims into physical facts.

**Selected candidate-binding sequence:** before generation, admit immutable policy/task/runtime capabilities, expected served-instance pins, criteria/aggregation and deterministic binding-derivation rules—not a guessed future candidate digest. After verified candidate adoption and before scene/policy release, derive and durably freeze the candidate-specific policy binding under the existing fenced transaction boundary. Preserve original task/policy pins, seed, budgets and deadline; recovery reopens that exact binding rather than deriving it from current defaults. The current scene-start check (`neo4j_store.py:3493–3495`) correctly rejects a mismatched preconstructed profile; later repair-candidate substitution is not a solution for the initial unknown digest. P1/P2 must join this derivation without weakening the check and prove a genuine generation output whose digest was unknown at initial admission. Candidate-specific unsupported geometry/evidence still stops before policy effects; capability readiness is not a prediction that generation will yield a valid scene.

P1 owns the typed provenance-preserving **reuse admission** needed for the second seed; P2 owns its native/policy consumption. Reuse immutable candidate bytes/digest with authoritative linkage to the original generated candidate, not another run's candidate ownership, generation fence or scene evidence. The new seed-bound run gets its own accepted command, candidate identity, fresh prerequisites and evaluation receipts. `ExistingSource` text alone does not implement this path, and current same-run adoption guards must remain. Test successful exact reuse plus cross-scope, tampered-byte and foreign-receipt rejection before P5; do not discover the missing command after paying for the first seed.

**Bounded A04-04 status:** workflow schema 2 now freezes the explicit legacy Neo4j source/database/credential alias, eligibility/settings digest, required/optional policy and permitted-empty selection; schema 1 wire bytes remain unchanged. Separate `retrieval_read` authority, operation-boundary expiry/revocation checks and the existing GraphRAG snapshot path precede any worker release, without operational-credential fallback. A scope-locked one-shot selection and authoritative Neo4j linkage bind the exact immutable prior bytes, contract, disposition and context digest. The installed `prior` case interrupts after retention and before release, mutates the disposable corpus, withdraws the prior login, and verifies restart/reapproval consumes the original context in the actual deterministic SDK payload without a second retrieval. Missing, tampered, oversized or ambiguous linkage/bytes fail closed, including query-only authenticated readback. Required/optional/empty outcomes and expiry failures have focused regressions. Current read authority remains necessary; no cross-store atomicity, automatic retry of incomplete selection, every-crash-point recovery, managed-prior migration or live-provider/production acceptance is claimed. Final-source seven-case and privacy/cleanup evidence is in the canonical handoff, including explicit historical diagnostic-redaction limitations.

Expose the already modeled candidate→assessment→evidence and generation→prior detail through bounded typed queries, plus an authenticated exact artifact-byte retrieval contract. Reuse service/artifact validation, scope and historical ownership checks; no public filesystem paths or arbitrary Cypher. Enforce whole-response and per-artifact ceilings, including encoded expansion, and reject tampered/cross-scope access. Host-side artifact inspection is corroboration, not a substitute for fresh-client access. Keep command receipts, available actions and capability reporting honest about still-unsupported resume branches; either complete initial-no-reservation/validation-to-first-scene continuation or prove explicit truthful blockers before live use.

Exit: installed second-client submission→owned synthetic execution→durable result→fresh-client readback passes with egress denied and disposable Neo4j, plus active keyed cancellation/recovery and no-SQLite selected-path positives. Existing query-only regressions stay green. Parser-generated help and exercised commands become the only runbook syntax.

### P2 — Finish concrete native/policy composition (isolated, then dedicated native rehearsal)

Implement the owned policy child around `run_managed_policy`, actual runtime/task binding verification, existing GR00T policy adapter and retained episode decoder. Register before release; no initialization/inference/reset before its exact authorization. Preserve original deadlines and campaign step/episode accounting, including per-reset prerequisite work. Retain reports before process-terminating Kit shutdown and require genuine child success/verified cleanup for adoption.

Resolve G04-05 explicitly: capture and policy are separate registered workers, so policy construction cannot borrow earlier state-dependent evidence merely by candidate/seed. Freeze which prerequisites need fresh numeric or visual evidence on each reset. If a visual obligation needs slow VLM reapproval of the new cohort, design and test an explicit bounded pause/retained-state/revalidation protocol before claiming support; keeping a GPU lease across the call or restarting into an unmeasured scene is not an acceptable shortcut. Limit the first supported profile if necessary and report the narrower capability.

Provide a preparation seam for an **already-reset** cohort: `initialize_and_settle` currently calls `env.reset()` (`native_realization.py:217`), while the policy runner invokes its hook after reset/autoreset. Calling that helper unchanged would add a hidden reset. Preserve the legacy entrypoint while reusing hold/settle mechanics without resetting twice; test initial entry, autoreset, termination during preparation and the actual recorder's reset/index order.

Exercise receipt corruption, EOF/nonzero child, failed Kit close, cancel before/after release, monotonic deadline across blocking inference, parent death and recovery. Use existing isolated harnesses; any necessary native-capable test profile is an explicit reviewed addition, never a widened synthetic admission check. Native failure-injection jobs use a dedicated owned child and disposable/inert DB/provider dependencies. Do not crash shared Neo4j or kill the shared GR00T server.

**Separate P2 exits:** the scene exit covers owned capture/assessment/repair composition and dedicated native-child lifecycle rehearsal. It can advance to scene calibration and P4 without GR00T. The additional policy exit requires concrete policy-worker/runtime/evaluator ports and a named A2 per-reset prerequisite strategy with an isolated positive witness; it gates policy calibration and P5. P3 owns the corresponding separately approved native witnesses. A strategy requiring fresh cloud visual assessment needs an explicit model-role/call/cost calibration suballocation; excluding cloud generation/refinement is not assessment permission. Unsupported policy prerequisites block required-policy release before paid initialization, not a calibrated scene-only request. Do not weaken obligations or treat callback assertions as native proof.

### P3 — Bounded fixed-candidate native and policy calibration

After the calibration approval packet, use the exact A2 asset/task profile but no cloud generation/refinement. This is diagnostic calibration, not V1/V2 delivery. Run cheap existing geometry/reach/camera checks before expensive rollouts; consult authorized exact historical priors/diagnostics before repeating known failures. Diagnostics inform selection, not fabricated physical success.

Native calibration must record:
- graph subject→effective instance/prim→solver pose→reset/settled measured pose, environment origin and quaternion convention; placement-validator verdict including fallback/nonconvergence;
- actual action term ordering/scales/offsets/gripper semantics and measured posture preservation; strict unrounded `||v|| < 10^-3 m/s` for every required subject through the frozen consecutive window, with declared angular bound and every settle step charged;
- actual camera enablement/observation keys, timestamps/step offsets, RGB freshness and view alignment; evaluator encoding/resizing/byte ceilings separately from unmodified policy images;
- physical filter/source/endpoints, real nonzero contact vectors and absent-contact negatives; supported numeric predicate thresholds, proximity limitations and independent video inspection;
- no hidden reset between settle and capture; every policy reset separately prepared, termination/truncation detected before post-autoreset state is mislabelled, terminal image absence explicitly retained;
- successful normal teardown, common-lease release and native process/device cleanup while leaving unrelated services untouched.

These are not all existing receipt fields. Add bounded versioned provenance where necessary: `relation_solver_interface.py:98–109` currently warns about best-loss layouts that failed strict validation, and native capture does not yet retain a selected-layout validator receipt. Preserve the selected reset layout and verdict before logs are suppressed; include a no-effect/relation-overridden edit as a negative repair witness. Current spawnable-object mapping and center-proximity support do not certify arbitrary tabletop/background subassets. Resolve the actual A2 table/plate/banana sensor paths; unsupported mapping needs a tested evaluator/mapping revision, not removed assertions. Retain actual observed camera pose/calibration/resolution and timestamp correspondence, not only requested camera names.

Use real GR00T readiness on the actual served instance: pinned checkpoint/processor/statistics/embodiment/config/module identities, native serializer/modalities and bounded synthetic-array echo. Metadata readiness is not inference. Only after separate policy-effect approval perform bounded real inference and check action shape/order/finiteness/horizon; recheck instance identity at receiving action/reset dispatch and before buffered action release. Do not replace an expected identity with whatever a reconnect returns. Record remote-policy randomness and reset/seeding limitations.

Managed policy admission must require independent expected-server pins even though the legacy remote-policy path permits them to be absent. The runtime attestor observes actual objects/settings; it must not echo the requested binding. Extend provenance beyond model-directory hashes to the loaded SDK/Eagle configuration/module dependencies, policy joint mapping and preprocessing, because `isaaclab_arena_gr00t/policy/serving_metadata.py` explicitly limits its model-directory attestation. Retain exact locally resolved artifacts without downloading replacements during readiness.

**Selected resolved asset provenance:** after candidate selection and before its native release, retain a bounded content manifest for the actually selected robot/object/table USD dependency closure, including loaded geometry, collision/material/texture resources and resolver context. Registry IDs and top-level staging URIs do not identify all resolved bytes. Bind the manifest to the candidate/calibration/trial receipts, retain approved immutable local content, and reject missing/unapproved dependencies or nested-resource drift under an unchanged URI. Discovery or fetching is separately budgeted/authorized preparation; native execution must not silently download replacements or fall back. This covers selected assets only, not the whole catalogue/cache or a new asset-management service. Test unchanged cached resolution and changed nested content. Content identity does not establish deterministic physics.

Freeze tolerances and acceptance predicates before each calibration experiment. If settings must change, retain the rejected result and produce a new versioned profile/contract; never reinterpret earlier evidence under new thresholds. No physics/gravity/friction/task/asset change to force a green result. **Separate P3 exits:** scene/native calibration, target-grounded visual assessment and supported repair gate P4; the additional actual policy-runtime/co-residency and per-policy-reset proof gate P5. A policy blocker causes zero subsequent paid initialization for required-policy requests but does not block supported scene-only generation. Final Plan 04 completion still requires both V1 and V2.

### P4 — First complete live scene workflow (Plan 03 V1)

After production read/write/provider/native approval, P0/P1 and the **scene-required portions** of P2/P3, submit once through the installed GraphQL client. Policy composition, GR00T readiness and per-policy-reset calibration gate P5, not this scene-only V1 milestone. Prove the distinction in isolation: ready scene dependencies permit a policy-free request while unavailable policy capability rejects the equivalent required-policy request before provider initialization. Use the exact catalogue prompt, frozen retrieval policy and generation/assessment profiles. The application, not the operator, drives all stage transitions. Capture/cleanup/GPU release precede model assessment; retained assessment must not rerender. Numeric-only branches must not create idle model workers.

Retain all generated candidates and failed attempts. For an allowed visibility failure with physical prerequisites established, actual refiner receives original/current candidate, exact failing evidence and allowed original-centered XY envelope. Validate the full returned candidate and solver-effective displacement; fresh capture/reassessment is mandatory. No Z/task/physics/topology edit or invented physical diagnosis. If the chosen candidate passes without repair, run one separately budgeted controlled visibility-repair witness under the same application composition; label it controlled, not spontaneous generation evidence. A naturally unrepairable result stops honestly.

Fresh client queries the complete causal chain and artifacts after the submitting client disconnects. Lost ACK replay uses the same operation identity and makes no duplicate provider/native calls. A supported normal workflow stop is an integration observation, not a positive scene gate; at least one valid generated scene and the supported repair witness are required for V1 closure.

### P5 — Required-policy A2 pilot through the same API (Plan 03 V2)

Reference instruction: **“Grasp the yellow banana from the right side of the table and set it onto the white ceramic plate on the left.”** Bind `banana_ycb_robolab`, `plate_large_vomp_robolab`, `droid_abs_joint_pos`, the selected table/runtime and exact policy identity. Preserve the catalogue instruction rather than the paraphrase in the notes.

Pin the actual PickAndPlace task/evaluator and its lift-before-place, airborne dwell, destination contact/velocity/proximity sequence. Height peak, XY proximity, a VLM verdict or termination alone is not this task's success. Operational task acceptance is not independent grasp certification.

Require both scene and policy P2/P3 exits. Use fixed immutable generated candidate bytes and policy identity, with **one invariant experiment template instantiated into two predeclared immutable seed-bound profiles**, and one completed episode per distinct simulation seed. Simulation seed is part of the current native settings digest, so the literal profile digest cannot remain identical across seeds. Before launch, explicitly choose and freeze simulation versus placement seeds, effective reset-layout mode and pool/reset-consumption policy. Either fixed placement or predeclared seeded layout variation is valid; record which scientific comparison is intended. Do not leave placement entropy or inherited reset defaults undeclared. Verify compiled profiles differ only in approved seed-dependent fields and that both simulation and placement/reset settings reach the builder; stale first-seed profiles reject second-seed execution. Retain actual selected layout/reset identities and fresh prerequisites, without claiming bitwise physics or remote-policy determinism.

Use two explicitly linked one-seed run IDs with the current scalar-seed bindings; do not count repeated resets as independent seeds. Candidate reuse follows P1's provenance-preserving admission and the one-time dependent-allocation binding, not manual DB insertion. Preserve every attempted/stopped/incomplete episode. At least one joined submission must include generation→scene→policy, not just evaluation of a hand-fed YAML.

“Predeclared profiles” means the invariant template, seed schedule and derivation rules are approved before generation. Candidate-dependent subject/layout bindings and exact seed-profile digests are sealed once after candidate adoption and before the relevant native release, using the same P1/§4.1 derivation discipline. Do not require unknown candidate fields to be guessed at setup or allow mutable reinterpretation on seed-two recovery.

Budget initial prerequisite work, every policy step, subsequent reset preparation, episodes, policy-server calls and all cleanup. Scene acceptance is nonterminal for these requests. Failed/unknown/incomplete policy results must preserve scene facts but never leave the run accepted. Empty episode files mean unknown denominator. Report raw successes/completed episodes, unknown/stopped counts and seed-specific outcomes; no cherry-picking, dropping failed seeds or automatic reroll. Two successful seeds establish a narrow pilot, not robustness or method efficacy.

Exit for positive A2 pilot: both predeclared seed obligations pass the frozen operational task predicates, episode bytes/video agree, exact served-instance/task/candidate pins remain valid, budgets/cleanup hold and fresh-client queries reconstruct the results. If either seed fails, close the attempt as failed/partial, diagnose from retained evidence, and obtain a separately versioned/approved next experiment rather than relaxing this one.

### P6 — Independent readback, reconciliation and handoff

Use existing exact retained queries and proof readers, not new graph labels or a parallel lifecycle ledger. Another authenticated client reconstructs the original command, frozen request, exact prior bytes, candidate lineage, capture/reset/window/measurement/frame identities, assessment, repair permission/decision, policy trial/episode accounting and cleanup disposition. Validate artifact bytes against their manifests from the allowed root; distinguish intended producer target from observed-state attribution.

Verify production writes only within the approved role/scope contract and retain administrator evidence appropriate to the concurrency limits. Verify owned processes/leases and exact trial-created resources, plus shared-service state preservation. Read access/replay must survive loss of execution grants; uncertain released work remains blocked and never silently redispatched. Controlled fault recovery belongs to rehearsal, not destructive tests on production.

Deliver a bounded report with separate implemented / isolated-tested / live-verified / deployed statuses, failed attempts, residual risks and exact evidence URLs/paths. Update the canonical handoff, not every historical paragraph. Recommend frontend work only after the required joined V1/V2 evidence is complete; any accepted exception needs an explicit user decision and must remain visible.

## 6. Acceptance matrix

Each row names new verification obligations, not tests already performed. Track executable test names, final source digest, evidence path and pass/fail/blocked/unrun in the review/handoff record. No row may be discharged by a different evidence level.

| ID | Obligation and positive/negative witness | Environment | Upstream coverage |
| --- | --- | --- | --- |
| A04-01 | Current-source traceability and all selected runtime imports; Neo4j-only positive route, forbidden SQLite/ambient effects rejected | Static + admitted isolation | T01/T13/T20, G01–G10 |
| A04-02 | Installed GraphQL submit/receipt/status from second client; query-only mode unchanged; wrong principal/scope/payload rejects | Disposable DB, denied external egress | T02/T09/T16/T21, G01/G02/G03 |
| A04-03 | Active keyed cancel, client disconnect, lost ACK replay, restart/reapproval and known-unreleased resume; uncertain release is not retried | Owned isolated processes + disposable DB | T04/T10/T11/T15, G01/G10 |
| A04-04 | Frozen retrieval source/eligibility/empty policy; full-outcome readiness blocks before constructor ping; nonempty bytes consumed; interrupted continuation reopens original bytes without retrieval despite changed corpus/credentials | Isolated, then approved bounded live read | T03/T05/T13/T17, G02/G04 |
| A04-05 | Private sentinel transport/error/log screening; no secrets in argv/public files/DB/artifacts; stale/revoked grants block new release | Isolated; runtime masked checks only | T03/T09/T21 |
| A04-06 | Exact production DB/schema/role/scope, recoverable backup and restore evidence, reviewed DDL/ONLINE indexes, scoped noninterference | Admin-approved metadata/read/write windows | T13/T15/T17 |
| A04-07 | Native source/object/pose/contact mapping, posture hold, strict settle equality/failure cases, camera/default/override and real arrays | Dedicated approved native calibration | T06/T14 |
| A04-08 | Same capture cohort and absolute windows; new policy reset remeasures prerequisites; terminal/autoreset pixels not confused | Isolated + dedicated native | T06/T08/T14 |
| A04-09 | Real child normal/failure/timeout/cancel/parent-death cleanup; common GPU release before slow VLM; persistent GR00T memory accounted | Dedicated owned-child rehearsal, then observed live | T09/T14/T15 |
| A04-10 | Pinned real policy/serializer/modalities; peer replacement fails before effect/release; actual action interface and episode decoder match | Isolated adversarial + approved GR00T calibration | T08/T19 |
| A04-11 | One installed submission drives generation→native→numeric/visual assessment; fresh client gets exact result; actual role transport/cost bound | Approved live V1 | T03/T05/T06/T18 |
| A04-12 | Actual permitted visibility repair→effective XY change→fresh capture→reassessment; forbidden changes reject | Isolated negatives + approved controlled live witness | T07/T14/T18 |
| A04-13 | Unknown-generated-digest→durably bound policy positive; nonterminal scene→owned policy; provenance-preserving second-seed reuse with new ownership/fresh evidence; complete task predicates on both seeds; failures remain nonaccepted | Isolated joining + approved live V2 | T08/T18/T19, G09 |
| A04-14 | Exact command→event cause→candidate/evidence→decision→trial GraphQL traversal, typed prior/detail and authenticated bounded artifact bytes after disconnect/restart; tamper/cross-scope rejection | Disposable tests + fresh live reader | T10/T12/T15/T16, G03–G10 |
| A04-15 | Fixed cross-run suballocations sum within campaign ceilings; exact-operation grants reject extra/mismatched/restarted duplicate spending; per-run monetary/call/token/step/episode/time bounds include preparation/retries/uncertainty; no refund | Isolated multi-run/restart exhaustion + observed live ledger | T03/T08/T14/T15 |
| A04-16 | Final owned cleanup and shared service preservation; original/failed evidence retained; publication remains absent without grant | All effectful phases, final exact readback | T09/T11/T15/T18 |
| A04-17 | Preserve the approved orchestration/runtime boundary: an explicit adapter contract contains the vocabulary actually consumed by the workflow, genuine bytes/digest and required validation; bind it at server, owned-thread and worker seams, reject mismatches before model work, preserve replay/query-only behavior and verify joined E1 readback/cleanup. Optional simulator discovery must not become a startup dependency. This certifies the selected orchestration adapter contract, not complete simulator metadata, extension parity, registration, native execution or model quality; those remain separately authorized compatibility/live gates. | Approved isolated orchestration/database slice; existing effect isolation, exact cleanup and no fabricated production metadata | G04-13; A04-01/02/03; adapter contract and separate simulator compatibility |

The renewed review adds the following explicit witnesses to these existing rows (not another test framework):
- **A04-02/A04-03/A04-05:** first-operator report, selected DB transport/auth compatibility, C1→C2 checked handover, unchanged-config key rotation, unresolved-owner block and stale-token rejection. Test with sentinels/disposable targets; neither principal names nor status labels stand in for actual privileges/readiness.
- **A04-04/A04-11:** calibrated scene-only positive with policy unavailable, and equivalent required-policy zero-paid-initialization rejection.
- **A04-06/A04-14:** restore a compatible DB cut plus referenced artifact coverage on a separate target; authenticated byte readback, missing/tampered-byte rejection and divergent-history cursor invalidation. No provider/native effects during recovery verification.
- **A04-07/A04-08/A04-13:** approved invariant template compiled to distinct simulation/placement/reset-bound profiles, observed layouts, stale-seed-profile rejection and selected asset closure/nested-byte-drift checks.
- **A04-11/A04-12:** actual owned-worker prompt carries opaque-ID grounding; swapped/missing descriptors or ambiguous duplicate instances do not establish visibility or trigger repair. Calibrate actual transformed visible/occluded targets with distractors separately from transport tests.
- **A04-13/A04-15:** unknown upstream candidate becomes the single sealed downstream operation payload; changed predecessor/candidate/seed/cap, competing binding and restart rebinding reject. Actual serialized provider requests at admitted limits pass; oversized aggregate context/schema/images, unsupported output/routing and invalid price basis reject before send while failed/unknown reservations remain charged.

Mandatory review cross-checks: maximum writer-admitted payload remains readable; retained history pins the exact producing intent/fence; scene disposition survives all policy failure/budget/cancel branches; per-run evidence never inherits another owner's cleanup; numeric settings are executable frozen bytes rather than labels; general task semantics remain outside core coordinator/GraphQL branches.

## 7. Evidence and operator deliverables

Reuse the existing artifact/receipt store. Proposed campaign root `outputs/workflow/plan04-live/<campaign-id>/` is an evidence bundle, not a new authority database; actual durable artifacts belong on the approved mounted evaluation volume and public reports may reference them. Do not put private roots, credentials, raw auth headers or unrelated DB contents into distributable bundles.

Required bundle contents:
- approval reference and secret-free frozen campaign specification, explicit seed schedule and all ceilings;
- source/dependency/image/profile/task/policy digests, deployment mapping and observed readiness times;
- exact operation/run/intent/registration/decision/trial IDs and canonical query responses, with database and scope binding;
- prior consumed-byte manifest, candidates/repair lineage, solver and measurement evidence, calibrated settings/version, frame transforms and reset-relative timestamps;
- raw bounded episode JSONL, task predicates/progress, policy pin/transport receipts, video/frame manifest and counts;
- reservation/usage/latency/resource observations; diagnostics for every failed/unknown/aborted attempt;
- cleanup/reconciliation and backup/restore references, schema/admin readback and selected noninterference evidence;
- final acceptance matrix and limitations, distinguishing actual observation from interpretation.

Create the eventual operator runbook from exercised installed commands: no-effect inspection → approved metadata readiness → explicit launch → submit with fixed operation ID → observe from another client → exact cancel/reconcile controls → retained readback → owned shutdown. Every command is labelled by where it executes, under which account, which effects it can cause and its expected exit/result semantics. No speculative executable `run` command is supplied in this plan while installed execution is absent.

## 8. Stop conditions and response

Stop new releases immediately for wrong source/profile/instance/database binding; missing backup/approval; unexpected asset/network/model fallback; possible credential disclosure; unavailable required prior; lease contention or unknown cleanup; nonfinite/mismapped native data; ineffective placement repair; unsupported reset prerequisite; budget/deadline breach; inconsistent episode/video/result; or ambiguous commit/remote effect.

Cancel only the exact owned child/job through existing control boundaries. Preserve diagnostics and unfinished/unknown outcomes. Do not retry until exact reconciliation proves the next effect is known-unreleased and fresh authority permits it. Do not kill shared services to clear a blocker. A task failure with intact orchestration is valuable evidence but is not a positive pilot. A safe stop is successful safety behavior, not permission to mark the entire plan complete.

Before restoring earlier application code, verify it can read the codecs/schema already written by the pilot. Code rollback is separately approved and cannot silently downgrade retained records or trigger a whole-production-DB restore. Production outage/corruption/permission-denial injection remains prohibited; rehearse it on disposable targets.

### Architectural investigation budget — software Goal A

Keep the existing three-unsuccessful-corrections stop rule for a single blocking defect. Add a **per-architectural-blocker investigation budget** covering the entire newly discovered dependency chain, not each import error separately. A child dependency, renamed issue, new agent/session or amended source snapshot cannot reset that budget.

Classify first: a newly established dependency/effect is an investigation result; a demonstrated failure to implement an already agreed contract is a defect. Do not consume fictitious correction rounds for learning, or evade the investigation limit by renaming experiments “bug fixes.” A correction inside an unresolved feasibility investigation uses both the per-defect correction limit and the remaining investigation allowance. A test can pass by demonstrating the predeclared expected failure and exact cleanup; an unexpected incomplete/unknown outcome cannot.

Planning default for the next explicitly approved investigation: stop at the first of **two discovery/challenge rounds, three isolated diagnostic invocations, or 60 minutes of active investigation wall time**. Each round has one bounded discovery owner and a fresh read-only critic; parallel work shares the same blocker budget. Diagnostic permission is zero unless an existing exact harness/admission and current authorization permit the invocation; these ceilings never authorize new effects. Start/end times, elapsed active time (excluding user wait), rounds, attempts including failures, source/evidence identities and remaining allowance belong in the existing handoff/review ledger. Resume carries consumption forward. If timing or consumption cannot be reconstructed, stop rather than assume a fresh allowance. This is a prospective coordination default, open to discussion before execution, not a claim that it was enforced during the earlier probes.

Accounting definitions for the future approved feasibility contract: a round is one bounded hypothesis/discovery plan plus its fresh challenge and evidence adjudication, not each individual tool read. Every initiated diagnostic harness attempt counts, including staging failures, timeouts and failed runs; internal child roles/cases are separately predeclared and capped, so batching cannot hide unlimited probes. Record the source-preparation/review/experiment active intervals under one parent clock; overlapping agents count shared elapsed wall time, not a refreshed per-agent allowance. User-wait pauses are explicit. The contract must declare its start and remaining allowance; the earlier stopped investigation is retained history, not an excuse to infer unused permission. This documentation review is not a new runtime allowance.

IF-B/IF-C consume that feasibility budget, with their entire finite run matrix fitting the approved caps. IF-D's ordinary joined acceptance matrix has its own predeclared test/resource envelope, activated only after the feasibility gates pass and its effect scope is approved. If IF-D reveals another unresolved registration dependency, reenter the same architectural blocker with its **remaining** allowance (or stop if exhausted); do not label renewed exploration as ordinary regression testing. Neither gate passage nor a source change renews authority.

Stop the current experiment on an authority/design boundary, unexpected prohibited effect or disproved hypothesis; first reconcile owned cleanup and classify the result. Only an already-approved alternative hypothesis inside remaining scope/budget may proceed. At a budget limit, stop new launches, finish already-authorized bounded cleanup/evidence preservation (never abandon owned children to obey a time cap), and produce **one consolidated plan amendment/decision packet**: demonstrated chain, smallest alternatives, compatibility/effect changes, unresolved decisions, evidence needed and requested bounded authority/budget. Cleanup allowance never funds another experiment; uncertain cleanup blocks all new runs. No chain of small approval requests or automatic limit extension. Continue unrelated dependency-ready work only if the current user instruction permits it; the present discussion hold does not.

After the user settles the design and authorizes a bounded implementation contract, return to select → implement → source freeze → fresh critique → correct → final-source verification → checkpoint. Predeclare requirement-to-test rows, file ownership and integration exit criteria. If implementation opens another dependency of the same unresolved architectural blocker, return to its remaining investigation budget; do not relabel exploratory probes as acceptance tests. Agent usage is separate from Arena inference allocations. No automatic goal continuation, cached gate or document approval grants native/provider/production effects.

## 9. Completion and scope discipline

Plan 04 is complete only when the approved acceptance rows have source-bound evidence, V1 is joined through the installed API, V2 meets the fixed A2 pilot obligations, fresh-client causal readback and owned cleanup pass, and independent review identifies no unresolved blocking contradiction within the declared scope. Otherwise report the exact blocked rows and preserve the usable partial deliverables.

Do not declare M0–M5 or V0–V2 complete merely because this plan lists their names. Do not restart the architectural debate or repeat completed extractions without a new source-backed counterexample. Make the smallest missing connections, verify them under isolation, then spend live GPU/provider/database resources only on the evidence that isolation cannot supply.
