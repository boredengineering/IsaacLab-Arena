# Architectural Session Checkpoint: GR00T Image Build, Host Path Resolution & Policy Server Bring-Up

- **Date / Timestamp**: 2026-08-27 04:30:00 UTC
- **Session UUID**: `gr00tport`
- **Topic**: Submodule Dockerfile Fix, DooD Mount Resolution, Policy Server Validation on Port 5558
- **Status**: Completed / Active

---

## 1. Issues Diagnosed & Resolved
1. **Dockerfile COPY Path**: Fixed `COPY src/gr00t/ /workspace/gr00t/` to `COPY . /workspace/gr00t/` in `submodules/Isaac-GR00T/docker/Dockerfile` and added `.dockerignore`.
2. **Container Working Directory**: Added `-w /workspace/gr00t` to `docker/run_gr00t_server.sh` and `working_dir: /workspace/gr00t` to `docker/docker-compose.sim.yml`.
3. **Docker-outside-of-Docker Path Resolution**: Updated `docker/run_gr00t_server.sh` to auto-detect host filesystem source paths for DevContainer mounts (`/home/tarfy/...`).
4. **Host Port Binding**: Host VS Code helper services occupy ports 5555-5557; verified port 5558 binds cleanly with ZeroMQ.

---

## 2. Validation & Live State
- Built `gr00t-dev:latest` (18.8 GB).
- Successfully started policy daemon for `checkpoint-20000` with `g1_sim_wbc_data_config.py` on `tcp://127.0.0.1:5558`.
- Verified via ZeroMQ client:
  - `ping` -> `{"status": "ok", "message": "Server is running"}`
  - `get_modality_config` -> `['video', 'state', 'action', 'language']`
