# Plan 04 planning review ledger

Canonical plan: [event-mapping-refactoring_plan_04.md](event-mapping-refactoring_plan_04.md).
Implementation progress remains in the [canonical handoff](dashboard_cli_workflow_parity/research-stack-implementation-handoff.md). This ledger records source/document review only, not execution authorization or runtime acceptance.

## Baseline and scope

- User requested a new gap-closing plan grounded in Plan 03 and the v03 notes, with a solid live trial using production Neo4j, heavy Arena/GR00T containers and live API keys.
- Planning baseline: clean `dev/0.3.0-prerelease`, HEAD `d8a75da704be268b32a11952cb84be638a80222d`.
- Direct-read inputs: Plan 03, its review ledger, `.agents/references/quick_notes/event-mapping-refactoring_v03_notes.md`, latest native/policy handoff and developer documentation, repository startup/container skills, current API/application/policy composition and project manifest.
- Local retained baseline: `outputs/workflow/plan04-planning/baseline.json`, including input/source hashes. Fingerprinted inventory is not reviewed coverage.
- Parent checked Plan 03's final verification manifest: 1,550 case occurrences across 11 recorded cohorts; recorded repository-source hashes match current files. This was evidence-file readback, not rerunning tests or inspecting live resources. Its explicit scope excludes live M2/M3 and V1/V2 acceptance.
- No package imports/tests, credential-file access, service/container actions, GPU/model effects, production DB access, provisioning, protected configuration edits, staging or commits are part of this planning review.

## Parent findings incorporated in draft

| ID | Classification / finding | Existing positive evidence / bounded conclusion | Correction |
| --- | --- | --- | --- |
| PR04-01 | High status contradiction: v03 notes seal V0/V1/V2 and broadly retire SQLite | 1,550 isolated case occurrences really are retained; they do not establish live delivery or global legacy removal | Plan §2 retains evidence and explicitly corrects claims without rewriting notes |
| PR04-02 | High integration gap: installed mode is query-only | Current `api/schema.py`, `installed_config.py`, `server.py` and docs deliberately enforce it; not a broken execution feature | G04-01/02, P1 preserve query-only and add separately admitted joined owner/commands |
| PR04-03 | High integration gap: policy bridge requires supplied runtime/cohort/evaluator ports | `policy_evaluation.py:26–53` documents caller ownership; split foreground supports capture/assess/repair | G04-03/05, P2 require concrete owned policy adapter and fresh-reset prerequisites |
| PR04-04 | High live-trial risk: production database, shared heavy services and keys must not inherit test-harness assumptions | Existing bounded lifecycle/private-source mechanisms are reusable, not live safety proof | §4 separates read/write/admin/publication, backup/restore, identity/topology/VRAM, role secrets and approval ceilings |
| PR04-05 | Medium scientific claim gap: safe completion is not successful manipulation | Plan 03 already requires two predeclared seeds and frozen PickAndPlace predicates | §§1/5 retain four verdicts, fixed candidate/seeds, all failures, no robustness claim |
| PR04-06 | Medium onboarding contradictions: speculative CLI/ports/RGB-D | Current docs state query-only and RGB, actual parser is authoritative | §2 correction table and §7 exercised runbook contract |

## Independent review

Round 1 `deleg_9f19dd7a` completed three independent source reviews: native/policy integration, application/production persistence, and operational/scientific trial design. All reported no edits/runtime effects. Their findings predominantly confirm unfinished Plan 03 integrations, not newly reproduced defects in completed components.

| ID | Adjudication / strongest existing counterargument | Correction and required evidence |
| --- | --- | --- |
| IR04-01 | Supported integration gap: policy worker is missing, but service cleanup/adoption guards already exist | G04-03/P2 reuse existing stage registration/service rather than rebuilding a controller |
| IR04-02 | Supported native reset hazard: parent directly confirmed `initialize_and_settle` unconditionally resets; existing runner hooks support initialized cohorts | P2 explicitly requires already-reset preparation and actual reset/recorder-order regressions |
| IR04-03 | Supported provenance gap: parent read solver fallback warning; builder/sampler checks exist but do not retain selected-layout validation disposition | P3 bounded selected-layout/validator/actual-pose receipt and effective/no-effect repair witnesses |
| IR04-04 | Supported calibration limitation, not an instruction to remove guards: current object/contact mapping and support predicate are narrow; RGB capture safeguards exist | P3 requires actual A2 endpoint/mapping/calibration or a versioned supported adapter, plus camera calibration/pose receipts |
| IR04-05 | Supported managed-policy attestation gap: existing server has guarded dispatch/codec verification; parent confirmed model-directory fingerprint excludes SDK/Eagle dependencies | P3 requires expected-server pins and observed runtime identity, not request echo or ping-as-inference |
| IR04-06 | Supported resource/coverage gate: native flock is not VRAM; seed-bound trial model and real decoder are reusable | §4.4 and P3/P5 require measured co-residency, separate seed-bound runs if necessary, exact completed records and task-stage diagnostics |
| IR04-07 | Supported retrieval-selection/recovery gap: existing immutable prior promotion rejects conflicting bytes but does not prevent a new query first; parent confirmed `_generate`→factory and capture→retriever ordering | P1/A04-04 freeze request retrieval policy, recover authoritative retained reference before retrieval, test interrupted retention with changed corpus/revoked retrieval access |
| IR04-08 | Supported query-detail gap: parent directly read `Capabilities.unsupported` listing detail/artifact gaps; summary queries genuinely work | P1/G04-12/A04-14 add bounded typed traversal and authenticated exact-byte access, not filesystem-only proof |
| IR04-09 | Supported operational release requirement, not a demonstrated production DB defect | §4.3 and §8 select admin-compatible recovery, separate role/DDL permissions, bounded noninterference claims, no production chaos tests, codec-compatible code rollback |
| IR04-10 | Supported unsupported-resume boundary; current keyed handlers already have exact receipt/selection/latch guards | P1 requires completing needed branches or truthful blocked capability and tests, not generic retry on unknown effects |

Round 2 `deleg_69b27e6a` completed two fresh challenges against draft SHA-256 `3d0bb32392597e246018e9fdfda11c4969d0acd4c3e2ea54daea4204104b2ae1`. No architecture-level circularity was demonstrated. Two narrow planning corrections survived; broader objections were rejected or narrowed to already-declared implementation gates.

| ID | Adjudication / counterargument | Correction / verification obligation |
| --- | --- | --- |
| CR04-01 | Supported sequencing omission: current scene start requires the policy profile to match a candidate not known before generation. Later repair substitution exists but occurs too late to solve the initial check. Parent directly read `neo4j_store.py:3493–3495` | P1/P2 now select pre-generation capability/pin/derivation-rule admission, then candidate-specific durable binding after verified adoption. A04-13 requires unknown-generated-digest positive and exact recovery; no weaker mismatch check |
| CR04-02 | Narrowed campaign-budget omission: cumulative intent was explicit, but parent confirmed `neo4j_store.py:3302–3339` only totals one run | §4.1 selects fixed nonoverlapping suballocations and exact-operation/payload allowlist, summed conservative bounds, one-shot diagnostic receipts, original expiry and no unknown/refund recycling; A04-15 cross-run/restart exhaustion. Both V1 and separate V2 generation are allocated |
| CR04-03 | Narrowed, already-required implementation: scalar seeds and same-run generation guards make reuse absent, but P5 explicitly prohibited manual insertion and required a supported path | P1 owns typed reuse admission; P2 consumes exact bytes/digest with new ownership/fresh evidence and original generation linkage. A04-13 second-seed positive plus foreign/tampered negatives before live spending |
| CR04-04 | Narrowed feasibility gate, not demonstrated impossible cycle: a paused live environment does not satisfy cleanup-gated GPU release; draft already forbids that shortcut | P2 exit explicitly names the reset-prerequisite strategy and isolated witness; P3 owns native witness. Any cloud reassessment needs its own explicit calibration allocation. Unsupported obligations block rather than weaken |
| CR04-05 | Rejected planning gap: runtime attestation treated as measurement proof | Plan already requires observed bindings, concrete evidence producers, extended SDK/Eagle pins and real native/inference witnesses; current helpers remain unverified at live level |
| CR04-06 | Rejected planning gap: production DB recovery materially unspecified | Explicit DB/scope choice, admin-selected recoverable backup/restore, separate DDL, bounded noninterference and codec-compatible rollback already present; residual scoping/ACL limits disclosed |
| CR04-07 | Rejected planning gap: isolated passes substitute for live acceptance | Distinct P1 isolated, P2 rehearsal, P3 calibration and positive P4/P5 joined trials explicitly required; no completed live claim |
| CR04-08 | Rejected planning gap: independent causal/artifact API readback absent | G04-12/P1/A04-14 explicitly add currently missing typed detail/bytes, scope/tamper checks and fresh-reader reconstruction; local filesystem inspection alone cannot close the gate |

Round 3 `deleg_d102b466` returned **PASS at planning level**, with no blocking contradiction in its bounded review. It read the full plan/ledger and checked decisive source for candidate binding, per-run accounting, same-run adoption and unconditional native reset. It verified the corrected candidate-binding sequence, fixed cross-run allocations, provenance-preserving second-seed reuse, reset/lease feasibility gate and positive application-owned path. Implementation/calibration/deployment choices remain future gates, not actions performed during planning. The bounded review stops here; agreement is not exhaustive defect detection or live readiness.

## Verification

Reviewed plan SHA-256: `9f9e1757c58db6adb04f6412d9837837889f94832c72a360ddfee540d2568d9a`.

Parent checks passed: scoped host pre-commit hooks, local Markdown links, balanced fences, unique/sequential G04/A04/PR04/IR04/CR04 definitions, P0–P6 headings, whitespace/EOF, `git diff --check`, unchanged baseline HEAD and unchanged fingerprinted source/input files. Exact readback: `outputs/workflow/plan04-planning/document-verification.json`; repeatable stdlib-only checker: `verify_documents.py` beside it. No application test or live-readiness claim follows from these documentation checks.

Deliverables are this review ledger and the new Plan 04; historical notes, Plan 03 and canonical implementation handoff were preserved. All A04 rows remain future implementation/live obligations, not performed acceptance tests. No production DB access, credential inspection, container/service operation, native/model execution, staging or commit occurred.

## User-directed amendment: setup and readiness first

At clean HEAD `b1ff86c6401065a237a9f8267915357e0d3793d7`, the user explicitly requested making the setup-and-readiness report the first operator-facing deliverable. P0 now requires the early nonsecret role/provider/model/credential-alias inventory, a consolidated owner-labelled blocker checklist and separate configured/access/capability/authorization states. P1 owns implementation and sentinel-only tests of the private setup/report path before heavy-runtime integration. Existing query-only credential support is not misrepresented as full provider setup. Authorized probes consume campaign allocations; changed bindings invalidate affected readiness. No setup or live effects were executed by this amendment.

This is a scoped user-directed clarification, not another independent-review round. The prior reviewed digest/verdict above applies to the preceding plan revision; the amendment receives scoped documentation, link and diff checks only.

## Renewed user-requested thorough review

Scope: the amended plan, including first-operator readiness, persistent credentials, provider/pricing allocation and actual native/policy delivery. Intake HEAD `b1ff86c6401065a237a9f8267915357e0d3793d7`; the plan and this ledger were already modified and are preserved. Intake plan SHA-256 `24139575731e70df6af6bbd09cb3543872db4e116d523eb02527e6e450ba2af8`. Local baseline with source/input fingerprints: `outputs/workflow/plan04-planning/renewed-review/baseline.json`. Fingerprinting is not reviewed coverage.

Round 1 `deleg_2c14b76c` completed four independent source reviews: setup/restart, provider/pricing, native/scientific validity, and persistence/API operations. They reported no edits or runtime/service/credential access. Parent read decisive request/assessment, instance-transition, resource-verification and native-settings source. The following table preserves the initial allegations; final bounded adjudications follow. These are not reproduced runtime defects:

| ID | Finding / strongest existing counterargument | Proposed bounded correction |
| --- | --- | --- |
| NR04-01 | Provider token/USD attestation is not checked against the complete serialized request; exact model/endpoint and pre-call accounting already work conditional on a correct bound | Per-role admitted request envelope and reviewable worst-case pricing derivation, with actual serialization-limit witnesses |
| NR04-02 | Preapproving exact downstream payload digests conflicts with later candidate-derived reuse/repair requests; fixed finite budgets and exact replay already prevent unsafe substitution | Freeze trusted upstream derivation rules, bind concrete payload once before admission, retain no-rebinding recovery |
| NR04-03 | Plan04 makes scene-only V1 wait on all policy integration/calibration, contrary to inherited Plan03 independence; whole-outcome readiness remains correct for V2 | Split scene-required and policy-required exits, retain required-policy zero-call rejection |
| NR04-04 | Actual VLM request can identify target only by opaque subject ID; native identity and exact image/answer integrity already checked | Bounded candidate-derived visual grounding and ambiguous-target rejection; calibrate visible/occluded target with distractor |
| NR04-05 | Two different seeds cannot share one literal native-profile digest; placement may separately use entropy | One profile template plus exact seed-specific profiles, declared placement/reset-layout semantics and retained layout IDs |
| NR04-06 | Registry/source/candidate identity does not necessarily pin remote USD/dependency content; asset-source approval already required | Freeze selected resolved asset closure/content identities and reject URI-stable byte drift |
| NR04-07 | Neo4j backup alone may restore receipts without matching immutable files; artifact verification already fails closed | Recoverable DB cut plus matching artifact inventory/superset and separate-target joined readback; no regeneration on missing bytes |
| NR04-08 | Installed endpoint/credential shapes may not fit target TLS/DNS/auth/admin separation; local restricted support is deliberate | Early compatibility row, explicit supported topology or narrow extension, no TLS downgrade/DDL privilege workaround |
| NR04-09 | Changed configuration digest prevents old-instance observation/replacement under new configuration; credential-only replacement is not this problem | Preserve original config for exact stop/reconcile, checked new-config handover without tombstone deletion or duplicate scope owner |

Round 2 `deleg_c22b5142` completed fresh source challenges. Parent adjudication and applied corrections:

- **NR04-01 — high priority, narrowed:** no demonstrated overspend or absent accounting. Universal conservative attestations are legitimate, but the complete request must be inside their assumptions. P1 now selects per-role serialized-envelope enforcement and price derivation; A04-11/15 test actual text/image/repair/probe requests and overflow rejection. Decisive source: `workflow/accounting.py:19–41`, `inference_transport.py:134–158`, `scene_engines.py:63–70` and actual assess prompt at `173–190`.
- **NR04-02 — medium, narrowed:** fixed allocation is not intrinsically impossible; the original timing wording was ambiguous and lacked a selected dependency-binding method. §4.1 now freezes all known fields, slices and designated-predecessor derivation rules, then seals the concrete payload/source decision once before dependent admission. Recovery cannot choose a new “latest” candidate. P1 reuse and A04-13/15 cover positive unknown-candidate and competing-binding negatives.
- **NR04-03 — medium, supported scheduling contradiction:** scene-only semantics were not removed, but P4's blanket P0–P3 dependency delayed V1 behind policy. P0 now labels blockers by outcome, P2/P3 have separate scene/policy exits, and P4 requires only scene dependencies. Final completion still needs V2. A04-04/11 exercise missing-policy scene-only positive and required-policy zero-call rejection.
- **NR04-04 — high priority, supported for opaque/ambiguous targets:** not every visual request is unusable; descriptive singleton IDs can work. Parent confirmed digest-only `CandidateBinding` and actual prompt/request omit a semantic object association. P1 adds bounded candidate-derived grounding through request/worker/replay codecs; P3/A04-11/12 test transformed visible/occluded targets and distractors. Descriptors do not prove observation; ambiguity stays inconclusive and cannot authorize repair.
- **NR04-05 — medium, supported wording/experiment-selection gap:** fixed candidate need not imply identical physical states. P5 now specifies one invariant template and exact seed-specific profiles, simulation/placement seeds and reset/pool policy. Candidate-dependent fields seal after adoption, not before unknown generation. A04-07/08/13 verify approved differences and fresh layout/reset evidence; no deterministic-physics claim.
- **NR04-06 — medium, narrowed to selected assets:** no evidence of actual asset mutation; broad dependency-source approval was insufficiently specific about resolved external bytes. P3 now requires selected bounded robot/object/table dependency content manifests and immutable approved resolution, with nested drift tests; no whole-catalogue mirror or new asset service. A04-07/13 carry this obligation.
- **NR04-07 — high priority, narrowed paired recoverability:** reject the allegation that backup policy is absent. §4.3 now requires DB recovery point plus manifest-verified artifact coverage for the approved recovered workflow scope, separate-target authenticated readback, no regeneration and explicit divergent-history cursor/identity treatment. A04-06/14 verify this; no atomic cross-store snapshot or routine production restore is claimed.
- **NR04-08 — medium, narrowed compatibility gate:** no requirement to add every TLS/DNS/auth mode. P0 reports actual requirements versus the installed numeric-IPv4 Basic-only slice and actual credential selection; incompatible deployments block or get a narrow adapter. No plaintext downgrade, privilege escalation or confusion between application principals and DB roles. A04-02/05/06 verify selected support.
- **NR04-09 — medium, supported changed-config handover only:** old-config stop alone does not make new-config launch work under the same root; parent read `api/instance.py:66–82,203–224`. P1 selects explicit checked C1→C2 transition under existing locks, preserving old tombstones/scope exclusion. Credential-content-only update and ordinary session reconnect remain distinct. A04-03/05 test both positives and unresolved-owner/stale-token negatives.

Persistent host keys/non-root setup, ordinary client reconnect, API-auth rotation, readiness expiry, disabled SDK retries, managed fallback blocking, paid probe allocation and basic backup policy remain covered by inherited/current requirements; they are not additional omissions. Runtime model/price selections, placement strategy, native reset-prerequisite feasibility and production topology still need declared P0/P2/P3 decisions and evidence. A documentation correction does not implement them.

Round 3 `deleg_aa3c8537` returned **PASS within the bounded planning-review scope**, with no surviving blocker or wording fix. It read the full corrected plan/ledger and checked decisive accounting/admission, configuration-transition and endpoint source. All nine counterexamples, unknown-candidate derivation versus predeclared profiles, independent scene-only delivery, scoped paired recovery and paid visual-calibration allocations are addressed. No implementation or live-readiness acceptance follows.

Final reviewed plan SHA-256: `addf4d0a6d3b12c6ad5896a48b311c3c20a1163314c724ac8c88fddc7af4ccdc`. Scoped pre-commit, link/fence checks, unique sequential G04/A04/NR04 IDs, P0–P6 headings, `git diff --check` and baseline-source/HEAD comparison pass. Exact local evidence: `outputs/workflow/plan04-planning/renewed-review/document-verification.json`. Only the already-edited plan and companion ledger changed in tracked repository scope; source implementation is unchanged. No package tests, provider calls, credential reads, DB/Docker/service access or GPU workloads occurred.

Remaining selections are explicit release gates: literal models/rates/request bounds and allocation amounts; DB transport/roles/recovery/identity transition; reset prerequisite and GPU co-residency strategy; placement/reset comparison, asset closure and calibrated visual/physical profiles. Their implementation and positive/negative evidence still need to be produced. The renewed review is complete, not a guarantee that no future implementation/runtime defect can occur.

## Registration prerequisite consolidation — 2026-09-22

This section retains the first consolidation's metadata-focused proposal and verification disposition. The later NR04-12 below revises the decision: S1 metadata separation is conditional, not a compulsory P1 solution. Its unrun test obligations remain valid if S1 is chosen; current Plan 04 P1/A04-17 govern the route comparison and shared acceptance.

**Authority:** documentation consolidation and discussion only. The user explicitly wants further debate before proceeding. The prior isolated-cohort and bounded import/registration approvals do not authorize this redesign, another diagnostic run or native initialization. Preserve the previous review/hash as a historical snapshot; the amended Plan 04 is a new document snapshot, not newly verified software. Five-document verification lives at `outputs/workflow/plan04-planning/registration-consolidation/`. The [canonical handoff](dashboard_cli_workflow_parity/research-stack-implementation-handoff.md) remains the implementation-status owner; [shared session memory](../quick_notes/session_memory.md) gives cross-agent restart context.

### NR04-10 — architectural prerequisite, supported; implementation still blocked

- **Observed:** E1 failed before a joined installed submission/result. The retained boot diagnostic `arena-neo4j-9114cd29901a48fdb3e6c46a64f85607` reached `installed_execution.build` → `execution_catalogue_sha256` → `environment_generation_agent` → blocked model/runtime import. Subsequent approved immutable-image **byte inspection**, culminating in `arena-neo4j-a82eef70f240418d8897a8c566bb9a02`, traced default registry loading through Isaac Lab to `utils/warp/__init__.py:12–13` (`quiet=True`, `wp.init()`). Selected complete Warp units show first-init `Runtime()`, native library loading and `wp_init` before conditional CUDA queries; native initializer effects remain unproven. No expanded registration import or Warp initialization was executed.
- **Decisive evidence:** `outputs/workflow/plan04-implementation/installed-execution/native-initialization-decision.md`, `registration-context-parent-verification.json`, `registration-final-checkpoint.json`, retained failed joined-run directories and source review `deleg_1c02878c`. Unit hashes establish inspected bytes, not a complete native-effects proof. Outer disposable-resource cleanup is not application drain or active-worker shutdown evidence.
- **Rejected simpler fixes:** moving catalogue functions or lazy package exports alone eventually reenter the same registry cascade; fake registry entries/digests, bypassed real validators and suppressed initialization violate acceptance. `quiet`, absent devices and later CPU selection do not prove no CUDA probing. Package-name-only exemptions would widen effects without establishing safety.
- **Selected direction, still a proposal:** immutable opt-in first-party metadata shared by pure catalogue builders and real-schema validators, separate lazy real-class resolution, unchanged legacy APIs. Preserve hidden membership (`light`, `NoTask`), inherited tags/readiness/signatures, direct docstrings, duplicates/aliases and ordered bytes where parity is claimed. Full validation metadata needs its own identity; the vocabulary digest alone omits hidden members. No independent static registry or second lightweight schema.
- **Scope narrowed by counterexample review:** E1 already uses `proposal_only=True`; source early returns precede USD grounding, and earlier affordance classification is string-only. That avoids claiming full native-free generation; it does not remove top-level imports or nested schema revalidation requirements. Read-only analyses `deleg_42c38c89` and final design challenge `deleg_b369d6f0` passed **limited design**, not implementation, runtime parity or E1.
- **Correction:** Plan 04 G04-13/P1/A04-17 now makes this an explicit dependency and acceptance gate. The [registration-only design](event-mapping-registration-only-design.md#first-implementation-contract-to-approve) freezes proposed ordering/version, extension/mutation, full identity/wire and context policies for debate. Exact ordered manifest, canonical encoding, codec/call-site map and eventual parity authority are unresolved before implementation. No candidate declaration or working binding is claimed.

### NR04-11 — investigation-control gap, supported; prospective process amendment

The three-correction rule bounds repeated fixes to one defect, not a chain of newly discovered import/dependency frontiers. Earlier bounded probes yielded useful evidence but did not deliver the joined workflow. Plan 04 §8 now proposes a shared architectural-blocker budget: **two discovery/challenge rounds, three admitted isolated diagnostic invocations, or 60 active wall-clock minutes, whichever ends first**. It applies across child dependencies, agents and restarts; failed attempts count and unknown consumption stops. No permission to execute follows from an unused allowance. The current user hold permits documentation work only.

At an earlier authority/design boundary or the budget limit, preserve evidence and resource state, issue one consolidated amendment/decision packet and stop investigating. Do not reset the budget by renaming a dependency or relabel diagnostics as acceptance tests. Resume implementation only after the user settles the design and authorizes the bounded contract; retain the three-correction rule and final-source critic/verifier loop. The numerical default is a proposed planning policy, not a retroactive claim about earlier probes or a new implementation grant.

### Requirement-to-verification disposition

| Requirement | Required next proof | Current disposition |
| --- | --- | --- |
| G04-13 / A04-17 metadata/schema | Cold catalogue plus actual real-schema construction under unchanged native denials; hidden membership, ordering/inheritance/signatures/arity, normalized output and trace-only negatives | Design reviewed; source redesign and tests **not implemented/run** |
| A04-17 identity/context | Complete versioned server→worker→receipt→adoption binding; context at every parser; missing/mixed revision rejects before model initialization; concurrency and unchanged retained replay | Proposed contract; concrete codec/context map and implementation pending |
| A04-01/02/17 legacy and joined E1 | Preserve query-only/legacy behavior and real accepted old codecs; installed submit→client exit→owned synthetic workers→fresh readback with exact cleanup | Earlier narrow regressions passed; E1 positive **not established**; no rerun in this consolidation |
| A04-03 lifecycle | Active-worker keyed cancel, incomplete-body shutdown, lost-response replay and supported recovery in the installed application | In-process lifecycle evidence only; joined obligations remain unverified |
| Runtime parity / A04-07 onward | Separately permitted real class identity/constructor/ancestry and native/live evidence | Outside current authority; no class/native equivalence or live/deployed acceptance claimed |

Concrete test names and final source hashes are assigned only with the approved implementation slice. Existing historical passing cohorts are not evidence for the unimplemented metadata redesign. The next action is **debate this consolidated contract**, not launch another probe or resume Goal A automatically.

Fresh five-document review `deleg_8f4c8148` found one medium sequencing contradiction: the handoff required implementation approval before preparing the manifest/codec map, while the design required that packet before implementation approval. Parent accepted and corrected the handoff: discussion decision → separately authorized bounded source-only contract preparation → implementation/test approval. Preparation grants no production edits or runtime probes; the present discussion hold remains. Other reviewed boundaries were consistent. This records the review and parent correction, not a second independent PASS or software acceptance. The externally advanced HEAD `fd67f5022f20d6854cf407989d10eac153081313` is preserved alongside the intake identity; captured source hashes still match. Final documentation checks/hashes are in `outputs/workflow/plan04-planning/registration-consolidation/document-verification.json`.

### NR04-12 — distinguish testing objective from initialization policy; solution undecided

**User discussion:** the user questioned whether forcing no-CUDA behavior could be impractical or misleading, initially understood the blocked work as a two-seed simulator run, and then affirmed isolated workflow/database verification before simulation. They request engineering strategies and a Plan 04 update, while explicitly leaving the solution unresolved. The assistant's suggestion to assess runtime-faithful initialization first is a recommendation, not the user's selection or an execution grant.

**Source-grounded correction:** `workflow/api/installed_execution.py:251–259` computes the catalogue hash during application composition; `web_api/catalogues.py:36–48` imports real builders; `assets/registries.py:365–381` imports class libraries lazily. The image-source Warp chain is recorded in NR04-10. The observed E1 startup denial and the deeper source-inferred initialization chain are different evidence levels. Neither is a failed two-seed simulation or a demonstrated broken CUDA installation. No expanded Warp initialization was executed.

**Engineering interpretation:** lazy loading changes when work occurs, not whether imported modules have side effects. Eager registration and another deferred import are not sufficient remedies. Side-effect-free declarations/explicit bootstrap (S1), a faithful isolated runtime profile (S2), and an owned-process adapter (S3) address different requirements and costs. Dependency inversion with explicitly synthetic ports (S4) is useful for contract tests but cannot discharge real registration/schema integration. Process isolation relocates native effects; it is not permission to execute them. No devices, no GPU computation and no initialization/probing are distinct promises, and containment has limits.

**Applied plan changes:** G04-13/P1 now require a boundary/strategy decision rather than mandatory metadata redesign. A04-17 retains common real catalogue/schema, identity/replay, legacy/query compatibility, joined readback and cleanup, with additional route-specific evidence. If S1 is chosen, all existing metadata/inheritance/order/context/runtime-parity obligations remain. S2 requires explicit initialization/effect admission and unchanged workload prohibitions; S3 additionally needs owned process, full validation and result-binding proofs. S4 is never an E1 substitute. The shared budget and discussion hold are unchanged.

**Next decision packet:** compare production fidelity, change scope, desired API/runtime separation, permitted initialization effects, supported schema/extension semantics, exact environment/resource/cleanup controls and test traces. The user has not approved a route, new native effects, probe execution or implementation. No requirement to fully reverse-engineer every third-party native library is newly imposed; any proposed admission must instead state its trusted dependencies, containment, observations and residual risks honestly for approval. Retain the earlier immutable-source evidence rather than restart a chain of import probes. Documentation verification for this revision: `outputs/workflow/plan04-planning/runtime-boundary-discussion/`.

Fresh independent review `deleg_433f1df5`: **PASS limited to the five-document runtime-boundary revision**, no correction required. It checked the lazy-registration/effect distinction, E1 versus P5, separate device/computation/initialization promises, real integration obligations for S1–S3, S4's complementary scope, conditional S1 design and unchanged discussion-only authority. Source/evidence references and HEAD `fd67f5022f20d6854cf407989d10eac153081313` were checked read-only. This is documentation acceptance, not a selected solution, implementation acceptance or native execution proof. Final scoped hooks, link/ID checks and document/source hashes are recorded in `outputs/workflow/plan04-planning/runtime-boundary-discussion/document-verification.json`; no application tests were run.

### NR04-13 — hypothesis-driven initialization feasibility, proposed protocol

Latest user request: update the plan with the discussed controlled-initialization approach and review for gaps. This is documentation/review authority, not permission for library initialization, probe runs, harness/production implementation or broader native effects. S2 feasibility is the proposed next planning path; the concrete admission packet and execution decision remain open. No S1 metadata redesign is made mandatory.

P1 now defines IF-A (source-only contract/critique), IF-B (real cold-role catalogue/schema witness), IF-C (owned startup failure/deadline/cleanup witness), and IF-D (return to real joined E1). The proposed startup lifecycle makes initialization explicit before execution readiness/admission, while requiring analysis of imports that precede the nominal bootstrap and fresh worker-role initialization. This is not implemented. Parent/worker catalogue and real-schema semantics remain mandatory; synthetic contract tests cannot substitute.

The packet must separate enforced prohibitions, permitted observed effects and explicitly accepted trust/observation limits. It pins sources/roles/environment, no-device configuration, owned cache/filesystem paths, constructor/transport order, network/resource/child limits, outer deadlines and cleanup ownership. Logging is not containment. Unknown effects cannot be retroactively authorized as a pass. Trusted native dependencies are disclosed rather than claiming complete static effects proof or universal sandboxing.

§8 distinguishes investigation results from implementation defects, retains three unsuccessful corrections per defect and shares investigation consumption across all dependent work. IF-B/IF-C fit a finite predeclared matrix; repeats require a changed discriminating hypothesis and outcomes. IF-D is a separate approved acceptance envelope, not a budget-reset route for the same dependency chain. Time/counter definitions, failed/staging attempts, overlapping agents, restart consumption and bounded cleanup after the launch budget ends are explicit. Uncertain cleanup blocks further runs. No numerical run/resource contract has yet been approved or executed.

Required review coverage: authority/effect-enforcement gaps; native/worker lifecycle and query-only compatibility; finite-budget/phase-transition loopholes; evidence identity and real joined acceptance. Current disposition of every IF gate is **proposed/unrun**. Final document/source verification for this revision: `outputs/workflow/plan04-planning/initialization-feasibility/`. Prior review PASS/hash applies to the earlier revision, not automatically to this extension.

Independent review `deleg_531830dd`: both read-only critics returned **PASS limited planning**, with no concrete blocking counterexample or required correction. The authority/lifecycle critic checked pre-bootstrap imports, cold worker catalogue/revalidation, readiness refusal, partial-init disposal, bounded cleanup and explicit prohibitions versus accepted assurance limitations. The budget/acceptance critic checked shared consumption, dual charging for corrections, staging failures, internal role caps, IF-D reentry, meaningful repeats and final-source evidence continuity. Both confirmed the actual joined E1 obligation remains and IF-A must provide the concrete finite matrix and numerical controls before execution. This is not proof of feasibility, an execution-packet approval or permission to initialize native libraries. Final scoped documentation checks and source/document hashes are retained in `outputs/workflow/plan04-planning/initialization-feasibility/document-verification.json`.
