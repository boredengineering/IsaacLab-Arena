# Event Mapping Refactoring Plan 03: Bottom-Up Application Refactoring for GraphQL and Live Generation

**Document Identity**: `.agents/references/plans/event-mapping-refactoring_plan_03.md`
**Status**: Design proposal for live integration; not implementation or live-execution authorization.
**Provenance**: Initial local draft attributed to Antigravity; source-grounded continuation and review recorded in the companion review ledger.
**Date**: 2026-09-19
**Upstream Dependencies**:
- [Event Mapping Refactoring Plan 01](event-mapping-refactoring_plan_01.md) (Architectural Foundation & Neo4j Store)
- [Event Mapping Refactoring Plan 02](event-mapping-refactoring_plan_02.md) (Core Application-Owned State Machine & Process Fencing)
- **Review baseline**: `8f40c55584c4998ac657128f433d4dd407a2905b`, branch `dev/0.3.0-prerelease`. Changes since the prior review at `57a7d1f3fe67b1d2c3fa4a89a54f8ce5f8797b77` are documentation only; source anchors remain applicable. Earlier review baselines/verdicts remain historical in the companion ledger.
- **Implementation-status owner**: [current handoff](dashboard_cli_workflow_parity/research-stack-implementation-handoff.md). This plan defines proposed work, not a second completion history.
- **Review**: [plan 03 review ledger](event-mapping-refactoring_plan_03-review.md). Source anchors refer to the baseline above.

---

## 1. Executive Summary & Problem Definition

### 1.1 Context and Why Plan 03 is Required
Plan 02 closed its **guarded isolated workflow milestone**, joining actual generation/model entrypoints, synthetic HTTP/capture, owned workers, Neo4j decisions and fresh CLI processes. Its five bounded review findings were closed within that scope; this document does not re-run or broaden that proof. Current managed workflow authority is Neo4j. Legacy workbench jobs, model catalogues and SSE still use SQLite Journal; SQLite was **not retired**. That is a current-code gap, not an allowed target architecture: the refactored application must use Neo4j exclusively for durable domain/application records. See plan 02's status and the handoff's final bounded correction gate.

However, **Plan 02 intentionally kept native simulation and live inference isolated behind synthetic test fixtures.**
Consequently:
1. **No Live Developer Entrypoint**: A new developer cannot simply run `arena-workflow run --prompt "..."` with their `OPENROUTER_API_KEY` or `OPENAI_API_KEY` and get a real environment.
2. **Missing Live Graph-RAG Retrieval**: The Neo4j store stores workflow events, but live prior retrieval from existing accepted environment graphs remains disconnected at the application boundary.
3. **No Native `SimulationApp` Stage Realization**: Realizing the generated YAML into a live USD stage with PhysX settling and multi-camera trajectory capture is still tied to legacy standalone runner scripts (`environment_generation_runner.py`).
4. **No Real Policy Rollout Assessment**: Autonomous verification against manipulation policies (e.g., Franka + DROID or G1) remains unintegrated in the live execution loop.

**Goal:** transfer coordination of the researched workflow from an external orchestrator such as Hermes into Arena: submit → retrieve exact priors → generate → validate → realize/capture → assess → permitted repair → fresh evidence → accept or explain the stop. Reuse the engines already exercised; do not create another orchestrator. The coherent model is research entities → identity/owner/lifecycle → retained facts and available actions → shared application commands/read models → GraphQL. A dashboard is a later consumer, outside the current refactoring scope.

**Scope correction from the user:** refactor the codebase bottom-up for GraphQL; do not make the current dashboard a design constraint or delivery dependency. Establish the domain/application model, persistence, engine adapters and command/query contracts that GraphQL will expose. The installed CLI is a GraphQL client of that same application; direct application calls remain for tests and an explicitly selected foreground mode. Neither is a separate architecture or a reason to postpone GraphQL contract design. Dashboard routes, SQLite SSE compatibility, frontend integration and browser parity are outside this plan's current scope.

The target is a general Physical AI application architecture, not an A2 or pick-and-place application. A2 is only the first proposed reference test profile, not a domain requirement or proof that every embodiment works. Scene acceptance, policy success, publication and successful-prior eligibility remain distinct. Writing this plan authorizes no inference, GPU use, service startup, research-database access, dependency installation or protected deployment-file changes. Deferring dashboard work does not authorize deleting unrelated legacy code or data.

---

## 2. Architecture and invariant guardrails

### Mandatory persistence boundary: Neo4j, no SQLite

Neo4j is the sole authoritative database for the refactored application and its GraphQL/CLI interfaces: domain entities and revisions, accepted requests, workflow/attempt state, decisions, reservations, command receipts, events, persisted public profiles/catalogues and publication/evaluation metadata. No SQLite Journal, parallel job store, dual writes, SQLite cache or SQLite fallback is permitted in this application. Neo4j unavailability produces an explicit unavailable/unknown outcome, never a switch to local database state.

Immutable files remain the evidence/blob store, with identities/manifests and authoritative relationships recorded in Neo4j; they are not a second lifecycle database. For the local development bootstrap, private credentials may persist in an explicitly configured owner-private file outside the repository; they enter execution through the private channel/in-memory grant boundary, never either database or public workflow records. This secret-source file is not an alternative domain/lifecycle store. Static configuration files may supply explicit configuration but cannot become a competing mutable application registry.

Any existing functionality selected for reuse that depends on Journal/SQLite must be extracted and reimplemented against the Neo4j persistence contracts before it enters the refactored application. This migration is required backend work, not deferred until dashboard integration. Preserve existing data for an explicit, verified import if required; do not delete or read private legacy databases during this planning task. No compatibility adapter may hide SQLite behind an otherwise Neo4j-facing GraphQL resolver.

```text
Domain/application contracts designed for GraphQL; CLI as an executable consumer
    → shared application facade (extracted ForegroundWorkflow + WorkflowService)
    → Neo4j admission / decisions / reservations / released intents
    → existing owned workers and private effect authority
        → GraphRAGRetriever → immutable prior receipt
        → EnvironmentGenerationAgent / BoundedSceneModels
        → ArenaEnvGraphSpec → ArenaEnvBuilder → native capture/measurements
        → existing policy_runner, when policy criteria are required
    → immutable artifacts + validated receipt adoption
    → explicit application decision: observe / repair / accept / stop / reconcile

Publication is a separate authorized command, never an acceptance side effect.
```

- Keep reusable application composition under `isaaclab_arena/agentic_environment_generation/`, with examples/HTTP/CLI as thin adapters. Domain imports must not import examples, FastAPI, SDK/native runtimes or resolve credentials.
- Preserve `WorkflowService`, `GenerationCoordinator`, `scene_loop`, Neo4j fences, immutable artifacts and physical ownership. A facade delegates; it does not introduce another job store or controller.
- Resolve secrets only at a trusted execution boundary. Configuration validation, profile listing and read/replay must not construct model backends or ping providers. Existing constructor pings remain charged effects after release; removing them globally is not required for this integration.
- Derive readiness from the **whole requested outcome**, including assessment, capture and policy where required, before the first model call. Missing required dependencies, unavailable required priors or unsupported criteria cause zero model calls, including initialization pings.
- Retain every candidate, failed assessment and decision diagnostically. Eligibility for retrieved successful priors requires existing publication/evaluation validation; do not invent replacement `AcceptedEnvironmentGraph` labels.
- Run native initialization/imports inside an owned runtime worker after authorization and resource acquisition. Official Isaac Sim documentation requires Kit initialization before dependent Omniverse imports.[14] Treat cleanup/ownership as verified receipts, not an assumption about process exit.
- A visual verdict cannot satisfy physical support, collision, causal diagnosis or policy-success requirements. Profile-specific measurements, frames, tolerances and windows must be frozen before release; missing measurement capability is unsupported, not a pass.
- Commit decision, cumulative reservation and next intent together. No refund or retry merely because remote effects are unknown. Current authenticated reads and exact replay remain possible without fresh execution grants.

---

## 3. Detailed Work Breakdown & Phased Execution

### Phase 1: Supported launch configuration and live model composition
**Target Files**:
- `isaaclab_arena/agentic_environment_generation/inference_backend.py`
- `isaaclab_arena/agentic_environment_generation/workflow/inference_transport.py`
- `isaaclab_arena/agentic_environment_generation/inference_profiles.py`

#### Changes:
1. Extract the shared foreground composition and add an explicit installed profile, leaving `isolated-synthetic-v1` and its guards unchanged. Current `application_from_cli` is harness-only (`foreground_workflow.py:44`); it is not a production launcher.
2. Reuse `inference_profiles.py:29–175`, `BoundedSceneModels`, `ForegroundAuthority` and `ExecutionGrants`. Freeze role-specific literal endpoint/model/request policy and accounting metadata. The current built-in resolver only recognizes its enumerated OpenAI pairs; fixed endpoint support or a user-defined profile is not compatibility verification for every model.
3. Give a new operator a secret-free profile template, explicit local credential setup/storage and exact launch/run/status/cancel/resume walkthrough (§6). The bootstrap default is an owner-private credential file outside Git, loaded only from the configured path and handed to the owner/workers privately. Private-pipe and named-environment sources remain explicit alternatives, never silent fallbacks. No secret-valued `--api-key` argv, automatic `.env` search or hidden provider fallback. This is proposed bootstrap functionality, not present behavior.
4. Metadata readiness and effect authorization stay separate. Initialize model clients only in released bounded workers with a shared allowance installed first. Constructor pings, SDK retries, image calls and failures consume the original cumulative call/token/cost/time ceilings. SDK defaults can retry; actual managed transport must be tested rather than relying on those defaults.[26]
5. Support one explicitly verified generation/assessment profile pair first. For OpenRouter, freeze routing, fallback and parameter requirements as part of the profile: provider fallback is enabled by default in its documentation, so identity/cost assumptions must not rely on an omitted field.[18] Add serialization and capability tests before advertising more pairs.
6. Reserve conservative attested bounds before every effect; explicit zero pricing is distinct from missing pricing. Provider model metadata can contain pricing overrides, so a single headline price is not a complete conservative bound.[8] Retain reservations separately from measured usage/billing.

Provider binding must be explicit, not inferred from model-name substrings or whichever key exists first. `inference_backend.py:224–245` can choose an OpenAI key for an OpenRouter model when both are present; pass the selected key/model/endpoint explicitly with dotenv disabled. Legacy NVIDIA uses `NV_API_KEY`; retain that as the documented named source, treating any proposed `NVIDIA_API_KEY` alias as opt-in with conflicting values rejected. Reuse/extract `web_api/provider_security.py` and `provider_readiness.py`; current metadata readiness is not universal provider support. Test mixed-key environments, literal model preservation, slow/error responses and total probe deadlines. Do not promise a new `validate_connection()` API or a two-second bound without implementing transport and overall deadline enforcement.

---

### Phase 2: Live Read-Only Graph-RAG Prior Retrieval from Neo4j
**Target Files**:
- `isaaclab_arena/agentic_environment_generation/workflow/prior_artifacts.py`
- `isaaclab_arena/agentic_environment_generation/graph_rag.py`
- `isaaclab_arena/agentic_environment_generation/workbench/research_retrieval.py`
- `isaaclab_arena/agentic_environment_generation/workflow/neo4j_store.py`

#### Changes:
1. Reuse `GraphRAGRetriever.retrieve_prior_snapshot` (`graph_rag.py:387`) with an explicitly authorized injected driver/database and, when selected, the existing managed publication/evaluation-readback callback. Do not introduce a second retrieval schema or bypass the current provenance validators with a keyword-only query.
2. Separate operational Neo4j write authority from research-prior read authority, even when endpoints share infrastructure. Use fixed reviewed queries, explicit database, least-privilege credentials, query/connection/overall deadlines, row/byte bounds and verified resource cleanup. READ routing alone is not a database security boundary. Do not call inference/publication inside a retried transaction callback; managed Neo4j transactions may re-run callbacks.[10]
3. Retain and verify the exact snapshot under prompt, contract digest and run before backend construction. Generation consumes `exact_context` once; refinement does not silently retrieve a newer corpus. Retain selection/version/evaluation counts and uncertainty, not merely graph names.
4. Distinguish `not_requested`, successful retrieval with no eligible priors, and unavailable/invalid retrieval. Required unavailable priors block with zero model calls. Optional fallback must be explicitly permitted and labelled; a bundled exemplar must never masquerade as a measured retrieved prior. Define whether an empty successful retrieval is acceptable in the selected retrieval profile before release.
5. Managed publication membership and evaluation evidence are not automatically one immutable database snapshot (`research_retrieval.py:14`). Retain validated consumed bytes and report that limitation; do not claim a stronger corpus snapshot guarantee.

The managed selection adapter is **not reusable unchanged** under the Neo4j-only rule. `research_retrieval.py:219–225,324–328,367–376` checks Journal ownership and reads SQL publication receipts/reservations; `web_api/managed_retrieval.py` also opens an existing Journal. Extract its validation/ranking logic behind Neo4j repositories for publication membership, reservation, attempt and verified-readback receipts. A read-only SQLite adapter is still prohibited. Prove a nonempty eligible managed prior and malformed-receipt rejection with SQLite denied. This migrates evidence needed for retrieval; it does not enable a new publication mutation or weaken eligibility. If a first slice supports only existing Neo4j-native legacy priors, declare that narrower retrieval profile explicitly and keep managed-prior capability unavailable until its repository migration passes; do not silently drop managed eligibility to pass T13 or call the full transition complete. Freeze the selected retrieval profile/database/eligibility policy into the versioned request rather than leaving them as mutable launcher defaults.

---

### Phase 3: Connect Existing Arena Native Execution and Capture
**Target Files**:
- `isaaclab_arena/agentic_environment_generation/workflow/native_realization.py` (proposed adapter, not a replacement engine)
- `isaaclab_arena/agentic_environment_generation/workflow/scene_engines.py`
- `isaaclab_arena_examples/agentic_environment_generation/web_api/generation_worker.py`

#### Changes:
1. Extract only reusable construction from `environment_generation_runner.py:660`: `ArenaEnvGraphSpec` → `to_arena_env()` → `ArenaEnvBuilder.make_registered()`. Use typed builder/runtime configuration rather than `argparse.Namespace`; leave CLI parsing, printing and the legacy best-effort critic outside the new adapter. Reuse existing builder and relation solver. Access Isaac Lab state through `env.unwrapped`.
2. Add a separately admitted native capture worker/profile beside the existing synthetic composition. Reuse owned prepare/register/release/cleanup and immutable receipts. Do not simply remove `synthetic_only` checks or run native callbacks in the coordinator. Acquire the common GPU lease; pausing a legacy queue is insufficient if a warm renderer still owns it.
3. Reuse `trajectory_capture.capture_trajectory` and `scene_observation.make_native_sampler`; verify actual tensors, coordinate/quaternion conventions, origin offsets, prim identity and filtered contact correspondence. The current sampler requires one environment, literal contact paths and force shape `(1, 1, 1, 3)` (`scene_observation.py:368–471`); ordinary templated paths and broader tables need a tested adapter/evaluator revision, not assertion removal.
4. Freeze bounded settle/capture windows, actual cameras/modalities and calibrated thresholds in an immutable profile. Current support constants are `0.01 m/s`, `0.05 rad/s` and `0.05 m` XY distance (`scene_observation.py:31`), not established universal acceptance limits. Its center-distance rule is not generic tabletop support. Do not substitute guessed 40-step settling, invented camera names, penetration measurements or unsupported depth channels.
5. Retain authored → solver-effective → realized pose mapping, placement-validator disposition and reset/realization identity. Relation-controlled non-anchor `initial_pose` edits may have no effect. Permit only supported effective XY repairs from the original baseline; fixed-Z repairs cannot fix support/Z or embodiment errors.
6. Record synchronized images and raw measurements through `SceneEvidenceArtifacts`/`ArtifactArea`, binding candidate/profile/seed/reset/window/step and camera calibration. Capture terminal state before autoreset or mark terminal images unavailable. A later rollout is not evidence of the failed episode.
7. Every repair receives fresh relevant native evidence from a compatible complete cohort. Retain partial capture before cleanup and preserve the failing process exit code; successful cleanup is not acceptance and retained images are not contact proof.

#### Native integration details that must not be hidden by adapter reuse

Adapter wiring and bounded runtime-correctness fixes are separate obligations: reuse Arena's construction, capture and policy execution; characterize and test the hold/reset/window fixes below without rewriting the simulator or silently changing legacy callers. Settling observes the configured physics. Do not tune gravity, friction, collisions, solver settings or timestep to obtain acceptance, or relax frozen criteria after failure.

- **Hold actions:** `DroidAbsoluteJointPositionActionsCfg` (`isaaclab_arena/embodiments/droid/droid.py:347`) sets `use_default_offset=False`. Zero arm commands are not a posture hold. Reuse the runner seam but add an explicit embodiment-correct hold implementation using the selected action terms/joint ordering and measured/reset posture; preserve gripper semantics. `evaluation/policy_runner.py:104–114` currently returns zeros for non-WBC embodiments, so the existing helper is not sufficient for DROID. Native calibration must demonstrate posture retention before interpreting object motion.
- **Blocking settle gate:** `verify_and_settle_scene` currently permits at least 50 steps and returns an `all_objects_settled` report; `rollout_policy` discards that report (`policy_runner.py:160,247,469–477`). The managed path must enforce its frozen maximum, charge every settling step and reject unsettled/terminated/truncated cohorts before policy effects. Preserve legacy behavior unless a separately tested common fix is selected. Do not round a measured value before evaluating its threshold.
- **One reset sequence:** both `trajectory_capture.py:35` and `evaluation/policy_runner.py:466` reset unconditionally. Provide a tested capture/runner hook for an already initialized reset cohort or place hold/settling inside the owned reset lifecycle. Sequence is reset → embodiment-correct hold/settle → exact observation window → policy rollout when requested, with no hidden reset between these steps. Preserve existing helper defaults for legacy callers. If a policy worker rebuilds/resets, it must re-establish state-dependent scene prerequisites for that episode; a matching spec/seed is insufficient. Success/termination records remain separate from unavailable terminal images.
- **Actual cameras and bounds:** DROID sensors are `external_camera`, `external_camera_2`, `wrist_camera` with RGB-only configuration (`droid.py:425–468`). The checked-in policy consumes `external_camera_rgb` and `wrist_camera_rgb` (`isaaclab_arena_gr00t/policy/config/droid_manip_gr00t_closedloop_config.yaml:21`); verify the corresponding observation keys before selecting extra evaluator views. Preserve the policy's view/preprocessing contract. Existing scene retention allows 16 PNG frames, at most 128 KiB each, and exact camera-by-step coverage (`scene_observation.py:186–235`), whereas the generic capture helper has a separate 64-image ceiling. A short post-settle window must satisfy both and the whole payload bound. Predeclare evaluator image resizing/encoding and record the transform; do not silently resize policy input or drop frames. Broader windows/depth require a versioned bounded evidence extension, not an incidental guard relaxation.
- **Native provenance:** current producer payloads recognize `synthetic` and `native-unverified`, while foreground admission/profile assurance remains synthetic-only. Add explicit native producer/profile/receipt support and calibrated provenance through the service/read model. Changing a string is not native verification, and hashes prove integrity rather than measurement authenticity.

For profiles requiring images, enable cameras in both Kit startup and graph-to-embodiment conversion, then verify the exact required observation keys. The cited legacy runner calls bare `to_arena_env()` (`environment_generation_runner.py:666–676`); its camera argument defaults to false (`arena_env_graph_spec.py:218–226`), and conversion's `setdefault` preserves an explicit `enable_cameras=False` (`arena_env_graph_conversion_utils.py:144–146`). Rendering mode or selected camera names alone do not spawn sensors. Pass the frozen profile setting explicitly; reject an incompatible explicit graph override rather than silently rewriting the candidate. Preserve legacy defaults and camera-free profiles. T06 covers omitted/default, explicit true, explicit false conflict and missing-key cases.

#### Selected native composition contract

Use **separately fenced capture and assessment substages** under the existing run coordinator/store, not an unregistered second child inside one intent. Current `SceneIntent` permits one registration/cleanup (`scene_loop.py:90–99`), and `ForegroundScenePorts.prepare_worker` prepares the model child before parent-side capture. The native implementation must extend the action/result/reservation codecs, service transitions, worker adapters, recovery and projection together:

1. Reserve/prepare/register/release a native `capture` intent. Its one owned child initializes Kit, resets, holds/settles and captures the frozen candidate. It retains the immutable native receipt before Kit shutdown. The owner verifies the bytes, exit and actual cleanup; only then may it adopt the observation and advance. The child has no model credentials or model permission.
2. Adoption of that exact observation and selection of the next `assess` intent commit atomically. Prepare the model worker only now. It consumes retained images/measurements and their manifest, not a live simulator object. If no model criterion is required, the application uses the supported numeric evaluator without creating a model child.
3. The execution owner holds the common GPU lease on behalf of the native child for native work and cleanup. Release it after verified native cleanup, before VLM waiting or refinement. The scope's workflow-owner lease is different and may remain held across substages. If cleanup is unknown, keep the run blocked; do not transfer the GPU slot based on a DB row alone.
4. Bound each child's preparation/startup/execution/cleanup within its stage allowance and the original run deadline. Capture charges realization and all settle/capture steps; assessment charges model attempts/tokens/cost. Do not reserve those quantities twice or reset the run budget between substages. A slow but within-budget capture must still leave the separately budgeted assessment able to complete. Unknown capture cannot be repeated; a verified retained capture can feed known-unreleased assessment after recovery without rerendering.

This is a proposed extension, not a capability of the current synthetic profile. Version the new intent/result/packet/profile codecs explicitly; retain v1 record readers and never reinterpret a previously accepted synthetic contract as native permission. Preserve the existing synthetic mode as a regression path. Initial live topology is one non-root API/execution-owner process and its supervised children inside the discovered simulation container, using the simulation interpreter and one shared namespace/artifact/lease layout. The existing `sys.executable` process supervisor is not a Docker-exec or cross-host worker bridge.

**Executable native settings:** introduce one validated versioned settings payload referenced by the existing immutable runtime/capture profile identity and settings digest, not a competing profile registry. It contains actual threshold values/evaluator version, subject/contact/coordinate mappings, reset-relative step window, image keys/transforms and budget ceilings. Admission, capture, numeric evaluation, displacement verification and retained replay consume the same frozen bytes; a hash alone does not configure them. Current module-global support constants must either be selected as an explicitly calibrated fixed evaluator revision or replaced by validated settings passed into that evaluator. A changed catalogue default cannot change interpretation of old evidence.

**Absolute step contract:** propagate the reset-relative control-step offset through capture hooks, sample/filename labels, cohort/window metadata, evaluator coverage and repair-displacement witnesses. Current `ScenePorts.admit` only accepts `(0, capture_steps)` and displacement checks hardcode step zero (`scene_ports.py:131–142,405–420`); supporting a post-settle window requires changing all these consumers together. Freeze a bounded settle allocation and exact nonzero observation window; if settling fails within its allocation, stop before assessment. Count settling even when only the short post-settle image window is retained. Do not relabel post-settle samples as reset step zero. Existing numeric and visual evaluators already accept exact nonzero windows (`scene_observation.py:113–120,221–225`); reuse those checks rather than rewrite them. Change the restrictive composition/reset/displacement consumers and verify byte-bound settings at admission, release and worker consumption.

**Repair proposal contract:** before invoking the actual refiner, retain and send a bounded envelope containing original and current candidate identities/placements, selected failing assessment/evidence, exact allowed effective XY paths, the original-centered admissible XY disk and allowed axes, preserved fields and the prohibition on task/physics/Z/topology changes. Current `_refine_child` does not pass these permissions to the model. Do not compute a shrinking “remaining radius” as radius minus current displacement: the existing bound is displacement from the original, not total path length. A valid inward or tangential move remains admissible. The existing guard remains authoritative over the returned full candidate. First repair acceptance is specifically **visibility failure with required physical criteria already established**, matching `scene_loop.py:165–179`; unsupported physical failures stop or request permitted observation, not an invented general physical repair. Test the actual model entrypoint receiving this envelope and producing a permitted child without external-agent feedback.

---

### Phase 4: General Policy Evaluation and Task-Specific Acceptance
**Target Files**:
- `isaaclab_arena/agentic_environment_generation/workflow/policy_evaluation.py` (New)
- `isaaclab_arena/agentic_environment_generation/trajectory_assessment.py`
- `isaaclab_arena/agentic_environment_generation/trajectory_capture.py`

#### Changes:
1. Reuse `evaluation/policy_runner.py` and the trusted completion bridge in `web_api/evaluation_worker.py:24`, not a new rollout algorithm. Existing profiles include `gr00t-droid` and `openpi-droid`; generic OpenVLA support is not established. Separate shared runner logic from fixed legacy endpoint/1000-step assumptions.
2. Extend the application contract, reservation codecs/store, worker packet, evidence producers, decision rules and read model together for required policy outcomes. `read_model.py:159` currently reports `policy_outcome="not_run"`; scene admission is not a supported policy executor. Merely filling `execution.policy` or changing a display label is insufficient.
3. Freeze the selected policy's artifact/configuration, observation/action interface and adapter/transport identity, task definition/evaluator/instruction, exact environment, episode/reset/seed, deadlines and aggregation before release. Requirements derive from that profile, not a globally required GR00T/DROID service. For the GR00T example adapter, verify its expected served instance at the receiving effect boundary, not only a successful ping. A client YAML path or display label is not proof of the loaded policy.
4. Evaluate the selected task's frozen success/failure/termination rules and metrics through its registered evaluator. Do not impose lift, grasp, plate contact or manipulation-specific metrics on unrelated tasks. Reuse existing Arena task contracts such as `TaskBase.get_termination_cfg()` and `get_metrics()` (`tasks/task_base.py:33–47`); keep lifecycle, provenance, resource bounds and evidence validation in the common application. Preserve completed episode records and denominators; no completed episodes means unknown/not established, never success.
5. The initial evaluation slice runs the selected policy against the fixed candidate after its declared prerequisites are established; policy failure remains a retained nonaccepted outcome. Automatic policy-diagnosis-driven repair/DCRG is a later explicit capability. No invented reachability diagnosis or VLM prose is converted directly into pose deltas. Existing DCRG retains its own research invariants and must eventually compose under the outer workflow budgets without competing authority.
6. Disable legacy graph synchronization and version-tree side effects explicitly for local-only evaluation. Research publication is separate; do not automatically promote the pilot into successful priors.

**Generalization boundary:** the workflow, Neo4j identities/events and GraphQL schema describe task definitions, embodiments, policy/runtime/evaluator profiles, declared criteria, trials, observations and outcomes. Scenario names, assets, camera names, joint/action layouts, services and numerical success predicates live in versioned profiles/adapters or fixtures—not `if scenario == A2` branches in the coordinator, repositories or resolvers. Different supported tasks use the same submit/evaluate/query/cancel/recover contracts. Unsupported combinations fail capability admission explicitly; a generic interface does not imply every task or embodiment is already operational.

For any request with required policy criteria, completion of environment prerequisites is nonterminal: atomically retain their disposition and reserve the policy intent, then terminally accept only after the selected task's obligations pass. Preserve scene-only terminal acceptance for scene-only requests. Stop/failure in required policy evaluation must never leave overall workflow state `accepted`.

#### Reference acceptance example only: A2

The example profile targets `droid_abs_joint_pos`, the served `nvidia/GR00T-N1.6-DROID` family and `OXE_DROID`, with actual checkpoint/config/serializer/modality pins independently verified. This is source feasibility, not a claim that the model is installed or A2 succeeds. Its `PickAndPlaceTask` uses the frozen lift-before-place sequence, airborne dwell and destination-contact/velocity/proximity predicates (`tasks/pick_and_place_task.py:133–197`). Proximity, a height peak, XY-in-bounds or VLM approval alone cannot substitute for that complete operational sequence. Default `min_airborne_steps=1` does not certify sustained multi-step lift or grasp; freeze/report the actual dwell/lift/destination settings before release. These requirements belong to this task profile, not to the general policy-evaluation contract.

---

### Phase 5: Shared application contracts and GraphQL boundary
**Target Files**:
- `isaaclab_arena/agentic_environment_generation/workflow/facade.py` (New Core Module)
- `isaaclab_arena_examples/agentic_environment_generation/web_api/` (existing FastAPI backend)
- Proposed GraphQL schema/resolver modules in the backend adapter layer; no frontend targets.

#### Mandatory domain study before schema implementation

The implementing agent must read the following **repository-held design inputs**, not rely on conversation memory, inferred database structure or the illustrative SDL:

1. [Session study](../quick_notes/session_memory.md#purpose-and-method-of-the-study), especially the purpose/method, domain distinctions and proposed ownership at lines 25–60.
2. [Event-mapping source inventory](../quick_notes/event_mapping_code_references.md), tracing the relevant existing tools/callers and their proof boundaries against current source.
3. [Plan 02](event-mapping-refactoring_plan_02.md): §3 identities/contracts, §4 commands/events/ownership, §11 extraction and §12 interface/read-model semantics.
4. This plan's §7 ownership/facts/transitions and §9 G01–G10 coverage, applying the latest explicit decisions: Neo4j-only persistence, GraphQL as the application interface, general Physical AI profiles and no dashboard implementation.

Older notes are historical design evidence, not current implementation status or permission to restore their superseded CLI-first/dashboard-compatibility/SQLite allowances. Recheck code anchors; use the current handoff for implemented/tested state. The five questions below are copied here so an agent without session-history access still has the method. Recover original session detail only when these retained notes leave an important decision ambiguous; never invent prior agreement.

For every in-scope workflow interaction, answer the original questions before selecting GraphQL fields:

| Original question | Required modeling answer |
| --- | --- |
| What happened? | Distinguish the requested command from the fact actually established; admission, execution, output retention, evaluation and acceptance are separate outcomes |
| To which identifiable object? | Name the entity/value/attempt/version and its stable scoped identity; do not conflate request, candidate, runtime realization, evidence or publication |
| Who knows authoritatively? | Name the producer that observes the outcome and the application/domain owner that validates and commits it; a resolver, database label or caller claim is not itself proof |
| Where is that fact retained? | Identify the Neo4j record/transaction and any immutable artifact/manifest; distinguish durable facts from process-local authority, observations and unsaved client input |
| What should the user see or be able to do next? | Define the query projection, meaningful status/blockers and permitted commands, including scope, freshness and revalidation on execution; do not design dashboard controls here |

Then resolve the associated DDD and recovery questions:

- Which actor intends what, under which preconditions and authorization? Which invariants must hold, and which aggregate/handler owns their atomic enforcement?
- What is immutable, what may transition, and which command causes each transition? How are parent/original/evidence/decision relationships retained?
- After client disconnect, refresh or process restart, which exact identities/results can a newly authenticated query reconstruct? Which work continues independently, which private grants are lost, and which input was never durably saved?
- What happens on duplicate submission, conflicting input, lost acknowledgement, expired authorization, resource contention, cancellation or uncertain execution? A generic `queued` label is not a sufficient answer; distinguish retained state from current observed blockers and their freshness.
- Which answer belongs in a mutation input/result, entity/query projection, typed failure or available-action contract? Which task-specific details belong in profiles/evaluators instead of universal GraphQL fields?

**Required output and gate:** extend the existing §7 event map and this plan's companion review ledger with completed per-interaction records in the form `actor/intent → command/preconditions → entity/identity → invariant owner/atomic write → fact/source/cause → retention/recovery → query/available actions → GraphQL G-row → Given/When/Then test`. Reuse existing answers; do not create another competing design document. Link every G01–G10 operation and its domain-significant fields to these records. Record unresolved semantic choices with a recommended decision and provenance; do not hide them behind a JSON scalar or infer them from local DTOs. Schema implementation for an affected operation starts only when its identity, authority, lifecycle, persistence/recovery and next-action answers are explicit. Existing proposed type names and package choices are downstream of this gate, not substitutes for it.

For example, `WorkflowAdmitted` means the request/run and frozen inputs were retained; it does not mean a candidate exists or execution began. After reconnect, command/run lookup retrieves those same identities without generating again. A pending run's readiness/resource/authorization explanation must come from its appropriate authoritative record or explicitly fresh observation, not from a browser guessing what `pending` means. This backend recovery requirement remains in scope even though dashboard implementation is deferred.

#### Changes:
1. Complete the mandatory domain-study records above alongside M0, then derive the application/GraphQL contract—not after a dashboard integration milestone. Extract the existing foreground application root into core. Proposed facade methods are `submit`, `status`, `cancel`, `resume`, `profiles`, `snapshot`, `events`; internal `drive_admitted` belongs to the execution owner. Names are design targets, not implemented APIs.
2. Build GraphQL queries/mutations over those shared commands/read models; use CLI and direct application tests to exercise the same behavior. Define continuation, authorization renewal and execution ownership from the managed domain model, not by mapping old dashboard buttons or queue endpoints. Maintaining legacy routes, reproducing SQLite SSE or implementing REST parity is not an acceptance requirement here.
3. Authenticate at the boundary and authorize through shared application rules. GraphQL's official guidance places authorization in business logic, avoiding inconsistent resolver-only checks.[16] Scope nested artifact/evidence/profile access as well as top-level operations.
4. Deliver backend entity identities, typed outcomes, available actions, durable query projections, command receipts and event/revision contracts that any future client can consume. Validate them without a dashboard. The schema sketch and backend recovery/security obligations are in §9; dashboard adaptation is a separate later task.

---

## 4. Verification Protocol & Acceptance Criteria

Verification progresses from pure/static boundaries to admitted isolated integration, then separately authorized native/live pilots. Preserve the plan-02 positive and zero-effect negative paths. §10 defines acceptance IDs and evidence; passing synthetic tests does not remove native limitations.

For the **A2 reference test only**, retain the original catalogue instruction: “Grasp the yellow banana from the right side of the table and set it onto the white ceramic plate on the left.” Pin `banana_ycb_robolab`, `plate_large_vomp_robolab`, `droid_abs_joint_pos` and the selected table/runtime identities from the [scenario catalogue](../agentic_env_generation/env_gen_test.md#-category-a-fresh-food--kitchen-tabletop-franka-droid--single-arm). Its historical geometry and ports are not current deployment facts.

Proposed pilot: one completed manipulation episode on each of two predeclared distinct simulation seeds, with the unchanged frozen operational PickAndPlace success sequence established for both before claiming pilot manipulation acceptance. This is not independent grasp certification. Record all stopped/failed/incomplete attempts and remote-policy randomness limitations. Freeze numerical horizons, thresholds and total budgets before launch; do not invent them in this plan or relax them after failure. Two successful seeds would not establish robustness, DCRG efficacy, causal diagnosis or prior eligibility.

---

## 5. Implementation Sequence & Milestones

| Milestone | Concrete deliverable / exit gate | Dependencies |
| :--- | :--- | :--- |
| M0: Domain/application foundation | Coherent identities, ownership, commands/queries and GraphQL contract sketch; Neo4j-only repository contracts and dependency inventory; no SQLite-backed runtime path | Approved implementation scope; existing researched workflow and engines |
| M1: Local bootstrap + model/prior wiring | Secret-free profiles, explicit credential-file setup/update/loading, private runtime handoff, full readiness, actual generation and exact retained prior adapter; isolated positive/zero-call proofs | M0; chosen supported role profiles; local single-operator scope, not production deployment |
| M2: Native scene loop | Owned native builder/capture/sampler; calibrated effective-placement and support evidence; application-owned repair/reassessment; usable CLI runbook | M1; separate native/live approval and declared resource scope |
| M3: General policy-evaluation integration | Task/evaluator/embodiment-independent lifecycle and evidence contracts; A2 as one reference pilot plus cross-task contract tests | Declared task prerequisites; selected supported policy adapter and separate live-effect approval |
| M4: Durable backend query/command model | Execution owner independent of requests, typed result projections, bounded events/revisions, command lookup and recovery | M0; extend and verify per engine slice as M1–M3 are implemented, without frontend work |
| M5: GraphQL application adapter | Schema/resolvers over the same application; query/mutation, authorization and CLI/direct-call contract tests | M0/M4 contracts and selected API dependency; isolated adapter work need not wait for live native approval |

### Concrete delivery checkpoints

- **V0 — GraphQL-to-application foundation:** implement the M0 invariant/event/query map (§7.1–7.3), explicit Neo4j/artifact/profile administration (§6.5), local API owner and complete schema contract (§9). Start with admitted isolated transports. A GraphQL client can submit, recover its command receipt and query a causal candidate/evidence history; the default CLI uses that API. No native/provider effect is claimed.
- **V1 — first complete live scene workflow:** combine the required parts of M0/M1/M2/M4/M5. One GraphQL submission performs actual generation with the declared retained prior policy, owned native capture, measured plus image assessment, permitted visibility repair when needed, fresh capture and a retained accepted or explained nonaccepted result. A fresh GraphQL/CLI client reads the exact artifacts and decisions. Prove a separate supported repair case if the generated positive candidate requires no repair. This checkpoint does not require policy evaluation; unsupported required-policy requests block before model calls.
- **V2 — required-policy extension:** M3 extends the same schema, run and owner with task-bound policy evaluation and its episode/evidence aggregation. A2 is one reference proof of that generic path, not its implementation contract. Exercise it through GraphQL, not only a standalone policy CLI. Scene-only V1 remains supported; other supported tasks select their own prerequisites/evaluators.

Order work inside V1 by dependency: typed contracts and owner/API skeleton → isolated capture/assessment topology and repair-envelope composition → separately approved fixed-candidate native calibration → one supported native visibility-repair proof → join real generation/retained priors through the same API. Fixed-candidate calibration is an explicitly labelled diagnostic/producer gate, not a substitute for the final from-scratch submission. Do not claim V1 from separate native CLI and synthetic GraphQL tests.

Milestone numbers group work, not a requirement to finish native pilots before designing GraphQL. Define contracts in M0 and implement/test the backend/GraphQL boundary incrementally against the same engine-backed application. Dashboard compatibility does not gate this work; removing SQLite dependencies from each reused backend capability is a mandatory gate for that capability, not optional legacy follow-up. No fixed-hour total is supported by the current native calibration, installation and policy unknowns. Assign file ownership per vertical slice; use red→green isolated tests, bounded independent review, then explicitly authorized live acceptance.

## 6. Operator journey and launch contract

### 6.1 Current versus proposed entry points

Current core `workflow.cli` implements **only `inspect-contract`** (`workflow/cli.py:41`). Execution is in `isaaclab_arena_examples.agentic_environment_generation.foreground_workflow_cli`, whose default factory is admitted only by the isolated harness. There is no established `arena-workflow` console command. Do not document speculative `--prompt`, `--provider` or `--eval-policy` flags as working now.

Select a new **installed GraphQL client** as the default CLI, not a new execution owner per invocation. The following is the **proposed target walkthrough**, not an executable live recipe today. Commands execute as the mapped non-root operator inside the simulation container; an editor-terminal wrapper selects that exact runtime/client context as specified in §6.2. `/private` paths are examples resolved inside that runtime, not the editor's filesystem; client/server configuration and entrypoints must be implemented first. Existing examples may delegate through thin wrappers; direct foreground execution is an explicitly named compatibility/test mode with the same Neo4j/application rules, never an automatic fallback when the API is unavailable.

```text
python -m isaaclab_arena.agentic_environment_generation.workflow.api.client run \
  --config /private/client.json \
  --operation-id a2-pilot-001 /private/a2-contract.json
```

Command → application mapping: CLI issues `submitWorkflow` → server authenticates the configured operator → normalize/freeze request → exact operation replay lookup → full required-role/profile/readiness/authority checks → durable admission and returned run handle → long-lived owner drives bounded priors/generation/capture/assessment/repair → optional required policy phase → retained result. The generated client configuration selects the local API-auth file explicitly; the client/launcher handles private loading without manually assembled shell pipes. Provider/DB credentials belong only to the owner's private launcher channel. `--auth-fd` remains a proposed advanced alternative when explicitly selected instead of file-based API authentication, never an automatic fallback. Admission can return before work completes. A waiting CLI observes queries; its exit/disconnect does not close the owner or cancel the workflow. Preserve the exact admitted run ID.

```text
python -m isaaclab_arena.agentic_environment_generation.workflow.api.client status \
  --config /private/client.json EXACT_RUN_ID
python -m isaaclab_arena.agentic_environment_generation.workflow.api.client cancel \
  --config /private/client.json --operation-id a2-cancel-001 EXACT_RUN_ID
python -m isaaclab_arena.agentic_environment_generation.workflow.api.client resume \
  --config /private/client.json --operation-id a2-resume-001 \
  --expected-version EXACT_VERSION EXACT_RUN_ID
```

Status → authenticated retained read only. Cancel → owner-local stop plus durable cancellation, separately reporting delivery/cleanup/unknown remote effects. Resume → exact reconciliation or known-unreleased continuation; fresh private authority is supplied only if required, without changing the frozen request. Same key + same contract returns the same admission even after a profile disappears; same key + changed contract conflicts. A different seed/model/intervention/budget requires a new contract/run, not authorization renewal.

### 6.2 Required onboarding deliverables

1. A secret-free server configuration binding deployment/workspace, operational DB, separate prior-read profile, artifact/lease roots, runtime/capture and generation/assessment/policy roles, plus a client configuration containing the pinned local API endpoint/scope, public protocol options and an explicit local auth-source reference, never the token value. Public profiles contain immutable revisions/digests and capabilities, never secrets or executable import paths.
2. A profile/contract compilation and validation command, designed in M1, that shows planned dependencies, exact selected request policies, budgets and unsupported criteria without probing/starting anything. Prompt/scenario convenience must compile to the same strict contract; it is not an alternative admission path.
3. A local bootstrap setup/launcher that creates and updates an owner-private credential file outside the repository, validates its structure/permissions, and loads it through the existing private runtime channel. Select exactly one configured source per role: local file by default, or an explicit pipe/named environment source. Conflicting source selection rejects; missing file/key never triggers ambient fallback. No automatic dotenv scan, secret logging or keys in Neo4j/profiles/GraphQL. Current foreground parser/factory do not support this installed flow; implement the setup, separate server/client schemas and loader together, rather than documenting an unimplemented flag.
4. A separately invoked bounded readiness check distinguishing configured, supported, observed-ready and execution-authorized. It must not infer simulation capability from a model ping or policy capability from metadata alone.
5. An installation/admin checklist: discover the clone's runtime/container, run native work non-root with `/isaac-sim/python.sh`, verify mounts/UID/GPU visibility/common lease, explicitly initialize new operational schema/artifact scope, configure read-only prior access and check pinned service identities. Runtime constructors never lazily migrate schema or initialize an unrelated store.
6. A fresh-operator walkthrough that produces an inspectable candidate, manifest, criterion/decision history and terminal result; a second process reads status and requests cancellation. Preserve JSONL admission-before-effects. Define admission-only `submit` success separately from the existing terminal `run`/wait exit semantics (accepted 0, invalid 2, blocked/unknown 3, denied 4, failed 5, stopped 6, cancelled 7); returning a run handle is not terminal scientific acceptance. Replace synthetic limitation labels only when supported by the new producer's actual receipts.

#### Local credential bootstrap: creation, storage and lifecycle

This is a **single-operator development/research bootstrap**, not a production secret-management system. Deliver a small setup command and launcher using the existing configuration/private-channel boundaries; no vault service, browser credential UI, encrypted database or custom cryptography is needed for this phase. Proposed command responsibilities below are implementation deliverables, not currently available commands.

- **Configure:** the operator explicitly chooses provider/model/roles and supplies already-issued provider keys and database credentials using hidden prompts. The launcher cannot create third-party provider keys; it creates only local configuration files and the application's own authentication token. Noninteractive setup accepts a private descriptor, never secret-valued argv. Validate bounded JSON/field structure offline; saving configuration must not initialize SDKs, ping providers, contact Neo4j, start simulation or spend inference budget.
- **Store:** default host location is `$XDG_CONFIG_HOME/isaaclab-arena/credentials.json` when XDG_CONFIG_HOME is a nonempty absolute path, otherwise use `~/.config/isaaclab-arena/credentials.json` for an unset/empty value and reject a relative XDG path, resolved for the actual host operator, outside the repository/build context. Create the Arena private directory with mode `0700` and credential file with mode `0600`, owned by that operator. Use a versioned JSON format with enumerated provider/DB slots; public profiles refer to logical credential bindings, not embedded values. An explicit different path is allowed only under the same private-file checks. Ship a tracked secret-free example, never a populated credential file.
- **Create/update safely:** first creation is exclusive; existing content is preserved unless the operator explicitly requests an update. Reject symlinks, nonregular files, unsafe ownership/permissions and malformed/oversized data. Write updates through a same-directory private temporary file and atomic replacement so interruption does not truncate the previous working file; remove owned temporary files without emitting their contents. Require a single writer/local setup lock for concurrent edits. Do not generate plaintext backup copies automatically.
- **Launch:** read the exact configured file, check the selected role/provider binding, and pass only required values into the long-lived owner and operation-specific worker channel. Do not `source` the JSON as shell code or copy keys into global process environment. Existing file contents make restart convenient but do not constitute execution authorization; frozen profiles, grants, budgets and release checks still apply.
- **Container handoff:** select the explicit host-owned **directory** mount described below for this development bootstrap: optional editor setup access, read-only simulator access, and `/run/arena-credentials/credentials.json` as the configured container path. Directory mounting permits a later controlled restart to read an atomically replaced file; do not use a stale single-file bind mount. The non-root runtime launcher reads the file and supplies the private envelope inside the simulator. Host/editor file descriptors do not automatically cross Docker. Pipe/named-environment sources remain explicitly selected alternatives, not the default editor-to-runtime bridge. Do not bake secrets into images or mount the whole host home. This is proposed wiring; protected Docker changes require separate approval.
- **Application API authentication:** generate a fresh cryptographically random, finite-lived local API token for an explicit owner launch. Supply it privately to the server and place client access information in a separate owner-private, instance-bound runtime auth file, not in the long-lived provider-key file or public profile. The owner writes that runtime auth file in its private runtime directory, accessible to the initial same-container CLI clients; their configuration names that exact local path. The wrapper/loader handles private authentication without assuming host and container home paths match. Bind it to endpoint/scope/owner instance/auth generation/expiry; stale files do not authorize a replacement server. Rotate explicitly, invalidate the previous generation, and remove only the matching instance file on orderly shutdown. Crash leftovers remain invalid on restart. This short-lived bootstrap file is an explicit exception to the earlier memory-only-token proposal.
- **Update/remove:** provide explicit credential update/removal with masked configured/not-configured status. File edits do not silently mutate active grants or accepted contracts. For the first bootstrap, applying changed credentials to a running owner requires explicit controlled restart/reapproval and the existing cleanup/reconciliation rules; no hot-reload watcher is required. Deleting the local file does not revoke a provider key or erase values already loaded into a running process. Explain provider-side revocation and avoid claiming secure erasure from disk/backups.
- **Usability and limits:** one setup → explicit launch → status/readiness → user-authorized run path must work without manually assembling file descriptors. Plaintext local storage remains readable by the owning OS user/root and may appear in backups; directory permissions and Git exclusion are not encryption or isolation from same-user processes. Document those risks. Never ask an agent to print/read real keys during verification; use synthetic sentinels only.

#### Selected devcontainer-to-runtime bootstrap

Current `.devcontainer/devcontainer.json:24–32` and `docker/run_docker.sh:164–175` do not mount the Arena credential directory. The editor's `/root/.config` is not the host operator's home. Both configured containers use host networking, but endpoint reachability does not make the simulator's private API-auth file accessible to an editor-side client. The selected minimal flow keeps API authentication local to the simulator rather than adding a shared-token mount.

| Step / execution location | Selected responsibility and identity |
| --- | --- |
| Host initialization, explicitly invoked as the host operator | Resolve the actual host config directory, nonzero UID, GID and intended runtime account into public bootstrap configuration; create/validate only the private directory, not keys. Never infer them from root's `HOME`/`id` in the editor |
| Editor setup, opt-in mounted directory | Bind that host directory to `/run/arena-credentials` read-write for setup. Run hidden-prompt setup under the configured host-mapped non-root identity, not editor root. A host-terminal setup command is the supported alternative if that identity/mount is unavailable |
| Simulator launcher and API owner | Bind the same host directory to `/run/arena-credentials` read-only. Use the resolved runtime account, its supplementary simulation-access groups, explicit HOME, working directory and configuration paths; invoke `/isaac-sim/python.sh`, not an editor interpreter or shell alias |
| Initial CLI/admin clients from either terminal | A fixed-target wrapper invokes the installed command in that discovered simulator under the same operator identity. It uses simulator-local client configuration/authentication; it does not copy tokens to the editor or create another execution owner |

Implement these as a small location/identity-aware launcher/runbook extension, not a generic Docker console or a cross-container worker supervisor. The wrapper selects an already approved runtime for this clone; native/model children remain supervised locally by the API owner. Never run `run_docker.sh` unchanged from editor root and treat its caller-derived UID as the host identity. Mount changes require explicit container recreation approval, not an automatic rebuild during setup or submission.

Order the walkthrough: host directory/identity initialization → approved mount wiring → explicit credential setup → admin initialization/profile registration → explicit API launch → second CLI profile/status query → separately authorized workload. Host credential initialization must be a mandatory opt-in step **outside** the current `initializeCommand` fallback (`.devcontainer/devcontainer.json:89`); a private-path rejection must not be swallowed by `ensure_host_directories.sh || mkdir -p ...`. Fail without permission broadening, fallback storage or launch. Validate the effective runtime account/groups and path access; a numeric `docker exec --user` alone is not proof that the account setup in `docker/setup/entrypoint.sh:23–28,59–60` was reproduced.

The installed launcher must create/populate/close any reused private credential pipe inside the simulator **after selecting that identity**. Preserve the existing reader's same-UID FIFO, mode, FD ≥3, byte/deadline and EOF requirements (`foreground_workflow_cli.py:171–188`); passing the mounted regular file or stdin as that descriptor is not equivalent. Do not weaken the isolated factory or reader to make the installed flow work.

Select **explicit detached API launch in the already-running simulator**, not a per-command foreground application. Return a public instance/endpoint readiness acknowledgement only after the owner and private client-auth file are ready; on ambiguous launch response, inspect that same instance instead of starting another. Client/editor-terminal loss and editor rebuild leave the simulator owner running; stopping/recreating the simulator does not. Exact-instance stop/restart goes through the private local control path and existing cleanup/reconciliation rules, rotates authentication and requires execution reapproval. Never call the legacy foreground CLI's command-finally-close path to host this server. No container auto-start, persistent restart daemon or new service manager is required by this choice.

T21 must start from a root editor with a different home, using synthetic values only: configure as the mapped operator, launch the simulator-local owner, authenticate a second client, disconnect/rebuild the editor, update credentials atomically and explicitly restart/reapprove. Confirm host persistence, new-value loading, old-token rejection and no effects during setup. The read-write setup mount and Docker socket make the editor trusted; keeping auth local reduces copying, not hostile-root access. Native readiness and actual provider use remain separate gates.

The future production transition replaces this local credential-source/loader with a reviewed secret store or managed secret injection and production identity/deployment controls. Keep that source boundary separate from domain records and runtime execution grants so replacement does not redesign GraphQL, Neo4j or workflow semantics. Production rotation/auditing/multi-user access, encryption/key management and deployment hardening are separate work; passing bootstrap tests does not certify them.

### 6.3 Paired public/private configuration example

This is a **proposed mapping example**, not a loadable current configuration or a claim of live model compatibility. M1 must deliver matching versioned server/client/private-launch JSON templates and validators and the new installed factory together; the isolated foreground factory stays unchanged. Only explicitly selected compatibility mode extends the old foreground parser; the default client does not construct that factory.

| Role | Public profile / literal binding example | Explicit private source and worker mapping |
| --- | --- | --- |
| generation | Operator profile `generation-openai-example`, revision 1; provider `openai`, model `gpt-4.1`, endpoint `https://api.openai.com/v1`; checked frozen request policy and settings digest | Explicit local credential-file binding mapped to private slot `models.generation`; named `env:OPENAI_API_KEY` or a private pipe is an opt-in alternative, never ambient priority scanning. Resolve nonempty key only for authorized fresh execution, then explicitly set `api_key`, `model`, `base_url`, `inference_profile`, `workflow_accounting`, `load_dotenv=False` |
| assessment | Distinct profile `assessment-openai-example`, revision 1; same literal pair only if its required image/response capability is admitted | Private slot `models.assessment`, explicit source may be shared by operator choice; separate role scope/deadline/accounting despite equal key/model |
| operational DB | Named database/profile, deployment/workspace, public endpoint and immutable settings revision | Separate private slot `databases.operational`; grants operational lifecycle access, not research-prior/publication authority |
| prior read | Explicit database and reviewed retrieval/eligibility profile | Separate private slot `databases.prior_read`, read-authorized source only; no fallback to operational DB credentials |

The local file and runtime envelope use versioned, enumerated secret/role slots, not arbitrary imports/configuration overrides. Exact byte/type limits and redaction follow the existing private channel; secrets never contribute to public settings hashes. Public config freezes literal binding and profile digest, not the key. File/pipe selection disables environment discovery. If environment mode is explicitly selected, other provider keys must not affect the mapping; `NV_API_KEY` remains the legacy NVIDIA source, not an implicit synonym for `NVIDIA_API_KEY`. On exact replay, perform current API authentication and lookup before resolving execution-only provider slots; missing provider keys must not block an otherwise authorized retained read. End-to-end tests must traverse setup, loader, parser and installed factory, not just validate a dictionary in isolation.

### 6.4 Startup and deployment boundaries

Default is observe-only readiness. `--allow-startup` cannot mean arbitrary Docker/shell access: use the existing scoped bootstrap/paired host-control protocol only after an installed helper and exact service allowlist are explicitly configured. Provisioning, model downloads and DB migration are separate administrative tasks. Changes under `docker/`, CI, pre-commit configuration or submodules require separate approval.

Select one long-lived API/application-owner process per scope, inside the discovered simulation container as its non-root user. Use the simulation interpreter for owned native children; no cross-container `docker exec` supervision is implied. ASGI request handling remains responsive while one bounded application driver owns the existing coordinator. Offload blocking queries and effect-driving work appropriately; never hold a request task or global mutation lock while waiting for native/model completion. Neo4j intents are authoritative and wakeups are hints. Request completion/disconnect must not call `ForegroundWorkflow.close()` or own worker lifetime. No SQLite Supervisor, broker or new service stack.

Initial API authentication is **single-operator, loopback-only bearer authentication** with a launcher-generated finite-lived token held by the server in memory and delivered to local clients through the restricted instance-bound runtime auth file/private channel described in §6.2. Bind the authenticated token to a server-configured principal/deployment/workspace; reject caller-supplied identity overrides. Provider and DB credentials load from the explicitly configured local secret source into the same owner's private channel, never GraphQL variables, argv or durable domain/public records. The API token and provider keys have different purposes; do not reuse one as the other. No browser cookies/session persistence or multi-user RBAC is implied. Loopback is not authentication: reject unauthenticated requests, restrict binding, redact headers/logs and do not expose the port outside the declared topology. Broader deployment/authentication requires a separately reviewed boundary. Provision a finite API-token expiry and auth-generation identifier; authenticate every request against the current generation/deadline. Explicit token rotation/revocation uses the owner-private administrative channel, not GraphQL secret input, and invalidates old API tokens. Expiry/revocation/rotation fences future releases under the affected authorization generation; explicit reapproval is needed for further work. Already released remote effects cannot be undone. Never log token values or include them in public settings hashes.

Keep the private grant registry and execution owner in that same process. Requests pass typed commands, not unresolvable cross-process grant IDs. Owner restart loses private grants; after freshly authenticating, retained queries/replay remain possible, but new release needs explicit reapproval/private credential injection for the frozen contract. Grant expiry/revocation is rechecked at release; request disconnect alone is not revocation. Cap grant expiry by the original authentication/credential/run deadlines. Reuse one authority interlock to serialize grant capture, purge, revocation, resolution and bounded private handoff across API/driver threads; do not await between validation and byte release or hold that lock across a whole model/native operation. Current ExecutionGrants requires caller-provided serialization (`execution_grants.py:134–143`). Verify common lease/socket/artifact layout and UID/host/boot/process identity. The existing exact-owner stop channel remains available during DB outage; an unreachable owner or expired lease is not permission to signal foreign PIDs or redispatch uncertain effects.

### 6.5 Explicit initialization and profile administration

Deliver proposed `workflow.admin` commands using shared application/repository contracts, separate from normal API startup:

1. Operator-reviewed provisioning establishes the runtime, dependency packages and Neo4j schema. Constraint/schema changes are explicit, quiescent administrative operations, never request-triggered migration or service startup.
2. `initialize-scope` binds database/deployment/workspace to the intended operational schema/version and store/registry identities. `initialize-artifacts` establishes the matching artifact area and verifies its identity. Same exact identity/settings replay; changed binding conflicts. Preserve an explicit incomplete initialization outcome across filesystem/Neo4j boundaries—no cross-store atomicity claim.
3. `register-profile` stores a bounded immutable public profile revision/body/digest in Neo4j, after validation and public-data screening; it grants no model/runtime permission. Register the required model, retrieval, native capture/runtime and policy profiles. Same ID/revision/body replays; changed content conflicts. Do not compute an accepted contract from whatever mutable defaults happen to exist at runtime.
4. Start the installed API in open/verify mode, supplying API auth and execution credentials privately. It verifies existing schema/scope/artifact/profile bindings and serves profile/retained queries; it does not initialize missing resources. Explicit readiness precedes submission effects.

Profile registration/schema initialization are intentionally administrative interfaces, not generic GraphQL CRUD. GraphQL must expose exact public profile revisions and compatibility/readiness distinctions. Positive acceptance starts from an empty disposable operational scope, performs only these explicit admin steps with SQLite denied, then starts a fresh API process and queries/submits through GraphQL. Missing or mismatched scope/schema/profile/artifact identity must produce not-ready with no lazy initialization or model calls.

## 7. Coherent event/domain model

These are logical responsibilities in one application, not new services or a demand for full event sourcing. Neo4j transactionally owns all durable domain/application records in the refactored backend; immutable files hold large evidence bytes. Existing SQLite data is legacy migration input only, never a runtime authority or fallback for this application. The proposed event names below are conceptual mappings; do not claim every name is an existing event enum.

| Entity / identity | Authority and retained facts | What survives refresh / user action |
| --- | --- | --- |
| Input source / revision | Explicit source bytes or immutable revision accepted by the application | Run binds frozen source identity, never an implicit mutable editor state; editor draft lifecycle is outside this scope |
| Request / operation ID / contract digest | Neo4j admission and canonical immutable contract | Same request can be looked up after lost acknowledgement; intentional new experiment gets a new operation ID |
| Workflow run / version | Neo4j current state, phase, decisions, budget and events | Status and allowed actions; version does not itself describe all owner-derived result changes |
| Execution intent / attempt / owner epoch | Neo4j reservation/release fence plus physical owner's independent locality/cleanup witness | Known pending work may continue; released unknown work must reconcile |
| Prior snapshot / context hash | Validated immutable artifact, bound to prompt/run/contract and authorized retrieval profile | Show exact consumed context/provenance and unavailable/empty distinction; no re-retrieval on refresh |
| Candidate / digest / parent / original baseline | Immutable source bytes + Neo4j selection/lineage | Inspect every candidate and repair delta, not only latest; raw and canonical digests remain distinct |
| Realization / reset / cohort / window | Owned runtime receipt and immutable native evidence | Distinguish authored scene from realized state; no stitching complementary passes across resets |
| Assessment / decision | Exact evidence selection, criterion/producer/model/rubric version, structured result and next action | Explain accept/repair/stop; unknown and advisory evidence never become required passes |
| Policy trial / episode | Frozen policy/task/scene/seeds, episode records and aggregation receipt | Display numerator and denominator, incomplete episodes, failures and actual provenance |
| Publication / prior eligibility | Existing explicit publication/readback and evaluation-eligibility contracts | Independent status; scene acceptance never silently publishes or qualifies a successful prior |

| Command | Retained fact / owner | Next action and failure boundary |
| --- | --- | --- |
| Submit | Request admitted, service/store | Announce stable run before execution; conflict/not-ready is not acceptance |
| Retrieve priors | Exact snapshot retained, authorized retrieval adapter | Required unavailable → block without model construction |
| Generate / validate | Candidate produced and validation disposition, existing engine + receiver | Application chooses native realization; no implicit agent auto-heal/publication fallback |
| Realize / observe / assess | Compatible cohort and criterion assessments, native producer + assessment adapters | Required pass → scene accepted; supported failure → permitted repair; unknown → bounded observation or stop |
| Repair | Child candidate and enforced cumulative delta, refiner + application guard | Fresh validation and evidence; no physical/task gate changes |
| Evaluate policy | Episode/aggregation receipt, existing policy runner + application | Overall acceptance only when all requested scene AND policy obligations pass |
| Cancel | Stop delivery, cancellation and cleanup are separate facts | Owner-local interruption must remain possible when DB access fails |
| Resume | Continuation/reconciliation disposition | Never renew authority or repeat remote effects on read-only replay |
| Publish | Exact target/readback/eligibility result | Separate authority and receipt; ambiguous write does not retry blindly |

Budget accounting spans all stages and restarts: model attempts including pings/retries, tokens/cost upper bounds, generation/repair candidates, realized scenes, settle/capture/policy steps, observations, episodes and wall deadlines. Retain the original deadline; resume never resets it. Unknown consumption is not zero. Producer failure and scientific rejection are separate from operational failure.

### 7.1 From the original event study to explicit DDD ownership

The recovered study is the user's hypothetical command-by-command walkthrough, recorded in [session memory, purpose and method](../quick_notes/session_memory.md#purpose-and-method-of-the-study) and the [source inventory](../quick_notes/event_mapping_code_references.md). The original questions remain: **what happened, to which identity, who owns that fact, where is it retained, and what may happen next?** EventStorming supplies collaborative process/design discovery; its official resources distinguish process modelling and software-design formats.[32] Event Modeling connects each workflow step to a command or read model and concrete Given/When/Then examples.[33] Here the consumer is GraphQL/CLI, not a screen requirement.

The following bounded-context/aggregate choices are **proposed concretizations in this review**, not claims that the earlier discussion already selected these exact boundaries. An aggregate is a consistency boundary, not every noun or every Neo4j node.[34] These are logical responsibilities inside the existing Python application, not services, brokers, a replacement controller or an event-sourcing requirement.

| Logical context / owner | Root or retained record | Invariants and mutation boundary |
| --- | --- | --- |
| Workflow coordination | `WorkflowRun` consistency root, identified by scoped run ID; accepted request/contract, selected candidate/evidence, decisions, budget and next intents belong to its lifecycle | Only application command/result-adoption handlers advance it. Selection + decision + reservation + next intent + run version/event commit atomically. Two completions against one version cannot independently advance candidate or policy state. Preserve the existing `neo4j_store._scene_decide` boundary rather than adding CRUD setters |
| Execution control | Run-owned attempts/intents plus a separate scope owner/epoch/resource authority | Application owns reserve/claim/release/cancel/adopt transitions; trusted runtime owner supplies physical registration/cleanup evidence. Store checks owner/fence/cancellation with each transition. A logical context boundary does not prohibit these explicit coordination checks in the same Neo4j transaction; do not weaken current atomicity to imitate distributed services |
| Scene and policy evidence | Immutable candidate revisions, realizations/cohorts, assessments, policy trials/episodes; referenced by the workflow root | Producers report bytes and measurements; domain evaluators establish criterion results; workflow policy selects evidence/next work. These records are not independently mutable `acceptCandidate`/`markPolicySuccessful` aggregates. Parent/original lineage, frozen evaluator settings, task predicates and trial denominators cannot be rewritten by a resolver |
| Research memory | Immutable publication identity/verified receipt and eligible prior selection; separate from workflow acceptance | Read-authorized retrieval consumes verified publication/evaluation facts. Operational candidate retention does not confer publication or successful-prior eligibility. This slice migrates required read-side metadata to Neo4j but does not introduce automatic publication |
| Execution-profile catalogue | Immutable public profile identity + revision + settings digest | Create/register through an explicit administrative application command; same identity/body replays, changed body conflicts. Role compatibility and private execution authority are separate. Resolvers cannot mutate a frozen profile to change an accepted run |

Dependency direction: transport adapters → application commands/queries → domain rules and repository/engine ports. Neo4j/runtime/provider implementations satisfy those ports. Domain rules do not import Strawberry, FastAPI, examples, SDKs or Kit. Reuse existing contracts/repairs/evidence/scene rules rather than creating duplicate model hierarchies. Neo4j is the selected repository implementation, but database connectivity does not replace domain ownership.

### 7.2 Command → domain fact → retained cause → query

Names below are domain vocabulary, not a claim that every name is already a separate stream event. Reuse existing stored event kinds where the exact immutable source record represents the fact; extend schemas only where linkage is missing. Transport invalidation and domain history are different contracts.

| Actor / command | Past-tense fact and authoritative retained record | Causation and next query/action |
| --- | --- | --- |
| Researcher / `SubmitWorkflow` | `WorkflowAdmitted`: canonical accepted request and run, initially pending | Submission command ID + contract digest → admission/command lookup and workflow query. Refusal before admission is not a retained run or terminal success |
| Owner / `RetrievePriors` | `PriorSnapshotRetained`: byte-verified exact snapshot or explicit unavailable/empty disposition | Run + authorized retrieval profile + source intent → prior query; required unavailable blocks generation |
| Released generator / result adoption | `CandidateRetained`: verified generation receipt and immutable candidate | Original generation intent/attempt + prior/catalogue identities → candidate/source query; output existence is not scene acceptance |
| Released native worker / capture adoption | `ObservationRetained`: native receipt, realization/reset/window/settings and cleanup | Exact capture intent/attempt + candidate → realization/evidence query; reserve assessment only from adopted evidence |
| Evaluator / assessment adoption | `CriterionAssessmentRecorded`: exact selected evidence, rubric/settings and verdicts | Assessment intent + observation/criterion IDs → assessment query; missing/inconclusive never becomes pass |
| Application policy / `DecideNext` | `RepairRequested`, `SceneAccepted` or `WorkflowStopped`: immutable decision linked to selected assessment | Decision ID + source assessment + expected run version → decision query. Existing `SceneDecisionRecorded` may notify this fact if its exact decision remains traversable |
| Released refiner / result adoption | `CandidateRevisionRetained`: parent/original, repair envelope, full delta and validation | Repair intent/attempt + deciding assessment → child lineage query; reserve fresh capture for the child |
| Released policy runner / trial adoption | `PolicyTrialRecorded`: episode records and declared aggregation | Policy intent + candidate + episode/reset/seed/checkpoint identities → policy-trial query; complete request only under §7.3 |
| Researcher / `CancelWorkflow` or `ResumeWorkflow` | `CancellationRequested`, `OwnerCleanupRecorded`, or `ContinuationAdmitted`: distinct command/attempt dispositions | Exact cancel/resume command key + target run + source attempt; stop delivery, durable cancellation and cleanup remain independent facts |

M0 event contract: retain event schema version, scoped sequence/identity, run/aggregate identity and committed version, causation command/intent, and typed source-record identity. Worker facts link to their released intent/attempt and original submission; cancel/resume facts link to their own command receipt rather than overloading the submission key. Current `_event` stores `source_id`, but public events omit it and no complete aggregate/causation envelope exists (`neo4j_store.py:362–377,2070–2077`): this is explicit implementation work. Events can remain small invalidation messages, provided an authenticated query can traverse the **exact historical decision/receipt**, not merely the latest run. No native sample-per-step event stream or complete state reconstruction by replay is required.

Example acceptance scenario: given a failed visual assessment and a later accepted child, when a fresh client follows both decision/event source identities, then it retrieves different retained assessments/candidates and their causes even if both notifications arrived before its first query. Given competing results for one expected run version, only one selected decision/reservation commits. Both are required domain tests, not GraphQL-specific resolver behavior.

### 7.3 Outcome/transition contract

| Trigger and guard | Retained scene / policy dispositions | Workflow transition and permitted next action |
| --- | --- | --- |
| Required scene criteria established; no policy required | Scene accepted; policy not requested | Terminal accepted after applicable verified cleanup; no further execution |
| Scene established; required policy supported/authorized and budget available | Scene accepted independently; policy pending | Stay nonterminal; atomically reserve exactly one policy intent. Scene-only terminal acceptance cannot run first |
| Selected complete policy aggregation establishes required criteria | Retain scene acceptance and exact policy success/denominator | Terminal accepted after cleanup |
| Policy trial completes but criteria fail, or frozen coverage/budget is exhausted | Keep prior scene acceptance; policy failed/not established or incomplete as actually observed | Stopped with criterion/coverage/budget reason, never accepted; no automatic scene/policy modification |
| Infrastructure/producer fails before a valid outcome | Prior verified scene/evidence remains diagnostic; failure is not a measured policy failure | Failed or explicit blocked disposition according to retained execution state |
| Released effect has uncertain outcome | Preserve last verified scene/policy facts; effect uncertainty separate | Reconciliation required; no redispatch/refund without exact retained proof |
| Cancellation fences release; physical cleanup confirmed | Preserve completed evidence and independent scene/policy facts | Cancelled; stop delivery and remote-effect uncertainty still reported |
| Exact continuation of known-unreleased intent with current authority | Frozen inputs, selected evidence and original budgets/deadline unchanged | Resume only that intent. Exact receipt recovery performs no repeated completed work |

`SubmissionAccepted`/`WorkflowAdmitted` describes command admission; `workflowOutcome=accepted` describes satisfaction of the frozen required criteria. Keep unknown command acknowledgement, uncertain external execution, inconclusive criterion evidence and zero completed episodes as distinct fields/reasons. The current `read_model.py` coupling of scene acceptance to terminal run state must be replaced by the retained independent disposition, not just renamed.

**Tabletop validation, not execution evidence:** one authenticated GraphQL command obtains a retained admission; its read-authorized exact replay precedes mutable profile/grant lookup. The owner retains exact priors, reserves and releases generation, verifies/adopts its output, then releases capture. Native cleanup and observation adoption precede model assessment. A supported visual failure produces one guarded repair and a fresh capture; a complete passing cohort yields scene-only acceptance or the required-policy handoff. At each lost acknowledgement, query the original command/intent/receipt; before release, known pending work may continue under current authority; after ambiguous release, reconcile rather than resend. Cancellation reaches the physical owner before DB dependence and separately reports durable disposition. This walkthrough must become the T14–T18 tests, not remain prose.

### 7.4 M0 per-interaction records (Phase 5 / T20)

**Provenance and status.** These completed modeling records apply the questions in `session_memory.md:25–61`, the full `event_mapping_code_references.md`, plan 02 §§3/4/11/12 and this plan §§7/9. They are **implementation-target decisions made here**, not recovered user agreement, executable schema or newly verified runtime behavior. “Existing” below means source inspected at `8f40c55584c4998ac657128f433d4dd407a2905b`; source guards remain binding until a versioned extension is implemented/tested. The current foreground factory is harness-only and rejects credentials/startup outside that admission (`E/foreground_workflow.py:44–73`). The records do not authorize native work or replace any engine.

**Common contracts for every record.** `S` means the trusted operational database/store authority + deployment + workspace; every entity reference, nested traversal and cursor is authorized within `S`, never by caller-selected principal. Keep existing IDs; add scoped typed references rather than rehash historical entities. Command identity is `(S, kind, operationId)`; canonical payloads and current read authorization follow §9. Command receipt, run version, projection revision and event sequence are different values. A query writes no domain fact, probes nothing and releases/renews nothing; internal repository locking is not a domain revision. For reads, “owner/write” names the owner of the *source fact*, not permission for a resolver to mutate it. Neo4j retains relationships/dispositions; immutable artifacts retain bytes. There is no cross-filesystem/database transaction: verify final bytes before adoption, retain incomplete/unknown adoption explicitly, and reconcile the exact receipt rather than regenerate. No history pruning/deletion is enabled in this slice; bounded pages are not deletion or snapshot reconstruction.

`availableActions` describes authenticated application commands and their typed prerequisites/reasons at an identified projection revision, not internal worker instructions or a grant. Nonterminal runs may offer cancel and either known-unreleased continuation or exact reconciliation through resume; cancelled/accepted/stopped/failed runs offer no fresh execution. An unresolved released attempt never offers retry. Read links remain available under current read permission. Authorization/resource/readiness observations carry freshness and are revalidated at mutation/release; absence of private authority is `reapproval_required`, not lost domain history. Missing/out-of-scope entities disclose no foreign existence; invalid cursor, replay gap and unavailable authority remain distinct typed outcomes. Unsaved client input has no retained identity.

The following source keys use `C/ = isaaclab_arena/agentic_environment_generation/`, `F/ = C/workflow/`, `E/ = isaaclab_arena_examples/agentic_environment_generation/`. They identify **current guards and gaps**, not proof that the target API exists:

| Key | Exact inspected source / constraint |
| --- | --- |
| S1 | `F/contracts.py:29–225,229–247` freezes/validates source, criteria, preserved/intervention paths, role profiles, seed/time, budgets and effect ceilings; profile references currently have ID/hash, not an explicit revision; policy intent consistency is not execution support |
| S2 | `F/service.py:257–305`; `F/neo4j_store.py:255–337,362–398`; `E/foreground_workflow.py:434–485` implement canonical submit replay before mutable checks, pending admission/event and typed readiness freshness; admission is not dispatch |
| S3 | `E/foreground_cancellation.py:98–160,164–204`; `F/neo4j_store.py:949–1079` preserve stop-before-DB, version-fenced cancellation and exact registration cleanup; no durable cancel operation-key receipt exists |
| S4 | `E/foreground_workflow.py:750–869`; `F/service.py:50–128,166–182` distinguish pending continuation from exact generation receipt recovery and refuse released scene redispatch; resume key/expected-version command receipts are new |
| S5 | `F/neo4j_store.py:193–241`; `C/workbench/research_artifacts.py:220–276`; `C/workbench/model_profile_store.py:15–51` distinguish explicit schema/scope/artifact initialization from open; current create-only model catalogue is Journal/SQL, not a permitted target repository |
| S6 | `F/read_model.py:20–42,120–170`; `F/neo4j_store.py:1134–1195,1987–2045` expose conservative reservations, selected result and owners; current scene acceptance depends on terminal state, policy is `not_run`, cleanup uses current owner dirty flag—none suffices for the target independent outcomes |
| S7 | `C/graph_rag.py:387–459`; `C/prior_receipt.py:150–256`; `F/prior_artifacts.py:23–92`; `C/workbench/research_retrieval.py:219–225,324–328,367–376` bind exact prior bytes/status/settings and expose the managed-selection SQL dependency requiring migration |
| S8 | `F/service.py:75–128`; `F/scene_loop.py:39–46,118–141`; `F/neo4j_store.py:1670–1795` verify generation bytes/cleanup, retain candidates with parent/original and reject conflicting or stale scene completion; rejected repair output is not currently a complete historical candidate projection |
| S9 | `F/evidence.py:52–112,115–165`; `F/evidence_contracts.py:19–68`; `F/scene_ports.py:131–147`; `F/scene_loop.py:74–99,144–181` bind selected cohort/criterion semantics, reject required policy/differing windows, constrain current capture to `(0, capture_steps)` and route only supported visual repair with physical prerequisites |
| S10 | `F/scene_evidence_artifacts.py:19–56,59–150`; `C/workbench/research_artifacts.py:310–353`; `F/neo4j_store.py:1204–1311,1670–1795` enforce bounded screened immutable payloads and atomically retain result/decision/selection/reservation/next intent; split capture/assessment remains target work |
| S11 | `E/web_api/evaluation_worker.py:24–117`; `F/contracts.py:159–175,203–220`; `F/evidence_contracts.py:34–42`; `F/read_model.py:157–170` show a separate pinned, fixed legacy policy bridge, policy-intent validation but no managed policy producer, and unsupported publication/DCRG |
| S12 | `F/readiness.py:81–115,118–135,156–209,245–302`; `F/neo4j_store.py:147–164,380–398`; `E/foreground_workflow.py:425–432,684–692` distinguish local monotonic reports, durable oldest-observation wall time, exact profile/instance checks and release TTL from effect-free status |
| S13 | `F/neo4j_store.py:362–377,1204–1311,1987–2045,2047–2090` already has atomic scoped events and floor/ceiling checks; public events omit source/revision/causation, snapshot is unpaged, and owner changes lack run events |

**M01 — Submit / recover admission.** Actor/intent → authenticated researcher requests one frozen experiment, not completion. Command/preconditions → `submitWorkflow(operationId, request)`; bounded canonical S1 input, operational-write permission, supported whole-outcome profiles and readiness; read-authorized exact replay precedes mutable profile/grant lookup. Identity → `(S, SUBMIT, operationId)`, canonical contract digest, stable run ID, source and exact profile references. Owner/write → service + admission transaction create accepted request/run/version/event together; no reservation or effect implied. Fact/source/cause → `WorkflowAdmitted` maps existing `WorkflowRequested` to this submission (S2). Retention/recovery → return the same run after lost ACK; changed canonical payload conflicts; validation/refusal before admission creates no run/reserved key; uncertain commit returns unknown, not refusal. Query/actions → `workflowSubmission`, `workflowCommand`, `workflow`, admission disposition/run/version/digest; observe or separately cancel/resume, never resubmit with a new key to recover. **G01/G03; GWT (T02/T11/T20):** Given admitted input and subsequently missing profile/expired execution grant, when a currently read-authorized client replays, then the same admission returns with zero probes/releases; changed input conflicts.

**M02 — Cancel.** Actor/intent → authorized researcher stops the exact run. Command/preconditions → `cancelWorkflow(operationId, runId)`; current API authentication and exact owner-local stop authority, no fresh execution grant and no caller version requirement. Identity → `(S, CANCEL, operationId)`, payload digest of exact run ID; affected attempt/fence/owner epoch remain separate. Owner/write → deliver sticky local stop before DB dependence; application then commits command disposition + cancellation fence/version/event atomically, using current-version CAS. Fact/source/cause → local delivery observation, durable `CancellationRequested`, and later cleanup receipts are three facts (S3); command cause is not the submission key. Retention/recovery → store outage means durable outcome unknown even if delivery succeeded; replay/lookup cannot certify remote cancellation. Safe exact-run local stop may already have occurred before discovering a key conflict; report that explicitly rather than promise mutation rollback across OS/DB. Query/actions → receipt delivery/durable disposition/cleanup/remote-effect uncertainty; only reconciliation, no execution resurrection after cancellation. **G01/G03/G10; GWT (T04/T09/T11):** Given an active owned worker and DB outage or stale run version, when cancel arrives, then local stop remains deliverable and persistence/cleanup are independently reported; replay never sends work.

**M03 — Resume / reconcile.** Actor/intent → researcher reconciles or continues the same experiment. Command/preconditions → `resumeWorkflow(operationId, runId, expectedVersion, renewAuthorization)`; exact retained lookup first, then version/scope/lifecycle checks; continuation needs fresh readiness/current private authority, while exact receipt recovery needs read authority only. Identity → `(S, RESUME, operationId)` plus digest of all three payload fields, source intent/attempt and frozen contract. Owner/write → shared handler retains the command's continuation/reconciliation disposition and any authorized run transition/next-intent selection atomically; never a resolver retry or second coordinator. Fact/source/cause → `ContinuationAdmitted` is permission to pursue the stated branch, not evidence of release/completion; S4 already blocks prepared/released scene redispatch. Retention/recovery → replay returns original receipt without renewal; renewal is private, single-owner, release-fenced work, not a durable secret or renewable query. Restart loses grants, not budgets/deadline/results; missing proof leaves reconciliation required. Query/actions → command target/version/disposition/uncertainty and separate current run status; known-unreleased continuation versus reconciliation explicitly distinguished. **G01/G03/G10; GWT (T04/T11):** Given lost resume ACK or an already released unknown intent, when replay/lookup occurs, then no grant is renewed, allowance reset or effect repeated; changed expectedVersion/renewal flag under the same key conflicts.

**M04 — Command lookup.** Actor/intent → fresh client resolves an uncertain response without requesting work. Query/preconditions → `workflowSubmission(operationId)` or `workflowCommand(kind, operationId)` under current scoped read authority; no mutable execution configuration. Identity → common scoped kind/key; SUBMIT wraps the existing identity without rekeying. Owner/write → application command handler owns immutable canonical payload/digest/target and retained receipt revisions; query performs no transition. Fact/source/cause → exact original command disposition, not latest run state (S2–S4); cancel/resume receipts are target additions. Retention/recovery → choose typed `not_found`, retained receipt or store-unavailable/unknown. Authorized cancel/resume application refusals after durable key binding are retained and replayed; malformed/unauthenticated input is not a durable command. Query/actions → kind/key/target/payload digest/command version/disposition/reason/uncertainty, optional run link; a new command is explicit new intent, never inferred from GraphQL partial data/errors. **G01; GWT (T11/T12):** Given the same literal operation ID in different kinds, when another adapter queries each, then distinct receipts resolve and no effects occur; an unavailable store is not reported as not-found.

**M05 — Explicit administration and immutable profiles.** Actor/intent → operator establishes a usable scope/catalogue; reader selects exact compatible roles. Command/preconditions → quiescent `initialize-scope`, `initialize-artifacts`, `register-profile` through admin, after explicit provisioning/public-data screening; API startup only opens/verifies. Identity → `S` + schema/store/registry/artifact binding; profile `(S, profileId, revision)` with immutable kind, declared roles and canonical body/settings digest; profile IDs cannot alias across kinds. Owner/write → admin repository transaction owns each identity/conflict; filesystem initialization is a separate verified step, with incomplete setup retained/reported, never a false atomic success. Fact/source/cause → scope/artifact binding verified or profile revision registered by admin invocation, granting no execution authority (S5). Retention/recovery → exact existing binding/body replays by verify/open, changed content conflicts; old accepted references remain readable after catalogue changes. Query/actions → `workflowProfiles`, `workflowProfile(id, revision)` expose bounded public role/settings/capability fields, supported/configured separately from M14 readiness. No profile mutation/secret/startup GraphQL operation. **G02/G03; GWT (T13/T17):** Given an empty disposable scope, when explicit admin completes and a fresh API opens it, then exact profile/binding reads succeed; conflicting revision or mismatched artifact identity refuses without lazy initialization or SQLite. Missing catalogue data cannot mutate a retained contract.

**M06 — Inspect/list runs and frozen intent.** Actor/intent → researcher reconstructs work after disconnect. Query/preconditions → `workflow(id)`, `workflows(first, after)` and nested connections, current scope/read permission. Identity → run ID + contract/source digest + run version; projection revision includes retained owner/cleanup/readiness dependencies, not just run version. Owner/write → workflow root owns source/request/profile/criteria/intervention/effect/budget records; list reads commit no scientific fact (S1/S6). Fact/source/cause → retained phase, selected identities, independent candidate-production/scene/policy/workflow/publication/cleanup dispositions and uncertainty caused by exact decisions/attempts. Retention/recovery → frozen prompt or explicit existing-source bytes/identity, role revisions/settings, criterion subjects/frames/windows/limits and original deadline survive restart; never read editor drafts/current defaults. Query/actions → conservative reserved/remaining allowance separately from unknown actual usage; typed blockers with source/freshness; permission-filtered actions per common contract. **G03; GWT (T10/T15/T20):** Given scene pass followed by policy failure, or clean run A with still-active/replacement owner B, when a fresh client reads, then scene pass/A's cleanup persist independently of overall stop/B's activity. Bounded keyset live lists use `(runId)` and scoped query-bound cursors, not snapshot claims.

**M07 — Retrieve and inspect priors.** Actor/intent → owner supplies exact authorized context; reader inspects what generation consumed. Command/preconditions → internal `RetrievePriors` under frozen retrieval profile/database/eligibility/empty-result policy and separate read authority before model construction. Identity → `(S, run, prior receipt)`, prompt/contract/context hashes, selection/version/evaluation references. Owner/write → retriever validates S7 provenance/bytes; workflow adoption retains snapshot manifest/status/source intent links before generation consumes it. Fact/source/cause → `PriorSnapshotRetained`, with exact context, filters, effective settings, source/evaluation counts and warnings; `empty` is successful retrieval with no eligible rows, `unavailable` is not empty, `not_requested` performs no retrieval. Retention/recovery → reopen exact consumed bytes; refinement/queries do not retrieve anew; retain membership-versus-mutable-evaluation snapshot limitation. Required unavailable blocks; optional fallback must be frozen/labeled. Query/actions → `workflowPrior`, run/candidate prior links and authorized manifest; no publish/promote action. **G04/G08; GWT (T05/T13):** Given a changed corpus after admission, when historical prior is queried, then original context/provenance returns without retrieval; invalid receipt/required unavailable yields zero model calls. Managed eligibility remains unsupported until SQL-dependent metadata is migrated, never silently reduced to legacy eligibility.

**M08 — Produce/validate and traverse candidates.** Actor/intent → released generator produces a candidate; researcher inspects every retained revision. Command/preconditions → generation result adoption after exact released fence, registration, contract/profile and byte verification; actual schema validation remains in the admitted adapter, not pure request construction. Identity → scoped candidate ID/raw-source digest/canonical-spec digest plus digest codec, run, original/parent and producing attempt/intent; original has itself as original and no parent (S8). Owner/write → service verifies receipt/cleanup; workflow-root transaction retains candidate, validation disposition and selected decision/next intent, not an independent mutable candidate aggregate. Fact/source/cause → output produced, validation/nonconvergence and scene acceptance are separate; cause links submission → generation intent → prior/catalogue/model settings → receipt. Retention/recovery → preserve failed/nonselected outputs diagnostically, including target retention missing from current rejected-repair projection; unsafely screened output retains a rejection/identity record, not exposed unsafe bytes. Query/actions → `workflowCandidate`, `Workflow.candidates`: source manifest, parent/original, producing decision/attempt, validation findings/delta, evidence links; no `acceptCandidate` setter. **G05/G07/G08; GWT (T02/T15):** Given a failed parent then accepted child, when both IDs are queried after restart, then distinct immutable sources/validation/causes return, not two copies of latest candidate.

**M09 — Permitted repair.** Actor/intent → application requests only a supported correction; refiner proposes, never authorizes. Command/preconditions → failed visual criterion with required physical preconditions established, explicit effective XY paths, budget and original-centered bounds; frozen repair envelope precedes model effect (§3 Phase 3; S8/S9). Identity → repair decision/intent/attempt, parent/original/child and envelope/delta manifests. Owner/write → refiner supplies full candidate; authoritative repair guard checks unchanged task/physics/Z/topology and original displacement, then root atomically adopts child/validation and reserves fresh observation. Fact/source/cause → `CandidateRevisionRetained` or rejected proposal, caused by exact failing assessment/evidence/decision, not user-authored feedback history. Retention/recovery → retain proposal/delta/rejection safely even when not selected; no-op/ineffective/forbidden changes never replace parent; unknown released refinement never repeats. Query/actions → child `producedByDecision`, parent/original, allowed-versus-actual delta/validation and fresh-evidence requirement; internal repair is not an extra public mutation. **G05/G06/G07; GWT (T07/T14/T15):** Given failed parent evidence, when a permitted child arrives, then it cannot inherit a state-dependent pass; forbidden Z edit is retained as rejected and inward valid XY motion is not rejected by an invented remaining-radius rule.

**M10 — Realize/capture and inspect evidence.** Actor/intent → owned runtime observes the frozen candidate; reader distinguishes authored from realized state. Command/preconditions → separately reserved/registered/released capture, actual held resource lease, validated settings and candidate; cleanup verified before assessment release. Identity → `(S, run, capture intent/attempt, candidate, realization, environment/reset, cohort/window)` plus seed/settings, absolute reset-relative steps, subject/frame/camera mappings. Owner/write → runtime produces manifest/raw measurements; application verifies exact bytes/provenance/cleanup and atomically adopts observation + next assessment intent. Fact/source/cause → `ObservationRetained` is capture evidence, not physical acceptance; authorship→solver-effective→realized mapping, partial coverage/termination and unavailable terminal images stay explicit. Retention/recovery → exact retained capture feeds known-unreleased assessment without rerender; uncertain capture cannot repeat. S9/S10 currently combine observe/assess and admit synthetic profiles only: native/substage/offset support requires versioned codecs, not removing guards. Query/actions → `workflowEvidence`, candidate evidence connection, realization/cohort/settings/step/measurement/manifest/limitation fields; no arbitrary camera/PID operation. **G06/G08/G10; GWT (T06/T14):** Given complementary passes from different resets or partial frames, when evidence is adopted/read, then coverage stays distinct and cannot satisfy a complete cohort; restart between capture and assessment reuses exact bytes without native work.

**M11 — Assess, decide and traverse historical causes.** Actor/intent → evaluator establishes criterion findings; application selects next work; researcher asks why. Command/preconditions → assessment of verified selected evidence under frozen criterion digest, subjects, modalities, frames/window, limits, rubric/evaluator/model version. Identity → `(S, assessmentId, evidenceId/cohort, candidateId)` and distinct decision ID/run version; IDs are not timestamps. Owner/write → evaluator owns observed verdict; workflow root alone commits assessment/selected evidence, decision reason/action, cumulative reservation/next intent/version/event together (S9/S10). Fact/source/cause → `CriterionAssessmentRecorded` and decision link exact observation/capture/assessment attempt, with missing/failed/historical-conflict IDs; VLM prose is not physical truth. Retention/recovery → old failure survives later pass but is not a permanent veto; latest projection never substitutes for historic decision. Query/actions → `workflowAssessment`, `workflowDecision`, `Workflow.decisions`, `Decision.selectedAssessment → Assessment.evidence → Evidence.realization/cohort/manifests`; required/advisory verdicts, reason and next intent are typed; policy handoff follows §7.3. **G07/G05/G06; GWT (T15/T20):** Given two completions at one expected version, when competing handlers adopt them, then only one decision/reservation advances; following earlier failed and later accepted event sources still yields different causal records with zero execution.

**M12 — Artifact inspection/byte access.** Actor/intent → authenticated reader retrieves exact retained source/evidence bytes. Query/preconditions → `workflowArtifact(id)` and separately authenticated immutable-byte access, current scope plus owning run/evidence authorization; never an arbitrary path. Identity → `(S, artifact area/store/registry, manifest digest, member identity)` tied to candidate/evidence/prior/assessment. Owner/write → producer stages/promotes bounded bytes; application adopts verified manifest relationship in Neo4j; download is read-only (S10). Fact/source/cause → verified integrity/size/binding, not measurement authenticity, linked to producing receipt/attempt. Retention/recovery → missing/tampered bytes produce unavailable/integrity failure, never silent regeneration or a fresh manifest; DB adoption uncertainty reconciles exact staged/final identity. Query/actions → digest algorithm/version, manifest/member size, media type/encoding and scoped byte-access reference; no absolute filesystem paths, credentials or active report HTML on API origin. **G08 (and G04–G07/G09); GWT (T12/T15):** Given a historical failed-candidate manifest, when an authorized reader traverses it after child selection, then its original bytes return; cross-scope nested lookup/download is denied and tampering cannot be reported as verified.

**M13 — Policy intent, trials and aggregation.** Actor/intent → researcher requests task-bound policy evidence; released runner produces trials, application judges full outcome. Command/preconditions → required policy criteria need supported registered adapter/task/evaluator/interface and explicit budgets/authority before any generation; S11 currently has no such managed producer. Identity → `(S, trialId, run/candidate, policy artifact/config/served-instance, task/evaluator/instruction, embodiment, episode/reset/seed)` plus aggregation revision/coverage. Owner/write → runner retains episode metrics/termination/completion; application verifies and atomically adopts trial/aggregation and next decision. Scene pass reserves policy while nonterminal; only complete required success accepts overall. Fact/source/cause → `PolicyTrialRecorded`, measured failure versus producer failure/incomplete/unknown, with exact numerator/denominator and episode causes. Retention/recovery → completed failures/incomplete attempts remain; rebuilt/reset policy episodes re-establish state-dependent prerequisites; no automatic policy repair/publication. Query/actions → `workflowPolicyTrial`, `Workflow.policyTrials`, bounded episode pages and registered typed metric variants, not universal A2/lift fields or opaque JSON. **G09/G03/G02; GWT (T08/T16/T19):** Given scene-only and required-unsupported-policy requests, when admission/projection evaluates each, then the former exposes `not_requested` and the latter refuses with `not_supported` capability/reason and zero model calls, not `not_requested` or a fabricated trial. Supported zero-completion trials are not success; policy failure preserves scene pass but stops overall. Historical v1 `not_run` is not retrospectively relabeled as proof.

**M14 — Readiness/blocker observation.** Actor/intent → operator explicitly checks dependencies or owner gates fresh release; readers understand waiting without probing. Command/preconditions → separately invoked bounded readiness check, or admission/release gate derived from whole frozen outcome; no implicit startup/provider constructor. Identity → observation ID + `S`, contract/profile revisions/settings, expected/observed instance identities, observer/clock provenance and validity bound. Owner/write → trusted gate observes; application retains sanitized success/blocker disposition for admitted work and its observation time/source, alongside but not replacing lifecycle state. Pre-admission/store-unavailable results remain explicitly local/non-durable (S12). Fact/source/cause → configured/supported are metadata; observed-ready is a time-bound observation; execution-authorized/actual resource lease are separate. Preserve oldest probe time and release TTL; a fresh status response does not refresh readiness. Retention/recovery → query may return retained-stale/not-checked/unknown and original blocker source; no persisted monotonic time reused across restart. Query/actions → G02 capability/readiness and G03/G10 blocker fields carry checked-at/valid-until, observed-versus-retained provenance and recommended explicit action; authority/lease are rechecked at release. **G02/G03/G10; GWT (T03/T10/T20):** Given a retained passing check followed by expiry/service replacement, when status is read, then it remains historical with zero probes; fresh release must fail/recheck, never use cached ready as authorization.

**M15 — Events, owner activity and bounded history.** Actor/intent → observer resumes invalidation polling and follows causes after disconnect. Query/preconditions → `workflowEvents(after, first)` under current scope/read authority; opaque cursor binds `S`/stream/version, full-width sequence, floor/ceiling and bounded page. Identity → event sequence/schema + run/aggregate committed version, command kind/key, typed source record/intent/attempt; historical owner epoch distinct from current scope owner. Owner/write → workflow transaction appends source-linked event with its fact; owner/cleanup changes must advance the relevant projection revision/invalidation without inventing a run transition (S13). Fact/source/cause → invalidation points to exact historical record, not event-time reconstruction by latest status. Retention/recovery → duplicate delivery is harmless; gap requires fresh bounded snapshot/list then continued stream; no pruning until atomic floor/page design exists. Query/actions → event source traversal, run version/projection revision/cursor, per-run attempt cleanup and separate current owner observation; no release/renewal/foreign-PID action. **G10/G03/G05–G09; GWT (T10/T15):** Given multi-run interleaving, owner retirement and two historic decisions, when a fresh client first polls, then scoped pages retain original sources, projection changes are visible, and future/wrong-scope/expired cursors reject distinctly. Candidate/evidence/decision/trial connections use scoped parent/query-bound `(recordId)` keysets; these are live lists, not chronology or frozen multi-page snapshots.

**Gate disposition and first slice.** All G01–G10 operations/meaningful field families now have records; §9 names remain proposed downstream mappings. No unresolved ownership/identity/recovery choice blocks an effect-free M0 implementation slice. **Enabling-profile blockers remain explicit:** select/version actual retrieval empty-result/eligibility behavior (recommend required-empty blocks unless the frozen profile expressly allows it); freeze native settings/calibration and supported task-specific typed metrics/aggregation before admitting those producers. These are operator/adapter selections under §11, not inferred past user decisions; retain `not_supported` until resolved. Old receipts lacking causal/native/policy fields expose unavailable/unknown with provenance, never invented backfill. Public catalogue revision, command receipt and event-link codec changes require compatibility readers and T20/T15 verification, not opaque JSON escape fields.

Proposed first implementation slice: characterize S1/S2/S6/S13 under existing admitted isolation, then add shared typed **effect-free** run/command/historical-record query contracts and Neo4j source-link traversal preserving current IDs, exact replay and synthetic guards; explicitly initialized Neo4j profile/admin contracts follow the same slice boundary. Start with T01/T02/T10/T15/T17/T20; add executable G01–G10 schema only from these contracts, exposing unsupported native/policy capabilities honestly. This is a recommendation, not code, schema or test execution in this modeling edit; the companion ledger/status updates remain with their assigned owner.

**M05 model-catalogue implementation choices.** The first catalogue slice supports `kind=model` and the declared `generation_model`/`assessment_model` roles only; other kinds remain unsupported, not scenario-specific branches. Use a strict positive signed-64-bit catalogue revision, separate from the existing inference-policy revision 1. Bound a scope to 64 retained revisions and a canonical registration body to 16 KiB; exact replay precedes capacity rejection, roles are a canonical sorted set with duplicates rejected, and whole public responses remain bounded/reject-only screened. Register explicit checked builtin or user-defined inference-policy bodies; never resolve an implicit current default on reads. Validate against the current builtin catalogue only for a genuinely new registration, after exact replay lookup. Historical reads validate the retained codec, identities and digests without consulting mutable catalogue definitions, and remain readable when defaults/catalogue membership change. Stored registration is not credential configuration, live readiness, universal role compatibility or execution authority. Settings hashing preserves the existing public payload/encoding exactly: version/model/literal endpoint/billing/inference policy, optional accounting omitted when absent, sorted compact JSON with default ASCII escaping. The pure hash helper retains legacy null-policy handling; new registrations require an explicit policy. Canonical body digest additionally binds catalogue identity/revision/kind/roles. Profile kind belongs to the immutable scoped ID anchor, not an alternate identity namespace. These are bounded implementation choices, not claims of prior user-selected numeric limits. Existing ID/hash-only workflow references remain unchanged; wiring catalogue revisions into accepted requests requires an explicit versioned contract/compatibility slice, never an invented default revision.

**M06/M15 cleanup-query implementation choice.** Add an authenticated typed per-run cleanup query without reinterpreting the legacy schema-v1 `workflow_result.cleanup` field. Return all bounded retained intent obligations, current scope-owner metadata separately, and a content-based public projection revision. Use compact exact references (attempt fence, registration ID, cleanup evidence reference/observation and matching owner retirement), validating the complete retained registration/cleanup internally; do not duplicate full process-registration bodies into every public item. States are `not_started` for a known-unclaimed reservation, `recorded` for an exactly bound retained owner cleanup report, `unknown` when claimed/registered/released work lacks it, and `not_applicable` only to the owned-worker receipt contract of an explicit legacy inline-synthetic profile. Known-unreleased does not establish cleanup; an unregistered prepared child is possible. Missing/contradictory ownership metadata never becomes inline or clean. No-intent output means no retained intents, not OS absence. Do not add one overall clean boolean or infer physical/native assurance from receipt spelling. Reuse the existing control-lock transaction for a consistent mutable-owner/obligation read; its internal revision is not a public/domain revision. No OS probe, grant resolution, cleanup mutation or current-owner fencing of historical attempts occurs. Preserve the existing intent-count ceiling, finite whole-response bounds and static corruption errors; overflow cannot truncate into a favorable result. Later GraphQL/versioned adapters expose this separately from scope-owner activity; legacy CLI migration and overall cleanup aggregation remain separate work.

**M01/M04/M06 admission-and-intent read increment.** The next shared reader first exposes request-free submission lookup and the retained frozen intent/lifecycle by run ID. Label the existing run record as legacy retained admission, never an immutable terminal command outcome; keep unavailable original command-receipt revision/cause linkage absent rather than using the current run version. Preserve the literal submission key, existing run identity and exact canonical request/contract digests. Frozen-intent reads use the retained supported contract codec, not mutable profiles, readiness or grants; unversioned profile references remain ID/settings-hash references without an invented catalogue revision. Use full-width retained version/event counters, and distinguish the public intent-view content hash from a complete operational projection revision. This is a first G01/G03 read path, not the complete M06 aggregate: budgets consumed, independent outcomes, permission-filtered actions, cleanup/owner joins, event/list cursors and versioned CLI/GraphQL delivery follow explicitly. Do not concatenate separate reads and call them atomic or change legacy schema-v1 status semantics in this increment.

**M02/M04 keyed-cancel increment.** Bind canonical `{runId}` to the scoped `CANCEL` operation key; no caller version or principal/adapter discriminator enters that identity. In one existing control-lock transaction, exact receipt replay precedes target lookup, and a new receipt commits with the existing cancellation transition and its event links. Select explicit dispositions `cancellation_requested`, `already_requested`, `already_cancelled`, or `refused`, with bounded refusal reasons `target_not_found` / `inactive_run`; missing and foreign targets share the first reason. Preserve the original receipt after later cleanup/lifecycle changes. Keep the first retained local-stop observation separate from each invocation's current delivery result. Current API read authentication plus an explicit trusted check-only cancellation-authority port must precede safe exact-target stop; stop still precedes DB/command lookup and never needs an execution grant. Do not silently equate read permission with cancel permission. Changed-key payload conflicts can therefore accompany a real local-stop observation; no OS rollback is promised. Ambiguous commit/store failure is durable unknown, not refusal; later cleanup/read failure does not erase an already known receipt. Receipt lookup performs no stop, renewal or mutation. Preserve submission keys and legacy unkeyed cancellation behavior; add typed command causation without overwriting the submission identity or actual event source. Keyed CLI/GraphQL coverage and resume receipts remain subsequent gates, not implied by existing unkeyed CLI tests.

**M03/M04 first keyed-resume increment.** Implement internal command admission/lookup and a pinned selection guard before adding the executing application handler. Support selection of an existing sole never-claimed generation reservation, an existing reserved/unclaimed scene intent, or a separate exact registered-generation reconciliation branch. Pending-without-reservation and validation-to-first-scene remain explicitly unsupported in this increment, not delegated to unkeyed `resume()`. Canonical input binds `runId`, strict full-width `expectedVersion` and strict boolean `renewAuthorization`; exact read-authorized replay precedes all mutable permission/eligibility/readiness callbacks. Fresh authorized refusals are retained. For continuation, an explicit check-only eligibility port must establish current authority/readiness or eligibility for requested renewal; no callback issues grants, prepares workers or performs effects inside admission. The store rechecks the exact version/contract/selection atomically with the immutable receipt and its cause event. Return fresh-versus-replayed admission separately from that receipt; an unknown commit cannot confer a fresh execution ticket. A claim must validate the retained selected intent and committed selection version, not choose current work again. Missing bindings with `renewAuthorization=False` block continuation; `True` permits later explicit fresh binding under the unchanged contract, never automatic binding on replay. Choose a retained `renewal_not_applicable` refusal when renewal is requested for reconciliation-only work. The later acknowledged-fresh handler must apply private authority at most once and use the existing coordinator with the pinned claim; interrupted admitted-but-unexecuted work requires a new explicit command, not effectful replay. That executing handoff, remaining branches and active/CLI/GraphQL tests remain full-M03 gates; this internal increment does not claim resume execution.

**M06/M15 joined-inspection increment.** Add one shared typed point-read, leaving legacy schema-v1 status unchanged. Join frozen intent/lifecycle, current selected scene references and criterion assessment, conservative reservations, exact per-run cleanup/current owner, retained readiness and command prerequisites in one existing cooperative control-lock transaction. Extract transaction-local helpers rather than composing separately timed public reads; perform no execution, probes, private grant lookup, artifact-byte verification, schema initialization or public callback under that lock. Use full-width run/event counters throughout, finite selected-row and whole-envelope byte limits, and reject overflow rather than truncate. Reuse the pure required-criterion projector/matcher and existing historical readers. Resolve current decision identity from the latest retained `SceneDecisionRecorded` source using a genuinely scoped indexed lookup and exact current binding validation—not opaque-ID ordering or the current run version. Missing causal history may leave identity explicitly unavailable while preserving retained payload; broken non-null record pointers remain corruption. Do not skip a latest source-less event to borrow an older identity. A repaired child cannot inherit parent evidence. Respect writer-produced stale-cohort stops and absent assessment/candidate links: raw reported passes alone cannot become an established selected assessment. Keep recorded scene assessment, accepting decision and workflow lifecycle independent; policy intent without a result stays explicitly unsupported/unretained, and publication permission alone is not a publication request/result. Actual consumption and fresh physical/artifact validity remain unknown. Historical successful readiness includes exact stored source/time, not invented TTL or current readiness. Actions expose retained applicability, supported resume branch and command-time prerequisites; read permission never grants mutation permission. An optional trusted check-only permission observation occurs after the retained snapshot and is labelled separately, defaulting to unobserved. Keep the retained snapshot revision distinct from any full response revision including permission observations; neither promises event-only invalidation. This point-read does not finish history pagination, all profile kinds/policy results, G03/G10, or GraphQL delivery.

**M05 explicit scope-binding increment.** Add a bounded immutable binding for an explicitly supplied operational authority, named database/deployment/workspace, supported binding/operational schema versions, and the existing artifact marker's `store_id`, `registry_id` and schema. Authority is not inferred from hostnames, artifact IDs or paths; real values remain operator configuration. Preserve existing run/profile identities and legacy initialization. New binding initialization is an explicit quiescent admin operation after schema provisioning: exact supported replay verifies without advancing operational counters; changed content conflicts; an existing unbound legacy control requires separate adoption/migration rather than silent stamping. Authenticate admin and reject unsafe public data before writes. Commit/read back the DB binding before attempting filesystem setup; ambiguous DB acknowledgement stops before filesystem creation. Reuse `ArtifactArea.create/open`, never a second artifact registry or SQL-backed `ResearchStore`. The narrower artifact-ID grammar applies. Artifact creation, existing-layout verification and durability uncertainty are distinct outcomes, not a cross-store atomic success. Partial/foreign/truncated/unsafe areas are preserved and rejected, not repaired. An existing area's successful open verifies current bytes/layout only, not a prior failed fsync; do not silently promote that to a durability certificate. Any durability-confirmation/reconciliation extension must be explicit. Boot verification uses bounded MATCH/SHOW-only store reads, exact binding/required-profile checks and existing-area open; no `_lock`, `get_owner`, lazy schema/scope/artifact initialization, ownership acquisition, grant or readiness probe. Report current verification, not a permanent ready flag or execution authority. This increment does not implement real deployment identities, legacy adoption, all profile kinds, GraphQL startup or full T17/T21.

**M06/M15 first paging increment.** Use compact run discovery rows followed by existing point inspection, not full inspection documents in each list. Trusted composition explicitly supplies the expected `ScopeBinding`; new page methods refuse missing configuration and compare the retained binding inside the same cooperative control-lock transaction as boundaries and rows. Run pages are live `(runId ASC)` keysets, not multi-page snapshots. Event `after=None` starts at the current retained floor; tail polling uses an event watermark returned by a run page. Preserve the first enumeration page's watermark when subsequently polling, and restart bootstrap on replay expiry. Supplied expired, future, malformed or wrong-binding/query cursors never silently become first-page requests. Select strict `first=1..1000` (default 100), canonical bounded base64url JSON cursors (maximum 4 KiB encoded), and a 2 MiB whole-page envelope. Counter positions and public full-width versions/cursors use canonical decimal strings with signed-64-bit bounds, not JavaScript Number/GraphQL Int. Cursor codec/query identity and full binding digest are explicit; event cursors carry issued floor/ceiling, while later pages validate against the current boundary and may include later committed events. Fetch one lookahead row, validate it too, and derive has-more only from that row; empty continuation pages preserve position. Cursors are neither authorization nor tamper-proof capabilities. Keep historical source/command fields exactly as retained, without invented event-time versions or typed source categories. No new retention/reset operation or stream-incarnation guarantee is introduced: destructive reset/divergent restore under the same binding is unsupported, and identity rotation/adoption needs separate explicit administration. Owner-only changes still require point polling. This increment does not complete history connections, all invalidation, G03/G10 or API delivery.

**M06/M08/M15 candidate-connection increment.** Add a fixed run-owned candidate-reference connection using the retained scope binding and one cooperative transaction for parent, boundary and selected-row validation. Compact items expose candidate ID and owning run only; they certify identity/ownership discovery, not candidate payload, digest, artifact or lineage validity. Existing candidate point reads retain those checks and their candidate-specific size allowances. Missing/foreign parents share not-found behavior; an existing run with no retained candidates returns an empty connection. Bind the exact parent run ID into a dedicated candidate cursor family and reject parent mismatch before IO; preserve first-paging limits, validated lookahead and empty continuation position. Enumerate by live `recordId ASC`, not chronology, run-version snapshots or every rejected proposal. Validate exact scoped identity uniqueness and ownership without DISTINCT or filtering away contradictory edges. Profile first and continuation queries against foreign scopes and dense unrelated runs in the same scope; add a nonunique parent-key RANGE index through explicit administration only if actual work demonstrates the need. No payload hydration merely to enumerate IDs, lazy DDL, writer denormalization or generic history framework. Join fresh direct-client run discovery, candidate enumeration, candidate point reads and event-source decision/assessment/evidence traversal in the existing isolated cohort; this does not establish fresh-process/API/native acceptance or complete the separate decision/evidence/policy connections.

**M11/M15 decision-discovery increment.** Enumerate compact run-qualified decision references across both retained producer families (`generation_reservation` and `scene_decision`), not scene DTOs under a shared label. Generation decision IDs remain general Identifiers and may repeat across runs; scene IDs and the two intent derivations remain unchanged. Add strict membership codec 1, owning run ID and producer-family metadata atomically at both decision-creation sites, including newly appended decisions on old runs; preserve payload bytes and exact replay without stamping replayed records. Only fresh run admission writes a coverage-protocol marker. Marker absence returns typed coverage-provenance-unavailable, not empty/partial success or proof of legacy chronology; malformed or unsupported non-null markers reject distinctly. Never stamp an old run because one new tagged decision was added. A valid marker records a continuing cooperative-writer obligation, not a whole-graph integrity audit: quiescent cutover is required, mixed old writers/rollback against marked runs are unsupported, and later producers must preserve or explicitly version the protocol. Indexed reads cannot detect members whose damaged keys exclude them; do not claim otherwise. Authenticate before IO, bind the parent/query/full scope in a dedicated cursor, and validate parent, coverage, boundaries, selected rows and lookahead in one transaction. Use live Identifier-order keysets and existing page bounds; do not filter by kind/codec to hide malformed selected rows. Validate run-qualified duplicate identity independently of kind and exactly one correct `HAS_DECISION` owner. Compact family metadata is a retained producer assertion, not verified payload, action, verdict or acceptance. Keep generation detail unsupported in this increment and existing scene point-read ambiguity refusal unchanged, including a generation ID colliding with another run's scene ID. Use explicit-admin nonunique parent-key indexing with actual first/continuation and selected-validation PROFILE evidence; no lazy DDL, legacy scan/backfill or arbitrary whole-history cap. This is discovery, not full G07/G10 or API acceptance; candidate-evidence producer attribution remains a separate compatibility increment.

**M05/M06 installed query-transport increment.** After the bounded query-only ASGI slice, add thin installed configuration/admin/client adapters and one detached API instance per explicit private root, reusing `create_app` and its shared service. Preserve `inspect-contract`; no Mutation root, foreground execution constructor, workflow owner, grants or startup probes. Explicit setup is filesystem-only, explicit administration provisions resources, and launch only opens/verifies. Use versioned secret-free configuration, explicit operator identity/paths, owner-private operational DB credentials and instance-specific bearer descriptors; plaintext remains readable by the owning user/root. No ambient HOME/XDG inference, request-selected factories, credential argv/environment, legacy SQLite runtime or whole-stack Start. Serialize a durable launch intent before one fixed gated spawn, bind exact process/start/namespace identity and inherited lifetime lease, and acknowledge ready only after verified app startup, listener, private auth publication and authenticated HTTP readback. Lost ACK means reconcile the same ID, not spawn again. Private same-UID, exact-peer UDS status/stop is independent of bearer expiry/DB health; stopping is not clean until owned work drains, resources close, a terminal receipt exists and exact process absence/lease release are observed. Unexpected death remains unclean; old cleanup cannot erase newer instance state. Query commands use fixed documents through real loopback HTTP with no direct-store fallback. Prove the path in a separate fixed fresh-process `workflow-graphql-network` cohort sharing one owned test container's loopback/private storage, preserving the existing no-child ASGI role and all legacy guards. Detailed selected wire/configuration contract is retained at `outputs/workflow/plan03-implementation/graphql-query-launch/SELECTED-CONTRACT.md`. This proves installed-module/fresh-process query behavior only: protected runtime mounts/container recreation, real credential/shared-service deployment, editor-rebuild T21, catalogue-bound submissions and full V0 remain separate gates.

## 8. File-level reuse and extraction map

Prefixes: `core/` = `isaaclab_arena/agentic_environment_generation/`; `examples/` = `isaaclab_arena_examples/agentic_environment_generation/`. Proposed files are explicitly marked; the map is an implementation contract, not a claim they exist. Existing modules under `workbench/` or `web_api/` may contain reusable backend capabilities; their names do not imply frontend work or a requirement to preserve dashboard interfaces.

| Existing source / entry point | Proposed ownership / change | Compatibility and acceptance gate |
| --- | --- | --- |
| `examples/foreground_workflow.py:320` (`ForegroundWorkflow`), `:44` (harness factory) | Extract orchestration wiring to proposed `core/workflow/application.py` / `facade.py`; keep separate trusted isolated/installed composition factories | Same service/controller, not rewritten state machine; old import/CLI wrappers preserved; T01/T02 |
| `examples/foreground_authorization.py`, `foreground_owner.py`, `foreground_control.py`, `foreground_cancellation.py`, `foreground_recovery.py` | Move transport-neutral authority/owner interfaces and reusable local runtime adapters to core incrementally | Keep browser authentication/FastAPI dependencies in examples; preserve lock ordering, cancellation-before-DB and exact historical receipts; T04/T09 |
| `examples/foreground_initial_generation.py`, `foreground_scene.py`, `foreground_scene_ports.py`; `web_api/generation_worker.py` | Reuse actual worker protocol, SDK allowance and model callbacks; separate adapter imports from web-only bootstrap | Native capture is a new explicitly admitted worker action, not a synthetic-profile bypass; T03/T06 |
| `core/workflow/service.py`, `coordinator.py`, `scene_loop.py`, `neo4j_store.py` | Remain authoritative application decisions/transactional state; add policy-stage intents and command receipts only where missing | Current replay and reserved-before-effect behavior preserved; no second SQLite lifecycle; T02/T04/T08 |
| `core/inference_profiles.py`, `workflow/scene_engines.py`, `inference_transport.py`; `examples/foreground_authorization.py` | Narrow operator-configured profile registry + role-specific private resolution; reuse existing model engines | Registry description never grants authority or starts effects; T03 |
| `core/workbench/model_profile_store.py:15–50`, `workbench/journal.py` and direct SQL consumers selected for reuse | Inventory each retained capability; replace its SQL persistence with Neo4j repositories, immutable IDs, transactions and conflict/replay semantics | Current ModelProfileStore creates SQL tables/triggers via Journal; do not wire it into the new backend. Migrate required profile records or explicitly initialize a new Neo4j catalogue; T13 |
| `core/graph_rag.py:387`, `workbench/research_retrieval.py`, `workflow/prior_artifacts.py` | Installed read-authorized retriever callback, exact retained receipt, no new graph schema | Existing eligibility/readback validators and optional/required semantics; T05 |
| `examples/environment_generation_runner.py:660` → `ArenaEnvGraphSpec.to_arena_env` / `ArenaEnvBuilder.make_registered` | Proposed `core/workflow/native_realization.py` typed adapter; existing runner delegates | Do not copy legacy broad critic exception fallback into managed acceptance; T06/T07 |
| `core/trajectory_capture.py:12`, `trajectory_assessment.py:48`, `workflow/scene_observation.py:368`, `scene_evidence_artifacts.py` | Retain existing helpers; add native sampler/camera/profile calibration and receipt assembly | Preserve task-driven prompts, terminal/autoreset behavior and nested cleanup; no redo of earlier trajectory fix; T06/T07 |
| `isaaclab_arena/evaluation/policy_runner.py`, `examples/web_api/evaluation_worker.py:24`, `evaluation_artifacts.py`, `evaluation_profiles.py` | Proposed `core/workflow/policy_evaluation.py` adapter and shared pure evaluation contract | Keep runner, metrics and task predicates; remove fixed legacy policy budget as a hidden managed default; T08 |
| `core/workflow/read_model.py:20`, `neo4j_store.py:2047,2061` | Extend shared outcome/action projection, bounded list/evidence queries and full projection revision | Existing events are reused, not re-created; T10 |
| `examples/web_api/application.py`, `security.py`, `workflow_authorization.py` | Extract reusable HTTP/security functions into a backend composition that constructs only the refactored application and Neo4j repositories | Do not mount GraphQL into the unchanged legacy bootstrap if that creates Journal/SQLite. No route/SSE compatibility requirement; T11/T12/T13 |

Extraction order: characterize existing callers → move one reusable seam → leave compatibility exports → run isolated parity → migrate its managed caller. Do not relocate every legacy diagnostic script or copy their implicit environment/graph defaults into core.

For SQLite-backed capabilities, the sequence is inventory domain records/invariants and direct SQL callers → define the Neo4j repository/transaction contract → implement and test against disposable Neo4j → explicitly import required historical records with identity/relationship/readback checks → remove the SQLite dependency from the new composition. An import, if needed, is an explicit one-way administrative operation with retained mapping/reconciliation evidence, not online dual writes or a runtime fallback. Avoid copying an entire legacy bootstrap merely to reuse a helper. Compatibility exports may delegate to the Neo4j implementation; they must not retain SQL execution inside the refactored backend.

## 9. GraphQL contracts over the refactored application

### 9.1 API shape and dependency choice

Use the same facade for GraphQL, direct application calls and CLI. New REST endpoints are not a prerequisite or required parallel interface. The schema models research/workflow commands and results, not autogenerated Neo4j CRUD or arbitrary Cypher. Authentication creates a secret-free `AuthContext` (principal, deployment/workspace, capabilities, deadline, correlation); never trust a mutation's claimed principal or serialize cookies/provider keys/private grants into context/logs.

Select **Strawberry + FastAPI** for the proposed GraphQL adapter, using an explicit optional API dependency group so domain-only/native tooling does not import the web stack. Strawberry is not currently declared in `pyproject.toml:36–42`; pin/test a compatible version and update the dependency/license inventory during implementation. This is a design selection, not an installation or live compatibility claim. Do not retain a framework-selection backlog or introduce another server framework. Strawberry documents that synchronous resolver functions run on its event loop, so blocking Neo4j/filesystem work must be explicitly offloaded and long workflow execution delegated to the owner.[4]

Proposed package boundary, using existing domain/engine modules rather than duplicating them:

```text
isaaclab_arena/agentic_environment_generation/
  workflow/                       existing contracts, rules, service and Neo4j store
    application.py, facade.py     extracted composition + typed command/query boundary
    admin.py                      explicit scope/artifact/profile administration
    api/                          new transport-only package; never imported by domain
      schema.py, resolvers.py     Strawberry types and thin mappings to facade
      application.py, server.py  independent FastAPI lifespan and local owner bootstrap
      client.py                  default installed CLI using GraphQL
  [existing engines]              generation, Graph-RAG, capture and policy adapters reused
```

Importing `examples.web_api.provider_security` currently executes `web_api/__init__.py:8`, which imports the legacy application/router/Journal tree. Extract the shared provider validation/readiness, grant primitives, catalogue identity helpers and runtime ownership interfaces **outside that eager package** before core or the new API imports them. Test the transitive closure, not just direct `sqlite3` statements. The new lifespan must not create legacy Sessions/ModelSettings/ResearchStore/Journal implicitly. API authentication follows §6.2–6.4's local bootstrap token/private-file boundary, while execution grants remain process-local; persisted public catalogues and reused research receipts are Neo4j repositories.

Illustrative contract sketch, **not complete executable SDL**; names/types are proposed and must be generated/tested against the shared wire contract:

```graphql
scalar Cursor
scalar Revision
scalar Digest

type Query {
  workflow(id: ID!): Workflow
  workflowSubmission(operationId: ID!): SubmissionLookup!
  workflowCommand(kind: WorkflowCommandKind!, operationId: ID!): CommandLookup!
  workflowProfiles: [WorkflowProfile!]!
  workflowProfile(id: ID!, revision: Revision!): WorkflowProfile
  workflows(first: Int!, after: Cursor): WorkflowConnection!
  workflowEvents(after: Cursor!, first: Int!): WorkflowEventPageResult!
  workflowCandidate(id: ID!): Candidate
  workflowAssessment(id: ID!): Assessment
  workflowDecision(id: ID!): Decision
  workflowEvidence(id: ID!): Evidence
  workflowPrior(id: ID!): PriorSnapshot
  workflowPolicyTrial(id: ID!): PolicyTrial
  workflowArtifact(id: ID!): ArtifactManifest
}

type Mutation {
  submitWorkflow(input: SubmitWorkflowInput!): SubmitWorkflowResult!
  cancelWorkflow(input: CancelWorkflowInput!): CancelWorkflowResult!
  resumeWorkflow(input: ResumeWorkflowInput!): ResumeWorkflowResult!
}

input SubmitWorkflowInput { operationId: ID!, request: WorkflowRequestInput! }
enum WorkflowCommandKind { SUBMIT CANCEL RESUME }
input CancelWorkflowInput { operationId: ID!, runId: ID! }
input ResumeWorkflowInput {
  operationId: ID!
  runId: ID!
  expectedVersion: Revision!
  renewAuthorization: Boolean!
}

union SubmitWorkflowResult = WorkflowAdmitted | RequestConflict | AdmissionRefused | OutcomeUnknown
union CancelWorkflowResult = CancellationObservation | RequestConflict | CommandRefused | OutcomeUnknown
union ResumeWorkflowResult = ContinuationAccepted | RequestConflict | CommandRefused | OutcomeUnknown
union WorkflowEventPageResult = WorkflowEventPage | ReplayGap

type Workflow {
  id: ID!
  operationId: ID!
  version: Revision!
  projectionRevision: Revision!
  eventCursor: Cursor!
  state: WorkflowState!
  phase: WorkflowPhase!
  result: WorkflowResult!
  availableActions: [WorkflowAction!]!
  candidates(first: Int!, after: Cursor): CandidateConnection!
  decisions(first: Int!, after: Cursor): DecisionConnection!
  policyTrials(first: Int!, after: Cursor): PolicyTrialConnection!
}
```

The sketch is an orientation aid, **not the schema deliverable**. Before calling V0 complete, implement/export a valid executable schema with every referenced type, enum, scalar, input, connection and result-union member; validate concrete operation documents for each G-row below. No undefined `WorkflowResult`, generic JSON blob for domain history or resolver-specific Cypher substitutes for the shared query contract.

| Coverage ID / capability | Required application DTO/command and GraphQL surface | Completion obligation |
| --- | --- | --- |
| G01 — admission and recovery | Typed submit/cancel/resume commands; `submitWorkflow`, `cancelWorkflow`, `resumeWorkflow`, `workflowSubmission`, `workflowCommand` | Admission distinct from terminal acceptance; every result union and scope/key conflict defined; lost ACK and current-authorized replay |
| G02 — profiles/capabilities | Public `ProfileRevision` and required-role capability view; `workflowProfiles`, `workflowProfile` | Exact immutable revision/settings digest and supported/configured/ready distinctions; registration through §6.5 admin command, not silently omitted or generic mutable CRUD |
| G03 — run/request/source | `WorkflowView`/`WorkflowResult`; `workflow`, `workflows` | Frozen request/source/profile references, phase, independent scene/policy/workflow/publication/cleanup outcomes, budget reservations and unknown actual consumption, allowed actions and revision |
| G04 — prior provenance | `PriorSnapshotView`; `workflowPrior` and run/candidate prior reference | Exact consumed context/manifest identity, retrieval settings/source and eligibility evidence; empty/unavailable/not-requested distinctions without reretrieval |
| G05 — candidate lineage | `CandidateView`; `workflowCandidate`, `Workflow.candidates` | Candidate digest/source manifest, run, parent and original references, producing attempt/decision, validation and delta; no mutable acceptance setter |
| G06 — realization/evidence | `EvidenceView`/`RealizationView`; `workflowEvidence` and candidate evidence connection | Exact capture intent, runtime/settings, seed/reset/window/frame/measurement identities, limitations and verified manifests; bounded history |
| G07 — assessment/decision causation | `AssessmentView`/`DecisionView`; `workflowAssessment`, `workflowDecision`, `Workflow.decisions` | Per-criterion verdicts, selected evidence/cohort, rubric/settings/model identity, reason and next intent; exact historic cause accessible independently of latest projection |
| G08 — artifacts | `ArtifactManifestView`; `workflowArtifact` and evidence/candidate manifests | Scoped immutable identity, digest, encoding/media type, size and authorized byte-access reference; never arbitrary filesystem paths or untrusted report HTML on API origin |
| G09 — policy evidence | `PolicyTrialView`, `EpisodeView`, task/evaluator-bound aggregation result; `workflowPolicyTrial`, `Workflow.policyTrials` | Task/embodiment/evaluator/policy/config/instruction identities, environment/episode/reset/seed linkage, typed task metrics, bounded episode pages and completed/incomplete counts; no mandatory A2 metric fields; V2 supplies real producers; V0/V1 expose honest not-requested/unsupported capability |
| G10 — events/operational state | Typed event page/source reference, readiness observation, owner/cleanup view; `workflowEvents` and workflow result fields | Scoped cursors and replay gaps; original cause/record traversal; owner-derived projection revisions; reads do not probe services or grant/dispatch work |

Minimum typed traversal: `Workflow.candidates → Candidate.parent/original/producedByDecision → Decision.selectedAssessment → Assessment.evidence → Evidence.realization/cohort/manifests`; policy trials link the exact candidate, episodes and aggregation. Field resolvers authenticate/decode/delegate/map **only**. Shared application query handlers own authorization, pagination and projection; domain handlers own transitions; repositories own Cypher. A fresh CLI/direct-call/GraphQL client must reconstruct the same failed-parent → permitted-child → fresh-pass history with no new effects.

"Full GraphQL transition" for this plan means every in-scope application operation and retained research result in G01–G10 is reachable through this boundary, and default installed clients use it. It does not mean replacing Neo4j Bolt, provider SDKs, private worker IPC, administrative provisioning or binary transfer with GraphQL. A bounded authenticated artifact-download handler may deliver bytes selected through the GraphQL artifact contract; it is not an alternative business-operation REST API. New publication/DCRG execution, browser/editor behavior and subscriptions remain explicitly deferred/unsupported, not hidden fallback routes.

`WorkflowRequestInput` maps the existing source, criteria, preservation/intervention, profile, budget and effect fields; add versioned policy coverage fields rather than accepting arbitrary JSON. `WorkflowResult` distinguishes candidate production, scene acceptance, policy outcome, workflow outcome, publication, cleanup and uncertainty. Artifact access resolves authorized immutable IDs/manifests, never arbitrary local paths. Cursor/revision scalars must preserve exact large values; GraphQL `Int` is only signed 32-bit.[29]

Submit already has exact replay semantics. Durable operation-key receipts for cancel/resume are **new work**, not supplied by adding SDL arguments. Specify scope/key/canonical payload digest, retained disposition and conflict behavior. A stale expected version must not block emergency owner-local stop; report cancellation delivery independently. Unknown mutation responses trigger lookup of the same operation, never a new key and fresh effects. `renewAuthorization` requests renewal under shared rules; it does not itself authorize anything. Ordinary GraphQL partial data/errors cannot be interpreted as a completed mutation.[23] Serial root mutation execution within one request does not serialize separate clients or provide transaction/effect idempotency.[29]

#### Command recovery across adapters

Add shared `command_status(auth, kind, operation_id)` and expose it through the proposed GraphQL `workflowCommand` and CLI `command-status --kind … --operation-id …`. Namespace is `(operational database/store authority, deployment, workspace, command kind, operation ID)`, never adapter name or a caller-selected principal. For `SUBMIT`, this wraps the existing scoped submission identity unchanged; no rekeying of old runs. Kind is explicit for new cancel/resume receipts, so the same literal ID in a different kind cannot alias the wrong receipt. Current read authorization always applies.

Freeze canonical command payloads: submit → accepted canonical request; cancel → exact run ID; resume → exact run ID, expected run version and renewal-request flag. Retain payload digest, target, disposition and uncertainty with the command version; same scoped kind/key + changed payload conflicts. Queries return `not_found`, a retained receipt, or explicit store-unavailable/unknown, without dispatch or fresh credential resolution. The lookup result is the retained command disposition, while a separate status query observes newer run state.

Propose `--operation-id` for installed-profile cancel/resume, `--expected-version` for installed-profile resume, and the read-only `command-status` command. These flags/command do **not** exist in the current parser; retain legacy invocation behavior separately. Direct application calls, CLI and GraphQL must share payload normalization and receipt lookup. Verify lost acknowledgements and cross-adapter replay with zero new released effects/grant renewal. Emergency owner-local stop remains deliverable before DB/receipt access; if persistence fails, report local delivery and durable outcome unknown separately. Command idempotency cannot promise exactly-once remote effects.

### 9.2 Existing events, revisions and pagination gaps

- `Neo4jWorkflowStore._event` (`neo4j_store.py:362`) already appends scoped events atomically with transitions. `snapshot` and `events_after` (`:2047`, `:2061`) already use a shared control-lock boundary; event pages enforce 1–1000 entries and reject cursors outside floor/ceiling. Do not propose rebuilding a missing event store.
- Current public events expose sequence, run ID, operation ID, kind and schema version, not a rich payload/aggregate revision/source linkage. Define a backend workflow-event DTO with **invalidation semantics followed by authorized projection queries**; extend sanitized envelopes only for explicit domain needs. Keep scope-wide event cursors when querying an individual run; a later status read is not the historical state of an older event. Test duplicate/multi-run events, query/event races and replay gaps at the application/API boundary. No frontend reconciler or legacy JobEvent conversion is part of this work.
- Snapshot currently returns all scoped runs; bounded result loading is not pagination. Add bounded keyset run/evidence/candidate connections with scoped opaque cursors. Choose explicit live-list consistency initially; invalidate/refetch on changes rather than claiming unimplemented historical snapshot reconstruction. GraphQL guidance supports opaque cursor connections, but that alone does not create storage snapshot semantics.[22]
- Run version and projection revision differ. `retire_owner` (`:1987`) changes owner-derived fields without a run event. Add owner-dependent result invalidation/revision before promising stable ETags. Also separate **retained run/intent cleanup** from **current scope-owner activity/retirement**: `result_records` currently supplies the latest scope owner (`neo4j_store.py:1175,1872–1877`), and `read_model.py:162–166` derives cleanup from its dirty flag. A long-lived owner can stay active after one run's workers are clean; a replacement owner must not regress the predecessor run's cleanup. Reuse exact attempt/registration/cleanup receipts and historical owner epochs/retirement records rather than replacing the ownership subsystem. Missing or unresolved run cleanup remains unknown; neither a new owner nor an unrelated clean scope flag establishes it. T10/T15 cover clean run A while its owner remains active and after owner B replaces it, with B's current activity separately visible. `_lock` increments an internal control revision even on these reads; do not expose it blindly as domain revision.
- Define the event authority/stream namespace from the Neo4j-backed application; SQLite SSE is not the target contract and requires no compatibility adapter here. Floor checking is not proof of a retention/pruning implementation. Design pruning and cursor validation/page materialization together before enabling retention.
- Begin with bounded authenticated polling/event pages. Subscriptions are deferred: GraphQL does not prescribe their transport, and periodic polling is a documented alternative for less frequent updates.[1] A later transport must retain replay gaps, authentication renewal and bounded queues; it must not own execution lifetime.

### 9.3 Durable query semantics and API security

Every backend entity/query result binds `(authority namespace, deployment, workspace, entity ID)` and the complete result revision. A fresh authenticated client/process can reconstruct selected run/candidate/evidence and command dispositions from retained state. Query/profile/event reads never resolve execution grants, start services or dispatch work. `availableActions` is computed from retained state and current permission, not a promise that a later command cannot lose a race. Client caching, browser refresh/navigation, editor drafts and the historical disappearance report are deferred dashboard concerns, not acceptance criteria here.

Define API authentication independently of the dashboard; reuse suitable existing security code without importing UI lifecycle assumptions. For cookie-authenticated HTTP, enforce exact Origin and CSRF mutation checks. Deny GET mutations, scope nested resolvers and artifact fetches, cap body/depth/complexity/aliases/batching/list sizes, sanitize errors and rate-limit expensive reads. Consider trusted operation documents; GraphQL security guidance describes such allowlists.[25] No GraphQL Docker/shell/host-PID operations. Test authorized positive paths as well as denial cases through backend clients, without requiring a browser.

## 10. Acceptance matrix and evidence ladder

Tests below are obligations for implementation, **not results of this planning session**. Use the repository's admitted isolated harness for package execution and disposable Neo4j, without weakening ordinary F0 guards. Native/live/shared services require separate explicit authorization. Record exact source/profile/contract digests, proof artifacts, cleanup and limits for each gate; do not sum overlapping test cohorts into end-to-end coverage.

| ID | Positive path | Required counterexamples / retained evidence | Gate |
| --- | --- | --- | --- |
| T01 | Pure contracts, profile catalogue and facade import without runtime effects; wrappers delegate to the refactored application | Deny native/SDK/web imports and credential discovery at pure boundary; no dynamic request-selected factories; no Journal/SQLite dependency in refactored application paths | M0 isolated |
| T02 | Fresh submission → handle → real generation entrypoint → scene decisions; exact replay before mutable config/grants | Same key changed contract conflicts; removed profile/expired execution grant does not break authenticated replay; no duplicate admission/dispatch | M0/M1 isolated |
| T03 | Actual SDK serialization for selected generation and assessment roles, shared allowance before construction | Every required unavailable dependency gives zero calls incl. ping; role mismatch, revoke/expiry, retry/image failure, unknown pricing and direct-HTTP fallback; static secret-free diagnostics | M1 isolated, then bounded provider approval |
| T04 | Reserve/register/release/adopt/cleanup and exact restart recovery | Lost commit/release ACK, cancel during callback, DB unavailable cancel-client, dead owner, missing/tampered receipt; unknown released work never redispatches; original budget/deadline unchanged | M1/M2 isolated and intended-process topology |
| T05 | Existing retriever returns eligible provenance and exact context consumed once | Empty versus unavailable; required prior blocks; wrong database, malformed/mutable evaluation, duplicate identity, timeout/cleanup; read-only credential denial of writes and exact property/relationship readback (node count alone is insufficient) | M1 isolated DB; separately approved prior read |
| T06 | Native builder yields wrapper, literal runtime subject mapping and aligned state/RGB evidence under one owned lease; frozen camera activation reaches Kit and graph conversion for omitted/default or explicit-true graph settings | Explicit-false camera conflict, missing required key/modality, templated prim path, wrong tensor/force filter/frame, falling/sliding object, nonfinite sample, terminal/autoreset, startup/close failure, warm legacy renderer contention; no silent candidate rewrite | M2 isolated adapter tests then native calibration |
| T07 | Actual refiner receives the bounded repair envelope and corrects a supported visibility-only failure with physical criteria already passing; fresh native cohort establishes acceptance | Missing permissions in model input; no-effect initial_pose edit; original cumulative radius exceeded; Z/task/physics/topology mutation; unsupported physical failure; mixed-reset complementary passes; exhausted budget retains nonaccepted outcome | M2 isolated and approved native scene loop |
| T08 | Policy runner evaluates the selected task using frozen evaluator/criteria, policy/embodiment identities and trial coverage; A2 exercises its own lift/dwell/destination rules as one example | Wrong task/evaluator/policy binding, server replacement/wrong modalities, no completed episodes, unmet declared prerequisites, incomplete criteria, policy budget exhaustion; crash/restart at prerequisite→policy handoff; no implicit publication; failure cannot leave overall accepted | M3 isolated then approved profile-specific live pilot |
| T09 | Separate process reaches exact owner's stop channel and verified cleanup; API response/disconnect does not own work | Wrong UID/scope/namespace, unreachable owner, DB outage, expired in-memory grants, restarted owner; no arbitrary PID kill/takeover | M2/M4 deployment topology |
| T10 | Bounded status/list/events reconstruct selection and per-run cleanup with complete projection revision; cleaned run remains clean while its owner is active and after a replacement owner starts | Wrong-scope/floor/ceiling cursors, owner retirement without run transition, unresolved run cleanup masked by unrelated scope state, overflow, concurrent mutation/pruning; every read leaves domain execution unchanged | M4 isolated DB |
| T11 | Direct application/CLI/GraphQL share normalized contracts, command dispositions and stable IDs across fresh clients/processes | Duplicate/lost submit/cancel/resume response recovered by scoped kind/key lookup; cross-adapter replay and changed-payload conflict; event/query revision races; observer never dispatches or renews | M4/M5 backend integration tests |
| T12 | GraphQL queries/mutations implement the shared application contract without dashboard dependencies | Applicable CSRF/origin/GET mutation checks, nested cross-scope artifact, alias/batch bypass, body/complexity limits, large revision, blocking resolver, partial error/unknown mutation, secret sentinels | M5 isolated API tests |
| T13 | Neo4j-only backend boots and exercises profiles, managed eligible-prior retrieval, commands/events and GraphQL with no legacy DB present | Transitive eager-package/Journal import denial at pure boundary plus runtime SQLite connection denial; migrate selected publication/reservation/readback metadata; all application records verified in Neo4j; outage has no fallback/dual writes | M0 and every reused capability; M4/M5 full backend gate |
| T14 | Separately fenced native capture→retained observation→assessment→repair→fresh capture; delayed within-budget capture still permits successful assessment | One registration/cleanup per substage; GPU lease released only after physical cleanup; no pre-prepared expiring assessor; nonzero absolute windows/displacement witnesses; original settings after defaults change; crash between adopted capture and assessment recovers without rerender | V0 isolated topology, V1 native |
| T15 | DDD invariant/causation contracts in §7: one run decision/reservation wins; exact historical assessment/parent/original and run-bound cleanup remain traversable across owner epochs | Independent candidate/policy CRUD cannot bypass run version/budget; latest read cannot impersonate two historical decisions or replace historical cleanup with current scope-owner activity; scene pass survives policy failure but overall acceptance does not | M0/M4 real disposable Neo4j |
| T16 | Executable Strawberry schema and operation documents cover every G01–G10 row; network CLI and fresh API client use the same application | Undefined types/generic JSON history, resolver Cypher/decision logic, unauthenticated/spoofed principal, hidden direct-CLI fallback, request-close stops owner, new release after restart without reapproval | V0 isolated API; V2 adds policy producers |
| T17 | Explicit admin initialization/profile registration from empty disposable Neo4j, then fresh API boot/profile query/submit with SQLite denied | Same binding replay/changed-content conflict; incomplete cross-store setup remains explicit; missing scope/schema/artifact/profile fails without lazy writes; no credentials in public profile/API records | M0/M1 isolated bootstrap |
| T18 | One approved GraphQL-submitted V1 run produces actual provider/native receipts and a queryable terminal result; V2 adds required policy through the same boundary | Separate synthetic GraphQL and native CLI proofs cannot satisfy this gate; fresh-client causal history/artifact readback, lost-response recovery and cancel are checked on the same admitted run | Separately authorized V1/V2 live acceptance |
| T19 | Same application/Neo4j/GraphQL contracts handle at least two distinct task semantics through registered adapters, e.g. pick-and-place and OpenDoorTask's openness criterion (`tasks/open_door_task.py:43–51`) | A second task must not require banana/plate/lift fields, DROID cameras/actions or a GR00T service unless its own profile declares them; test another embodiment/action-interface binding in isolation; no scenario-name branches or schema fork; unsupported adapters are explicit | M0/M3 isolated generality contract gate; broader native support separately verified |
| T20 | Every G01–G10 operation traces to completed original-question/DDD records, retained sources and Given/When/Then cases before schema implementation | Schema inferred only from local DTOs/Neo4j nodes; admission confused with completion; generic pending/queued without authoritative blocker semantics; reconnect/restart loses identity or dispatches work; unresolved ownership hidden in JSON fields | M0/Phase 5 design gate plus application/schema contract tests |
| T21 | Synthetic setup from the root editor uses the mapped operator and host-persistent directory; simulator-local launcher/second client work without manual pipes; detached owner survives client/editor disconnect, host credentials survive editor rebuild, and controlled restart loads atomically updated values | Unsafe paths/ownership/modes, swallowed initializer failure, missing runtime groups/HOME, root-owned pipe/regular-file FD/FD 0/missing EOF, stale token/inode, conflicting sources, interrupted/concurrent update, accidental overwrite, raw secrets in output/logs/Neo4j/GraphQL/argv; setup makes zero provider/DB/native calls; missing keys never fall back; removal is not provider revocation | M1 isolated bootstrap and approved disposable-container topology using sentinels only; no real secrets, current-container rebuild or production-security claim |

Live proof ladder: (a) explicitly initialized GraphQL API/profile/status with no workload; (b) authorized bounded provider/prior checks; (c) embodiment-correct hold/native calibration without policy inference; (d) the complete V1 GraphQL-submitted generation/scene loop and fresh-client readback; (e) V2 policy pilot through that same API. Isolated GraphQL contract tests run earlier without live effects. Each is a separate receipt/claim. A valid rejection completes execution honestly but does not satisfy the positive scene/manipulation acceptance gate. Always report implemented, isolated-tested, live-verified and deployed separately.

## 11. Decisions to freeze before release and deferred scope

| Decision | Default proposal / owner | Release condition |
| --- | --- | --- |
| Provider/model pair and accounting | Operator selects one literal generation/assessment pair; trusted adapter verifies supported policy | Capabilities, route/fallback policy, positive token and conservative cost bounds frozen; no guessed current prices |
| Credential bootstrap | Explicit owner-private local file outside Git; launcher handles creation/update and private runtime handoff; optional pipe/named-env sources | Single-operator research setup, not production secret management; safe file lifecycle, no dotenv/argv fallback, synthetic leakage tests and controlled restart/reapproval |
| Prior source and empty behavior | Explicit read-only profile; optionality selected in contract | Authorized database, eligibility callback and empty/unavailable semantics fixed before first inference |
| Native producer | One-environment calibrated scene profile | Resolved prim/contact/camera/coordinate mapping, effective XY placement and thresholds demonstrated |
| Reference policy pilot | A2 is one example profile, with its verified N1.6 DROID-compatible service and two distinct declared simulator seeds | Example-specific evidence only; common task/policy/evaluator contracts must also pass T19; broader Physical AI support is not inferred from this pilot |
| Installed owner topology | Existing local host/container boundaries, one owner per scope | Same lease/socket/artifact resources and cancellation verified; no inferred cross-host support |
| GraphQL dependency | Strawberry + existing FastAPI, separate optional API dependencies | Pin compatible versions, review licenses and pass schema/application/security tests; no dashboard or REST migration prerequisite |
| Durable profiles | Explicit immutable public profile definitions with Neo4j-backed application catalogue/revisions | No Journal-backed ModelProfileStore or read-through SQLite adapter; retained settings and identity/conflict checks verified in Neo4j |

Deferred: dashboard/frontend work, browser parity and legacy dashboard-route/SSE migration; generic service orchestration/distributed scheduling, automatic research publication, live implementation/verification of additional policy/embodiment adapters (such as OpenVLA/G1/GR1), policy-diagnosis-driven DCRG/VLM intervention, GraphQL subscriptions and robustness claims. SQLite removal from the refactored application's dependencies is **not deferred**; historical data transfer requires explicit administration, not compatibility at runtime. Preserve existing DCRG engines/research history; deferral is not a claim those experiments never existed. Native calibration failures may require a versioned producer or constrained initial scope, not silent weakening of the research question.

## Sources

[1] https://graphql.org/learn/subscriptions

[4] https://strawberry.rocks/docs/integrations/fastapi

[8] https://openrouter.ai/docs/guides/overview/models

[10] https://neo4j.com/docs/python-manual/current/transactions

[14] https://docs.isaacsim.omniverse.nvidia.com/6.0.0/py/source/extensions/isaacsim.simulation_app/docs/index.html

[16] https://graphql.org/learn/authorization

[18] https://openrouter.ai/docs/guides/routing/provider-selection

[22] https://graphql.org/learn/pagination

[23] https://graphql.org/learn/response

[25] https://graphql.org/learn/security

[26] https://github.com/openai/openai-python

[29] https://spec.graphql.org/September2025

[32] https://eventstorming.com/resources

[33] https://eventmodeling.org/posts/what-is-event-modeling

[34] https://learn.microsoft.com/en-us/azure/architecture/microservices/model/tactical-domain-driven-design
