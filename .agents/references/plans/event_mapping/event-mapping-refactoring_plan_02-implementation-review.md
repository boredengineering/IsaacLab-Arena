# Plan 02 implementation review — bounded milestone gate

## Baseline and scope

Independent read-only review `deleg_c5b778aa` examined the 55 Python source fingerprints in `outputs/workflow/milestone-review-scope.json`, at HEAD `8e6842190ad4fe3af095b8a26c261e8bd1f7f4bb`. This is distinct from the historical design review. Static flags were closed fixture credentials and fixed-source test compilation, not demonstrated leaks or request-controlled execution.

The parent independently reran the joined isolated cohort (10 cases), core cohorts (465) and real store cohort (138). These passing paths do not discharge interruption or executable-interface obligations. Native physical validity, live inference, shared/research services, dashboard and legacy migration remain outside authorization.

## Initial adjudication (historical)

All five findings were accepted from decisive source inspection. Their original counterexamples and verification obligations are preserved below. The original verdict was **not passed**; the final correction verdict at the end of this ledger supersedes it.

| ID | Accepted defect/gap | Smallest required correction | Verification obligation |
| --- | --- | --- | --- |
| CLI-01 | Default CLI requires an already initialized test interpreter; even fresh `status` is not available. Shared bootstrap is not wired; default prior is always unavailable. | A guarded documented fresh-process isolated entrypoint, retaining exact topology/source/effect denial. Wire shared readiness, distinguish retained-prior input from explicit fallback. Keep production/native composition explicitly unimplemented rather than remove safety checks. | Separate interpreter run and subsequent status without injected application factory or inherited Python singleton; zero SDK requests on required dependency failure. |
| RESUME-01 | Same-application resume reissues `bind_run` for an existing binding and blocks after successful generation recovery. | Reuse exact valid authority; explicit renewal only when necessary; never reset original deadlines or repeat generation. | Interrupt after generation adoption/retirement before scene creation, restore readiness, continue in same and fresh applications; expired/renewed authority controls. |
| RESUME-02 | Known-unreleased pending generation/scene work is treated like unknown released work; scene feedback caches cannot reopen retained observations. | Bounded continuation from exact retained intents and actual artifact bytes, with conservative ownership recovery. Unknown released work remains blocked unless exact outcome is reconciled. | Interrupt before claim, after release and after durable completion; same/fresh interpreter, no repeated completed inference, no automatic uncertain resend. |
| CANCEL-01 | A separate CLI cannot deliver owner-local stop without Neo4j; externally requested scene cancellation can leave a cleaned owner dirty. | Authenticated scoped local stop IPC owned by the existing foreground process, not another service stack or arbitrary-PID kill. Distinguish stop delivery from durable cancellation and have the owner finish cleanup/retirement. | Distinct run/cancel processes with active child; healthy DB/outage/completion race/unreachable owner; stop before DB access, no later release, exact retirement and subsequent ownership. |
| RESULT-01 | No flushed handle until the expensive run completes; status omits evidence/budget/publication/experiment detail and outcome exit mapping. | Admission notification before release, one sanitized shared read model and explicit outcome/exit contract. Keep reserved upper bounds distinct from actual consumption. | Pause at admission and observe handle before SDK activity; recover identity through status; schema/secret/exit tests for accepted/stopped/blocked/cancelled/failed. |

## Preserved counterarguments and limits

- The positive workflow is real application orchestration over actual model entrypoints, SDK serialization, owned processes and disposable Neo4j. HTTP responses and capture are explicitly synthetic.
- Normal scene completion checks physical cleanup, commits cleanup before stage completion and retains the same physical lease across stages. The accepted gaps concern interrupted/cancelled paths.
- Unknown released effects must not be retried. RESUME-02 does not weaken that rule; it distinguishes known-unreleased work.
- No demonstrated cumulative-budget reset or managed direct-HTTP fallback escape was found.
- Core execution CLI importing the examples adapter conflicts with plan I12; correct the boundary while completing CLI-01, without redesigning the domain service.
- Retained-prior consumption exists, but automatic research-database retrieval remains unauthorized and unverified. Do not claim live Graph-RAG retrieval from a synthetic fallback.

## Correction evidence so far

- CANCEL-01 transport component: `foreground_control.py` adds scoped SO_PEERCRED owner-local stop and `GenerationCoordinator.stop_local` separates local fencing/cleanup from database cancellation. Parent read back 16 passing cases and all four matching source hashes in `outputs/workflow/arena-owner-stop-f0d1e52dcdaa47b9b551ee36c0111bcc`; distinct interpreters stop an actual sleeping child. This is not full workflow cancellation or retirement proof. The new standalone test launcher used unnecessary full Docker inspection and incomplete ambiguous-create handling; it is being corrected to reuse existing confined/owned harness helpers before another real run. Its historical pass is not relabelled as corrected-launcher evidence.
- Launcher correction is now parent-verified: seven stdlib fake-Docker regressions pass, followed by the corrected real 16-case UDS run `outputs/workflow/arena-owner-stop-1bf94bdb4a274f6094872ef64baec8f0`. It reuses metadata-only F0 discovery, confined capture, pre-start isolation and exact-ID/label cleanup; unresolved create ACKs remain non-authoritative in this wrapper. Parent checked zero failures/errors/skips/forbidden events, matching current source hashes, no unresolved ACKs and an empty owned-resource listing. Shared helper behavior outside this wrapper was not changed or newly certified.
- CLI-01/RESULT-01 fresh-entry increment: `python3 scripts/run-workflow-neo4j-checks.py workflow-cli` produced `arena-neo4j-3d2e2ff1f3ff43cfae900584a7703584`. Parent read back the passing one-case composite proof, ten fresh CLI interpreter records, admission-before-SDK evidence, four model children, six required-profile mismatch negatives with no workers, and terminal status/cancel/resume replay against the same disposable database. Captured output includes criteria/manifests, evidence relationships and conservative budget categories. This is not active cancellation or interrupted resume. Shared bootstrap binding, final read-model semantics and root recovery fixes remain open; model-profile mismatch errors currently use exit 5 rather than the other not-ready cases' exit 3. Native/live/production composition remains deferred.
- Resume increment: parent read back `arena-neo4j-fdde4dc129cb48d1a5ef05ca22b403df` (11 cases) and `arena-neo4j-7c0ca925d501457985c99bc234f96faf` (139 store cases), with zero failures/errors/skips and verified cleanup. This fixes same-application binding reuse, released recovery-handle reuse and continuation/restoration under the original held scene lease. Positive fresh-interpreter continuation, pending generation, explicit renewal and integrated cancellation remain separate obligations; see `outputs/workflow/resume-application-handoff.md` for witnessed REDs.
- Parent RESULT-01 regressions: repeated observations of one candidate incorrectly merged current/history (`arena-f0-backend-036d96785c33`), and mismatched producer claims incorrectly displayed passes (`arena-f0-backend-aa63cce1ff0d`). Projection now selects authoritative evidence identity and reuses the pure criterion matcher. Seven isolated cases and scoped lint pass in `arena-f0-backend-df0b00b0da62`; store selection linkage is being integrated by the resume owner. This is retained-metadata matching, not fresh file/physics verification.
- Parent bootstrap bridge: `ObservationOnlyPort` feeds pinned, metadata-only observations through `ScopedHostBootstrap`, never grants startup capability, and rejects mismatched report bindings. RED `arena-f0-backend-b5bf911bc028`; 17-case GREEN `arena-f0-backend-b3d05d645dde` and scoped lint pass. Default-factory wiring remains pending; no installed service was started.
- CANCEL-01 application helper: `foreground_cancellation.py` adds the sticky run latch, local-stop-before-DB dispatch, separate delivery/durable outcomes and supervisor-only cancellation finalization. Parent read back 27 passing application/transport cases in `outputs/workflow/arena-owner-stop-0c6c83e95b5244b081542bdccd60402e`, with zero failures/errors/skips/forbidden events and verified cleanup. Helper/transport/test source hashes match; coordinator has since changed in the active resume work, so this is revision-specific evidence. Root binding and an active fresh-CLI/real-Neo4j cancellation proof remain required.
- Root/CLI cancellation integration and pending-work recovery remain in progress. No entire review finding is closed by these component increments alone.

## Focused correction recheck 1

Independent `deleg_e0295b4d` checked current code against the original five counterexamples and matching retained proof sources. CLI-01, RESUME-01 and CANCEL-01 are **closed for the guarded isolated scope**. RESUME-02 and RESULT-01 are partially closed, with only these confirmed residuals:

- **B1 / RESUME-02:** a real dispatch interruption before preparation can retain a never-prepared physical lease (same application), or a begun but unclaimed dirty owner (fresh process). Seeded pending records do not reproduce these boundaries. Fix and verify actual run-path failures before reservation and after begin-owner/before-claim, without loosening claimed/uncertain ownership refusal.
- **B2 / RESULT-01:** required generation/assessment profile mismatch returned generic application failure/exit 5. Parent added SDK-free `ModelProfileUnavailable(ValueError)` only at the specific approval/settings mismatch guards, preserving legacy exception compatibility. CLI maps that type to static `model_profile_not_ready`/exit 3; unrelated `ValueError` remains exit 5. RED `arena-f0-backend-49f230eda8f5` → grant GREEN `arena-f0-backend-faba8aabffc6`; CLI RED `arena-f0-backend-d02b121d4341` → GREEN `arena-f0-backend-bfce99c4be9f` and scoped lint. Exact fresh-process assertions remain part of the final correction run.

Closure evidence includes `arena-neo4j-233f07663f79447089a2948192b03167` (28 fresh CLI processes, 20 SDK workers), `arena-neo4j-239614d83cb742db8c3fc16ce87af539` (11 application cases), and 28 UDS/application cases in `outputs/workflow/arena-owner-stop-c1b5e336b3c04edc8727387424f749b5`. Active cancellation covers generation and scene workers, delivery before cancel-client DB access, controlled client `StoreUnavailable`, bounded reconciliation and owner retirement. It does not claim an actual DB-server outage or native/live execution.

Parent background rerun `arena-neo4j-ffc87f10090e463ca39d084f5235602a` completed successfully after the earlier transport-limited interruption: one composite CLI case, no failures/errors/skips, all forbidden counters zero, empty remaining-owned resources and verified container/network cleanup. Its evidence confirms admission before SDK activity, acceptance, owner retirement, retained uncertainty and cancellation/handoff assertions. This is the **pre-B1/B2 captured revision**; several implementation/test files changed during the run. It does not close either residual or substitute for their final source-bound proof.

## B1/B2 final correction evidence

Parent verified `arena-neo4j-58c39b49bd3b4ea9a1e79554b0bae724`: 31 distinct fresh CLI processes and 28 SDK-worker evidence records, zero failures/errors/skips/forbidden counters, verified empty container/network cleanup, and matching captured Python source at handoff. Real `run` failures witnessed both B1 REDs (`96bd9a81fa8c461fa7bf307c1b4f7652`, `bcac35ac57ae4823abbd68dece08d9a8`); successful continuation preserves original admission/reservation and zero pre-interruption model calls. B2 now asserts the exact static frame and exit 3 for both model roles, no admission and no SDK child. `outputs/workflow/resume02-b1.diff` and `resume02-b1-witness.json` retain the narrow change and actual witnesses.

Parent reran the scene cohort on the final harness (`arena-neo4j-3c602a82680e48cb95166879c7e293b9`: 11 passing cases, verified cleanup), 13 host runner units and scoped application/CLI hooks. The license hook normalized only the CLI integration test's copyright header; its executable AST matches the captured passing CLI source. A final parent CLI rerun is running to capture those final bytes too. The expanded CLI cohort now uses a **780-second outer collector**; ordinary modes, worker deadlines and isolation remain unchanged. Final focused recheck 2/2 is pending; no broader review is being opened.

## Final focused recheck 2/2

Independent `deleg_52a9b06d` closes **B1 / RESUME-02 and B2 / RESULT-01** for the guarded isolated scope. CLI-01, RESUME-01 and CANCEL-01 remain closed; no direct regression or confirmed residual blocker was found. Parent reread the decisive dispatch and exclusive unprepared-owner recovery paths and accepts the verdict.

- B1: actual run-bound failures and assertions are in `scripts/workflow_process_harness.py:627–676` and `test_environment_workflow_cli_process_neo4j.py:129–159`; the narrow root correction is at `foreground_workflow.py:545–562,818–835`. Same-root continuation fences the old coordinator before exact never-prepared lease release. Fresh continuation adopts only through the held exclusive lease, complete durable obligation inspection and repeated readback. Original admission/reservation/owner identity are preserved.
- B2: `inference_profiles.ModelProfileUnavailable` remains a `ValueError` subtype; only its specific frozen-profile refusal maps to static exit 3. Fresh role-negative assertions and CLI units distinguish unexpected `ValueError`/exit 5. Both roles fail before admission/model effects.
- The current CLI proof verifies released uncertainty remains blocked with zero new child/SDK effects. Additional claimed/ambiguous exclusions were source-traced, not advertised as a newly executed exhaustive rejection matrix.
- Drift qualification: after the first passing correction capture, the parent license hook changed only the test copyright header and verified identical executable AST. The final parent byte-exact rerun passed; no production-code changes followed the correction proof.

## Correction budget and closure

The original bounded review and both allowed finding-focused rechecks are complete. All five findings are closed for the guarded isolated scope; no further review loop is planned. **Final parent proof `arena-neo4j-6fe820f218de4774a8cb46c049125986` passed:** one composite case exercising 31 distinct CLI processes and 28 verified SDK workers, zero failures/errors/skips/forbidden effects, no Python source-hash mismatches, empty remaining-owned resources and verified container/network cleanup. Acceptance, owner retirement and uncertain-effect refusal are retained. The isolated implementation milestone is closed. Native/GPU physical correctness, real provider quality, live Graph-RAG, installed-host startup, general production deployment, actual DB-server outage, dashboard and legacy migration remain explicitly outside this closure. Uncertain released effects and unprovable ownership remain fail-closed.
