# Test Tools

These are rapid prototyping and testing scripts designed to evaluate specific functionalities of the GR00T policies and models. Rather than full CI `pytest` suites, these scripts are used to perform localized, standalone smoke-testing on Hugging Face processor instantiation (`test_processor.py`), action decoding logic (`test_decode.py`), and tensor shapes (`test_shape.py`).
