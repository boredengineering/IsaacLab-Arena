# Architectural Session Checkpoint: Host Mounts Assurance & G1 Closed-Loop Eval Playbook

- **Date / Timestamp**: 2026-08-27 03:46:53 UTC
- **Session UUID**: `g1eval`
- **Topic**: Host Mount Persistence Assurance, Checkpoint Audit, and G1 Loco-Manipulation Eval Playbook
- **Status**: Completed / Active

---

## 1. Context & Architecture Invariants
- **Host Persistence Alignment**: Verified that `/datasets`, `/models`, and `/eval` directories mount directly from the host (`$HOME/{datasets,models,eval}`) into the DevContainer, simulation container (`isaaclab_arena`), and policy daemon containers.
- **Pre-Trained Policy Weights**: Audited that `GN1x-Tuned-Arena-G1-Loco-Manipulation` (revision `gn1_6` / `checkpoint-20000`) and the locomanipulation dataset files (~48GB models, ~23GB datasets) are present on the host and mapped into `/models` and `/datasets`.
- **Closed-Loop Evaluation Contract**: The canonical Unitree G1 locomanipulation benchmark (`galileo_g1_locomanip_pick_and_place`) uses `g1_wbc_joint` for closed-loop execution and connects to the GR00T policy server daemon at `tcp://127.0.0.1:5556` with embodiment `NEW_EMBODIMENT`.

---

## 2. Deliverables & Implementations

### A. Host Directory Assurance Script (`.devcontainer/ensure_host_directories.sh`)
- Created idempotent script executed on the host via DevContainer `initializeCommand` or CLI.
- Automatically validates and provisions required directories with host user permissions before Docker daemon mounts:
  - `$HOME/datasets`, `$HOME/models`, `$HOME/eval`
  - Subpaths for all official tutorials (`locomanipulation_tutorial`, `sequential_static_manipulation_tutorial`, `static_apple_tutorial`, `dexsuite_lift`, `reinforcement_learning`, `camera_sensitivity`)
  - Tooling and credential configs (`.aws`, `.config/gcloud`, `.azure`, `.config/gh`, `.config/osmo`, `.cloudxr`, `.nvidia-omniverse`, `.cache/huggingface`)

### B. DevContainer Persistence Configuration (`.devcontainer/devcontainer.json`)
- Hooked `initializeCommand` to `bash .devcontainer/ensure_host_directories.sh`.
- Added persistent Hugging Face cache mount (`${localEnv:HOME}/.cache/huggingface:/root/.cache/huggingface:rw`) and `HF_HOME="/root/.cache/huggingface"`.
- Updated container lifecycle script (`.devcontainer/init_agent_workspace.sh`) Step 16 to verify mounted volume availability upon container boot.

### C. Server CLI & Compose Enhancements (`docker/run_gr00t_server.sh` & `docker-compose.sim.yml`)
- Added `-c <modality_config_path>` option to `docker/run_gr00t_server.sh` to allow specifying external modality configurations (e.g. `isaaclab_arena_gr00t/embodiments/g1/g1_sim_wbc_data_config.py`).
- Added volume mounts for `/workspaces/isaaclab_arena` and `/models` inside the GR00T server container.
- Updated `docker/docker-compose.sim.yml` with matching workspace and `/models` mounts.

### D. Canonical Evaluation Playbook Documentation (`.agents/references/docs/setup_workflow.md`)
- Added **Section 9: End-to-End Evaluation: Unitree G1 Loco-Manipulation Box Pick & Place** detailing:
  - Checkpoint verification (`checkpoint-20000`)
  - GR00T policy server launch (containerized CLI or native `uv`)
  - Single-env interactive GUI policy runner (`--viz kit`, `--num_steps 5000`)
  - Parallel multi-env single GPU (`--num_envs 5`) and multi-GPU distributed evaluation
  - Invariant explanations (`g1_wbc_joint` vs `g1_wbc_pink`, port safety on `5556`).
