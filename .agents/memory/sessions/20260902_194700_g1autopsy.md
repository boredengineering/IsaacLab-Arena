# Architectural Session Checkpoint: G1 Humanoid VLM Failure Autopsy (v4–v6)

- **Date / Timestamp**: 2026-09-02 19:47:00 UTC
- **Session UUID**: `g1autopsy`
- **Topic**: Headless-first VLM autopsy workflow, 50-D action contract discovery, reference-scene control experiment
- **Status**: Completed / Active

---

## 1. The Workflow Invariant

> Run headless at full throughput; render keyframes and escalate to a VLM only when a progress milestone stalls.

Tooling built:
- `isaaclab_arena_examples/tools/render_env_camera.py` — headless onboard RGB capture after settling.
- `isaaclab_arena_examples/tools/render_policy_trajectory.py` — multi-step rollout, keyframe sampling, structured-JSON VLM autopsy via OpenRouter.
- `isaaclab_arena_examples/tools/render_perspective_view.py` — third-person/side cameras plus shoulder→object Euclidean reach checks.

## 2. The Embodiment Action-Space Contract

- `nvidia/GN1x-Tuned-Arena-G1-Static-PickNPlace` emits **50-D joint actions** (43 body/hand joints + 7 base commands).
- Correct embodiment is **`g1_wbc_agile_joint`**. `g1_wbc_agile_pink` fails twice over: `ValueError: Invalid action shape, expected: 23, received: 50` and Pinocchio Pink IK's `assert self.num_envs == 1`.
- Mandatory config: `initial_joint_pos = G1_STATIC_OPEN_ARM_JOINT_POS`, finger friction `static=6.0 / dynamic=5.0`, `action_chunk_length = 40` (checkpoint-native).

## 3. The Control Experiment That Localized the Fault

Evaluating the reference scene `galileo_g1_static_pick_and_place` on the *same* server (`127.0.0.1:5557`) returned `success_rate: 1.0, object_moved_rate: 1.0`. The server, the joint reordering, and the 50-D pipeline are all healthy — so v4–v6 failures are domain divergence, not execution bugs.

## 4. Iteration Outcomes

- `v4`: apple front_right, plate front_left → left arm reached into empty air. Lift 0/8.
- `v5`: laterality corrected (apple front_left) but objects 58 cm out — outside the dexterous envelope.
- `v6`: reach verified kinematically (0.385 m shoulder→apple, 59.2% extension) and *still* 0% lift. Wrist telemetry shows the arm reaching to step ~30, then plunging toward Z ≈ 0.69 m (trained shelf height), hitting the table plane, and retreating.

---

## 5. Authoritative References

- `.agents/references/quick_notes/g1_humanoid_vlm_agentic_debugging_and_remediation.md`
- `.agents/references/quick_notes/session_memory.md` (section 18)
