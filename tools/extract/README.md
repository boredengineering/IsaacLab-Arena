# Extract Tools

These scripts are used to unpack, extract, and convert raw data formats into readable outputs or specific isolated assets. They are heavily utilized for parsing binary arrays in `.npz` files (`extract_npz.py`), extracting specific edits or configurations (`extract_edit.py`), and parsing raw JSON/text responses generated by logs or APIs (`extract_response.py`).
