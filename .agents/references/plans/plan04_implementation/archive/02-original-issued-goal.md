# P04-I02 — Original issued goal (historical)

This is the original monetary/token-capped goal, retained for the prerequisite-stop
audit. It is not the revised prompt and grants no new allocation. Read the
[current package](../02-installed-visual-assessment.md#revised-goal-prompt) before
continuing; editing a prompt does not resume execution.

The original intake/critic/parent reports remain unchanged in
`outputs/workflow/plan04-implementation/milestone1/installed-native-20260925T012042Z/p04-i02/`.

```text
/goal Complete P04-I02 in .agents/references/plans/plan04_implementation/02-installed-visual-assessment.md using a bounded actor-critic development loop. Make the installed authenticated application assess a5's exact retained native images, retain a truthful structured visual result, recover it from a fresh client and replay without another provider request. Progress means real application acceptance, not more plans, tests, inspection packets or review passes.

AUTHORITY AND ROLES

This issued goal authorizes narrow Arena assessment/admission/orchestration changes, existing simulation-free checks, private setup through the approved application handover path, authenticated readback and scoped operational writes. Only after Gate A passes, it authorizes at most ONE Arena provider request in total for the assessment role using gpt-6-astra. It authorizes ZERO native launches, Kit starts, steps, resets, new captures, generation/repair calls, policy execution or new prior retrieval. Preserve the three original and two additional consumed native launches and the earlier consumed generation allowance. Do not reset allocations on restart, handoff or a new operation ID.

You are the ACTOR and sole writer/operator. Use one independent read-only CRITIC at a time in a fresh context through delegate_task(tasks=[{goal, context, output_schema}]). Give it the goal, effect limits, acceptance criteria, scoped diff and sanitized evidence. The critic may inspect public source/evidence but may not edit, run tests/services/constructors, submit operations, access credentials/private client files, call the workflow provider, write the database or grant authority. Do not create another agent framework, executor, ledger or reporting platform. Development-agent inference may use unchanged Hermes configuration and is separate from the Arena one-request budget; never read/export Hermes model credentials into the application.

FIXED SOURCE AND PROVENANCE

Use source operation p04-i01-native-a5, run fcc052f890be2d208783f04453d184f7d2455afe948aa8607e92a5a87d79ea48. Evidence root: outputs/workflow/plan04-implementation/milestone1/installed-native-20260925T012042Z/p04-i01/. Read LIVE_RESULT.txt, parent-closeout.json, critic-final.json, a5-http-readback.json and a5-http-evidence_json.json before implementation.

Preserve the original candidate file outputs/workflow/plan04-implementation/milestone1/realize-20260924T232748Z/candidate.json, SHA-256 8dcd08b813236ee0ccdcad1024594a70301d4a6fdcd0af41d013e05f5135a7e8; its canonical digest is 2dc9fa1eb2ec06c872366dc10bf709cb412b435b85e1417bc788854091051b53. Preserve evidence artifact SHA-256 3647e6b04a5d8be843d9aed008a705402ae4d49494161b23aa86b6efc438ea94, its exact original candidate/cohort binding and the three PNG identities in section 2. Use both subjects, red_block and blue_bin, and all three retained cameras at step 180. They are reused a5 evidence, not newly captured images.

Create a separate assessment consumer operation with explicit links to those unchanged producer records. Freeze its new rubric, model profile, budget and operation identity separately. Do not rewrite a5's contract/profile/receipt, pretend its numeric-only request contained visual criteria, fabricate a generation receipt or relabel old frames as a fresh cohort. Preserve candidate, assets, physics, seeds, thresholds, measurements, source manifest, prior failures and the documented shared-cache causal caveat.

BOUNDS AND PRIVATE SETUP

After Gate A, permit at most one provider send, with no SDK retries, constructor pings, fallback requests, model repair or automatic second assessment. Max total reserved model tokens: 32768, including input/images/overhead and output; max output tokens: 2048, including reasoning where applicable; hard workflow cost ceiling: USD 0.10. Provider timeout at most 180 seconds, cleanup reserve 10 seconds, owned-worker runtime allowance at most 190 seconds and total operation deadline at most 300 seconds. One owned assessment worker at a time. A failed or uncertain send consumes the only send opportunity; never retry it to obtain a pass.

Prove the actual final serialized request fits those ceilings using the existing request envelope and conservative accounting with explicit supported live pricing. PricingBasis currently only admits synthetic_fixture: introduce only the necessary explicit live declaration and preserve synthetic guards; never call a synthetic fixture, invented rates or billing=free live cost proof. If supported pricing or the complete image/request envelope does not fit the caps, stop before sending and report the computed requirement or missing evidence for an operator decision. No silent limit, endpoint or model changes.

Use only an operator-approved assessment-role private binding and the previously approved operational Neo4j binding. The native-only setup had no model role; do not assume a usable key or permission exists. Use the supported private setup/configuration handover, retaining old identities, store and accounting. If credentials/approval are missing, stop for private installation; never ask for a key in chat, inspect Hermes secrets, change provider configuration or treat credential installation as execution authority. Limit writes to the new assessment and required application-owned records under milestone1_live_20260924t232748z_r2. No prior-eligibility publication or mutation of existing native results/flags.

EXECUTION LOOP AND GATE A

1. Recheck git state, exact input bytes, source provenance, retained ownership and remaining authority. Read actual definitions/callers before edits. Inspect only the selected assessment boundary; no broad simulator metadata/dependency audit. Keep native execution unchanged. Reuse the existing coordinator, execution owner, profile registry, private role delivery, grants, BoundedSceneModels, RequestEnvelope, owned-worker boundary, artifact readers and durable reservations.
2. Identify the earliest missing or failing installed boundary, one falsifiable causal hypothesis, the smallest correction, its predicted observation and the exact existing check or installed command. A model-free native mode and a synthetic execution mode are not live VLM modes: add the smallest explicit assessment-only selection if necessary without weakening either guard or building a parallel runner.
3. Establish source-to-consumer provenance, exact subject mapping and the existing per-frame visibility request/result path. Freeze an honest rubric for both subjects in all three frames before the call. Visibility is not metric 3D grounding, support, convergence or policy success. Do not infer identity or visibility solely from a PNG's subject tags or coerce uncertain/malformed answers into a pass. No XY repair in this package.
4. Apply only supported in-scope corrections. Run affected existing checks; no test/mock/fixture additions or edits and no standalone acceptance harness. Host lint stays on the host. Arena package checks run in the verified existing checkout container, discovered through .agents/skills/dev-container/SKILL.md, as ubuntu UID 1000 using /isaac-sim/python.sh. All checks must be simulation-free; a Kit start is forbidden, not an exempt test. Do not recreate containers or reconfigure shared services.
5. Gate A must demonstrate the real installed admission/retained-read/serialization path with provider sends denied, preserved producer bindings and hashes, zero native authority, supported request/price bounds, send-time authorization, bounded cleanup and passing affected existing checks. Setup must not spend the single provider call. Freeze the new source, operation, profile, rubric and selected request before critique and keep them frozen through live execution, readback, replay and cleanup.
6. Obtain a cited CRITIC response at Gate A, after a materially changed correction to an unresolved blocker and after the live outcome. Require valid JSON through per-task output_schema with decision (CONTINUE, NEED_EVIDENCE, BLOCKED or ACCEPT), evidence_refs, earliest_failed_boundary, hypothesis, prediction, next_action, next_check, unmet_criteria and scope_or_budget_risks. Each finding must cite source or artifact fields. Allow at most one format-only correction, including any tool-managed schema correction; otherwise stop. Critic CONTINUE grants no authority; ACCEPT requires complete scoped runtime evidence. Verify its claims yourself and resolve disagreement with the smallest discriminating observation, not more reviewers.
7. Once Gate A, critique, private setup and issued authority all hold, submit the single assessment through the installed authenticated CLI/GraphQL path. The application must own release, the one model call, validation, retention and cleanup. Do not call BoundedSceneModels or a provider helper directly as acceptance, and do not manually chain internal stages.
8. Retain exact source-linked request and raw response, validated criterion-level result, limitations, usage/reservations and sanitized causal failures. Treat model output as data, never new execution authority. Recover request/response/assessment and referenced input bytes through a fresh authenticated client and compare hashes. Replay the same completed operation and verify identical retained outcome/receipt, no additional provider send or worker release, unchanged durable budgets and unchanged prior records. Distinguish aggregate owner/permission revisions from immutable record changes.
9. Verify exact owned-process cleanup, durable retirement and API drain. Keep initiating errors separate from cleanup errors and no-live-worker evidence separate from vanished zombie entries. Preserve shared services. Obtain final critique; parent independently verifies the complete evidence and alone closes or blocks P04-I02.

ACCEPTANCE, STOP CONDITIONS AND CLOSEOUT

Require an actual complete source-bound structured assessment, fresh authenticated byte recovery, unchanged replay, bounded accounting and owned cleanup. A valid negative or inconclusive visual verdict can satisfy software integration but is not scene acceptance; transport failure, malformed output or incomplete frame coverage cannot. Do not promote native_settled, converged, verified or prior eligibility, and do not claim calibration, full Milestone 1, the full scene workflow or Plan 04 completion.

Continue routine supported corrections within scope without asking after every patch. Stop after three unsuccessful provider-free correction cycles for one blocker, missing required authority/pricing/grounding, unresolved ownership, unusable critique, no supported next correction, any required native work or an out-of-scope remedy. After the one send, only non-sending recovery of already retained results is allowed; no allocation reset, provider retry or fabricated answer. Finish required bounded cleanup and report the exact blocker and operator decision needed.

Do not change simulator/submodule code, docker/, .github/workflows/, .pre-commit-config.yaml, tests/fixtures, shared caches/services or unrelated work. Do not commit, push, stash or reset. Keep a compact source/operation/input/criterion/budget/result/evidence/cleanup record in the existing evidence hierarchy; do not create a new platform. Update this work package, its index, Plan 04 and the canonical handoff with separate integration, visual, readback/replay and cleanup outcomes. A plan, test count or review pass is not completion.
```
