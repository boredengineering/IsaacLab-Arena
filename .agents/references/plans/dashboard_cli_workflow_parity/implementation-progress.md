# Implementation progress

Status: implementation in progress; not full parity. User authorized the complete agreed code implementation with independent critique and progress updates. The plan's phase safety gates remain binding. Live model spend, research-store/schema migration or writes, simulator/evaluation acceptance and protected infrastructure edits require their explicit budgets/destinations/permissions before execution.

## Current priority: prototype harness functionality, not release hardening

### Proposed next prerequisite: research stack validation and startup

The latest user clarification requests **plan review before implementing this new scope**. The [research-stack readiness appendix](research-stack-readiness.md), canonical-plan entry and endpoint companion now specify an API-independent stack panel, paired host helper, approved existing-container starts, explicit paused API recovery, worker-context Neo4j checks and correct GR00T protocol/model verification. Protected Docker changes, service starts, frontend rollout and any GR00T wrapper rollout remain unapproved. No implementation or runtime result is claimed for this addition.

Source discovery found partial readiness already present in `web_api/readiness.py` / `workflow-readiness.tsx`: metadata, authenticated database read and TCP checks only. The native GR00T server has no loaded-checkpoint identity RPC, so model verification requires a first-party serving metadata hook or remains explicitly unverified. The launcher depends on running Arena, and the CLI does not expose the application's existing paused-start option. These concrete gaps—not another research-engine rewrite—govern the proposal. Independent protocol/lifecycle discovery and a separate plan review are complete. The parent addressed the review's cold-bootstrap IPC ownership, mandatory codec-evidence and explicit API coverage-ledger requirements, plus its first usable vertical-checkpoint recommendation. Scoped documentation hooks and local-link checks passed before amendments; final checks cover the amended proposal. This is design review with parent corrections, not a separately rerun closure verdict or implementation acceptance. Existing implementation/evidence entries below retain their historical checkpoint meaning and do not establish current full-stack readiness.

### A2 model compatibility and profile-tool checkpoint

The user configured a session-scoped OpenAI key and explicitly authorized resuming the sole queued A2 New request, prompt `Grasp the yellow banana from the right side of the table and set it onto the white ceramic plate on the left.` Job `80b514d6c9774f76bc0a5a1b345b600e` first blocked before release because its 180-second execution grant had expired; the user reauthorized it in the credential-owning browser. It subsequently became indeterminate with `provider_request_rejected` at `agent_initializing`, released with no accepted candidate. Preserve that job; no automatic replay or no-spend claim follows from the diagnostic. The queue was resumed by explicit approval, not by startup.

Source/documentation diagnosis found unsupported Astra temperature parameters and a separate open-dictionary strict-scene-schema incompatibility. The shared backend now consumes an SDK-free profile catalogue for exact OpenAI GPT-4.1/Astra pairs. Original endpoint literals and actual SDK endpoints both govern selection; SDK normalization cannot promote excluded :443/uppercase spellings. Parent verified 39 SDK transport tests (`arena-f0-backend-c4a15acb7dd8`) and 164 profile/settings/protection tests (`arena-f0-backend-b825203b27fc`), source hashes and cleanup; endpoint closure `deleg_58090141` passed. The later raw-parser change also passed the 39-test suite (`arena-f0-backend-d421e9b07bc7`). All are offline synthetic-provider evidence, not live acceptance. Temporary-key PUT remains memory-only; candidate public metadata is screened before replacement.

The profile selector/parser passed 278 focused frontend tests (`arena-functional-frontend-903a4455fd18`), ordinary typecheck/build (`arena-f0-typecheck-3e7f0599a968`, `arena-f0-build-8f591624ed5b`) and scoped review. It distinguishes configured credentials, documented request policy and not-yet-run live verification. Unknown models remain custom/unverified. The backend catalogue is not loaded into the running API until an explicit restart, which would clear session keys. The paid structured-output test action is not implemented; do not show a successful or executable placeholder.

The strict SpecInference adapter keeps the full scene structure and represents only freeform parameter maps as closed key/value_json entry arrays; decoding preserves nested domain parameters before Arena validation. An optional raw parser now rejects duplicate keys/fences/nonfinite or malformed JSON before legacy tolerant extraction loses information. Infer/repair share the explicit wire format and legacy profiles retain existing behavior. Parent reproduced eight integration failures, then verified all 52 spec/adapter cases (`arena-f0-backend-901bb8dfc15c`) with current implementation hashes, zero forbidden counters, cleanup and targeted pre-commit. Final schema review `deleg_9f6c5de7` passed. Parent verified the four current generation/profile modules match the live runtime mount, API/jobs return 200, the queue is unpaused and no jobs or workers are active. One explicit new A2 test is ready for the credential-owning browser; no successful provider result is claimed yet. New workers import mounted source without restarting the credential-owning API. No additional live provider call, successful A2 generation, graph retrieval/publication, render or policy rollout is claimed by these tests.

### Resumption: runtime containment and scoped export completion

Current resumption checkpoint: supported `recover` and machine-sized resource limits are integrated. Independent review found and closed a shutdown regression: `ps`/`stop` bypass capacity discovery with interpolation-only values, while config/build/up remain fresh/fail-closed; explicit stop and startup rollback attempt API stop in finally. Parent verified 17 host lifecycle tests, eight resource tests, forwarding policy, 14 real non-root socket tests and targeted pre-commit. Closure review `deleg_a529c481` passed. The supported recovery command returned `recovered:false` for the already-cleared socket.

Parent inspected only queue metadata from a private main+WAL copy under lifecycle leases and a main-file exclusive lock; source bytes remained unchanged. There were no queued/running jobs or uncleaned workers and clean_shutdown was zero. The normal launcher subsequently started API and frontend successfully at `http://127.0.0.1:3010/?layout=v7`; readback shows owned healthy API, healthy frontend and enforced frontend 4 GiB/256-task cap. The real browser loaded the source document, showed schema-valid YAML, authored graph controls and a live shared stream; authenticated workspace GET returned 200 with the same terminal job counts. No generation/render/build/evaluation job was submitted and the shared queue was not resumed. Provider and Graph-RAG readiness remain unconfigured; live model/Neo4j/GPU and flattened attachment download acceptance remain open.

Export independent review `deleg_a1028351` passed. The graph browser now honors explicit NEO4J_DATABASE while preserving driver-default selection when absent; parent corrected an unintended hard-coded default via RED `arena-f0-backend-2e8bf785b932` then GREEN `arena-f0-backend-2e2a9e4163ee` (37 tests, current source hashes, zero forbidden counters and cleanup verified). The earlier default-image dependency failure is retained at `arena-f0-backend-84e80243a820`; the successful run reused the existing verified provisioned image, not a new install. The test runner admits only this additional explicit graph test file; default selections remain unchanged.

The user authorized safe stale-socket recovery and persistent memory/PID limits after the VS Code OOM/forwarding incident. Parent recovered the existing Arena container without recreating it and cleared its connection-refused, owned API socket under control/supervisor/state/socket leases; journal and queued work were untouched. Docker and kernel readback verify editor 8 GiB/512 tasks, Arena 58,886,307,840 bytes/2048 tasks, and Docker readback verifies stopped frontend 4 GiB/256 tasks, with memory-swap equal to memory. These caps fit the measured 98,143,846,400-byte host with headroom; they are not proof that the original spawning cause is eliminated. Persistent configuration and a supported recovery command remain in implementation. No dashboard restart or live model/graph/GPU acceptance is claimed.

The bounded C09/F3 durable flattened-export control is implemented. Parent parsed the five final logs (391 focused tests), matched changed source hashes to successful TypeScript/build proofs, and independently verified absence of their test containers. Evidence: `web/arena-workbench/tests/e2e/functional-v7/.runs/export-flattened-yaml-3b68c0b9b484.md`. Independent review is pending; attachment bytes have not been exercised through a live browser/backend. The configured Neo4j database-selection correction remains pending after the original isolated runner rejected its unlisted test file; a single explicit-only test allowlist extension is authorized, preserving default suite selection and isolation. These are items within the accepted V7 plan, not a reduced finish line or new architecture.

### Active feature: policy evaluation through the existing harness

Implementation `deleg_7ec553af` is in source across the core `policy_runner`/telemetry hooks, existing web job/GPU-lease adapter and local artifacts, and mounted production TanStack controls/results/downloads. Parent integration corrected the backend's unapproved rejection of zero completed episodes: a completed budget may return an empty episode file, `episode_count=0`, `success_count=null`, and an explicit no-evidence warning. Positive counts contradicted by empty bytes still fail. Six intended RED failures were retained in `arena-f0-backend-e796dd7c9b9a`; the intermediate combined run retained one stale zero-episode test expectation in `arena-f0-backend-ced96fa5dd87`, corrected without changing the agreed contract.

Current parent checkpoint: 125 backend tests passed across Evaluate, core harness, Build and Build environment at `arena-f0-backend-36d7d501ed41`; 389 current source hashes/eight artifacts/zero forbidden counters and cleanup verified. Frontend owner checkpoint has 229 tests across eight files and TypeScript/Vite build (`arena-functional-frontend-a6631b743599`, `arena-f0-build-64e546c235ad`); parent verified matching bytes and reran 78 Build/Evaluate tests at `arena-functional-frontend-f11124fddb98`. Counts overlap and are not additive. Scoped review `deleg_8ac0fdd6`: core harness/local-only telemetry PASS; API and frontend each found a concrete defect. Artifact escape decoding can miss a long credential spanning chunks, and idle download controls can become inert after reactivation. Targeted RED→GREEN repairs `deleg_795ffc31` are implemented: whole bounded artifact escape/unfold screening precedes chunk parsing, and committed download lifetime is explicitly published to React state. Parent verified 137 combined backend tests at `arena-f0-backend-43ce7b6c792f`, 214 frontend tests across Evaluate/Build/editor/App/jobs at `arena-functional-frontend-bc14a9a50328`, and source-matching TypeScript/Vite build `arena-f0-build-f3a9ad7eef1c`; source/artifact hashes and exact container cleanup checked. Final narrow closure review `deleg_a16a2512` passed with no findings. Evaluate source implementation, focused integration tests and scoped independent review are complete; live GPU/policy-server execution and real RDF serialization are not established. The next prototype validation should exercise the existing Build/Evaluate flow rather than add release-hardening prerequisites. The separately repaired N0 contract remains intact. Evidence index: `.runs/prototype-evaluation-integration.json`. No actual GPU/policy-server/Neo4j execution occurred; the approved test image lacks `rdflib`, so RDF serialization was explicitly mocked, not verified Turtle output.

The prototype selects fixed `gr00t-droid` or `openpi-droid` profiles from the README guides. Implemented wire contract: `GET /api/editor/evaluation-profiles`, `POST /api/editor/evaluate` with the current draft, optional frozen document ID, profile, optional language instruction and retained request ID, and exact-job artifact downloads. Server-owned settings are one headless environment, enabled cameras, 1000 policy iterations, no videos/distribution/variations, and a 900-second worker deadline. Settling consumes additional simulation steps; the iteration limit is not an episode count or task-success claim. Policy servers remain operator-started at the guide's loopback endpoints.

Reuse `policy_runner.main()` rather than zero-action Build or a parallel evaluator. A trusted Python callback captures actual metrics/output paths after rollout and report finalization but before process-terminating Kit shutdown. Web evaluation explicitly disables the CLI's automatic Neo4j telemetry sync and version-tree lineage mutation; local TTL/report/episode records remain outputs, while ordinary CLI defaults stay unchanged. Serve only exact verified allowlisted artifacts as attachments. No completed episodes means no completed-episode evidence, not automatic success or failure; unknown recorded success remains unknown. Existing job observation, cancellation, source identity, secret screening and GPU exclusion stay in use. No actual policy/GPU/graph execution is authorized or claimed by these code changes.

The user's latest instruction supersedes deployment-first prioritization. Implement the README's existing harness workflows in TanStack to validate the concept; preserve existing security/storage behavior without expanding that workstream. URL/proxy/authentication/deep-link and additional recovery acceptance are not prerequisites for feature work. The next implementation is the missing browser-to-harness execution connection, alongside closing concrete generation/save/publication UI gaps. Reuse existing runners and job observation; no new execution framework or shell-command console.

Build implementation (`deleg_90c8999f`) is now in source: `POST /api/editor/build`, the existing supervisor's `build` dispatch, an owned worker calling the existing `environment_generation_runner.main()` build mode, and the real EditorView action plus JobInspector result. The request carries current YAML, optional frozen document ID and idempotency key; the fixed profile is headless, one environment, 20 zero-action steps, no generation/publication/video. Existing job observation/cancellation is reused. A successful completion receipt must match the frozen input/canonical hashes and profile; it is not policy-task success.

Parent verified current source and artifact hashes for 172 backend tests (28 Build cases) in `arena-f0-backend-ef0e9c1bc3ed`, 209 frontend tests in `arena-functional-frontend-6c7ee22b42ac`, and TypeScript/Vite build `arena-f0-build-0148e15118ea`. Parent reran the 28 Build backend tests (`arena-f0-backend-608d886ce707`) and 31 focused frontend tests (`arena-functional-frontend-63b72cdcb765`); all passed, with owned-container absence checked. These are overlapping checkpoints, not additive totals. Record: `.runs/prototype-build-integration.json`. Independent review `deleg_1a843e56`: frontend/wire PASS; backend identified a completion handoff bug because Kit shutdown may terminate the process before a post-context receipt. The targeted repair (`deleg_67685fe8`) adds a trusted after-rollout/before-shutdown hook while retaining parent exit/receipt/cleanup checks; it does not redesign SimulationApp. No GPU execution was performed. Parent also reproduced and fixed the build environment projection: preserve the launcher's EXP_PATH and explicit CUDA/NVIDIA visibility restrictions without provider/graph credentials or streaming overrides. Six focused environment tests are GREEN at `arena-f0-backend-2740b31501d0`, following RED `arena-f0-backend-02074e9443ba`; The shutdown fix now passes 44 focused backend tests at `arena-f0-backend-96a8be57f3f8`. Parent then ran 188 tests across Build/environment/editor-revision/public-job suites at `arena-f0-backend-050471eaf17e`, with 375 current source hashes, eight artifacts, zero forbidden counters and exact container cleanup verified. The trusted hook reports rollout completion before process-terminating shutdown; successful exit, exact receipt and cleanup remain mandatory for job success. Final narrow closure review `deleg_e0dc923f` passed with no blockers: the completion hook, unchanged default CLI behavior, strict receipt/exit/cleanup gate and simulator environment projection are confirmed. Build source implementation, focused tests and scoped review are complete; actual GPU execution remains unperformed. Next functionality is the existing GR00T/OpenPI policy-evaluation harness adapter, with bounded source discovery active in `deleg_8aa57ee7`.

Discovery correction (`deleg_3e2c9a6d`, parent source inspection): production New/Refine, exact Graph-RAG context, generation review/Apply, editor/research saving and `ResearchVersions → PublicationPanel → PublicationControls` are already mounted. Do not rebuild those controls. Manual-version publication and retrospective preparation are separate unsupported contracts. The previously missing execution link was Build: the CLI already runs its `SimulationAppContext` and `build_env_and_run_policy`; the new adapter adds it to the existing web dispatcher. Build is a zero-action rollout, not zero simulation steps and not GR00T/OpenPI evaluation.

The user accepted the V7 design and then authorized the reviewed plan's implementation, parallel agents and independent code review. The [V7 functionalization plan](v7-functionalization.md) refines U3 into production integrations over existing controllers/services. Code implementation is proceeding; provider/graph/GPU effects, migrations and protected infrastructure still require their separate gates. Existing P1/P2 code and all archived previews are preserved.

### Current delivery status and active work

The user renewed the request to implement all necessary remaining features after the blockers were explained. Feature work is proceeding in parallel; no newly dispatched component or contract is counted as complete before integration, execution evidence and review. This progress document records implementation/evidence/remaining work; the [canonical plan](../dashboard_cli_workflow_parity.md) and [F0–F8 sequence](v7-functionalization.md) govern scope and dependencies.

| Slice | Implementation / owner | Verified checkpoint | Remaining exit work |
|---|---|---|---|
| F0 | Explicit stopped-container discovery passed independent safety review; real authoring profile works | Parent ran `arena-f0-933680638528` through the strict authoring checker and verified records/artifact hashes/cleanup | Reverify later source changes; dependencies remain trusted mutable input, not immutable provenance |
| F1 | Scoped mobile sizing fix now verified in Chromium | 1440px and 390px root widths match their viewports in both themes; parent inspected screenshots | Deployment and remaining whole-dashboard/runtime acceptance |
| F2 | Bounded table XYZ proposal/review/Apply exercised through real API/browser | Exact candidate and distinct fresh validation, consent retirement and actual download bytes passed | Unsupported poses remain unavailable; schema success is not physics acceptance |
| F3 revisions | Implicit-Generate error mapping and health capability passed bounded independent closure | Parent reran 52 API tests GREEN, verified result/cleanup; 79 core and process-death checkpoints remain scoped evidence | Manual research-source bridge and browser acceptance; no live data migration/power-loss claim |
| F3 recovery | Read/write admission separation passed bounded independent closure | Owner reports 296 tests plus typecheck/build; genuine browser Save→GET and explicit Open passed | Expanded restart/failure browser cases remain separate from happy-path acceptance |
| F3 Library | Catalogue retirement and receipt-authorized Open passed bounded independent closure | Actual browser verified one keyed save, no auto-open, exact Library reference, explicit Open and preserved root/hash/origin after reload | Research-version source integration and later-source revalidation |
| F3 research bridge | Focused UI-storage slice implemented and scoped verification complete | 1591 production frontend tests, typecheck/build, reviewed real manual-research browser journey and approved nine native cases; exact cleanup verified | Broader F3/CLI/backup/manual-preparation work and F4–F8 remain separate; not deployed |
| F4–F7 | Replacement-query retirement passed bounded closure; concurrent UI type error resolved | Parent verified 97 authority / 244 focused tests and fresh integrated build `arena-f0-build-ddef51f954e9` | Backend provenance/transport/eligibility and fresh browser/render acceptance; no full F4 claim |
| F8 | Parent integration / handoff | No verified production V7 handoff yet | Production build, same-origin browser journey, truthful capability report and legacy fallback verification |

Shared-response review `deleg_d75e4a77` confirmed the main HTTP/SSE protections but found unexpected screening exceptions after acceptance/cancellation fell through to generic 500, and public protect_yaml had changed its successful return from None. Parent reproduced six post-effect failures (RuntimeError/ValueError/TypeError × submission/cancellation) in `arena-f0-backend-df5dd4673291`, then broadened only the post-effect screening catches to Exception, preserving the existing explicit static 503 dispositions and leaving BaseException/cancellation propagation untouched. The full public-job file passed 66 tests in `arena-f0-backend-7a51997e370a`; parent checked JUnit/no failures/skips and cleanup. Bounded closure `deleg_1e577fe7` passed with no findings: only post-effect screening is caught, effects are not retried or rolled back, static dispositions are accurate, and cancellation control flow remains uncaught. Explicit recovery HTTP-200 and CancelledError assertions were suggested as optional coverage. The metadata/helper owner was separately steered to restore public-wrapper None semantics and add compatibility coverage; integrated security closure remains pending.

Shared HTTP/SSE fix `deleg_123ac57e` passed 60 focused tests (`arena-f0-backend-f948ffbf6dd5`) and a separate 78-test editor regression (`arena-f0-backend-af48559bf6a5`). Parent checked JUnit/no failures/skips, cleanup and the stable jobs/events/test files against staged bytes. Protected reads/replays return static 422. Authorized cancellation still executes; an unsafe response returns explicit static 503 saying it was handled but may remain pending, not a fabricated Job or terminal acknowledgement. Per-event SSE checks cover prefetched pages; a protected event emits unnumbered resync_required and terminates without advancing the client cursor. Session, heartbeat/resync, disconnect and stream-capacity cleanup have scoped tests. Review is `deleg_d75e4a77`; the shared helper is being refactored separately for settings/default capture, so integrated revalidation remains required.

The shared-response owner's four stale teardown assertions were already updated in the later generation-security branch: protected HTTP reads expect 422 and inspect unchanged Journal state internally. Parent confirmed that current fixture shape. The combined six-file timeout remains a verification limit, not a full-suite pass or a reason to weaken protection. No live disclosure/worker/provider/browser acceptance is claimed.

Resolved-generation admission fix `deleg_996a3707` passed 147 isolated tests in `arena-f0-backend-6431938ce945`; parent verified JUnit/no failures/errors/skips, cleanup and all four current implementation/test files against staged bytes. Complete assembled public submissions are screened before Journal.submit; all public model/retrieval profiles are resolved and screened before the first grant. Private credential configurations are excluded from public screening. Actual RED traces preceded the fix; clean recovery still avoids grants, view resolution and wake. Bounded review `deleg_1658e95e` passed with no security or logic findings for the frozen generation fix and matched its source/proof/JUnit hashes. It confirmed the rejection tests exercise real issue/submit ordering rather than masking a commit with rollback. Settings/default-capture work remains separate, and its stricter admission requires rerunning the generation regression fixtures before claiming integrated closure.

The settings/default-capture follow-up `deleg_6e7a93c9` implemented prospective metadata screening against current and incoming keys before replacement/invalidation, protected settings projections, and non-bypassable decoded profile checks before default capture/renewal grants. Optional guards receive detached public copies; private configs are excluded. The shared decoder is request-independent and public protect_yaml again returns None. Parent verified 269 distinct test cases, not an overlapping sum: 56 metadata (`arena-f0-backend-121485ae3c9a`), 78 editor (`arena-f0-backend-d85bb74dc0e3`), and 69 manual research plus 66 public-job (`arena-f0-backend-3cea03eb858f`). All had zero failures/skips and verified cleanup; the three production modules matched tested source. Review `deleg_4ad32470` found no new fault in the scoped metadata/default-capture changes and confirmed wrapper compatibility and private/public separation. Its report explicitly retains the known renewal-route response concern; the metadata verdict is not whole-API security approval. That remaining response boundary stays with `deleg_7bb17d61`. The oversized combined batch remains a timeout record; smaller complete batches supplied the evidence.

Renewal-route protection `deleg_7bb17d61` is implemented: accepted POST replay, disposition GET and concurrent accepted winners are screened without rewriting acceptance; fresh renewal screens frozen job, profiles and renewal record before durable CAS. Only fresh winners wake; losing grants roll back. Policy failures bypass rejection sealing, unexpected screening failures are static 503, and fresh accepted-response failure explicitly preserves acceptance. Its 37 renewal + 66 public-job tests passed at `arena-f0-backend-e1615375b8bc`.

The earlier combined `arena-f0-backend-f9e3895a9f30` recorded 155 passes/one failure: a legacy recovery fixture prohibited every configuration read, including current-policy scanning. Parent replaced that fixture with absent server configuration plus explicit forbidden original-credential/workflow/grant resolution, capture and wake checks. It now also requires actual current-policy execution, exact 202/snapshot recovery and unchanged full Journal dumps for replay/conflict. Spies are scoped before application shutdown. No production privacy guard or immutable acceptance assertion was relaxed. Final parent run `arena-f0-backend-b411cb7c29da` passed 159 distinct tests (37 renewal, 56 metadata, 66 public jobs), zero failures/errors/skips, verified cleanup and matching fixture/production source. The earlier 156-case run had only 34 renewal cases; counts are not additive. Fixture-only review `deleg_60f30bb5` passed with no findings: the real current-policy guard runs, fail-fast spies prohibit renewed authority and dispatch, exact replay/conflict preserve Journal bytes, and scoped spies restore before shutdown. This static fixture approval does not close the separate profile-screening exception gap or establish whole-API/browser acceptance.

Renewal review `deleg_616f38e6` identified a profile-scanner exception classification gap. `deleg_fb4d7f65` reproduced six injected failures in `arena-f0-backend-a9a86cfcc256`: unexpected ValueError sealed 409 rejection, while RuntimeError/TypeError returned 500. The narrow resolved-profile scanner boundary now maps unexpected Exception to static 503, preserving policy HTTPException, BaseException and genuine configuration/profile rejection. Tests verify no grants, Journal mutation, rejection sealing or wake, unknown disposition after scanner failure and identical-key acceptance/replay after removing the fault.

Correction to the review hypothesis: the actual lone-surrogate model already fails closed with policy 422 through execution-grant bounded-copy and existing ModelSettings protection; the proposed late UTF-8 failure was not reproduced. No guard was bypassed to manufacture it. Parent verified 401 distinct passing cases across five runs/six modules: 49 renewal (`023e7063e5f5`), 56 metadata + 66 public jobs (`a27d1d9c8cd2`), 78 editor (`de609605358b`), 69 manual API (`4a56faa784c0`), and 83 manual versions (`df95abcb3f0e`), all with `arena-f0-backend-` prefix, zero failures/errors/skips and verified cleanup. Changed source bytes match staged evidence; full hashes and retained RED/timeout records are in `.runs/resolved-profile-screening-verification.json`. Closure review `deleg_404d3130` passed with no security or logic findings and matched both scoped source hashes. It confirmed scanner faults remain retryable without durable rejection, policy/control-flow exceptions retain their semantics, and genuine profile mismatch still seals domain rejection after rolling back temporarily captured grants. The only suggestion was clearer naming for the surrogate-policy test; no blocker remains in this bounded scanner fix. These are bounded isolated regressions, not a full suite, live execution or browser acceptance. UI remains paused and manual browser acceptance remains NOT-RUN.

Current authorization: the user's “please proceed with updating the plan and then proceed with implementation” approves the focused controller/sessionStorage/IndexedDB/BroadcastChannel work in [ui-storage.md](ui-storage.md).

### Current assembled UI-storage checkpoint

Scoped completion: `deleg_21c08e34` approved the final two native causal corrections and the nine-case evidence with no findings. Parent rehashed both latest native and manual-browser artifacts, verified fresh empty Docker listings for their actual run labels and all four candidate names, and verified the shared frontend is still exited. All 184 production frontend source hashes still match the 1591-test/build checkpoint. Separate approval receipt: `.runs/ui-storage-scoped-acceptance.json`. Raw native PARTIAL/exit-2/acceptance:false records are preserved; this parent receipt approves the bounded nine-case contract without rewriting them. Draft/Library, read-query and v1/v2 compatibility reviews are closed, and the real manual-research workflow passed. The focused storage task is complete; full V7, deployment, cross-browser and power-loss durability are not claimed.

Latest draft fix `deleg_757c492f` added 87 cases and passed 443 focused tests at `arena-functional-frontend-87b62672d4cc`. Parent verified all 184 staged files against current source and cleanup. Sticky raw/prompt intent blocks late defaults; pre-load Discard preserves inputs unless explicit detached-local consent succeeds; current runtime/navigation ownership fences replacement/disposition/input handlers; implicit catalogue loads retire synchronously while exact research recovery stays independent. Closure review `deleg_181b8158` passed with no security or logic findings in the four fixes and matched both production fingerprints. It confirmed sticky intent, non-destructive pre-load disposition, runtime/navigation fences and catalogue-independent exact research recovery. Extra unloaded-generated-Apply and warm-remount retained-handler combinations were suggested; no blocker was found. The reviewer did not access the verification receipt or rerun tests; the separate parent-verified 1591-case/build evidence below remains the execution basis. This closes the scoped draft implementation review, not browser/deployment acceptance.

Parent reran the complete current production frontend selection after those changes: 1591 tests across all 64 files passed in ten bounded runs, with the same 11 offline-preview exclusions. All runs and build `arena-f0-build-38affc346c67` share identical 184-file input bytes and verified cleanup; tsc/Vite passed with existing warnings. Evidence is `.runs/storage-full-suite-after-authority-fix-batches.json` and `storage-full-suite-after-authority-fix-verification.json`. This supersedes the 1504-test snapshot below for current source, not historical failure artifacts.

Before the genuine manual-research browser run, parent found its checker still required v1 backups although production writes v2. `deleg_8978feef` implemented strict v1/v2 compatibility with exact fieldsets, UUID-shaped draftId, nonboolean safe-integer revision and UTF-16 field bounds, preserving exact source/view/hash/research identity and the unchanged browser journey. Parent verified `arena-f0-unit-ee1d61ba5f`: 143 Python + 10 Node passes (31 manual-suite tests), 15 artifact hashes, 22 staged sources, both current changed-file hashes and authoritative cleanup. Parsed-object capture does not prove raw serialized backup budgets, and no cross-remount metadata continuity is invented. Checker review `deleg_7a5658ff` passed; API-security and draft-code reviews were already closed. Parent then authorized and executed the real manual profile. First run `arena-f0-6776c36862fd` completed UI phases but failed strict proof on normal preview/version-list GET query targets because the query-free mutation rule had been applied to every exchange. Parent retained that failure, reproduced it in `arena-f0-unit-81ec4f30d3`, and limited query exceptions to those exact GET/HEAD read routes without changing API mutation admission/budgets or the browser journey. Unit GREEN `arena-f0-unit-56feb0dafd` passed 144 Python + 10 Node tests; the narrow rule review `deleg_91644d11` passed with no blocking findings. It confirmed endpoint-scoped read compatibility without relaxing mutation targets, field/identity checks or budgets, and distinguished the fresh passed run from the preserved failed proof.

Fresh real manual run `arena-f0-972cb716457b` passed with the strict v3 checker: one editor save, two numbered manual research versions, explicit exact Open, reload and v2 backup recovery. Parent verified all 43 artifact hashes, no live-source changes, zero jobs, all six forbidden counters zero, closed API lifespan/socket and authoritative empty cleanup. The recovered screenshot visibly shows browser-family v2 as a frozen research version with an unsaved recovered draft. This is current isolated API/browser authoring evidence—not native fault-case acceptance, production deployment, GPU/physics or full V7 completion. The preceding failed proof remains unchanged.

Both implementation owners `deleg_9489fdd2` finished. Draft-controller review `deleg_224d1613` blocked scoped approval on four static findings: pre-load Discard can rearm a load over current YAML; late default selection can overwrite authoring begun before metadata; retained generated/recovery/disposition callbacks miss runtime/navigation retirement; implicit catalogue-dependent loads miss synchronous catalogue retirement. `deleg_757c492f` subsequently implemented and tested the fix described above, preserving unbound inputs and exact research recovery. These gaps were not covered by the passing 1504-test checkpoint; browser acceptance remains held. Parent preserved the two initial integration failures and resolved them without restoring localStorage writes: the App pin/reload fixture now uses an explicitly scripted persistent adapter, and confirmed Recent admission is separately fenced from mutable catalogue source admission. Parent added two failing loading/unavailable catalogue regressions (`arena-functional-frontend-5ddef62b1031`), then the Library cohort passed 111 tests (`dcf66cd69ea9`). Receipt-history/fixture closure `deleg_5314117c` passed with no blocking regression: normalized confirmation identity/sequence and preference-owner fences protect catalogue-independent history; pin/Open still requires current exact catalogue authority. The scripted adapter cannot manufacture the separate unavailable-catalogue App Open/Recent success. Dedicated queued-confirmation mutation/ABA tests and an explicit top-level stored root-hash assertion remain suggested coverage, not executed evidence. The separate draft and harness blockers remain open.

The assembled seven-file cohort passed 356 tests at `arena-functional-frontend-0add77258619`; `arena-f0-build-9d2871f8c002` passed tsc and Vite with the existing outDir/chunk-size warnings. Parent verified eleven current/cohort/build source files and cleanup; `storage-integration-summary.json` records evidence. These results supersede the owner's 117/119 integration checkpoint, not its retained failure artifacts.

The one-shot 64-file invocation (`arena-functional-frontend-6a55e53836f9`) failed worker creation with EAGAIN. Sandbox limits were not raised. The same complete production selection was then exercised in bounded batches: 1504 tests across all 64 expected files passed, with 11 offline-preview files explicitly excluded by the runner's existing `src/preview/**` rule. Parent parsed counts, deduplicated files, verified all ten passing runs have identical 184-file staged source matching the current checkout, and verified cleanup including the failed one-shot. Evidence: `.runs/storage-full-suite-selection.json`, `storage-full-suite-batches.json`, `storage-full-suite-verification.json`. The earlier automatic-preview setup failure is not present in this current enumeration; no retrospective root-cause claim is made. This is full production frontend unit/component coverage in batches, not native IndexedDB/browser, backend, deployment or live acceptance.

### Storage implementation and harness history
Current native-browser checkpoint: security closure `deleg_ae004724` passed for the narrow host-output/streaming/asset-budget repair and explicitly supported a bounded PARTIAL smoke. Parent enabled `REVIEWED_BROWSER`, made negative-gate unit tests explicitly select the unreviewed state, and reran 158 Python + 24 Node units (`arena-f0-unit-d620a76190`). Parent then executed the actual fixture build and Chromium at `arena-storage-02494ebb5773`: two pages independently read the same native IndexedDB record containing both exact expected pins, with transaction.oncomplete settlement, no driver errors, and verified browser/container cleanup. All 12 artifact hashes and the strict partial checker were verified. Both run and checker return PARTIAL/exit 2, not acceptance; source confirmations are synthetic fixture props and no API workflow was exercised.

The original smoke's full-case flags remain NOT-RUN; its evidence was not upgraded. The later `deleg_7e7032d1` candidate run `arena-storage-2d401db8a27c` records all nine native cases as observed in Chromium 145.0.7632.6: queued transaction overlap, capacity contention, seeded legacy migration, post-commit notification failure, request-success then abort, blocked/late-upgrade abort, versionchange, missing record and notification non-authority. Blocked-open uses a disclosed v1→v2 factory fault and observes upgrade abort—not fabricated late success. Parent verified 21 artifacts, all 212 captured-source hashes, four current native executable inputs, empty cleanup and the strict partial checker. Safe-unit run `arena-f0-unit-32dadba93f` passed 161 Python + 25 Node tests, with current six executable inputs and artifacts verified. The owner reports 51 resealed negatives. Review `deleg_8615d613` found no production storage defect or unsafe fault escape in the recorded native effects, but withheld approval: seed/write terminals and independent readbacks were incompletely bound, semantic checks missed writes appended after audit, and legacy seeding happened after module execution rather than before it. `deleg_ca696ee7` owns complete closed-trace/transaction binding, pre-module seeding, stronger causal/production-refresh witnesses and resealed negatives, followed by a new native run. Existing evidence remains PARTIAL/exit 2 with acceptance false; real API/source verification is not claimed. The genuine manual-research API/browser journey remains held by the separate draft fixes and final UI closure.

Native evidence v2 checkpoint: `deleg_ca696ee7` repaired the missing seed/readback transaction bindings, post-audit mutation checks and pre-module seeding, and strengthened causal and production-refresh witnesses. Fresh native run `arena-storage-26240005661c` records all nine cases under `native-storage-candidate/v2`; parent verified 21 artifact hashes, 212 captured-source hashes, current native executable inputs, no errors and authoritative cleanup, and reran the strict partial checker. It remains PARTIAL/exit 2 with acceptance false pending closure `deleg_023c9b78`. The owner reports 104 resealed negatives and 168 Python + 26 Node units in `arena-f0-unit-a7aef3e157`; parent verified that evidence, then reran the assembled native/manual checker cohort after the separate read-query correction: `arena-f0-unit-06dde8971f` passed 169 Python + 26 Node tests, matching current inputs and verified cleanup/artifacts. No production storage change or defect was reported; old PARTIAL proofs remain unchanged.

Native causal follow-up: review `deleg_023c9b78` confirmed the three original proof repairs and actual nine-case consistency, but found two remaining gaps: missing-record production get/absence binding and versionchange/upgrade ordering within the retired cut. Parent reproduced five coherent resealed counterexamples (`arena-f0-unit-58906a16ea`), then bound both paths to the initialized production connection, required the exact null read before abort and the ordered versionchange→upgrade→retired-cut→command chain. Unit GREEN `arena-f0-unit-6cd4cda619` passed 169 Python + 26 Node tests. Fresh native `arena-storage-0edd1ff0837a` observed all nine cases and passed the stricter partial checker; parent verified 21 artifact hashes, 212 staged sources/current executable inputs and cleanup. Final narrow review `deleg_21c08e34` passed and recommended nine-case scoped approval. Parent approval is separately recorded above; runs retain original PARTIAL/exit 2. Production/browser driver/host lifecycle were not changed by this fix.

Earlier harness history:
Preimplementation review required retained baseline ownership, rendered-handler versus dispatched-operation tokens, StrictMode activation generations, explicit failure/reconciliation states and honest ambiguous migration bounds. Preference review required detached references, transaction admission/commit linearization, terminal connection-attempt lifecycle, bounded notifications and observable replacement/migration limits. Parent incorporated those exact contracts before production work. `deleg_9489fdd2/task-0` owns draft controller/storage/Editor; task-1 owns preferences/Library without shared-file writes. `deleg_a3507a95` delivered native IndexedDB probe preparation, not executable browser acceptance: its browser CLI still exits 2 before discovery and the prepared scenario is explicitly partial. Parent verified 141 Python + 17 Node safe-unit passes at `arena-f0-unit-73a74b5ba0`, 28 staged-source hashes, six current executable files, 16 artifact hashes, both zero process exits and authoritative cleanup. Prepared probe review `deleg_d5019f82` found shared safeunit attribute leakage, insufficient exact-pin durable readback, incomplete phase/failure proofs and request-budget rejection settlement; it also requires actual dependency PROBE execution bound to the inspected owned container rather than trusting a writable receipt. The locked CLI has not been bypassed or browser-executed. `deleg_b9010c42` delivered the executable owned-container lifecycle and strict partial-proof checker, still locked by `REVIEWED_BROWSER = False`. Parent verified `arena-f0-unit-a3e3df6002`: 151 Python and 24 Node passes, 16 artifact hashes, 28 staged-source hashes, six current executable files, both zero process exits and authoritative cleanup. Launch review `deleg_1dbb5ad9` blocked execution: host-captured stdout/stderr was written through container-writable evidence paths, allowing symlink redirection by misbehaving executable code, and the checker omitted the loader's aggregate asset limit. This is a reviewed host-boundary weakness, not an observed exploit or malicious dependency claim. `deleg_3867c7d9` implemented fresh unmounted host-capture files, exclusive no-follow writes, bounded 1 MiB combined streaming output and exact 4 MiB per-asset/8 MiB aggregate checking. Parent verified `arena-f0-unit-edd448e9b5`: 158 Python + 24 Node passes, both zero exits, current changed-source hashes, 28 staged-source hashes, 16 artifact hashes and authoritative cleanup. Narrow closure review is `deleg_ae004724`; browser execution remains locked. Container-written artifacts, Docker logs and shared metadata output have no newly added quota, and pipe/process tests use synthetic process identities rather than a live host-signaling claim. Remaining lifecycle safeguards were accepted within the review's static scope. No browser/build execution is claimed. Preference implementation review `deleg_94f83178` passed with no blocking security/logic findings; its source-only scope excludes the still-active draft owner. Parent then ran the current preference/native-adapter/Library cohort: 109 tests across three files passed at `arena-functional-frontend-cd25bc7a8b21`, with exact test exit, seven matching implementation/test/contract source files and authoritative cleanup verified. `library-cohort-verification.json` records parsed per-file counts and hashes. Nested research-reference mutation during queued admission and delayed persistent StrictMode initialization were suggested as additional coverage; this is not native-engine acceptance or an assembled build. Native capacity/migration/fault/notification cases remain explicit requirements, not implied by concurrent click scheduling or callback simulations. The new probe and manual-research browser remain NOT-RUN. The Library unit/component checkpoint is verified; the draft-controller and integrated/native-browser gates remain open. Independent plan review precedes disjoint implementation ownership. Parent verified stopped-dependency preflight `arena-functional-frontend-7adbba029ac8` and baseline `arena-functional-frontend-35b46302f9d0`: 193 passes across draft-storage, Library and actual-App Library integration, with authoritative cleanup. These are pre-change baselines, not proof of the new storage design. No production service was restarted; no new dependency or backend migration is authorized by this scope.

Historical pause (superseded by the authorization above): review `deleg_89756162` confirmed index/session/capability/confirmation/settlement fences but withheld overall closure for newer browser-storage ownership. Replacing draft or preference storage during a pending research Open can still be overwritten by settlement; retiring loading can also trigger old draft persistence. Parent inspected `draft-storage.ts` and Library persistence: writes replace the stored record without comparing a captured ownership baseline. The race itself has not been runtime-reproduced. After two targeted UI fix cycles, parent requested a dedicated storage-ownership decision rather than starting a third narrow patch. The clarification timed out with no answer; UI changes are paused pending that decision. Existing security and harness work continue, and manual browser acceptance remains held. No rollback or deletion was performed.

Second manual-UI cycle `deleg_ec84427b` added 18 actual-App regressions and passed 533 focused tests across eight files. Parent verified `arena-functional-frontend-5a81e73a374e`, its `research-open-admission-proof.json` checksum, the three current/staged source hashes and empty cleanup; the owner also reports passing typecheck/build. Both original research Open and Library research reopen now require captured editor-index epoch, current success/idle/noninvalidated state and strict capability before confirmation, dispatch and all settlements. Independently verified source identity does not imply mutable-catalogue membership, and editor-receipt GET-only recovery remains separate. Final bounded closure is `deleg_89756162`; security holds and real-browser revalidation remain outstanding.

Security closure `deleg_ad3416c9` confirmed recovery/origin ordering but withheld approval for a fresh-generation metadata path: the recursive pre-grant check covers only the request body, while subsequently resolved model/workflow metadata can contain an escaped active credential. Literal-only checks permit it until post-submit recursive screening rejects after grant/job/event creation. This is a static finding, not a live experiment. `deleg_996a3707` owns regression-first protection of resolved public profiles before grants and the complete assembled submission before Journal.submit, for explicit and legacy paths. Its editor admission regions are disjoint from the shared-response helper work; the owners were explicitly coordinated. Manual browser acceptance remains held.

Security fix `deleg_398ac821` passed 136 isolated tests (69 manual research, 67 editor revision) in `arena-f0-backend-b3f037e9215d`. Parent verified both supplied proof SHA-256 values, actual JUnit totals/no failures/skips, cleanup and all four current files against their staged bytes. Complete stored generation records now receive recursive YAML-aware current protection before recovery responses; fresh immutable child saves protect the complete original bundle after exact committed-replay resolution and before a new commit. Immutable editor siblings, legacy replay and pre-grant fresh-generation screening are also covered. `deleg_ad3416c9` independently reviews that bounded fix.

The owner identified generic `jobs.py` job/list/workspace responses and `events.py` emission still returning unprotected Journal data; parent source inspection confirmed those direct returns/yields. These sibling gaps remain a security blocker, not a live-disclosure finding. `deleg_123ac57e` owns current-policy protection across those shared HTTP/SSE paths, including cursor/session/stream cleanup and explicit cancellation semantics without fabricated records or new work. Manual browser acceptance remains held until shared-path security and UI closure; the original two fixes alone do not close the public-response boundary.

UI closure `deleg_c4233a11` confirmed search, bounded verified inventory and retained manual-POST admission, but found the original ResearchVersions→Editor Open does not check current editor-index epoch/status/capability. Cached true capability after a failed refresh, or capability true→false→true during confirmation/document GET, can leave that entry point active. Existing ABA coverage exercised Library reopen instead. This is a static finding pending regression reproduction. `deleg_ec84427b` owns the second bounded fix: fence original research Open at confirmation, dispatch and settlement without requiring research-version membership in the mutable file catalogue or breaking independent editor-receipt recovery. Browser acceptance remains held.

Manual UI fix checkpoint `arena-functional-frontend-5949b839d5cd` passed 515 tests; build `arena-f0-build-f0b6c0cdfee2` passed. Parent verified the actual test summary, five changed files against staged source, clean cleanup, and the `manual-ui-review-summary.json` checksum. Search now uses bounded scalar fields. Verified research GETs populate a 12-entry current-owner memory-only inventory; catalogue conflicts/duplicates and unavailable capability override it, and restored preferences remain unverified hints. `manualWriteAllowed` defaults false independently of source availability and fences retained POST through authenticated index epochs, including ABA. Exact retry without the original source remains possible only with current permission. Final UI closure is `deleg_c4233a11`; the browser/security hold remains in force.

Security priority from `deleg_4faa70c7` API review: (1) explicit generation POST replay and generation-operation GET can return stored inputs before current secret screening; (2) a fresh keyed child save can omit checking its original research-view root when clean replacement text removes the sensitive content. These are static review findings, not evidence of a live disclosure. `deleg_398ac821` owns regression-first complete-response/original-bundle protection, preserving committed recovery without expired-view resolution and refusing new work. Manual-research browser acceptance and rollout are held until security closure.

Manual numbered-save/open UI is now mounted over the existing research-version controls. It binds a verified durable editor revision, explicit store/family/parent, exact POST/GET commit agreement and explicit dirty-confirmed research opening with full identity/raw-hash checks. Parent verified actual 484-test output and all 173 current frontend src files against `arena-functional-frontend-f38dc61acbec`. Parent independently ran integrated `tsc --noEmit && vite build` successfully (`arena-f0-build-ddef51f954e9`) and verified cleanup, clearing the transient literal-type blocker. Review `deleg_0527e8c1` withheld approval: an unmatched Library search reaches object-valued research_identity and throws; actual research Open records a Recent without supplying a verified Library source entry, preventing usable pins/reopen; retained manual Retry can POST when manual_research_save is false/absent because source availability and capability are conflated. These are static findings awaiting regression reproduction, not a live-browser report. `deleg_c8eda01c` owns safe searchable-field projection, verified research-reference resolution and independent current manual-write admission. Restored preferences must not create verification authority, and source deletion alone must not forbid an otherwise authorized exact retry. No manual real-API browser result is claimed.

Source-consumer checkpoint `arena-f0-backend-2e706ac56cea` passed 192 tests (63 new consumer, 83 manual-domain, 46 API); parent checked JUnit, cleanup and all four changed consumer production files against their tested snapshot. `verify_frozen_spec` now feeds existing preparation/retrieval readers from verified copied source context, and manual source events do not fabricate candidate jobs. Review `deleg_61c2a753` passed with no security or logic findings in the consumer scope, confirming copied-bundle/spec bindings, current protection propagation, legacy codecs, projection/receipt checks and candidate-only job events. Non-blocking follow-ups are explicit real-store negative consumer tests and avoiding unrelated discovery work during pure normalization. This does not close the separate API security findings. Actual manual publication remains rejected: no manual projection/outbox/grant/worker enablement or live graph result follows from the hypothetical transport fixtures.

`deleg_f3afded1` completed the additive `manual-research-v1` harness, but its real browser/API acceptance remains **NOT-RUN**. It requires one private keyed editor save and two numbered versions with explicit lineage, exact POST/GET/Open/reload/recovery and copied-artifact verification, on the existing app Journal. Parent reran the isolated harness units in `arena-f0-unit-dee2c2e034`: 126 Python and 10 Node tests passed, with no failures/skips, all 15 artifact hashes verified and authoritative empty cleanup. This is synthetic checker/firewall/correlation evidence only. Independent profile review `deleg_654ec8af` withheld approval for three proof-consistency gaps; repair owner is `deleg_c19f0ea9`; commands and exclusions are in `web/arena-workbench/tests/e2e/functional-v7/MANUAL_RESEARCH.md`. Search/Recent/pinning browser coverage is explicitly not claimed. Execution still requires both UI closure and all API/shared jobs/SSE security closure; an acknowledgement flag is not approval. Original profiles/proofs remain unchanged.

The earlier manual-profile review found that query-suffixed captured mutation targets were not checked against their query-free paths; copied snapshot document_id/expected_source_hash and request/key-derived revision identity were not fully bound; and two commits could claim different registries despite shared-store lineage. The fix must reject independently resealed contradictions, bind an observed common registry, and reject duplicate request keys before an allowed write rather than only during later evidence checking. These are checker/runtime-boundary findings, not a failed or successful manual browser run. Profile execution remains blocked on its own closure as well as UI/API security closure.

Harness hardening `deleg_c19f0ea9` implemented those bindings and bounded recursive duplicate-key rejection before dispatch/budget changes. Final `arena-f0-unit-2227d7bb80` passed 136 Python and 10 Node unit tests, including 24 manual-profile tests. Parent checked per-suite totals/no failures/skips, all 15 artifact hashes and authoritative empty cleanup. Query-free targets, production snapshot fields, actual save-body/request/key-derived revision identity and observed common registry are now checked; original profiles remain separate. Bounded closure review `deleg_55c9222c` passed with no findings and matched current scoped files to retained unit sources. It confirmed target/source/registry bindings, duplicate-key rejection before dispatch and unchanged older-profile admission; this is source review, not additional execution evidence. Manual browser/API acceptance is still NOT-RUN; UI storage-ownership approval and security closure remain independent gates.

Second F4 retirement cycle `deleg_3c9f2d66` completed. It covers accepted-job/status/canonical/options query replacement, adopting an already-fetching query, and disposed same-key ABA events, preserving normal historical stale display and fresh recovery. Parent verified the final current/staged source hashes, proof/log hashes, clean cleanup and actual log totals: 97 authority tests (`arena-functional-frontend-a7200eda40d0`) and 244 focused tests (`arena-functional-frontend-664721f65187`). The latter run contains `snapshot-retirement-summary.json`, including retained failures and build diagnostics. These scopes overlap; they do not replace a new full-suite run.

Global typecheck/build at that checkpoint failed on the concurrent UI owner's `src/research-source.test.ts:11` literal-type mismatch (TS2345), so Vite did not run. Parent notified that owner rather than editing their in-progress files or using an alternate compiler path. Final snapshot closure `deleg_04703cc4` passed with no security or logic findings, confirming live replacement/adopted query retirement, disposed-ABA isolation, cancellation/late-completion fences and fresh recovery. Explicit query-key slot assertions were suggested as non-blocking additional test diagnostics. Full integrated build/browser verification remains pending after the research UI stabilizes. The earlier 1,306-test result remains a pre-second-fix source snapshot, not evidence for these new cases.

X05 API checkpoint `arena-f0-backend-5af73b51ffcd` passed 46 tests with zero failures/errors/skips; parent checked JUnit, cleanup and the four current implementation/test files against staged bytes. E038 preserves flat candidate input and adds strict tagged candidate/editor sources; manual parent is explicitly required/nullable, and server-derived source proofs remain separate from the minimal request. E004 now resolves exact configured-store `research-version:<store_id>:<reservation_id>:<manifest_digest>` descriptors into fresh UUID views with `research_identity`. Replay/open survives original editor deletion and API restart; protected copied includes retain their context through validation/refine/snapshot admission/child save. These are isolated HTTP/core tests, not deployment or browser evidence.

The domain review passed with no blockers, suggesting one combined escaped-unused-include/current-policy/deleted-original regression. `deleg_4faa70c7` now owns independent API/open review, production UI/manual-save/open integration, and source-aware publication/retrieval readers. Manual publication remains rejected and is not enabled by the new authenticated `manual_research_save` / `research_version_open` capabilities. CLI and backup/restore acceptance remain open. Source inventory hashes/locations were refreshed; static validation still reports 47 routes because existing endpoints were extended, not duplicated.

Closure review `deleg_330ebf9a` identified a remaining variant: accepted-job/status changes replace query A with query B for the same document/hash/options; B's fetch does not clear retained A because the first fix compares query objects. Invalidating the draft before B settles can therefore restore A. This is a static finding absent from the prior passing suite. `deleg_3c9f2d66` owns the second bounded RED→GREEN cycle, including related canonical/options replacement and cancellation/late completion, rather than another same-object-only patch. F4 closure remains withheld; earlier test/build evidence is preserved and accurately scoped.

Current complete production-frontend unit checkpoint: `web/arena-workbench/tests/e2e/functional-v7/.runs/arena-full-frontend-b7ac6c111a29/summary.json`. Parent executed eight bounded batches, parsed their actual per-file output and totals, verified all 60 expected production test files exactly once, and recorded **1,306 passed / zero failed / zero skipped**. The approved runner explicitly excludes `src/preview/**`; all 11 discovered preview test files are separately listed and not counted. Initial discovery included those files, so coverage was reconciled against the actual runner profile and per-file logs rather than assuming every argument executed. All staged src fingerprints matched the baseline and the unchanged ending source; every run's cleanup was verified. This supersedes the old failed full-suite checkpoint for this source snapshot without asserting a cause for the historical automatic-preflight setup failure.

F4 fix owner `deleg_abddc801` reproduced retained-pixel resurrection and cancellation/invalidation rollback, then implemented synchronous QueryCache retirement and current-epoch/owner/signal completion guards. Twelve exact-retry matrix rows cover retained handlers and preflight retirement while preserving frozen request bytes. Owner GREEN `arena-functional-frontend-4e233607afbe` passed 288 tests; typecheck `arena-f0-typecheck-b3ce0f362e0e` and build `arena-f0-build-0e10c2ca10c0` passed. Parent verified the two owned files matched tested bytes and clean cleanup before running the complete unit suite above. Final bounded closure is `deleg_330ebf9a`; ordinary stale display remains supported without refetch, and no browser/GPU result follows from these unit tests.

F4 review `deleg_16f7a4ed` withheld approval for one inherited control-flow gap: after displaying catalogue A, a deferred refetch hides its receipt without clearing retained pixels; invalidating the draft before that fetch settles can select those old pixels again through the null-canonical-hash branch. This was a static finding, not an observed new regression. Fix owner `deleg_abddc801` must reproduce it and retire catalogue authority independently of intermediate React renders, including cancellation/late completion. Ordinary explicitly stale display without a refetch remains distinct. The owner also expands retained exact-retry source/options/visibility/session ABA coverage; no runtime/render acceptance is implied by this review.

F4 frontend checkpoint: `receiptId` is separate from optional actual `requestJobId`; catalogue pixels no longer imply capture for the selected source. Manual/automatic preflight is source/options/session/visibility-owned, including ABA and retained handlers. Accepted old jobs remain observable without retargeting pixels; catalogue miss/error remains authoritative. Explicit exact snapshot retry retains its old request ID/bytes, and robot/scene partial labels no longer claim unavailable images exist. Existing wire payloads remain unchanged; authoritative physical origin, artifact digests and full dependency freshness are not supplied by this slice.

Owner evidence: `arena-functional-frontend-03fbc5a43ea5` (207 tests), `arena-f0-typecheck-ea1c0bca806a`, `arena-f0-build-72fe8c087908`. Parent verified the five changed production files matched the passing test snapshot, then ran 261 tests across eight assembled Editor/Library/recovery/snapshot files GREEN (`arena-functional-frontend-cb2bae2bf91e`) and checked cleanup. A preceding parent invocation used the wrong `.test.ts` suffix for `automatic-preview.test.tsx`; it failed staging before tests (`arena-functional-frontend-a289dff4a6a7`) and was preserved with clean cleanup. Counts overlap and are not a full-suite total. Earlier automatic-preflight setup failures are not declared root-caused merely because this run passed. The pre-F4 browser baseline remains historical until a new browser run covers the integrated source.

X05 domain checkpoint `deleg_3968f7c6`: portable editor bundles, strict manual source references and `persist_editor_revision` now reuse the existing Registry/Store/Journal and atomic artifact protocol. Parent read structured results for `arena-f0-backend-404db475f660` (83 manual research tests) and `arena-f0-backend-a912a8c10a77` (79 Documents tests), with zero failures/errors/skips and verified cleanup. Owner also reports 88 harness contract tests. Committed reads/retries work after original editor/source storage removal; fresh/incomplete retries require the same verified source. No jobs/attempts/candidate receipts are fabricated. Manual publication requests are rejected before loading/reservation; no capability advertisement changed.

Exact portable/source codecs and artifact layout are recorded in the functionalization appendix. `deleg_a07e843e` now owns independent domain review and strict authenticated API adapters plus exact ResearchVersion→Editor opening. Existing flat candidate requests and historical five-field source JSON must remain compatible. UI/CLI, source-aware publication/preparation/retrieval, backup/restore and real manual cross-process tests remain uncompleted; the earlier editor-revision SIGKILL proof is not research-version crash evidence.

Latest genuine authoring snapshot: parent ran `arena-f0-933680638528` successfully with actual production build/API/CodeMirror and unchanged `authoring-v1` checks. It exercised raw edit, three XYZ proposals, candidate review/consent, distinct fresh validation, one keyed Save POST, exact receipt GET, Library selection/explicit immutable Open, download and browser reload. All authoring checks passed with no remaining profile journeys. Root width was 1440/1440 and 390/390 in dark/light; parent inspected the captured screenshots. Parent verified all 39 artifact hashes, no browser errors/external requests, all six API forbidden counters zero, no jobs, closed lifespan/absent socket and empty cleanup. Fresh Documents-instance readback also passed; this is not an API process restart, deployed service, numbered research save or GPU/physics claim. See `web/arena-workbench/tests/e2e/functional-v7/AUTHORING.md` for current evidence and preserved failed predecessor.

The recovery owner completed `readEnabled` (defaulting to `enabled` for existing callers): active session/capability-owned GET is independent of current YAML validity/loading/backup recovery; POST retains stricter current-source/binding/retention admission. Hidden Library activity grants neither authoring read nor write authority. Reported GREEN is `arena-functional-frontend-71f5c05b95bf` (296 tests), typecheck `arena-f0-typecheck-08e4e89a54b5`, build `arena-f0-build-e5552c3f2efa`. Independent discovery review passed; Final bounded recovery/catalogue review `deleg_73002915` passed with no security or logic findings. It checked independent GET admission, stale/hidden/ABA retirement, catalogue Open cancellation, checked acceptance/CAS cleanup, busy release and explicit receipt Open identity/hash checks; it is a source review, not additional runtime evidence. Later X05/F4 source edits require fresh acceptance rather than inheriting this snapshot's result.

Discovery repair `deleg_448d8630` completed; runnable exact-ID commands and scoped proof paths are in `web/arena-workbench/tests/e2e/functional-v7/STOPPED_FRONTEND.md`. Parent ran the official `--preflight-only` successfully (`arena-functional-frontend-c6dc65a3c45d`) and all three CSS structural tests (`arena-functional-frontend-392ffa4d4566`), verifying passed records and empty owned cleanup. The shared frontend stayed stopped. The alternate CSS runner's earlier 30 passes remain limited structural evidence, not the official browser acceptance path.

Official repaired integration run `arena-functional-frontend-087865b72d67` reached 189 passes / three failures out of 192: explicit retained-save status checking is still disabled with a missing source, invalid source or pending draft recovery. This supplies the missing RED prerequisite. `deleg_4f3b2e2a` owns production recovery-admission completion and independent stopped-discovery safety review. Full authoring rerun remains after the UI fix; the previous failed authoring proof is preserved.

The preserved predecessor authoring profile `arena-f0-5c915a663225` failed acceptance, not a partial pass. Production load/raw edit/proposal/Apply/fresh validation, exact Blob bytes, retained CodeMirror and dirty cancellation ran; durable Save/readback/Library reopen did not run because `/api/health` lacked the capability consumed by Editor. Mobile width was 423px at viewport 390px in both themes. See `web/arena-workbench/tests/e2e/functional-v7/AUTHORING.md` and retained failure evidence. The harness did not substitute legacy save, direct fetches or fake capabilities.

Parent reproduced the missing capability in an API test (`arena-f0-backend-2ac62d8b1b48`), added only `durable_editor_save: true` to actual health metadata, and ran all 49 revision API tests GREEN (`arena-f0-backend-1dc30ccb3b51`). Authentication remains required and health still does not grant generation/preview admission. Parent also verified the 79-test core checkpoint `arena-f0-backend-11740cc29546` and both cleanups. The API owner completed recoverable 503/Retry-After and corruption mapping plus implicit issued-view loading; independent closure is active.

`deleg_fe0f0bf8` owns the two assembled UI fixes (pending Open versus catalogue retirement; independent save-status GET despite invalid/missing draft), the scoped mobile CSS correction, and API closure review. A clean full browser rerun is required after integration; no current source/mock count substitutes for it. API source hashes/locations were refreshed without adding routes; static inventory verification still reports 47 registered routes.

That batch returned partial results when shared frontend `964ddf2e645854708368230ff1770495df6f94c2ecca22b17c7d01ebc611f97d` stopped. Parent independently observed Exited (1); the cause is not established and no restart is authorized/performed. Official frontend tests/typecheck/build now fail discovery, not a new compiler error. `deleg_448d8630` repairs explicit exact-ID stopped-container dependency discovery without shared-service execution or weakening source/volume isolation. The CSS agent's `/tmp/arena-mobile-css-check.py` is alternate scoped evidence, not yet a parent-approved substitute.

Library retirement: RED `arena-functional-frontend-7699efacd75a`, 33-test GREEN `arena-functional-frontend-fc16f65826ff`; parent matched current editor.tsx to the GREEN snapshot. Additional tests for removed/changed/unavailable/ABA catalogues, retained callbacks, 2 MiB bounds and save recovery remain unexecuted. The independent recovery production fix was deliberately not written without its RED run. Mobile CSS changed only scoped sizing; owner reports 30 structural/component tests in `arena-mobile-css-284ad41effc9`, with no new build/geometry claim.

API closure found one sibling path missed by the earlier matrix: implicit Generate's storage errors. Parent reproduced busy→500, uncertain→500 and corrupt→404 in `arena-f0-backend-ba7da5b454f7`, added revision-specific catches before the legacy missing-document catch, and reran 52 tests GREEN (`arena-f0-backend-d53a67556d54`, no failures/skips; cleanup verified). Real preissued-view lock/fsync/corruption tests preserve durable bytes and accept no jobs. Final bounded closure `deleg_b81d4a8b` passed with no security or logic findings: revision exceptions precede legacy handling, protected 422 responses propagate unchanged, tests exercise real issued views and storage faults, and health capability does not grant workload admission. This is static closure, not full F3/browser acceptance; memory-retention bounds and event-loop offloading remain follow-ups.

Library handoff evidence: `web/arena-workbench/tests/e2e/functional-v7/.runs/arena-functional-frontend-263fe54eabbd/`. The component supplies search/pagination, explicit Inspect/Open, eight bounded immutable pins and twelve recents, exact metadata comparison, and guarded preference fallback. Its `onOpen` requests an action; only parent-confirmed `opened` events establish successful recents/pin eligibility. Restored preferences are not verification. Independent review `deleg_60af0cb3` found one accessibility ID collision. Parent reproduced it for `inspection`, `comparison`, and `search` (RED `arena-functional-frontend-e108d56855c9`) and separated card metadata IDs from panel/input IDs (GREEN: 65 tests, `arena-functional-frontend-dc693fd5965f`). Closure review `deleg_85817a89` passed with no blockers in the reported collision scope; it does not establish production mounting or browser acceptance. Parent integration must bound the catalogue, own concurrent-load cancellation/stale-success suppression, verify exact hashes, and publish confirmations only for the current owner. At that historical standalone checkpoint the component was not mounted; the later 316-test production integration supersedes that mounting status, but does not complete F3 acceptance.

Latest prerequisite checkpoint: provision manifest `web/arena-workbench/tests/e2e/functional-v7/.runs/arena-f0-provision-d94aed8076e8/provision-manifest.json` records test-only image `sha256:e20b3cc8258b793aaf1fe47c130f54e677fa9c0a6427991cfc1045b743162da5` with pinned Neo4j/PyTZ wheels verified against official PyPI hashes. The owned offline COPY-only image build did not modify live containers or protected Dockerfiles. Actual API proof `arena-f0-85a1b7ff104b` passed its load/validate/schema/catalogue/auth checks; parent ran the strict v3 artifact checker successfully. This is API-only legacy-layout evidence, not production V7 browser acceptance or hash-based authenticity.

Core-only command `PYTHONDONTWRITEBYTECODE=1 python3 web/arena-workbench/tests/e2e/functional-v7/backend_checks.py isaaclab_arena/tests/test_workbench_editor_revisions.py` reached the expected unsupported-idempotency-key failure after kernel-denial preflight, with zero forbidden counters and verified cleanup. The domain implementation is no longer waiting on missing dependencies. F2/F3 review fixes are assigned as `deleg_070dfd5d`. Backend API-image selection extension `deleg_1a595486` completed with 76 sandbox units and an actual missing-request-field RED.

Parent API integration: `SaveDraft` accepts an optional strict retry key; keyed save protects current raw content and full retained bundles while allowing committed replay before expired-view resolution. Added authenticated `GET /api/editor/save-requests/{idempotency_key}` (E047), protected revision catalogue/open/download calls and `resolve_view` for immutable refinement inputs. Eight tests passed in `arena-f0-backend-5987f2c55242`, including fresh API-instance recreation, same-key replay, changed-payload conflict, exact saved-source reopening/validation, and reaching the unconfigured-workflow gate instead of a false missing-document error. No generation or rendering job was accepted. The revision-only test profile explicitly makes the optional snapshot service unavailable; all six forbidden counters and the journal job list were empty, with verified cleanup. This is not a process-crash, power-loss, renderer or full F3 acceptance test.

The AST inventory now records 47 source routes with stable existing IDs and E047 appended; source hashes/locations were refreshed. N15's earlier alternate URL is marked superseded, not implemented as a duplicate. Canonical/functionalization/endpoint plans were updated; static route, coverage, link and whitespace checks passed.

Latest F3 checkpoints: core `arena-f0-backend-8049f6574082` passed 77 tests; API `arena-f0-backend-c2989cd3ff63` passed 16. Parent checked both structured results and cleanup. The API tests now configure only a synthetic dummy credential after saving and verify static 422s for replay, lookup, download, opening and listing—including hex/folded content retained in unused includes—with unchanged artifact bytes and no jobs. Request-model key syntax now matches the core's leading-alphanumeric requirement; malformed keys are rejected, not normalized. F2 fixes passed owner checkpoint `arena-functional-frontend-444238ee5eb7` (364 tests); save-control fixes passed `arena-functional-frontend-b30b72be3735` (59 tests). These are distinct/overlapping scopes, not a new summed full-suite count.

Review and integration batch `deleg_207e73fb` owns independent core/API review, F2/save-fix closure and production Library/save/recovery mounting. Core concurrency evidence uses threads and interruption evidence uses SystemExit injection; actual killed-process/power-loss and multiprocess verification remain explicit gaps.

The preceding concurrency/interruption limitation describes that batch's checkpoint. Later real-process verification `arena-revproc-ffc3f36fc4dc` passed: 18 fresh worker processes, five scenarios, three committed revisions, intentional SIGKILL witnesses with exit 137/no OOM, rejected unchanged partial evidence, identical post-promotion replay, and genuine flock contention for equal/conflicting requests. Parent verified the proof SHA-256 `ac4cb0f270102c6fd5d5d5ea7d9ac431fda0582f44ea8ddf02571408d5cc08ec`, all 160 artifact hashes, passed status and authoritative empty cleanup. See `web/arena-workbench/tests/e2e/functional-v7/REVISION_PROCESS_CHECKS.md`. This closes the bounded real-process gap for those scenarios, not physical power-loss durability. The earlier wrapper-exit failure remains preserved.

That batch completed: bounded F2/save-fix closure passed; production Library/save/recovery integration passed 316 focused tests plus typecheck/build (`arena-functional-frontend-4bce945bc4c1`, `arena-f0-typecheck-f5729b41c6fc`, `arena-f0-build-389fc976f8b2`). Parent verified unit exit/cleanup and eight current/staged source hashes. Actual source-origin/view grammar/root hashes and supplied revision hashes gate replacement/Recent updates; catalogue input is bounded to 2,048 rows or 2 MiB without truncating duplicates. These are real-App/ApiClient mock-transport tests, not live browser acceptance.

Core/API review found two remaining defects: RevisionBusy/uncertain/corrupt storage failures need consistent recoverable HTTP responses, and legacy implicit-base generation must load an issued immutable view without pretending it is a path. Fixes, real isolated V7 browser authoring acceptance, and final assembled UI review are assigned as `deleg_ddc19581`. A separate owned-process SIGKILL/multiprocess test workstream (`deleg_5f57a874`) addresses the real process-death gap; physical power-loss acceptance is not claimed. Read-only audits `deleg_787031a9` prepare the remaining manual research-version source bridge and F4 snapshot provenance integration without enabling live effects.

Those audits completed without runtime execution. X05 can reuse ResearchStore/Journal through a strict source union, exact legacy-candidate compatibility, a public verified editor-bundle loader, self-contained copied includes and source-aware projection/retrieval verification. Numbered manual versions and exact research-version reopening remain unimplemented; no synthetic candidate job/receipt or database migration is justified. F4 needs additive provenance/digests, exact verified-byte artifact transport and source/options/visibility dispatch fencing. A cache receipt's publishing job is not necessarily every thumbnail's physical origin because raw thumbnails have their own cache; unknown origins/dependency freshness must stay unknown. These requirements are now recorded in the functionalization plan, not counted as delivered features or real-render evidence.

Latest frontend execution checkpoint: `python3 scripts/run-functional-checks.py build --allow-frontend-verification` passed TypeScript checking and the Vite production build in the owned offline nonroot sandbox (`arena-f0-build-34b170077d25`). Earlier typecheck failures were real test-typing errors and were corrected without weakening assertions. The earlier permission blocker is no longer current. Build warnings concern bundle size and the deliberately external output directory; browser/deployment acceptance is still pending.

Parent subsequently ran 198 tests across six focused files—cancellation, position eligibility/review, revision contracts/save controls and Library—all passed (`arena-functional-frontend-2e13a453213b`). These counts overlap prior checkpoints and are not added to the historical full-suite total. F2 only permits explicit root XYZ edits for background registry `table`, identity XYZW, no additional constructor params, incident constraints/reifiers or reference children. It does not claim general pose editing or physical validation.

Earlier, the F0 v3 identity repair exposed a missing Neo4j driver in the original immutable image. That package-availability blocker is now resolved for the provisioned API profile, and the core-only runner independently reaches Documents tests without acquiring an API-only driver. The original image and live containers remain unchanged. Use the latest prerequisite checkpoint above rather than treating either historical bootstrap blocker as current.

Independent early contract review `deleg_292fa338` requires current secret protection even on committed replay/lookup, reject-only callback integrity, atomic manifest authority, immutable-origin resolution in generation/snapshot callers, source/include CAS, explicit workspace-global key/hash encoding and UI retention/readback checks. F2 and standalone save-control code reviews are now assigned as `deleg_33b1e839`.

An independent contract critic has reviewed F2/F3 boundaries while implementation proceeds. The keyed-save request/receipt, typed revision-open descriptor and source-preserving candidate-validation seams are active implementation contracts, not already verified API capabilities. Update the endpoint inventory when routes actually change, and record both failed and successful acceptance evidence rather than replacing failures with later overlapping test counts.

#### Last completed F0/F1 verification checkpoint

Scoped independent closure review `deleg_42f83ed8` passed the repaired F1 ownership gaps and the proof-checker contradiction fixes, with no blocking findings in those scopes. The focused Vitest runner is accepted under explicitly trusted mutable dependencies; its repaired preflight and authoritative cleanup were independently read back. Typecheck/build remain denied and unrun pending renewed permission.

Implemented production seams now include optional branded V7 chrome with legacy rollback, stable editor/query mounts, real read-only authored inspection, source/session/client-owned validation and metadata, exact-job cancellation, synchronized renewal retention, retired automatic-render consent, and single-owner runtime observation. Shared queue resume is truthfully labeled and confirmation-gated, with render-time authority and preflight retirement checks. The pure root-position edit utility exists; candidate-validation/Apply and durable-save/reopen integration are still pending.

Parent bounded frontend enumeration covered all 52 production test files at its recorded snapshot: 938 passed and one automatic-preview setup failure. `/tmp/arena-v7-full-unit-batches.json` preserves every batch, file and proof path; an all-at-once attempt exceeded the 256-PID sandbox limit and is not counted as acceptance. Later focused ownership fixes passed 139/179-test checkpoints, and parent verified their successful exits and cleanup; those overlapping counts are not added to the full-suite total.

The bounded automatic-preview investigation (`deleg_8bca4df6`) concluded **unresolved**, with no fix retained. Neither a fixture root cause nor a production defect was established. Clean focused runs passed 67 tests and a bounded cohort passed 170, but these do not erase the original failed setup. A stress probe exceeded its own deadline and contaminated subsequent tests; that evidence is not accepted as a root cause. Parent verified `editor.test.tsx` exactly matches the incoming staged source (SHA-256 `79fc2849264166da938bdea2087ed751e413cfcc6760decdeb9378b6731a3739`), and verified cleanup for the reported checkpoints. No timeout or zero-POST assertion was weakened. Investigation stopped rather than entering an unbounded retry loop; the setup failure remains an acceptance blocker.

The latest harness unit checkpoint (`tests/e2e/functional-v7/.runs/arena-f0-unit-1a0fe145b9/`) records 85 Python tests and six Node correlation tests. Parent inspected the 85-test Python summary; final static review closed create-ID/isolation mirrors, namespace validation, metadata-capture/replay accounting and cleanup/error contradictions. These are synthetic/unit consistency checks, not actual API/browser acceptance.

**Remaining F0 blocker:** immutable Neo4j dependency-donor identity/role is incompatible with the discovered-runtime API proof contract, and its package path/availability are unverified. The harness fails closed before API creation. Do not relabel the donor as the live runtime or weaken verification. The historical real-API baseline below remains withdrawn; no deployed V7, full authoring core, live renderer, model, graph publication or physical-task acceptance is claimed.

#### Earlier review failures and repairs — historical record

**Acceptance was withdrawn pending review fixes.** Independent review `deleg_4e208187` found blocking source-staging confinement, live Docker metadata projection, dependency-copy trust, subprocess-allowlist, immutable-ID cleanup, browser request-correlation and proof-schema gaps in the new functional-v7 harness. Its historical legacy API/browser run and `verified: true` output do not establish the full advertised acceptance contract. Preserve that evidence, but do not reuse the harness or temporary frontend runner until their metadata/staging boundaries are repaired. Typecheck/build commands were explicitly denied; those checks remain paused pending fresh permission, with no alternative-command retry.

Pure-code review also requires object-owned reference support, truthful unresolved parent-relative transform wording, and reference IDs in the YAML utility's duplicate-ID domain. These are assigned to a separate fix owner. No source-safe Apply capability or full F0/F1 acceptance is claimed. Bounded owners are repairing staging, orchestration/runner discovery, subprocess restrictions, proof/browser correlation, and pure-code semantics independently; final re-review remains required.

Production frontend baseline: 567 tests passed before changes in `/tmp/arena-functional-frontend-7x_7h4or`, under nonroot, read-only, pre-import-egress-probed network-none isolation. Parallel owners cover fresh real-API acceptance infrastructure, retained authoring/effect lifetime and V7 layout, real authored inspector, optional branded chrome, root-YAML edit utility, retained query lifetime, plus an early independent integration critic. Parent owns routing, assembly and final verification. See `web/arena-workbench/FUNCTIONAL_V7.md` for interfaces/ownership.

Parent observed RED→GREEN for layout selection and retained editor navigation. A shared active-status change caused legacy regressions and was corrected: `isActive` retains execution semantics; `isUnfinished` includes authorization waits for the journal filter. Subsequent focused checks passed (47 status/shell/renewal tests; 44 cancellation/shell tests; 35 optional chrome/shell tests at their respective source snapshots). Optional V7 chrome and the real read-only inspector are mounted over the production controller, and exact-job cancellation replaces the duplicate authoring controller on detail routes. These are mocked frontend integration tests, not complete real-API/deployed acceptance; the runner metadata issue above limits their isolation claim. Agent fixes, assembled checks and final independent review remain open.

### V7 functionalization plan review

Added F0–F8 integration slices: production ownership/layout/read data; real Arena YAML/constraint-aware source-safe editing; durable save/typed source opening and research-version bridge; recovery/preferences; actual snapshots/activity; existing generation/query/publication; remaining research execution/evidence; optional displays; per-slice rollout/fallback. The plan explicitly avoids connecting the preview entry, copying its JSON schema or inventing another store/queue. It defines exact saved-source reopening rather than treating download/revision IDs as editable document IDs.

Independent source audits `deleg_f204b273` covered frontend controller/effect lifetime and actual pose/include/save/render semantics. Parent verified relevant production and conversion/Pose/include source. Static checks passed: nine unique integration slices, 17 acceptance cases, valid links/route references, unchanged 46-route source inventory and V1–V7 archive hashes. Scoped pre-commit and diff checks passed. Final assembled-plan consistency review `deleg_b6a8527f` passed with no blockers; its scope-wording and exact-candidate Apply-validity clarifications are incorporated. The functionalization planning task is complete; implementation has not begun under this request. No application code, endpoints, services, provider/graph requests, GPU work or archive bytes changed in this planning task.

### V7 delivered — offline inspect and correct

The user authorized V7 implementation, not backend expansion. Separate workstreams own the inspection/proposal/recovery component, exact-version Library pins/recents/comparison and shared-fixture activity/job navigation; parent owns assembly, styling, isolation/browser checks and immutable export. V1–V6 archive checksums match their records and are retained in `/tmp/arena-v7-archive-before.json` for final comparison.

Implemented the cohesive specification/viewport/inspector, layout-only width and independent collapse controls, source-retired asset selection, frozen pose proposals, actionable bounded JSON-subset checks, raw download and explicit memory-copy recovery. Library pins/recents/comparisons bind exact versions and confirmed opens; activity links reuse the journal fixtures without connecting them to the draft. Generation/reference controls remain available. All new behavior has focused RED→GREEN evidence; the real browser exposed a populated-textarea accessible-label mismatch, corrected with explicit labels. An additional regression keeps a valid reference tab selected after candidate application.

Pre-review assembled run `/tmp/arena-ui-preview-myj951fu/` passed 173 UI tests, two packaging tests, TypeScript/build/package and a 53-screenshot browser walkthrough. Actual download bytes, three image checksums/decoding, source/options A→B→A fences, recovery staleness, exact comparison collision, shared-viewer DOM retention and desktop/mobile overflow checks passed. No attempted API/WebSocket/storage/worker/RTC operations; pre-import egress denial, read-only nonroot sandboxes and zero owned containers verified. Visually inspected desktop/Library/mobile PNGs match that run byte-for-byte. Scoped pre-commit passed. Independent review `deleg_3c3757ee` found one medium R01 blocker: the Specification width slider is active in the intermediate two-column layout where it has no effect. A separate fix context is adding a browser regression and correcting the breakpoint before export; source/consent/recovery/identity boundaries otherwise passed static review. Optional raw-download failure coverage is being added, and documentation now qualifies the deliberate external NVIDIA reference link. The final closure and export are recorded below. No current-draft rendering, stream, backend endpoint or live job is implemented by this slice.

Final verification `/tmp/arena-ui-preview-btwe3ip3/` passed **175 UI tests**, two packaging tests, typecheck/build/package and **55 browser screenshots**. The 1050/1200px regression first failed in `/tmp/arena-ui-preview-fvodidob/`, then passed after the CSS breakpoint fix; keyboard resizing still changes layout at 1440px. Added download-failure tests verify fallback, unchanged draft, anchor removal and URL revocation. Parent checked source/HTML hashes, browser proof, cleanup and matching previously inspected image bytes. Focused re-review `deleg_842fb770` passed with no blockers or suggestions; scoped hooks/diff checks passed.

V7 is exported immutably to `outputs/ui-previews/v7/index.html`, with checksum and source-hash records. Independent nonroot, network-none host-bind readback verified all V1–V7 HTML hashes and the comparison index links; V1–V6 remain unchanged. Desktop URL: `file:///home/tarfy/Documents/GitHub/BoredEngineer/IsaacLab-Arena/outputs/ui-previews/v7/index.html`. No production/API restart, provider/graph operation, GPU rendering, streaming or physics was performed. The optional external NVIDIA documentation link is outside the exercised zero-request walkthrough. U2 user coverage approval and U3 integration remain open.

### Current planning revision — experience and endpoint coverage

The user's request is to update the plan, not resume the backend queue. All seven recommendations are recorded as R01–R07 in [UI-first review](ui-first-review.md), with V7 proposed as a cohesive inspect-and-correct workspace. The [endpoint contract plan](endpoint-contract-plan.md) maps every C01–C21 capability and P0–P7 phase to source reuse, required extensions or explicitly proposed contracts. The [source inventory](api-source-inventory.json) records 46 registered method/path pairs and 11 route-source hashes without importing/constructing the API or probing services.

Independent domain audits `deleg_8790d9fb` were incorporated: editor revision versus research version, cache token versus job ID, non-idempotent editor save, typed/family discovery, publication feature-off/session limits, atomic whole-queue approval, safe pagination, strict execution adapters, policy-limit precedence, evidence/media and governed viewport lifecycle. Static checks passed for all seven recommendation rows, 21 capability rows, eight phase rows, 26 explicitly proposed route rows, 14 open contract gaps, exact source-route reconstruction/hash matching and relative links. Proposed routes are candidates across later phases, including conditional alternatives—not instructions to implement all of them now. Final focused consistency review `deleg_f37d9a74` passed with no blockers. Optional lost-ACK lookup/operation-namespace and dependency-inventory clarifications were incorporated. Scoped pre-commit and static consistency checks passed; this is documentation verification, not runtime acceptance.

This change does not implement V7 or any endpoint, modify a delivered preview, restart services, run application tests, access Neo4j/providers, install rendering SDKs or launch GPU work. U2 user coverage approval, U3 selection and separate live budgets/targets remain open.

### Preview v6 — saved render visualizer and ovrtx study

Assets & scene is directly discoverable and linked from Environment / Preview. The viewer displays three original historical 512px PNGs with checked byte hashes, exact camera availability, zoom/retry and supplied provenance; scene defects and uncertain freshness/input identity are explicit. It does not render the current draft or claim ovrtx-produced pixels. Render/live requirements reviews retain frozen inputs and invalidate consent after changes. Native modal focus restoration was corrected after a failing real-browser Escape assertion.

Latest isolated run `/tmp/arena-ui-preview-bl_z7vcd/`: 107 UI tests, two packaging tests, TypeScript/build, 45 screenshots; all three image hashes/decoded sizes, missing-camera behavior, zoom, injected error/retry, frozen/stale plans and disabled live connection checked. Source/cleanup evidence verified. Independent review `deleg_cd7df7e0` passed with no blockers. Suggestions concern additional WebRTC/storage construction guards, distinct synthetic capture pixels in unit tests and commit-pinned research excerpts; actual historical PNG hashes are already browser-checked. V6 is exported to `outputs/ui-previews/v6/index.html` with `ovrtx-study.md` and the source ledger alongside it. Host-side HTML/study checksums match; v1–v5 remain byte-identical.

[ovrtx research](ovrtx-visualization-study.md) verifies a documented ovstage/ovrtx/ovstream browser path and records source/driver/client/licensing/physics boundaries. Its citations pass the ledger verifier. No SDK installation, GPU renderer, live stream, simulation, query or job dispatch was performed. A runtime spike remains separately authorized.

### Preview v5 — restored query and operational workspaces

Implemented explicit Neo4j query and Jobs & diagnostics destinations. Query review and exact bundled Table/Graph fixtures are separate operations, with immutable review text and stale/custom-query notices. Operational journal/inspector and confirmation-gated action examples are separate from the Developer diagnostics tab, whose bounded request/retry/queue-resume reviews never dispatch. Query/jobs do not inherit editor selection. Runs & evidence now contains research reports/metrics/media without cancellation/recovery controls; publication remains in Research graph. Existing backend/production modules remain untouched.

Latest isolated run `/tmp/arena-ui-preview-iqog0gvv/` passed 89 UI tests, two packaging tests, TypeScript/build and a 38-screenshot browser walkthrough. Parent exercised fixture binding, finite JSON rejection, graph-node keyboard inspection, retained query state, exact job target/consent reset, identical diagnostic retry IDs/inputs and whole-queue review. Narrow graphs/journals use bounded horizontal scrolling with browser checks. The initial review found a Runs self-comparison defect; collision reset and defensive validation were fixed with RED→GREEN tests while preserving frozen historical reviews. Focused re-review `deleg_f9831e6d` passed with no blockers or suggestions, closing the sole finding from `deleg_9d393c36`. Browser checks also cover comparison collision and query/diagnostic retention across navigation. V5 is exported to `outputs/ui-previews/v5/index.html`; the host-side checksum matches the verified build. V1–v4 remain byte-identical. This is offline UI restoration, not deployment or live query/job acceptance.

### Preview v4 — control placement and original-dashboard comparison

The collapse/expand control now belongs to the left sidebar, including its compact state. The top bar uses the original dashboard's sun/moon switch presentation with preview-local state; account controls remain inert. Keyboard theme switching, Escape focus return, draft preservation and separate desktop/narrow sidebar preferences are covered. Instrumented failed browser evidence established that full-page capture temporarily resized to 1px; separate layout preferences prevent that narrow transition from overwriting desktop choice.

Isolated run `/tmp/arena-ui-preview-khm06x_b/` passed 52 UI tests, two packaging tests, TypeScript/build and a 28-screenshot browser walkthrough including 320px controls. Source hashes and owned-container cleanup verified. Exported to `outputs/ui-previews/v4/index.html`; v1/v2/v3 remain byte-identical. Focused static layout review passed with no blockers (`deleg_a0716653`). Nonblocking suggestions cover additional real-browser desktop-return and sidebar keyboard/containment assertions. The switch preserves the original presentation while adapting its track/thumb colors to the Cybernetic-Physics palette. The original-dashboard feature-retention audit is complete (`deleg_ffe92b51`): Neo4j query and integration diagnostic controls are missing, while the job journal/inspector and other original editor surfaces are only partially represented. See [retention review](original-dashboard-retention.md) for exact source references and the proposed UI-only restoration slice. This version does not add missing Neo4j query or diagnostic job screens; the user requested reviewing those gaps after the layout corrections.

### Preview v3 — Cybernetic-Physics branding

Inspected the company website, its computed typography, stylesheet tokens and SVG wordmark. V3 applies the company identity, Tomorrow/IBM Plex Sans/Kode Mono, neutral black surfaces and green accents. Light mode is a documented workbench adaptation. Fonts and SIL notices are embedded offline; the wordmark is path-only SVG. V2 navigation/account boundaries and local workflow behavior remain unchanged. See `web/arena-workbench/BRANDING.md` for source provenance.

Isolated run `/tmp/arena-ui-preview-85zqxbye/` passed 51 UI tests, two packaging tests, TypeScript/build and the 27-screenshot Chromium walkthrough; 11 mocked launcher tests passed separately. Browser evidence confirms all three fonts loaded without network attempts and primary hover contrast above 4.5:1 in both themes. Source hashes, artifact bytes and owned-container cleanup verified. Exported as `outputs/ui-previews/v3/index.html`; v1/v2 hashes remain unchanged. Focused static branding review passed with no blockers (`deleg_c8a9cf3c`). Nonblocking suggestions cover SVG-shape regression assertions, broader accessibility viewports/focus checks and automated provenance-manifest validation; the current evidence is not a complete accessibility audit. No backend rollout, authentication or live research execution is claimed.

### Preview v2 — layout iteration

The user requested versioned previews, a global top bar containing Login/Sign up and theme controls, and collapsible left navigation. These are implemented only in the offline preview. Account fields are disabled and empty; no authentication backend or credential submission was added. Collapse/theme changes preserve workspace inputs. Narrow layouts initially collapse; widening does not override manual collapse.

Versioned desktop archive: `outputs/ui-previews/index.html`, with immutable HTML/checksum records under `v1/` and `v2/`. V1 bytes remain unchanged, including the original `outputs/ui-preview-xxyej4qn/` copy. V2 is exported from `/tmp/arena-ui-preview-rcfs8ulb/`: 51 UI tests, two packaging tests, TypeScript/build and the isolated browser walkthrough pass. The 27 screenshots include both sidebar states, account dialogs, themes and narrow layouts. Source stability, artifact hash and cleanup were verified; host-side bind-mount hashes match. Focused static v2 review passed with no blockers (`deleg_f71b0651`); extra explicit widening/focus-restoration assertions remain nonblocking test suggestions. This remains U2 design iteration, not production rollout or backend expansion.

### Preview v1 — initial UI checkpoint

- Standalone React entry under `web/arena-workbench/src/preview/`, separate build/test configuration and offline HTML packaging. No production ApiClient, runtime/session provider, backend worker, real profile discovery or credentials flow is connected. Existing production files and services were not changed by this slice.
- Environment supports New/Refine, frozen request review, explicitly labelled example candidates, draft replacement and session-only example versions. Explicit reset/source replacement clears source-bound composer state; navigation preserves it.
- The former flat document concept is represented by a compact family/version picker plus searchable typed Library. The synthetic C1 case has one family row with numerically ordered history, an explicit baseline, exact-version inspection and unsaved-input confirmation. Policies/experiments/unclassified documents are separate. This is not a live inventory or modification of actual C1 records.
- Build/evaluate, Improve, Experiments, Runs/evidence, Research graph and Settings have local forms and example states. Workflow targets are pinned until explicit rebinding; repair fixtures declare scene/version/policy/config identities and block mismatches. Prepared publication and uncertain outcomes remain separate from saving.
- Coverage contains 394 exact historical records and 12 separately identified managed-CLI additions. Reference examples and source metadata are inspectable; all advanced options are not claimed to have effective typed runtime controls. Historical G01–G05 remain explicit.
- Latest complete isolated run: `/tmp/arena-ui-preview-xxyej4qn/`. Parent verified 49 UI tests, two Node packaging tests, TypeScript/build, and an offline Chromium walkthrough with 22 screenshots. Eleven mocked-Docker launcher tests separately pass. Browser checks cover New/apply/local-save, C1 selection/discard guard, Refine/source replacement/reset, explicit form rebinding, matching/mismatched repair evidence, proposal-acceptance and offset-consent invalidation, unknown publication observation, zero-episode metrics, themes and narrow viewport.
- Artifact: `/tmp/arena-ui-preview-xxyej4qn/artifact/arena-workflow-preview.html`. Artifact SHA and unchanged source snapshot verified. Launcher proof reports `cleanup_verified: true`, `owned_remaining: []`; parent independently checked no run-owned containers remain. These are preview/page-request and isolated-container checks, not global host activity or robotics runtime acceptance.
- Parent reset/launcher/path re-review passed (`deleg_2a4637b9`). Workflow review `deleg_55d8a846` found proposal acceptance persisting across rebinding; the fix also invalidates assistance consent on offset edits and strengthens stale-preparation/hidden-navigation regressions. Both new defects reproduced before the fixes; the updated browser checks pass. Final narrow static re-review passed with no blockers or suggestions (`deleg_c327445a`). The initial U1 preview is ready for U2 user walkthrough, not runtime parity or production rollout. Earlier artifacts are retained and do not contain every correction. Integration priorities, unresolved coverage entries and simplifications remain for user review before backend expansion.

To reproduce the isolated preview, run `python3 web/arena-workbench/scripts/run-preview.py` from the editor checkout. See `web/arena-workbench/PREVIEW.md`. No live rollout, graph write or incident recovery is authorized by this preview work.

Production execution tests remain paused after the isolation incident. A frontend-only preview may proceed only with separate entry/runtime and verified egress denial; no live services, credentials, API proxy or research data are needed. User approval of UI coverage is separate from live budgets, rollout or incident recovery.

### P2 checkpoint and incident — retained evidence

#### Test-isolation incident — supersedes verification status below

The legacy cleanup fix delegate reported that its initial regression suite unexpectedly reached live Neo4j, outside the authorized isolated-test scope. After explicit user approval for a read-only incident audit, parent queries confirmed persisted updates to both test-fixture roots: `llm_gen_kitchen_pick_and_open` at `2026-09-14T16:21:54.554Z` and `llm_gen_maple_table_robolab_PickAndPlaceTask` at `2026-09-14T16:21:55.629Z`. Current timestamps cannot distinguish creation from overwrite or establish the complete affected subgraph. Query/security logs were empty; available debug logs contained checkpoint information, not per-query attribution. The report and metadata-only evidence are at `/tmp/arena-neo4j-incident-audit/REPORT.md`. No rollback, deletion, configuration change, or further publication was authorized or performed. Parent checked the simulator container and found no remaining pytest processes. Tests remain paused pending enforced egress isolation.

The parent's broader checkpoint exited with 2,406 passed and one skipped, but passing tests do not establish isolation or absence of live effects. It precedes final legacy cleanup verification. The delegate's eight-file run returned 227 passed, three failed, three skipped; it reported reproducing the three failures with the prior handler restored in memory. These results are not a clean final acceptance claim.

Implemented since the older notes: the three original fixes and CLI parser validation passed review; publication controls are mounted with reviewed degraded GET-only behavior; managed retrieval is wired and its snapshot cleanup veto passed review. Legacy cleanup/diagnostic corrections are implemented but final verification is pending. Real isolated browser/API evidence for generation/save/lineage/preparation and control opening is at `/tmp/arena-real-acceptance-nd04u8he/`; it does not prove graph execution. Cross-session publication recovery, authorized real Neo4j acceptance, and live rollout remain open.

Historically, the user authorized fixing the three findings, retrieval wiring, publication UI/CLI integration and isolated browser/API acceptance. The older workstream notes below describe that stage, not the current work queue. The UI-first direction above now governs further work; missing live budgets do not require hiding planned UI surfaces, and preview coverage does not authorize real execution.

- Consolidated retrieval, callback and post-ACK UI fixes have been worked on by isolated owners; parent verification and independent consolidated review are underway. Do not infer review clearance solely from implementation reports.
- Managed retrieval generation wiring, CLI publication integration, UI mount integration audit, and a real-browser/real-API isolated acceptance harness are separate workstreams. Avoid conflicting edits and preserve all existing work.
- Real browser/API acceptance will use separate temporary state and deterministic workers, with no page-route API fabrication. This does not establish live inference or real Neo4j publication.
- Clarification of the exact Neo4j acceptance target and any live A2 provider/model/spend timed out without a choice. No existing research graph, live journal, provider budget, or simulator run is authorized by default. Continue code/isolated acceptance without treating missing live budgets as a code blocker.
- Full P2 and P3–P7 remain incomplete. No live rollout or commit has been performed.

### Previous checkpoint before renewed authorization

- **Managed retrieval is paused and remains unwired to generation.** Final review `deleg_e53e6f86` still found missing managed trial `run_id`/`policy_identity` projection and incomplete non-null evaluation root/version/policy bindings when a controller is present. Its controller positive fixture also omits these fields. The 212-test parent run does not clear these findings. After exceeding the two-cycle review budget, the parent requested a decision on a consolidated fix versus deferral; clarification timed out without an answer. Do not silently resume incremental retrieval fixes.
- **Candidate callback error-mapping fixes are also paused.** Review `deleg_713f068f` found that non-JSON/deeply nested callback mutations fail closed but produce 500 rather than the intended static 409: the first store-boundary post-callback serialization can raise TypeError/RecursionError. Ordinary JSON mutation is rejected before returning a candidate; durable data is unchanged. Parent verified 122 store/API tests after that fix, but those tests do not clear this remaining error-mapping finding. A consolidated-fix decision covering this and retrieval was requested after the two-cycle budget; no answer was received. Do not resume these paused edits without that decision.
- **Execution-control edits are paused and the component remains unmounted.** Parent verified 69 focused UI tests/typecheck and 88 graph-access/authorization tests after acceptance-capacity and blank-database fixes. Final review `deleg_919fb155` cleared those fixes but found that post-ACK storage failure loses known acceptance in mounted UI state and enables exact POST retry with ACK-unknown messaging. The backend acceptance replay fence still prevents another physical write; no double-write claim is made. This needs an in-memory accepted/read-only fallback plus a post-ACK storage-failure regression. Two fix/re-review cycles are exhausted; further fixes require the consolidated decision. Binding response/identity mutation guards are implemented, but the candidate sibling error-mapping finding above also remains open.
- Publication ledger, worker protocol, scheduler, HTTP adapters and exception-safe lifespan integration are implemented and focused review findings are resolved. Real subprocess tests use fake graph I/O. `publication_enabled=False` remains the default; enablement requires explicit configuration and preinitialized worker schema. Cleanup retains owned/borrowed leases until obligations close and binds journal/reset paths to the same validated directory. Dirty-reset compensation is **not crash-atomic fencing**. No live rollout occurred.
- Research-version save/recovery and optional save-time publication preparation passed backend/UI reviews. Source advertises `research_versions` only for configured roots. Preparation freezes the three-field target and creates an intent, not graph execution; unprepared versions are not retroactively changed. A separate execution-control component is being built unmounted. Cross-session publication execution recovery remains unsupported; this is not the same as supported cross-session save recovery.
- Parent verified 11 actual Chromium scenarios and 17 screenshot artifacts under `/eval/research-preparation-browser-p2/`, recomputing counters from per-test records. All APIs were synthetic; unexpected/external requests and forbidden execution requests were zero. Download evidence is fixed URL/browser fetch, not native completion. Initial fixture failures remain preserved.
- Managed CLI recovery and duplicate-key parsing passed final review; parent verified 80 CLI tests and scoped hooks. Accepted recovery no longer depends on current model readiness; fresh dispatch still requires normal preflight. Publication is not requested by this CLI path.
- Backup and administrative CLI reviews passed within the explicit offline trusted-owner Linux/POSIX scope. `init-publication-ledger` prepares local SQLite only; shared required-trigger inventory checks reject missing or misbound enforcement without repair. Parent admin/core/worker rerun passed 222 tests. Initialization/backup require `--attest-quiescent`; private main-plus-WAL preflight preserves rejected-source inventory/bytes/modes, excluding access timestamps. Idle rollback connections may hold no lock, so the operator must close every connection. No live source was opened or changed.
- Integrated parent checkpoint: **1,811 backend tests passed, 1 opt-in GPU test skipped** (`proc_c8dfdd40bbd0`, exit 0); **404 frontend tests passed**, TypeScript and production build passed. This checkpoint precedes the new unmounted execution-control component. Counts overlap earlier targeted runs and must not be summed. Passing tests do not clear the paused retrieval findings or establish live Cypher/provider/simulator acceptance. Existing frontend chunk warnings remain.
- README/runbooks reflect the permanent offline requirement and unimplemented integrations; parent Sphinx build and scoped checks pass at `/tmp/arena-managed-preview-docs`. P2 remains incomplete; P3-P7 and live provider/graph/simulator acceptance remain pending. No commit or push was performed.

### Earlier P2 integration checkpoint

- Implemented isolated P2 persistence: coordinated reservations, exact candidate/attempt and approval binding, immutable no-clobber artifacts, promotion recovery, explicit committed parents, committed-only latest, and atomic commit/event/optional publication intent. The authenticated save/read/download API uses explicit managed profiles; reads never initialize stores. Legacy output directories remain untouched.
- A capability-gated version UI supports confirmed saves, exact readback, retained ambiguous requests and explicitly confirmed same-operator session recovery. The backend does not yet advertise `research_versions`. Parent verified 48 version/editor tests and TypeScript after recovery/list-refresh fixes. Re-review found that a stale sibling instance can remove a newer pending request; its ownership-check fix is in progress.
- Five actual Chromium scenarios passed using exclusively synthetic APIs. Parent parsed result JSON, recomputed request-counter totals and verified all five screenshot files under `/eval/research-version-browser-p2-retry/run-3/`: zero unexpected/external requests. This covers explicit save, exact replay after session replacement, declined confirmation and mismatched readback. The download check verifies a fixed URL and browser-fetched attachment bytes, **not native download completion**; the two earlier download-test failures are preserved. These results do not close the sibling-instance review finding or constitute live persistence/publication acceptance.
- The opt-in managed CLI uses the existing API and journal rather than legacy writers, authenticates Unix peers before HTTP bytes, and enforces absolute transport deadlines. Publication remains explicitly unrequested. A fresh review found that accepted-operation recovery wrongly depends on current model configuration; a contract-backed recovery fix is in progress.
- Pure canonical graph projection and exact transport readback, separate memory-only graph grants, and durable publication attempts are implemented and isolated-tested. Transport requires explicit immutable-scope attestation and receipts disclose that readback is not a database snapshot. The executor adapter, production authorization/lifecycle wiring, generic retrieval revision selection/deduplication and live Cypher acceptance are not complete. No publication routes are enabled.
- Offline checkpoint/verification/prepared-restore code passes 34 parent-run tests. Independent review blocks acceptance on non-atomic success-seal publication and incomplete operation-wide deadlines; fixes and corruption/privacy regressions are in progress. An opaque private journal can contain session-derived authentication state. No live checkpoint, activation or graph rollback was performed; administrative CLI wiring remains pending.
- Most recent parent broad workbench plus research-projection/transport run: **1,055 passed, 1 opt-in GPU test skipped**. This is a source checkpoint before the next review fixes, not a full-plan acceptance assertion. Subsequent targeted runs include 155 CLI/authorization/publication-state tests and 19 profile/API tests; counts overlap and must not be summed.
- Persistence approval/mutation and graph schema/hydrated-value review findings were fixed with regressions (parent combined rerun: 311 passed). Backup and CLI findings above remain open. Passing tests do not supersede independent findings or authorize live state changes.
- P2 is still in progress. Remaining work includes reviewed backup/admin operations, accepted CLI recovery, publication executor and operator controls, retrieval compatibility, exports, integration/browser gates and documentation. P3-P7 remain pending. No real provider/graph/simulator acceptance, journal cutover, commit or push has been performed.

## Previous handoff: P1 code verified, rollout/live acceptance blocked

- New/refine, metadata inspection, exact prior receipts, attempt/grant fencing, candidate recovery, cancellation evidence and corrected authorization recovery are implemented. The revised API now advertises `generation_modes`; old API instances retain legacy behavior.
- Independent original-backend, second-round frontend and rejection-protocol security re-reviews passed (`deleg_cdbb5937`, `deleg_d02c49c1` frontend, `deleg_ba53cf66`). The last security finding was reproduced and fixed for POST persistence and GET reflection. An additional commit-failure test verifies no uncommitted rejection proof.
- Final parent runtime suite: **636 passed, 1 opt-in GPU test skipped** (all workbench tests plus snapshot/visual Graph-RAG and DCRG graph/controller regressions). Frontend: **317 passed**, TypeScript and production build pass. Documentation HTML build passes with warnings treated as errors.
- Twelve actual Chromium tests pass with synthetic APIs; the parent verified every artifact hash, twelve screenshots and twelve evidence records under `/eval/generation-browser-p2-races/`. That directory name does **not** denote P2 storage implementation. No unexpected/external requests or page errors were recorded.
- Changed-file hooks pass except four pre-existing agent-file lint findings (two R508, F541, SIM105), programmatically matched against baseline `149d72fc2`. No new lint finding was introduced. Existing large frontend chunk warnings remain.
- Read-only live check: `/api/editor` still reports no `generation_modes` capability and generation unavailable; `/api/model-settings` reports `configured=false`, source `none`. The existing API was not restarted and its journal was not migrated. No real model call, graph publication, research-store migration or simulator/policy run was launched; no commit or push was made.
- **Next required decisions:** controlled journal backup/API rollout approval, provider/model and inference budget, then review actual A2 from a prompt. The main plan's section 8 explicitly gates P2 storage/graph changes on that review. Prior budget/destination questions timed out unanswered. P2-P7 remain unimplemented, not silently excluded or declared complete.

The checkpoints below record earlier intermediate states, not the latest completion status.

## Baseline

- Started from clean commit `149d72fc2` on `dev/0.3.0-prerelease`; prior graph explorer and reviewed plan were already committed by the user.
- Matching runtime/frontend mounts rediscovered; commands use the source owner's non-root identity.
- Frontend baseline: 229 tests passed.
- Workbench Python baseline: 264 passed, one opt-in real snapshot test skipped. No live inference/database writes/simulator jobs used for this baseline.

## Active P0/P1 work

- Independent progress critique commissioned against the actual plan and source; this is not code approval.
- Explicit generation operation API now distinguishes `new` and `refine`, rejects any base/document field on `new`, validates an explicit refinement base, and retains legacy payload compatibility. Six new tests and the existing model-settings suite pass together (64 tests).
- `EditorIndex.capabilities.generation_modes` is an optional additive contract. The API does not advertise the new UI mode until its complete bounded execution path is integrated and verified.
- Parallel owned slices: journal attempts/candidate receipts; memory-only execution grants; exact Graph-RAG snapshots and supplied-context consumption; prompt-first UI with explicit new-result source detachment.
- These parallel slices are NOT complete until parent integration, relevant/full regressions and independent critique pass. No new field or visible control closes C03/C05/C06/C17 by itself.

## Integration checkpoint

- Attempt/CAS dispatch, pre/post-spawn authorization, protected durable candidate receipts and exact Graph-RAG context are integrated. Real subprocess/pipe/cleanup tests use an explicitly synthetic worker; both the valid receipt and graph-secret-after-revocation cases pass. This is not provider or database acceptance.
- Schema/catalogue APIs and explicit-open metadata UI are integrated. Generated evidence remains inspectable after applying a result; blocked-authorization transport states are recognized without being counted as executing.
- Parent full workbench suite: 377 passed, one opt-in GPU test skipped. Parent full frontend suite: 253 passed; TypeScript/production build pass with the existing chunk-size warning. Subsequent renewal/review fixes require reruns.
- Independent integrated-code critique and explicit blocked-authorization renewal are in progress. New live mode remains unadvertised pending gate closure.
- Read-only live readiness reported no configured provider. Requests for inference budget, database-write destination and bounded simulator permission timed out without answers. Live inference, graph writes/migration and simulator acceptance remain blocked; code and isolated tests continue. No permissions are inferred from unanswered questions.

## Remaining scope

### Critique and recovery checkpoint

- Initial integrated critique blocked grant/request binding, cancellation evidence, weak receipt validation and UI source isolation. The independent original-backend re-review (`deleg_cdbb5937`) now passes, scoped to those findings; it excludes newly changing rejected-renewal recovery and frontend work.
- Parent rerun: 114 strict-receipt, binding, cancellation, snapshot and real managed-process tests pass. A further real-subprocess renewal test verifies explicit replacement credentials, unchanged original inputs, one worker, accepted candidate and cleanup; its worker is synthetic, not a provider.
- Actual Chromium evidence: eight tests passed, independently parsed from `/eval/generation-browser-p1-stable/results.json`, with eight screenshots and zero recorded external/unexpected requests or page errors. All APIs are synthetic; session-replacement browser coverage remains outstanding.
- Second frontend critique found definitive-rejection recovery, workspace-only blocked-job controls and stale activity/submission ownership gaps. Regression-first fixes are underway. No new mode advertisement or full-P1 approval is justified yet.
- Cancellation UI now distinguishes unreleased work, unknown external outcome and preserved accepted candidate; receipt UI shows observed retrieval timing and effective limits without inventing unavailable measurements.

All C01-C21 capability acceptance remains open except for previously existing foundations. P2-P7 are not implemented by this checkpoint. The 394-entry static ledger is an inventory, not an implementation-completion report. No commit/push is authorized implicitly.
