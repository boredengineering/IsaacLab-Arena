SESSION HANDOFF — TanStack UI discussion

Current continuation: the user approved implementation, including new docker/workbench/ assets, then corrected the diagnostic-first UI direction. Their priority is the ArenaEnvGraphSpec live editor: prompts/YAML, authored graph, Neo4j queries/results, and real scene/object snapshots. The starter editor is implemented, verified and independently reviewed, running on http://127.0.0.1:3001. Live generation still needs server model credentials. Do not return to proposing the rejected diagnostic console as the main interface.

Implemented boundaries: web/arena-workbench/ source; docker/workbench/ deployment; non-root bridge-network Nginx frontend; HTTP/SSE over a narrow read-only Unix-socket directory to Python in the existing Arena runtime. Model/GPU dependencies stay out of the frontend. Diagnostics moved to /developer/diagnostics; /neo4j is separate from the authored graph. Port 3000 was occupied; use explicit --port 3001 consistently. Discover current containers/mounts before changing them.

Current implementation plan:
/workspaces/IsaacLab-Arena/.agents/references/plans/tanstack_agentic_workbench_plan.md
Canonical architecture/runbook: docs/pages/concepts/agentic_workbench.rst and docs/pages/example_workflows/agentic_env_gen/workbench.rst. The full original roadmap is not complete; the user requested the starter editor functionality first.

Verified: fixture load/validate, invalid YAML, stale-save rejection, immutable export without changing source files; actual Neo4j queries/table/graph (28 environment graphs); B1 shows four explicit reifiers and B4 shows none; real render produced five asset/reference PNGs and a scene overview. Parent ran 102 backend tests (one opt-in render skipped), 44 frontend tests, 13 launcher tests and 12 Chromium tests (live generation skipped). Typecheck, production image and warning-free Sphinx build passed. Build retains a large-JavaScript-chunk warning. Full repository suite not run; three pre-existing generator repair-loop failures were reproduced against HEAD by the backend agent. No assistant commits/pushes or Neo4j publication.

Review fixes passed independent follow-up: generation convenience loads preserve frozen view IDs; verified process-group cleanup handles exited leaders/resistant descendants before acknowledgement or lease release; query restrictions reject quoted-function bypasses; pending draft recovery survives navigation and respects symmetric limits without overwriting valid backups. Oversized drafts offer Download current YAML. Recovery is per-tab, not durable across tab closure. Frozen source/include mismatches require download/reload rather than silent replacement. No review blockers remain in the reviewed starter scope.

Generation blocker: no supported API key is present in the Arena runtime environment. Clarification about using .env and a paid-call budget timed out without permission. No .env/credential files were read and no paid inference was run. Workbench generation explicitly disables automatic dotenv loading and graph publication; configuration must remain server-side.

Last observed checkout: /workspaces/IsaacLab-Arena, branch dev/0.3.0-prerelease, HEAD396ad6835. Substantial uncommitted implementation changes; rediscover git status. Host clone /home/tarfy/Documents/GitHub/BoredEngineer/IsaacLab-Arena maps to /workspaces/isaaclab_arena inside the verified Arena runtime. Nonroot identity ubuntu:1234 required. /eval is host-backed at /home/tarfy/eval. These are observed values, not universal defaults.

Useful source context, to verify rather than treat as an approved design:
- Existing UI: isaaclab_arena_examples/agentic_environment_generation/gui_runner.py and review_gui/streamlit_ui.py.
- Streamlit calls generate_spec and flat save; CLI also supports refine_spec and numbered versions.
- Generation already retrieves priors and attempts Neo4j writes; validation/fallback and publication are not cleanly separated.
- Runtime relation solving can replace authored initial poses for non-anchored objects.
- Preview is zero-action first/last frames, not trained-policy evaluation.
- Legacy family/name/version/evaluation identities differ from DCRG immutable identities.
- DCRG is separate bounded CLI orchestration, not automatically selected by the generation agent.

Evidence: /eval/.wb/6e74675cd31c/state/browser-evidence/ (private tests/reports), /eval/arena-editor-snapshot-check-20260911/result.json, .agents/scratch/workbench_ui_comparison/ (locally ignored screenshots/DOM; initial report describes the old diagnostic mismatch, newer screenshots show the editor). Streamlit comparison server8502 was launched directly without SimApp. Keep primary UI verification focused on the user's scene-authoring workflow, not infrastructure counts. Do not restart model services or run a policy evaluation without new scope/budget.
